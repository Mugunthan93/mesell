# scripts/ — operator scripts for data refresh, seed, and tooling.
# This __init__.py makes the directory importable as a Python package
# so that tests/scripts/ can do `from scripts.build_pricing_lookup import ...`.
