# svc-export runtime dependencies (MS Sub-Plan A, spec §3.A).
#
# Versions PINNED to the monolith's exact pins (backend/requirements.txt) so
# the extracted pipeline behaves identically — openpyxl in particular is R5
# (the XLSX byte-output must match the monolith's golden-roundtrip).
#
# NO gemini / langfuse / msg91 / razorpay — export is deterministic (§14.A)
# and uses no AI / SMS / payment surface.

# ── Web framework ────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic-settings>=2.5,<3

# ── Database (async) ─────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.32
asyncpg==0.29.0

# ── Cache / queue ────────────────────────────────────────────────────────
redis==5.0.8
celery==5.4.0

# ── Auth (local JWT verification — A2) ───────────────────────────────────
pyjwt==2.9.0

# ── Storage ──────────────────────────────────────────────────────────────
google-cloud-storage==2.18.0

# ── XLSX (R5 — pinned to the monolith's exact openpyxl) ──────────────────
openpyxl==3.1.5

# ── HTTP shim transport (extracted_clients) ──────────────────────────────
httpx==0.27.0

# ── Observability ────────────────────────────────────────────────────────
prometheus-client>=0.20,<1
