fastapi==0.115.0
uvicorn[standard]==0.30.0
gunicorn==22.0.0
pydantic-settings>=2.5,<3
python-dotenv==1.0.1
python-multipart==0.0.9
sqlalchemy[asyncio]==2.0.32
asyncpg==0.29.0
alembic==1.13.2
redis==5.0.8
celery==5.4.0
pyjwt==2.9.0
google-generativeai==0.8.0
rembg==2.0.59
pillow==10.4.0
onnxruntime==1.19.0
google-cloud-storage==2.18.0
# §14 XLSX export builder — app/modules/export/service.py imports openpyxl at
# runtime (Workbook write + load_workbook round-trip). Genuine production
# dependency surfaced by the §19.D marker run (ModuleNotFoundError in CI).
openpyxl==3.1.5
httpx==0.27.0
pytest==8.3.0
pytest-asyncio==0.24.0
# Tunnel-free async Valkey/Redis fake for unit tests (dual-pepper read-path
# tests run without a live DB-0 connection). Compatible with redis==5.0.8.
fakeredis>=2.21,<3

# §15.J observability — Prometheus metrics scrape (F-15-2)
prometheus-client>=0.20,<1

# Scraping (Meesho competitive research)
playwright==1.59.0

# §19 CI gates — Contracts 1-7 import-linter rule set per §16.E
import-linter>=2.0,<3
