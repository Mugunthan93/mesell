# Memory — meesell-data-engineer

## Agent Identity
Data engineer coordinator for MeeSell. Orchestrates the 2 data specialists (xlsx-parser, scraper-maintainer). Owns STATUS_DATA.md, schema versioning of derived JSON, refresh cadence, coverage stats. Brand-master deferred to V1.5. Decentralized memory ecosystem.

## Notes & Learnings

### 2026-06-04 — Founder's batch workflow preference (LOCKED)
**Rule:** Full-corpus parse, batch by batch (12 batches), not random sampling. Each batch ends with a founder discussion BEFORE the next batch dispatches.

**Why:** Founder wants to see how diverse the 3,772 categories actually are before MVP design. Discussion-per-batch lets him shape the MVP iteratively.

**How to apply:**
- Specialist produces RAW findings only — never auto-appends to the SSoT
- Coordinator + founder manually integrate accepted findings into `docs/MEESHO_CATEGORY_INTELLIGENCE.md`
- After all 12 batches complete → SSoT drives MVP design (`docs/MVP_ARCHITECTURE.md`)
- 12-batch plan documented in STATUS_DATA.md current phase

### 2026-06-04 — Workspace hook conflict + one-time fallback pattern
**Rule:** When the workspace agent-routing hook blocks dispatching `meesell-*` agents (forces `nexus:level-*`) AND the founder cannot immediately fix the hook, the authorized fallback is:
- Dispatch `nexus:level-3:python-developer-agent` with the full `meesell-*` spec content embedded in the prompt
- Route ALL memory writes to the real `meesell-<role>/MEMORY.md` (NOT the nexus agent's own memory)
- Have the nexus agent prepend an ATTRIBUTION NOTE to the meesell agent's memory so continuity is preserved AND the fallback is visible
- Schedule a reminder to fix the hook properly before the next dispatch

**Why:** Founder mandate is "only `meesell-*` agents handle MeeSell work" (CLAUDE.md Stage 2). The workspace hook contradicts that. Fallback preserves architecture integrity (memory continuity) while keeping work moving.

**How to apply:** This is NOT a precedent. Use only when founder explicitly authorizes it ("go" / "ok") for a single dispatch. Hook must be fixed before any subsequent batch.

### 2026-06-04 — SSoT integration is manual, never automated
**Rule:** `docs/MEESHO_CATEGORY_INTELLIGENCE.md` is the single source of truth. It is integrated MANUALLY by coordinator + founder after discussing each batch's draft findings. Specialists never write to it directly.

**Why:** Founder explicitly said "dnt append the summary we need to discuss about it and intehrate it correctly" — wants editorial control over what becomes truth.

**How to apply:** Every batch dispatch prompt must explicitly forbid the specialist from creating/editing the SSoT. Specialist writes a DRAFT file (e.g. `data/parsed/batch_NN_summary.md`); coordinator + founder review together; coordinator (with founder direction) writes the SSoT.

### 2026-06-04 — TWO-SECTION DATA MODEL (founder-locked, FOUNDATIONAL)
**Rule:** Every parsed field is classified into one of two buckets:
1. **Onboarding inputs** — collected ONCE on the seller profile, auto-filled into every catalog form
2. **Catalog wizard inputs** — collected PER PRODUCT in the wizard

**Why:** Founder's instinct after seeing Batch 1: the 9-field compliance block (Manufacturer/Packer/Importer × Name/Address/Pincode) is the same for every product. Multiplying that across N products is pure waste. Same logic applies to any seller-constant field.

**How to apply:**
- Every batch's draft summary classifies each field as `onboarding | catalog | tbd`
- Onboarding fields go into `seller_profile` schema (backend) and onboarding wizard (frontend)
- Catalog fields go into `categories.attributes_jsonb` (backend) and the per-category wizard (frontend)
- Batch 1's identified onboarding fields: 9 compliance fields + Country of Origin (10 total, all seller-constant)
- More onboarding fields may surface in Batches 2-12 — keep the classification rule list open

### 2026-06-04 — Six locked MVP decisions from Batch 1 review
**Decisions locked by founder after reading batch_01_summary.md:**

1. **Seller profile auto-fills compliance block.** Block listing until profile complete.
2. **Brand picker = autocomplete + API search**. Same pattern will apply to other "field name same, enum differs by category" cases revealed in Batches 2-12. Brand is the first instance — collect more before generalizing.
3. **Two-tier form (Compulsory / Optional) for V1.** Add Recommended tier only if Batches 2-12 prove it exists.
4. **AI auto-fill is P0 — but ONLY suggests values from validated enum lists.** No free-text hallucination. Show source/confidence. AI never invents data.
5. **Wizard is data-driven, not hardcoded step count.** Step boundaries emerge from the field-schema structure. Build for evolution across Batches 2-12.
6. **Input primitive LIBRARY, not just a searchable picker.** Primitives needed (Batch 1 evidence): short text, long text/textarea, number, currency-number (₹), weight/unit-number, small dropdown (<20), medium searchable dropdown (20–500), large API-backed search (Brand, 500+), date picker, image upload, address-group bundle. Each catalogued during parsing so frontend can render mechanically.
7. **Backend stores schema by TEMPLATE, not by leaf.** Batch 1 proved 169 unique templates serve 179 leaves. `categories.attributes_jsonb` keyed by template-hash; leaves map many-to-one to templates. Saves storage + avoids drift between near-duplicate categories.
8. **Smart Category Picker returns TOP-5 + "browse manually" fallback** (not top-3). Women Fashion has near-duplicate categories (Kurtis / Kurti With Bottomwear / Suits) where Gemini disambiguation from a one-line description is shaky. Top-5 + manual escape hatch is the safer default for V1.

### 2026-06-04 evening — Six MVP-architecture follow-ups locked by founder
After full-corpus parse + MVP architecture draft, founder answered all 6 open questions:

9. **Books ISBN = follow Meesho (optional).** Do not enforce stricter than Meesho. Reduces friction for casual book sellers.

10. **Meesho source typos = auto-correct internally, restore on XLSX export.** UI displays corrected canonical names ("Primary", "Secondary"). XLSX exporter has a reverse map and emits the typo verbatim when generating Meesho-format files. Field-aliases table needs a `for_xlsx_export` boolean column.

11. **Long-tail super-categories = include all 3,772 in V1.** No filter. Reuses the existing 11 primitives. Adds ~40 leaf rows to seed.

12. **Group ID = show inline as Optional in V1.** No "Advanced" hide pattern. Simpler renderer. Revisit if user testing surfaces confusion.

13. **Warranty = per-product wizard step (match Meesho), NOT an onboarding extension.** Conditional step appears for categories with warranty fields (~190 leaves, Electronics + Appliances + some Cookware). compliance_extensions map does NOT include warranty.

14. **Eye-Serum collapsed compliance = store BOTH representations, render per category.** seller_profile has 9 standard fields + 3 combined "Details" fields. Backend auto-populates the combined fields on save (concatenating standard fields). Frontend renders per template's `compliance_shape` flag ("standard" or "collapsed"). Founder's exact words: "we need to keep it like how meesho handle it" — be faithful to Meesho's actual templates, don't normalise away their variation.

### 2026-06-04 evening — CORE_PHILOSOPHY.md locked + decisions #12 and #14 revised

Founder locked `docs/CORE_PHILOSOPHY.md` — 10 mandates, 8 forbids, 5 structural patterns. Architecture pattern: seller-facing flexibility + Meesho-faithful export, joined by a single Export Adapter component.

**Revised decisions after philosophy lock:**

12-REVISED. **Group ID = show behind "Advanced fields" toggle in wizard.** NOT default-visible (philosophy rejects "show field without explanation"). NOT fully hidden (founder preserved seller's optional choice). Sits between Pattern 2 (hidden + default-on-export) and a regular optional field. Introduces Pattern 5 (Advanced fields) — opt-in opacity, where seller's choice to expand acknowledges they may not understand the field.

14-REVISED. **Eye-Serum compliance = collect 9 standard fields UNIVERSALLY, Export Adapter concatenates to 3 combined fields ONLY at Eye-Serum XLSX export.** Drops the "store both representations" requirement. seller_profile has only the 9 standard fields. Cleaner internal model. Same Meesho compatibility. Better UX (typing into clearly-labeled inputs beats typing into one mushy "Details" textarea).

**Other locked decisions (1-11, 13) ALIGNED with philosophy as-is** — no revision needed. The philosophy actually strengthens several of them (especially #4 AI guardrail = 3-layer enforcement, #2 Brand picker = Pattern 3 escape-hatched dropdowns).

**Minor extensions to existing decisions (philosophy adds clarity, doesn't revise):**
- #2: Brand picker needs "request to add" workflow (Pattern 3)
- #4: 3-layer enum enforcement (AI prompt + frontend + backend validators)
- #5: Wizard step names are plain English, never generated codes
- #6: 10 primitive components consume display layer inputs (display_label, display_help, validation_message). Never read meesho_column_header.
- #8: Smart Picker shows seller-friendly category paths

**How to apply:**
- Every batch summary maps fields → primitive types
- Backend data model honors onboarding/catalog split
- Frontend builds primitive library first, then composes wizard from per-category schema
- AI prompt-engineer (when activated) must constrain Gemini to enum values where validations exist

### 2026-06-04 — Cross-batch watchpoint: field-name clashes with different enums
**Rule:** Track fields whose `name` is identical across categories but whose `enum_source` (resolved values) differs by category. Brand is the canonical example: same field name, but Sarees has 3,730 brands and Mobile Covers has 2,000+ different brands.

**Why:** These fields can't be modelled with a single global enum. They need backend tables keyed by `(category_id, field_name)` OR a runtime resolver. This shapes the data model.

**How to apply:**
- After each batch, run a "field name → distinct enum sources" analysis
- Maintain a running list in MEMORY of fields where same name = different enums
- Flag these as "API-resolved picker" fields in the input-primitive classification

### 2026-06-04 — All data-layer deliverables complete

`docs/MEESHO_CATEGORY_INTELLIGENCE.md` written and locked. 424 lines, 9 sections, co-authored with founder 2026-06-04.

All data deliverables complete:
- `data/parsed/batch_01` → `batch_12` (raw JSON + summary md, 24 files)
- `data/parsed/FULL_CORPUS_ANALYSIS.md` (12-section synthesis)
- `data/parsed/canonical_field_aliases.json` (16+ alias families)
- `docs/MVP_ARCHITECTURE.md` (2,796 lines, 15 sections, 14 founder decisions)
- `docs/MEESHO_CATEGORY_INTELLIGENCE.md` (SSoT, locked)

BACKEND / FRONTEND / AI sessions are now fully unblocked. Hand-offs documented in STATUS_DATA.md.

### 2026-06-05 — MVP_ARCHITECTURE gap analysis dispatched + delivered
**Output:** `docs/MVP_ARCHITECTURE_GAP_ANALYSIS.md` (~430 LOC, 6 sections, 18 gaps catalogued).

**Trigger:** DATABASE track complete (Phases 0–4 done, Alembic head `935e55b4852c`); database coordinator surfaced 4 schema deltas + 7 starting-list gaps where §2 drifted from §5.5/§5.6/§12. Founder authorized gap-analysis dispatch before API track begins.

**Inventory summary:**
- **Total: 18** gaps — 11 validated from database coordinator's starting list + 7 newly surfaced.
- **Tier-1 BLOCKER-V1: 4** — G1 (§2 DDL stale), G2 (pricing_calcs/exports → legacy V1_FEATURE_SPEC), G8 (15 export adapter golden fixtures unauthored), G16 (CI YAML path-triggers reference non-existent scripts).
- **3 most surprising:** G16 (CI gate doubly hollow — no fixtures AND wrong paths), G11 (`profile_complete` vs `onboarding_complete` — three sources of truth for one column name; DB has the right one only because builder followed SSoT), G13 (cache keyed by template_id in §6.4 but category_id in §6.7).

**Ranking heuristic used:** Tier × Severity × Blast-radius, with Tier-1 reserved for either schema drift live in DB or hollow V1 acceptance gates. Severity = BLOCKER-V1 only when a downstream agent will produce wrong code OR a V1 acceptance criterion has no test. Blast radius weighted highest for "any agent reading this in isolation gets it wrong" issues (G1, G11, G13).

**Systemic drift pattern observed:** every section originally drafted as `docs/draft_architecture_section_N_*.md` (5 files still present in `docs/`) was inserted as Section 8/9/10/11 of MVP_ARCHITECTURE.md without renumbering its draft-internal sub-headers. Root cause of G3 (numbering collision: Section 10 + Section 11 both use §11.x) and G14 (orphan draft files). Discipline rule for future: "promote draft to section → renumber + delete the draft in the same commit."

**Recommended ordering (Phases R1–R5):**
- **R1 (3–4 hr, founder approval)** — backport G1, G2, G11 into §2 DDL. Doc only, no code.
- **R2 (2–3 hr, no approval)** — code-lock step assignment + primitive classifier into `backend/app/i18n/` so quarterly refreshes can't silently drift (G6, G7).
- **R3 (4–6 hr, partial approval)** — fix §8/§9/§10/§11 numbering collision (G3); reconcile primitive count to 10 (G5); pick `category_id` for cache keys (G13); archive 5 orphan draft files (G14); G12 + G18 need founder rulings.
- **R4 (3–4 days, founder co-authors)** — author 15 round-trip fixtures + 50 picker descriptions + 30 autofill specs + 30 watermark images (G8, G9); fix `.gitlab-ci.yml` path-triggers (G16). Parallelisable across 3 specialists.
- **R5 (1 hr for V1)** — TTL on product_drafts via Celery beat (G10); ContextVar hardening deferred to V1.5 (G15).

**Explicit non-proposals (sidebar §5):** no schema migrations, no re-seeding, no new tables, no re-litigating §12 founder decisions, no new agents. All proposed work is doc reconciliation + small code-moves + artifact authoring.

**Disagreements with §12:** none. All §12 founder-locked decisions reaffirmed as correct; drift is documentation-only (§2 wasn't updated when §12 was authored).

**Verdict for founder review:** ready. Recommended dispatch order R1+R3 (parallel) → R2 → R5-G10 → R4. Total V1 effort ~5–6 person-days, half of which is founder-bounded content authoring.

## Session mesell-repo-management-session-1 — Step 5 — Data Engineer spec evolved into Data Lead spec; feature_board_data.md initialised

**Date:** 2026-06-10 (initial creation)
**Branch:** repo-management/foundation (off origin/develop)
**Trigger:** MASTER_PLAN.md ratified APPROVED 2026-06-10. Step 5 of the rollout: rewrite the 5 lead specs.

**Founder decisions locked verbatim:**
- D1 — Merge gate: Lead reviews/merges `feature/{name}/<group>` → `feature/{name}`. Founder reviews/merges `feature/{name}` → `develop`.
- D2 — Board updates: Specialist marks `IN REVIEW` on PR open. Lead marks `MERGED` on PR merge.
- D3 — Spec rewrite: Clean replacement. Slug stays `meesell-data-engineer`. Title and framing change to "Data Lead". "Coordinator" framing in the body is retired.

**Files written:**
- `.claude/agents/meesell-data-engineer.md` — top-to-bottom rewrite (6,268 → 19,212 bytes). Frontmatter slug/model/tools unchanged; description rewritten. Body now leads with Identity-as-Lead, then Owns / Merge gate / Update protocol / Cross-lead coordination / Session naming, then Mandatory First Action (updated read order to include MASTER_PLAN + feature_board_data.md), Decentralized Memory (preserved), Hard Constraints (preserved + 4 new NEVER + 2 new ALWAYS), Project Context (preserved), Specialists you dispatch (new dedicated section), Scope IN/OUT (preserved + merge gate + board), Operating Procedure (preserved + board update steps), Reporting Format (preserved + Session + Board sweep lines), Stop Conditions (preserved + 2 new), Hand-off Protocol (reframed around board).
- `docs/status/feature_board_data.md` — created per MASTER_PLAN §6.2 template. Empty active/recent/inter-lead tables, status vocabulary, brief Acceptance gate note pointing at `.github/PULL_REQUEST_TEMPLATE/data.md`.

**Behavioural change (forward-going):**
- I am now a **lead**, not a coordinator. I approve and merge `feature/{name}/data` → `feature/{name}` PRs in the data domain. I do NOT approve `feature/{name}` → `develop` — that's the founder's gate.
- I am the **sole writer** of `docs/status/feature_board_data.md`. I never touch another lead's board.
- Every specialist dispatch carries the session name `mesell-{feature}-data-session-{N}`.
- Cross-lead coordination uses the memo protocol — write to `.claude/agent-memory/meesell-data-engineer/handoff_<topic>.md`, open an "Inter-lead requests open" row on my own board, 48-hr SLA before founder escalation.
- Board sweeps at session start AND session end. 7+ day untouched rows flagged in Notes and STATUS_DATA.md.

**Pointers:**
- MASTER_PLAN.md §1 (branch model), §2 (merge flow — D1 lives at §2.1/§2.2), §4 (session naming), §5.5 (data PR template), §6 (feature_board.md — D2 lives at §6.5), §7 (lead responsibilities — §7.1 ownership, §7.3 escalation, §7.5 cross-lead).
- Decision D3 explicit at MASTER_PLAN.md "Decisions" table — slug retained, framing replaced.

**Non-impact areas (verified untouched per task constraints):**
- No other `meesell-*.md` agent spec touched.
- No other `feature_board_*.md` touched (only `feature_board_data.md` created).
- No `backend/`, `frontend/`, `k8s/`, `infra/`, `terraform/`, `data/`, `themes/` files touched.
- No commits made.
- No MASTER_PLAN modifications.
- No STATUS_DATA.md modifications (this is repo governance, not a feature chunk — confirmed scope-out).

### 2026-06-10 — Knowledge-sync survey: stale-stub + naming-drift findings (mesell-knowledge-sync-data-session-1)
Read-only pipeline survey. Surprising/non-obvious findings only:

1. **`backend/app/data/category_attributes.json` and `meesho_categories.json` are STALE HAND-STUBS, not pipeline output.** Both dated May 27 (pre-parse). `category_attributes.json` = 16 hardcoded categories (Kurtis/Sarees/etc.) with `required/optional/default_return_rate` — a quality-engine return-rate stub, NOT the 3,772-leaf attribute schema. `meesho_categories.json` = a hand-typed 6-super-cat nav tree with made-up sub-cats (e.g. "Beauty & Personal Care") that do NOT match the real `meesho_category_tree.json` super-category names. The REAL derived corpus lives in `backend/app/data/meesho_category_tree.json` (1.7MB, 3,772 leaves, API-sourced 2026-06-03) + the DB seed pipeline. The two stubs are legacy/quality-engine fixtures — must NOT be confused with the canonical category data. Watch for downstream code reading the wrong file.

2. **My spec names `category_attributes.json` as a derived file I schema-version. It is NOT pipeline-derived.** The actual derived artifacts are `meesho_category_tree.json` (tree) + DB tables seeded from `data/parsed/batch_*.json`. The schema-versioned surface is the batch JSON `parser_version` (currently 0.2) and the alias `_meta.version` (currently 1) — NOT a version stamp inside category_attributes.json.

3. **No scraper tooling exists yet.** No `scripts/scrape*`, no `data/snapshots/`, no Playwright scraper. `meesell-scraper-maintainer` has produced zero artifacts. The 2026-06-03 tree came from a direct Meesho API call (`api:bulkCatalogUpload/fetchCategoryTreeOld`), recorded in the tree's `source` field — not from the scraper. Quarterly refresh has no executable scraper pathway today.

4. **Count reconciliation (all within locked tolerance, documented in seed_all.py):** templates SSoT 3,557 → actual 3,566 (+9, from schema groups differing only by enum_source/help_text); field_enum_values SSoT 49,295 → actual 49,259 (−36, from alias-collision dedup of duplicate (category_id, canonical) pairs). field_aliases exact 67. categories exact 3,772. `leaf_id_to_schema_hash.json` confirms 3,772 leaves → 3,566 distinct hashes on disk.

5. **Naming drift: SSoT/analysis say "16+ alias families"; actual `canonical_field_aliases.json` has 23 families and seeds 67 field_aliases rows.** `_meta.version=1`. onboarding_extension_map is keyed by numeric super_id strings (e.g. "26", "19_36_37_14_88_34") not super-category names.

6. **30 super-categories in the real tree** (tree meta: super_category_count=30, category_count=234, sub_category_count=1046), vs the 6 invented in the stub `meesho_categories.json`. 12 parse batches grouped these 30 supers thematically.

### 2026-06-16 — Category seeding: Wave 0 ratified + Wave 1 (local seed) landed (mesell-category-seeding-session-1)

**Effort:** unblock the local visual gate (categories table = 0 rows). Branch `feature/category-seeding` (worktree `/tmp/mesell-wt/category-seeding`), off `origin/develop`. Architecture doc `docs/plans/architecture/CATEGORY_SEEDING_ARCHITECTURE.md` flipped DRAFT→APPROVED (rev 1.0, commit 69e3d8b).

**Founder decisions (D1/D2/D3), locked:**
- **D1 = LOCAL-ONLY** this pass. dev/staging K8s seed Job deferred (Wave 2 SKIPPED) — no infra handoff opened, no namespace, no spend.
- **D2 = commission_pct REAL values, two-phase.** Phase 1 (done) = seed NULL. Phase 2 (Wave 1.5, NOT yet done) = **scrape-sourced** backfill: `meesell-scraper-maintainer` captures Meesho's published commission **rate-card** → `backend/app/data/category_commissions.json` → idempotent re-seed via `scripts/seed_category_commissions.py` (NOT a migration; column exists). Founder will accuracy-review the scraped rate-card before it seeds.
- **D3 = PURE UPSERT** for quarterly refresh (no prune of removed leaves).

**VERIFIED DATA FACT (non-obvious — remember this):** `commission_pct` is **absent from ALL committed corpus data** — 0 hits across the 12 `data/parsed/batch_*.json`, `backend/app/data/meesho_category_tree.json`, and `canonical_field_aliases.json`. The gitignored raw `.xlsx` templates also carried none (their full-corpus parse produced zero commission). So the price-calculator FEATURE_PLAN §126 hypothesis ("extract commission from the per-category XLSX") is **UNSUPPORTED** — commission must be sourced externally (founder chose scrape). Pricing tolerates NULL: `commission_pct IS NULL` → `Decimal('0.00')` → `CommissionMissingError` 422 (`test_commission_missing.py`); the catalog wizard / visual gate never read commission, so NULL does not block the gate.

**VERIFIED DEFECT (architecture docs missed it):** the 5 seed scripts (`scripts/seed_all.py` + 4 sub-seeders) imported **stale pre-rebuild paths** `app.config` / `app.models.*`. Live modular-monolith tree is `app.shared.config` / `app.shared.models.*` — so `seed_all.py` ModuleNotFound'd at import until fixed. Wave-1 build applied a **mechanical import-path-only** substitution (class names + `settings` symbol identical; zero logic change; commission NULL line 137 untouched). If you re-run or extend these scripts, expect `app.shared.*` paths. `app.i18n.*` imports were already correct.

**Wave 1 result (HYBRID 3-step: SPEC→BUILD→MERGE-GATE all done):** `make seed` target added; seed ran twice against local Postgres (`:5432/meesell`); counts gate-clean + idempotent — field_aliases 67 (exact), templates 3566, categories 3772 (exact), field_enum_values 49259. Prewarm 100 schemas; `/browse` trgm live (kurti→5, saree→8); GIN indexes confirmed. Alembic head is now **`f31c75438e61`** (chain `935e55b4852c → a1b2c3d4e5f6 → f31c75438e61`), NOT the brief's `a1b2c3d4e5f6` (that's pg_trgm+GIN, still mid-chain). **PR #245** (`feature/category-seeding → develop`) OPEN + gate-APPROVED-for-founder — **founder merges** (never self-merge). Build commit d5e71a9; gate verdict 56a976c. Local-dev-only note: a stale Valkey empty-list cache entry had to be cleared before prewarm warmed >0; K8s migrate→seed→boot ordering is immune.

**Wave 3 CLOSE (done):** catalog-create→wizard proven end-to-end on localhost — 0 FK orphans (categories→templates), all 3772 leaves have field_enum_values, `GET /schema` renders a 71-field wizard from seeded data, `/field-enum/{brand,color}` returns seeded enums, `/suggest`(POST)+`/browse` live. `CATEGORY_SEEDING_DISCUSSION.md` flipped DRAFT→RESOLVED; `feature_board_data.md` category-seeding row → RESOLVED(local)/PR#245-open; infra K8s-Job inter-lead row → DEFERRED (D1 local-only).

**Wave 1.5 (commission backfill) — PREPARED, awaiting founder GO to run.** Correction to an earlier wrong belief: **PROVEN Meesho scraper tooling DOES exist** at `backend/scripts/` — `meesho_batch_scraper.py` (the gold standard; drained all 3,772 XLSX templates) + `meesho_template_scraper.py`. My 2026-06-10 knowledge-sync note "no scraper tooling exists" was scoped to root `scripts/` and WRONG. The **Akamai bypass that works**: WebKit + login with `.meesho_creds.env` (MEESHO_USERNAME/PASSWORD, in the MASTER tree root) → authenticated `BrowserContext` carries Akamai-valid cookies + TLS/JA3 fingerprint → call Meesho APIs via `ctx.request.get/post(...)` (rides the real-browser fingerprint; plain curl/httpx gets 463/403 — which is exactly why the first scraper-maintainer dispatch's server-side curl failed). `headless=True` works. Constants: `SUPPLIER_ID=4359160`, `IDENTIFIER="oinpw"`; hard-stops 401/403/463/429. The category tree came from this method (`api:bulkCatalogUpload/fetchCategoryTreeOld`). **Prepared (commit 2940578):** `backend/scripts/meesho_commission_scraper.py` (discovery mode auto-logs candidate referral-fee/commission endpoints under `/panel/v3/new/...`; direct mode harvests once endpoint set; multi-key parser; leaf-mapping proposer) + `docs/plans/architecture/CATEGORY_SEEDING_COMMISSION_RUNBOOK.md`. NOT yet run — it logs into the founder's REAL supplier account, so needs explicit founder GO + creds confirmation + a venv with `playwright`+`playwright install webkit`. Mapping caveat: Meesho publishes commission at category/sub-category granularity, not per-leaf — 5 overlapping super clusters (Personal Care/Home/Kids/Automotive/Office-Craft) need founder disambiguation. Post-capture: data-lead reviews → authors `scripts/seed_category_commissions.py` → idempotent backfill of `commission_pct`.

**Wave 1.5 Run-1 discovery (2026-06-16, founder GO given):** the capture METHOD IS PROVEN — `meesho_commission_scraper.py` logged into the real supplier account cleanly via the authenticated WebKit context; Akamai passed it through (`ctx.request`→404 not 403, NOT a WAF block). Prereqs all present: creds at `~/Project/mesell/.meesho_creds.env`; `backend/.venv` has playwright 1.60.0 + webkit-2287. **The real blocker is a FOUNDER ACTION, not tooling:** the supplier account has `is_agreement_accepted=false` — Meesho gates the referral-fee/commission page behind an **e-signature/agreement**, so the commission component never mounts and no commission XHR fires (35 direct endpoint probes → all 404; the page only fires generic supplier-config XHRs). Account exposes only `default_monetization_percent=4.0` (account-level flat rate, NOT the category rate-card). **QUEUED (founder chose 're-run the scraper'):** founder completes the "Add Signature" e-signature flow in `supplier.meesho.com` (interactive, only they can) → signals done → I re-dispatch scraper-maintainer for Run-1-rerun (captures endpoint on first load) + harvest → review leaf-mapping → author `scripts/seed_category_commissions.py` → idempotent `commission_pct` backfill. Discovery log: `logs/scraper/commission_2026-06-16_14-27.log` (gitignored).

**Wave 1.5 CLOSED — won't-fix (2026-06-16). KEY LEARNING: Meesho commission is DYNAMIC, not a seedable static value.** After 3 authenticated scrape logins (all gated by `is_agreement_accepted=false`; only an account-level flat `default_monetization_percent=4.0` ever surfaced, no category rate-card), the founder noted commission **differs per product and per date at catalog-upload time**. Internet cross-verification CONFIRMED it: Meesho commission varies by **category × selling-price slab × time-bound promotions** — "0% on most categories", 1–5% on some, no stable published rate-card (sources: novelwebcreation, infobeamsolution, digicommerce; official `supplier.meesho.com/pricing` is panel-gated). So a static per-category `commission_pct` is **wrong by construction**. FOUNDER DECISION: keep `commission_pct = NULL` (Wave-1 shipped state); pricing engine's NULL-tolerant 422 stands. Real commission, if ever needed, = **per-product capture at catalog-upload time** on the product/pricing record (a pricing-feature design item, NOT seeded reference data). Also a process lesson: the scraper's `…/growth/oinpw/referral-fee` candidate URLs were **agent-GUESSED** (pattern off the real dashboard URL), never confirmed pages — Meesho's SPA returns 200 for any path, which masked that. Don't present guessed URLs as real. Scrape scripts (`meesho_commission_scraper.py`, `meesho_commission_rerun.py`) + `category_commissions.json` (now `0.4.0-CLOSED`, DO-NOT-SEED) retained as investigation record only.

**Effort status (DONE):** Waves 0,1,3 COMPLETE (local visual gate unblocked + wizard proven end-to-end); Wave 2 SKIPPED (D1); Wave 1.5 CLOSED won't-fix (commission is dynamic). `commission_pct` stays NULL — final. The ONE remaining founder action: **merge PR #245** (lands `make seed` + import fix into develop). Nothing else open.

### 2026-06-18 — Settlement/payout READ-ONLY investigation (mesell-price-calculator-data-session-1)

**Effort:** founder-dispatched read-only investigation to extract the REAL Meesho payout/fee structure (the deduction side NOT in the catalog-upload XLSX) to fix the Price Calculator model. No code, no PR. New script `backend/scripts/meesho_settlement_probe.py` (READ-ONLY: navigates panel pages + intercepts XHR responses; never mutates; auto-detects supplier id; hard-stops on OTP/401/403/429).

**Login + auto-detect (PROVEN AGAIN):** the authenticated-WebKit + Akamai-bypass idiom works. Auto-detected `supplier_id=3661229` (MATCHES this dispatch's brief), `identifier=kdwec` (NOTE: different from the old `oinpw`/4359160 hardcoded in meesho_batch_scraper.py — that was a DIFFERENT account; the identifier→id mapping comes from `fetch-registration-status.decodedToken.supplier_identifier_to_id_mapping`). Account email vaishnaviramoorthy@gmail.com, registration_status=SUPPLIER_CREATED.

**KEY FINDING — e-signature wall is STILL UP (same as Wave-1.5):** `is_agreement_accepted=false` / `agreement_accepted="0"`. This gates BOTH the category commission rate-card AND the per-order settlement/payments breakdown. On the `payments` panel page ZERO settlement/transaction/payout XHRs fired — the monetization+payments subsystem doesn't mount without agreement acceptance. Account also has **0 orders** (`fetch-home` Orders="0") → no settled order to inspect even if the wall were down. So the ground-truth per-order P&L is doubly unreachable: e-sig wall + no orders.

**What the account-level config DOES expose (captured from `prefetch-supply-data`):**
- `default_monetization_percent="4.0"`, `default_monetization_type="1"` — account-level FLAT commission, NOT a category rate-card.
- `mall_commission_rate=0` (not a Meesho Mall seller).
- `shipping_charges="70"`, `shipping_bracket="1000"`, `sscat_shipping_adjustment=true`, `shipping_rating="4.0"`.
- `logistic_fee_enabled="1"` since `2025-12-20` — seller NOW bears a logistics fee (newish; relevant to P&L).
- `cod_charges="0"`, `cancellation_penalty="default"`, `pay_tds="default"`, `exchange_penalty_disabled="0"`.
- `gst_type="ENROLMENT"`, `gstin` present (GST-registered seller).
- RTO real data (`fetch-growth-overview`): of 15 products → cod_rto=19, prepaid_rto=2, wdrp_returns=3, all_returns=8, avg_price=69, recommended_wdrp_discount=5%.

**CONFIRMS + EXTENDS Wave-1.5 conclusion (commission is DYNAMIC, not seedable):** the panel still has no static per-category rate-card. Real payout = MeeshoPrice − [dynamic commission (category × price-slab × promo) + shipping/logistics (~₹70, bracketed) + RTO loss + GST-on-fee (18%) + TCS(1%)/TDS]. NONE of these are static reference data. Pricing engine's current model (single derived MRP, commission from categories table=NULL, NO shipping/RTO/fixed-fee/PG-fee/TCS/TDS) is structurally wrong. **Recommendation handed to BACKEND:** Price Calculator must (a) take commission as a SELLER-INPUT (cannot source it), defaulting to 4% account flat / category hint; (b) add shipping/logistics, RTO-weighted loss, fixed/collection fee, GST-on-fee, TCS/TDS as explicit deduction line-items; (c) compute payout from Meesho Price not MRP; (d) model the 3-price structure (MRP/Meesho Price/Wrong-Defective Price) — currently meesho_price=mrp.

**Safety:** stopped on Akamai 403 (repeat `prefetch-supply-data` call after capture) per stop condition; only 1 transient 463 (tolerated). No mutation. Log `logs/scraper/settlement_2026-06-18_*.log` (gitignored). No creds logged.

**OPEN founder actions:** (1) complete e-signature in supplier.meesho.com AND have ≥1 delivered order → only then can the true settlement breakdown be captured; (2) decide Price-Calculator model = seller-input commission + deduction line-items (recommended) vs wait for e-sig+order.

### 2026-06-18 — Catalog EDIT/CREATE commission re-scrape (mesell-price-calculator-data-session-3) — BREAKTHROUGH per-product payout source

**Effort:** founder redirected the commission source AWAY from the e-sig-blocked referral-fee rate-card TO the catalog EDIT view (per-product commission) + CREATE flow (live payout preview). READ-ONLY. New probe `backend/scripts/meesho_catalog_commission_probe.py` (navigates listing→edit→create, intercepts XHR, scrapes DOM commission %/₹, ZERO mutating clicks). 1 authenticated WebKit run (proven Akamai-bypass). No code/PR; script untracked (no git in master tree). Log `logs/scraper/catalog_commission_2026-06-18_14-46.log` (gitignored).

**THE KEY FINDING (remember this — it changes the pricing model):** the catalog LISTING fires `GET /api/growth/activation/fetch-supplier-products` (200) which returns, PER PRODUCT, the real economics and is reachable WITHOUT the e-signature wall:
- `current_price` = listing (Meesho/buyer) price
- `current_transfer_price` = **SELLER PAYOUT** (price net of ALL Meesho deductions: commission+shipping+logistics+fees, BLENDED)
- `current_wdrp_price` / `current_wdrp_transfer_price` = wrong-defective-return price band + its payout
- `minimum_recommendation_price` / `minimum_wdrp_price`
Sample product: price 106 → transfer 47 (≈56% blended deduction, NOT flat 4%). Sibling XHR: `GET /api/services/ipp/fetch-smart-pricing-products`. Effective per-product deduction = `current_price − current_transfer_price`.

**This RESOLVES the "where does real payout come from" question that rounds 1-2 couldn't:** the answer is per-product `transfer_price` from the catalog/listing data, NOT a per-category commission rate-card (which never existed — Wave 1.5) and NOT the settlement page (e-sig blocked — round 1). transfer_price already nets everything together and varies per product/date — fully consistent with the locked "commission is dynamic, not seedable" conclusion. So: existing products → use transfer_price as ground-truth payout; NEW products (no transfer yet) → seller-input commission (default 4%) + deduction line-items. STILL no static category commission_pct → stays NULL (founder-locked, unchanged).

**Account facts re-confirmed:** supplier_id=3661229, identifier=kdwec, name="Curl Candy", phone 8220476727, email vaishnaviramoorthy@gmail.com, registration_status=SUPPLIER_CREATED. `is_agreement_accepted=false` STILL (agreement_accepted="0", agreement_accepted_ip="default"). Flat default_monetization_percent="4.0", mall_commission_rate=0, shipping_charges="70", shipping_bracket="1000", logistic_fee_enabled="1", cod_charges=0.

**NOT reached this run (open follow-up):** edit-view DOM commission display + create-flow live payout widget — probe hard-stopped on Akamai 403 (repeat prefetch-supply-data on the 2nd listing nav). Pacing rule honored (did NOT hammer). To get the FULL per-product table (all ~15 catalogs WITH category names) + confirm the create-flow "you'll receive ₹X" widget + its endpoint: a FRESH PACED re-run of the probe (snapshot-regex now patched to capture the full fetch-supplier-products body and reach phases 2-3). Not blocking the model decision.

**Lessons:** (1) the catalog/listing surface is a richer, e-sig-FREE economic source than the referral-fee/settlement pages — try it FIRST next time, not last. (2) Akamai 403s on the 2nd repeat of prefetch-supply-data within one session — minimize repeat navigations that re-trigger prefetch; one listing nav is enough to capture fetch-supplier-products, then go straight to edit/create. (3) ZERO writes confirmed: probe issues no Save/Update/Submit/Publish clicks; only nav + response intercept + DOM text reads.

**Hand-off:** `handoff_pricing_transfer_price.md` → BACKEND lead; inter-lead row OPEN on feature_board_data.md (48h SLA).

### 2026-06-18 — DEEP re-scrape round 4: FULL payout table + e-sig re-diagnosis (mesell-price-calculator-data-session-4)

**Effort:** founder-dispatched DEEP read-only re-scrape (account 8220476727 / supplier 3661229 / kdwec / Curl Candy). TWO goals: (1) capture the FULL `fetch-supplier-products` body for all products (round-3 lost it to a 403), (2) re-diagnose the rate-card/settlement blocker now that founder says e-sig is DONE. New probes `backend/scripts/meesho_full_payout_probe.py` (persists raw body on FIRST arrival so a later 403 can't lose it; navigates fewest pages; retries Goal-2 only after Goal-1 captured) + `backend/scripts/meesho_goal2_diag.py` (captures EVERY XHR + DOM text on the referral-fee/payments pages). 2 authenticated WebKit runs, **ZERO Akamai 403** (only 1 tolerated 463 at login), no hard stop, zero mutations. Scripts untracked (no git in master tree). Raw artifacts gitignored: `logs/scraper/fetch_supplier_products_2026-06-18_17-52.json` (46KB, all 14 products), `status_snapshots_2026-06-18_17-52.json`.

**GOAL 1 — THE PATTERN (this CORRECTS round-3's "56% blended deduction" misread — remember this):** `fetch-supplier-products` returns `data.items[]` (total_entities=14, 1 page) with RICHER structure than round-3 parsed. Each item has BOTH `customer_price_details` AND `seller_price_details` (distinct objects!) plus top-level `current_price`/`current_transfer_price`/`shipping_charges`/`current_wdrp_*`/category_id/sku_id/name. The economics are a **FLAT-FEE structure, NOT a percentage**:
- `customer_price = seller_price + shipping_charges` — EXACT (56+50=106, 70+50=120, 81+54=135 …)
- `payout (current_transfer_price) = seller_price − ₹9` — **EXACT ₹9 FLAT** across ALL 14 products (seller-price 43→86, BOTH categories 753948 & 754435, AND the WDRP band: seller_wdrp−9=payout_wdrp)
- The "variable deduction %" (43–63% of customer, 10.5–20.9% of seller) was an ARTIFACT of a CONSTANT ₹9 over different prices. Round-3's 106→47 was NOT 56% — it was seller-set-56, fee-₹9, payout-47. Shipping (₹50–54) is BUYER-paid pass-through (Meesho→courier), neutral to the seller — dividing the customer↔payout gap by customer_price double-counts shipping and overstates the deduction. **Lesson: capture the FULL payload before interpreting a single field; `current_price` is the CUSTOMER price, the seller's basis is `seller_price_details.current_price`.**
- **Calibration verdict:** single-point does NOT generalize as a %; it DOES generalize as `payout = seller_price − small_flat_fee` for this band. ₹0 category commission + a flat ~₹9 collection/handling fee (matches public reports of 0% Meesho commission on many low-price categories). Only 2 low-price hair-accessory cats sampled → the flat fee may vary by category/price-slab at scale, but the MODEL SHAPE is flat-fee + pass-through-shipping, NOT %-commission.

**GOAL 2 — e-sig is NOT done server-side (founder belief mistaken/unsynced).** Not one stale flag but a CONSISTENT CLUSTER of unsigned-state fields in `prefetch-supply-data`: is_agreement_accepted=false, agreement_accepted="0", agreement_accepted_ip="default", agreement_accepted_time="default", `enable_supplier_signature_v2=false`, and crucially the clickwrap fields are ALL empty placeholders — `clickwrap_signing_name="-"`, `clickwrap_mobile_number="-"`, `clickwrap_business_registered_name="-"`, `clickwrap_gstin="-"` (smoking gun: no clickwrap agreement ever recorded). KYC/bank/GST ARE done (bank_details_status="approved", gstin_otp_verification_status=true, gst_type="ENROLMENT", registration_status="SUPPLIER_CREATED"). **WHY rate-card/settlement don't load (HTTP evidence via goal2_diag):** `/growth/kdwec/referral-fee` and `/payments/kdwec/payments` rendered ONLY the nav-chrome shell (sidebar menu text), title="Meesho | Supplier Panel", esig_wall_text=False, and fired ONLY the 5 universal container XHRs (prefetch-supply-data, supplier/config, notices fetch-unread-count, fetch-total-count, live-optin-event) — **ZERO rate-card XHR, ZERO settlement/payments XHR; the content component never mounts. ROOT CAUSE = (1)** those deep-link URLs are AGENT-GUESSED routes — the SPA serves its shell for ANY path and the inner router finds no matching content component, so nothing fires (sidebar shows real "Payments"/"Pricing" menu items → the genuine pages are reached by sidebar nav, not guessed URLs; SAME lesson as Wave-1.5's guessed referral-fee URLs) **+ (2)** the agreement is genuinely unsigned. So: NOT an "e-sig wall" in the simple sense — it's wrong-route + unsigned-agreement, and settlement is doubly unreachable (no real route hit + 0 delivered orders → nothing to settle). **The flat-fee Goal-1 finding came from the e-sig-FREE `fetch-supplier-products`, so pricing is NOT blocked by the e-sig question.**

**Hand-offs:** appended a ROUND-4 CORRECTION to `handoff_pricing_transfer_price.md` (flat-fee supersedes the %-blended framing) → BACKEND lead; inter-lead row stays OPEN (48h SLA). **OPEN founder actions:** (1) verify the e-signature actually persisted in supplier.meesho.com — server clickwrap fields are "-"; (2) confirm pricing model = `payout = seller_price − small flat fee` (NOT %-off-MRP) + buyer-side shipping line + seller-input fee/line-items for NEW products. **Process lessons:** capture full payload before interpreting one field; never present guessed SPA URLs as real pages; persisting raw on FIRST arrival (not at end) is what saved Goal-1 this round.

### 2026-06-18 — NEW-account install (8754713794) + single-catalog commission proof (mesell-price-calculator-data-session-6) — preview is IMAGE-GATED

**PART 1 — credential installed.** Founder gave a NEW Meesho cred (username 8754713794) → replaced 8220476727 in `.meesho_creds.env`. Verified still gitignored (`git check-ignore` returns the path), untracked, never committed, values never echoed. **This NEW cred maps to a DIFFERENT supplier than rounds 1-5:** auto-detected `supplier_id=4359160`, `identifier=oinpw`, **store name="meesell"**, email kskarthick93@gmail.com — i.e. the SAME 4359160/oinpw pair that was hardcoded in `backend/scripts/meesho_batch_scraper.py` (the original catalog-tree scraper account), NOT the Curl Candy / kdwec / 3661229 / vaishnaviramoorthy account of rounds 1-5. So we now have TWO known supplier accounts; the identifier→id mapping always comes from `fetch-registration-status.decodedToken.supplier_identifier_to_id_mapping`. This account: registration_status=SUPPLIER_CREATED, `is_agreement_accepted=false`, flat `default_monetization_percent="4.0"`, mall_commission_rate=0, shipping_charges="70", shipping_bracket="1000", logistic_fee_enabled="1", cod=0, gst_type=ENROLMENT — i.e. the SAME e-sig-wall + flat-4% + ₹70-shipping config as the other account (Meesho account-level defaults, not per-account-special).

**PART 2 — single-catalog commission proof. KEY FINDING: the create-flow live payout preview is IMAGE-GATED, NOT read-only-reachable.** New probe `backend/scripts/meesho_create_preview_probe.py` (READ-ONLY, NO-SUBMIT; sidebar-nav→category→price-fill, strict persist-verb deny-list, abandon-on-done). 3 paced authenticated WebKit runs, ZERO Akamai 403, ZERO hard stops, ZERO mutations.
- Reached ADD-SINGLE-CATALOG by REAL sidebar nav (lesson honored): real URL `https://supplier.meesho.com/panel/v3/new/cataloging/oinpw/catalogs/single/select-category`. (First sidebar attempt hit the BULK CSV flow `/catalogs/bulk/add` — refined to prefer the single-product control + skip any "bulk"-labelled control. Bulk = CSV, no live preview.)
- Selected ONE category: **"Hair Spa"** (Personal Care > Hair Care > Hair Spa). Category tree = `GET /api/cataloging/bulkCatalogUpload/fetchCategoryTreeOld` (200); also `GET /api/cataloging/catalog-upload/fetch-sscat-image` (200, returns `required_images_count`).
- **THE WIZARD ORDER (remember this): `select-category → MANDATORY product-image upload → product details/pricing`.** Immediately after category select, the page exposes ONLY buttons [Search Category, Add Product Images, View Full Image Guidelines, Discard Catalog] + ONE file input. NO Next/Continue, NO price field, NO step indicator. The PRICE section — and therefore any live commission/payout preview + its computing XHR — only renders AFTER you upload product images and advance. Uploading images + advancing = building a listing = forbidden by the guardrail. So no price was entered, no preview/computing XHR captured.
- **VERDICT: commission preview is NOT pre-submit read-only-safe in the create flow — it is post-image-gate.** Category-specificity undeterminable (never reached price step). **NOT templatizable** for a paced category-wise sampling run via the create flow (every category would need real image uploads to reach price → creates listing artifacts). The proven e-sig-FREE programmatic payout source stays round-4's `GET /api/growth/activation/fetch-supplier-products` → per-product `current_transfer_price` (payout = seller_price − ~₹9 flat for sampled low-price cats) — but that's EXISTING products only, not a NEW-product live preview.

**Process notes:** (1) Meesho's single-product create flow is image-first, price-later — any "live payout preview as you set price" lives behind mandatory image upload, so it's structurally unreachable read-only. If founder still wants the NEW-product preview, it needs an explicit ruling + a controlled approach that NEVER persists (out of current read-only scope). (2) Sidebar-nav lesson held again: guessed deep-links serve the SPA shell; the real `single/select-category` page was only reached by clicking the actual "Add Single Catalog" menu control. (3) Skip "bulk"-labelled add controls — bulk is CSV, no preview. (4) Two supplier accounts now known: 4359160/oinpw/"meesell" (cred 8754713794) and 3661229/kdwec/"Curl Candy" (cred 8220476727). Both behind the same e-sig wall. (5) No new backend handoff this round — finding reinforces the existing transfer_price handoff.

### 2026-06-18 — option-2 read-the-preview proof: image gate PASSED, create flow has NO commission preview (mesell-price-calculator-data-session-7)

**Effort:** founder REVISED the guardrail (option 2) from rounds 1-6: now AUTHORIZED to upload a placeholder image, advance the create wizard PAST the image gate r6 hit, enter a test price + minimal fields to trigger the commission/payout preview, READ + capture it, then **Discard Catalog**. ABSOLUTE hard line unchanged: never Publish/Submit/Submit-for-QC/List/Go-Live. Account 8754713794 → supplier 4359160/oinpw/"meesell". New probe `backend/scripts/meesho_preview_capture_probe.py` (extends r6's create_preview probe with image-upload + advance + Size-select + price-fill + Discard). Placeholder generated with Pillow (`/tmp/meesell_placeholder_1200.jpg`, 1200x1200 white JPEG + black square, 24KB). 8 paced authenticated WebKit runs, ZERO Akamai 403, ZERO hard stops, ZERO submits/go-live; 1 tolerated 463 per login.

**THE IMAGE GATE IS EASILY PASSED (this is the new capability vs r6):** set the placeholder on `input[type=file]` #0 → thumbnail renders instantly → `POST uploadSingleCatalogImages` (200, body `{image}`). 1 image satisfies `required_images_count` (=1 for Hair Spa/Hair Bands; from `fetch-sscat-image`). Clicking **"Continue"** then advances to the "Add Product Details" step and fires the form-schema XHRs.

**THE DECISIVE FINDING (remember this — it KILLS the create-flow-commission idea):** the single-catalog CREATE FLOW HAS NO COMMISSION / PAYOUT / TRANSFER / NET-AMOUNT / REFERRAL-FEE PREVIEW. The price section is `product_size_data` inside `POST singleCatalogUploadDesktop/fetchProductDetailsV3`, and it is EXACTLY 4 raw inputs: `meesho_price` (Meesho Price, NUMBER, min2/max10000, mandatory), `only_wrong_return_price` (Wrong/Defective Returns Price, NUMBER, optional), `product_mrp` (MRP, NUMBER, max30000, mandatory), `inventory` (NUMBER, mandatory). A grep of the FULL 46KB fetchProductDetailsV3 body for commission|payout|transfer|referral|monetiz|net_amount|settle|margin returned ZERO hits (only "only_wrong_return_price"). Meesho's create flow collects raw seller prices ONLY — it never shows a "you'll receive ₹X"/commission/payout line at upload time. The only price-namespace XHR reachable is `priceRecommendation/fetchDuplicatePid` (returns `{duplicate_pid, wu_shipping_charge:57}` — a duplicate-PID + shipping check, NOT a payout compute). No second priceRecommendation variant fired.

**The price INPUT cells are deeply gated (process detail):** even after the image gate + selecting Size="L", the Meesho Price/MRP/WDRP/Inventory input cells did NOT render (the only visible number inputs were product_weight_in_gms, manufacturer_pincode, packer_pincode). They are per-size table columns that surface only after the full ~30-field mandatory form is satisfied (Size + ~12 mandatory category-attribute dropdowns: Concern/Flavour/Generic Name/Hair Type for Hair Spa, Color/Material/Pattern/Occasion for Hair Bands + the 9-field Manufacturer/Packer/Importer compliance block). So the price field is gated behind the whole form AND even then carries no payout line.

**VERDICT — paced category-wise sampling run via the create flow = NOT VIABLE.** No live commission/payout preview exists to sample; reaching even the raw price inputs needs a full gated form per category; each attempt is a live-account interaction that triggers Akamai bot-sensor POSTs (the obfuscated `/6Vl2wCwO/...` path — benign 200s, but keep runs paced). The proven e-sig-FREE programmatic payout source STAYS round-4's `GET /api/growth/activation/fetch-supplier-products` → per-product `current_transfer_price` (payout net of all deductions; sampled low-price cats: payout = seller_price − ~₹9 flat) — EXISTING products only. For NEW products: seller-input commission (default 4%) + deduction line-items.

**What the create flow IS good for (silver lining):** the recipe login→Add Single Catalog (sidebar)→search+select category→upload placeholder image→Continue→read `fetchProductDetailsV3`→Discard cleanly yields, per category and WITHOUT submitting, that category's FULL field SCHEMA — including the price-field definitions (meesho_price/product_mrp/only_wrong_return_price/inventory with min/max/descriptions) and all mandatory attribute fields. This is a clean read-only per-category SCHEMA source (alternative to the XLSX corpus) — but it does NOT carry commission.

**SAFETY (proven clean):** `getCatalogUploadPerformance` shows total_upload_count=0 / single_upload_count=0 across ALL 8 runs start→finish → NOTHING went live, no catalog created, no QC submission. "Submit Catalog" NEVER clicked (only "Continue" to pass the image gate, then "Discard Catalog"). Placeholder images live only in the uncommitted wizard staging buffer of a never-submitted catalog. Probe untracked (no git in master tree); logs/screenshots gitignored; no creds logged.

**Process lessons:** (1) the image gate is trivial to pass (1 placeholder, set_input_files on file-input #0, Continue) — the REAL gate to the price field is the full mandatory product-details form, not the image. (2) The create flow has no payout preview at all — don't chase it again; payout = fetch-supplier-products.transfer_price (existing) or seller-input (new). (3) fetchProductDetailsV3 is a clean per-category schema source reachable read-only by passing the image gate then discarding. (4) Akamai sensor POSTs appear once you start typing in the form — pace runs, don't loop.

**Hand-off:** no NEW backend handoff — round 7 REINFORCES the existing `handoff_pricing_transfer_price.md` (create flow confirmed to have no commission, so transfer_price is the only programmatic payout source). Inter-lead row stays OPEN (48h SLA).

### 2026-06-18 — ROUND 8: OTP-relay built + DECISIVE "no right-side tax card in create flow" (mesell-price-calculator-data-session-8)

**Effort:** founder ROUND-2 of option-2 — round 7 reached "Add Product Details" but never reached the price input cells, so never triggered the RIGHT-SIDE TAX DETAIL CARD the founder asserts recalculates on price change. This round: OTP-relay login (account now allegedly needs SMS OTP) + FILL the FULL ~30-field mandatory form until price cells render + enter prices (150/400/140/5) + change Meesho price to 300 to watch the card recalc + capture the card + its computing XHR + Discard. Account 8754713794 → supplier 4359160/oinpw/store "meesell". New probe `backend/scripts/meesho_taxcard_probe.py` (OTP-relay + full-form-fill + dual-price capture + safety verify; untracked). Placeholder /tmp/meesell_placeholder_1200.jpg. Category "Hair Bands" (Women Fashion>Accessories>Hair Accessories>Hair Bands). 3 paced auth-WebKit runs today (a sibling session also ran the probe at 20-16/20-22; mine 20-34). ZERO Akamai 403, ZERO Submit/Publish/QC/Go-Live; 1 tolerated 463 per login.

**OTP RELAY — built + armed, but NOT EXERCISED.** Cleared /tmp/meesho_otp.txt at start; on an OTP page the probe prints `OTP_PAGE_REACHED — waiting for /tmp/meesho_otp.txt` and polls every 5s for up to 5 min for a 6-digit code, types it (handles both single-box and 6-split-box shapes), submits. BUT: the authenticated-WebKit context logged STRAIGHT THROUGH to /panel/v3/new/growth/oinpw/home with NO OTP page — the Akamai-valid session cookies + real-browser fingerprint accepted username+password directly. So OTP_PAGE_REACHED never fired this round. The relay is ready if a future COLD login (cookies expired) triggers SMS OTP. **Lesson: this account's WebKit-context login does NOT reliably hit an OTP wall when cookies are warm — don't assume OTP every time.**

**★ THE DECISIVE FINDING (remember this — it definitively kills the create-flow tax-card idea): the single-catalog CREATE FLOW HAS NO RIGHT-SIDE TAX DETAIL CARD, AND THE PRICE INPUT CELLS NEVER RENDER, on the product-details step — even with the ENTIRE mandatory form filled green.** The sibling 20-22 run filled ALL 12 attribute dropdowns (color/generic_name/ideal_for/material/multipack/occasion/pattern/product_dimension_unit/type/country_of_origin/brand/size=L) + the 9-field Manufacturer/Packer/Importer compliance block + weight + pincodes — a fully-completed form (screenshot taxcard_p1_150.png) — and STILL: the rendered DOM "Product, Size and Inventory" section contains ONLY Net Weight / Style code / Product Name / Size; there is ZERO "Meesho Price"/"MRP"/"Wrong-Defective"/"Inventory" input cell visible ANYWHERE; and the RIGHT RAIL shows ONLY "Image Guidelines" — no commission/payout/settlement/tax card. The only forward control is "Submit Catalog" (the forbidden go-live action). So the price inputs + any tax card are gated BEHIND "Submit Catalog", OR they simply don't exist in the create flow (consistent with round-7's schema finding). **Schema confirms:** fetchProductDetailsV3.product_size_data = EXACTLY 4 raw NUMBER inputs (meesho_price min2/max10000 mandatory; only_wrong_return_price optional; product_mrp max30000 mandatory; inventory min1/max1000 mandatory) — and a grep of the full 35KB body for commission|payout|transfer|referral|monetiz|net_amount|settle|margin = ZERO real hits (the 65 "tax" hits are ALL `is_taxonomy_attribute`/`is_psm_taxonomy_attribute` field metadata + the MRP description "...including taxes..."). The only price-namespace XHR is priceRecommendation/fetchDuplicatePid (dup-PID + wu_shipping_charge check, NOT a payout/tax compute). There is no tax-card computing XHR because there is no tax card. **So the founder's premise ("a right-side tax detail card that updates with price") does NOT hold for the single-catalog create flow on this account/category.** (Where a "you'll receive" figure DOES exist = the catalog LISTING surface for EXISTING products, fetch-supplier-products.current_transfer_price — round 4.)

**⚠️ SAFETY FINDING — total_upload_count is NOT 0 (it is 2), but NOTHING WENT LIVE. Remember the semantics:** getCatalogUploadPerformance.single_upload_count/total_upload_count counts catalog UPLOAD-STARTS (draft creation when you advance PAST the image gate via "Continue" to product-details), NOT live/published listings. Timeline proves it: 0 (before any run) → 1 (start of 20-22, i.e. after 20-16) → 2 (start of my 20-34, i.e. after 20-22). +1 per run that reached product-details. NO run EVER clicked Submit/Publish/QC/Go-Live (grep-verified across all logs); each clicked "Discard Catalog" — but the counter is cumulative attempts and does NOT decrement on discard. fetchBulkCatalogsListV2 file_count=0; nothing sellable/active. **So advancing past the image gate to reach the form is INHERENTLY a counted "upload-start" — the founder's "total_upload_count stays 0" criterion is technically unmeetable IF the task requires reaching the product-details form (you must click Continue, which counts).** Reaching the form ≠ going live; no listing is live. Recommend the founder verify the 2 discarded drafts are inactive in supplier.meesho.com. **Process lesson: clicking "Continue" past the image step IS the line that increments total_upload_count — if a future task truly needs upload_count=0, it cannot pass the image gate at all (so it cannot reach the form/price/tax). r6's image-gated finding + this counter finding together = the create-flow price/tax step is unreachable without leaving a draft footprint.**

**VERDICT (final on the create-flow tax card):** paced category-wise sampling via the create flow = NOT VIABLE — there is no tax/payout card to sample, the price cells don't render pre-Submit, and each attempt leaves a counted draft. The programmatic payout source STAYS round-4's `GET /api/growth/activation/fetch-supplier-products` → per-product current_transfer_price (payout net of all deductions; sampled low-price cats: payout = seller_price − ~₹9 flat) for EXISTING products; seller-input commission + deduction line-items for NEW products. The create flow IS a clean read-only per-category SCHEMA source (fetchProductDetailsV3, reachable by image-gate→Continue→read→Discard) but carries NO commission/tax. **Don't chase the create-flow tax card again — it doesn't exist there.**

**SAFETY (proven clean):** no Submit/Publish/Go-Live/QC in ANY run (grep-verified); Discard Catalog clicked each run; creds still gitignored (`git check-ignore` ✓), never echoed; probe untracked (no git in master tree); logs/screenshots gitignored. Artifacts (gitignored): logs/scraper/taxcard_2026-06-18_20-34.log, taxcard_xhrs_2026-06-18_20-34.json (182KB), taxcard_p1_150.png (fully-filled form screenshot, the key evidence), taxcard_no_price_cells.png, taxcard_no_price_dom.txt.

**Hand-off:** no NEW backend handoff — round 8 REINFORCES handoff_pricing_transfer_price.md (create flow confirmed to have neither commission nor a tax/settlement card). Inter-lead row stays OPEN (48h SLA). **OPEN founder actions:** (1) verify the 2 discarded drafts are inactive; (2) accept that no live tax card exists in the create flow → pricing model = transfer_price (existing) + seller-input + deduction line-items (new), payout = seller_price − flat fee + buyer-side shipping line.

### 2026-06-18 — ROUND 9: FOUNDER-CORRECTED "Free Size" — DECISIVE: Free Size is the VARIATION name, NOT a Size value (mesell-price-calculator-data-session-9)

**Effort:** founder's ROUND-9 fix of round-8. Round 8 filled the full form but the Meesho-Price/MRP/WDRP/Inventory cells never rendered; founder asserted the cause = Size was set to "L" and that I MUST select **"Free Size"** to unlock the price-detail section (and that filling the 4 prices then reveals a RIGHT-SIDE TAX DETAIL CARD). Single selector change to the round-8 probe `backend/scripts/meesho_taxcard_probe.py`: `select_a_size()` now types "free size" + explicitly clicks the Free Size option (PASS 1), falls back to first-valid + logs loudly only if absent (PASS 2). 1 paced authenticated WebKit run (21-47), ZERO Akamai 403, ZERO hard stops, ZERO Submit/Publish/QC/Go-Live; 1 tolerated 463 at login; login straight to /home (no OTP — OTP relay armed, not exercised). Account 4359160/oinpw/"meesell". Category "Hair Bands".

**★ THE DECISIVE FINDING (remember this — it explains the founder's whole premise): "Free Size" is the VARIATION ROW NAME, NOT a value in the Size attribute dropdown. The founder conflated the two.** Hard evidence from the `fetchProductDetailsV3` raw body (logs/scraper/taxcard_xhrs_2026-06-18_21-47.json):
- `variations` = `[{"id":167,"variation_name":"Free Size"}]` → the single product variation is LABELLED "Free Size" (Hair Bands is a one-variation, non-sized product).
- The Size DROPDOWN (`other_data[1]`, type DROPDOWN) has EXACTLY 7 values: **L, M, Onesize, Regular, S, Xl, Xs** — "Free Size" is NOT among them. So picking "Free Size" in the Size attribute is structurally IMPOSSIBLE for Hair Bands. The probe correctly logged "Free Size NOT found among Size options" and fell back to "L" (same as round 8 — so round 8 was NOT a mis-selection; "L" is one of the only available choices).
- `product_size_data` = 5 fields: Meesho Price (NUMBER min2/max10000 mandatory), Wrong/Defective Returns Price (NUMBER optional), MRP (NUMBER max30000 mandatory), Inventory (NUMBER min1/max1000 mandatory, "for each variation"), SKU ID (TEXT optional) — PER-VARIATION columns.

**★ The price cells STILL do not render and there is STILL no right-side tax card** on the product-details step (full DOM dump taxcard_no_price_dom.txt, 183 lines): "Product, Size and Inventory" (Net Weight/Style code/Product Name/Size) + all attributes + 9-field compliance + Other Attributes (Brand/Size/Description) + T&C; right rail = ONLY Image Guidelines; only forward control = "Submit Catalog" (forbidden). The 4 price fields exist in the SCHEMA but are gated behind "Submit Catalog" — they don't render pre-Submit regardless of Size value. Grep of ALL captured XHR for commission|payout|transfer|referral|monetiz|net_amount|settle|margin = ZERO real hits (only account-level config: mall_commission_rate=0, default_monetization_percent=4.0, referral toggles, an ad-payout tooltip). No tax-card computing XHR exists. Only price-namespace XHR = priceRecommendation/fetchDuplicatePid (dup-PID check). **So the founder's premise — "select Free Size → price cells → right-side tax card" — does NOT hold for the single-catalog create flow: Free Size is a variation label not a Size choice, the price cells are post-Submit-gated, and no tax card exists anywhere in the flow.**

**HONEST CAVEAT (don't over-claim):** this run did NOT fully fill the form — the 12 attribute dropdowns stayed empty because `fill_all_dropdowns()` doesn't match Meesho's combobox markup (these are `input[type=text]` placeholder="Select", NOT ant-select / native `<select>`). So this single run is not a clean fully-filled test. BUT the verdict is robust because: (a) round-8's sibling 20-22 run DID fully fill all 12 dropdowns + compliance and STILL got no price cells / no tax card; (b) the schema proves the price fields are post-Submit-gated; (c) Free Size is provably not a Size option for Hair Bands. If a future round wants a perfectly-filled form, fix the dropdown selector to target `input[type=text][placeholder="Select"]` + click the rendered option list — but it will not change the no-tax-card conclusion.

**SAFETY (proven clean):** total_upload_count=2 (UNCHANGED from round-8 end state — did NOT increment this run; the counter tracks upload-starts and may dedupe). fetchBulkCatalogsListV2 file_count=0 → nothing sellable/live. "Submit Catalog" NEVER clicked (only a button-inventory listing; grep-verified). Discard Catalog clicked + abandoned. Creds + logs still gitignored (git check-ignore ✓). Probe untracked (no git in master tree). Artifacts (gitignored): logs/scraper/taxcard_2026-06-18_21-47.log, taxcard_xhrs_2026-06-18_21-47.json, taxcard_no_price_dom.txt, taxcard_no_price_cells.png.

**Hand-off:** no NEW backend handoff — round 9 REINFORCES handoff_pricing_transfer_price.md (create flow confirmed to have neither commission nor a tax/settlement card; Free Size is a variation label). Inter-lead row stays OPEN (48h SLA). **OPEN founder actions:** (1) accept that no live tax card exists in the single-catalog create flow → pricing model = transfer_price (existing) + seller-input commission (default 4%) + deduction line-items (new), payout = seller_price − flat fee + buyer-side shipping line; (2) if the founder still believes a tax card exists, it is NOT on the create-flow product-details step — it would be on a POST-Submit screen or the BUYER-facing listing (both outside the read-only/no-go-live guardrail). **Process lesson: when the founder names a UI value ("Free Size"), verify it against the schema's distinct field namespaces — a `variation_name` and a Size-dropdown `value` can share a label and be different things; don't assume the founder's UI read maps to the field you're driving.**

## MEMORY.md
- [ROUND 9: FOUNDER-CORRECTED Free Size — Free Size is the VARIATION name not a Size value 2026-06-18](#2026-06-18--round-9-founder-corrected-free-size--decisive-free-size-is-the-variation-name-not-a-size-value-mesell-price-calculator-data-session-9) — Founder asserted "select Free Size (not L) to unlock the price cells + right-side tax card". DECISIVE via `fetchProductDetailsV3`: **"Free Size" is the VARIATION row name (`variations=[{id:167,variation_name:"Free Size"}]`), NOT a Size-dropdown value** — the Size DROPDOWN (`other_data[1]`) has EXACTLY 7 values L/M/Onesize/Regular/S/Xl/Xs, no Free Size. So selecting Free Size for Hair Bands is impossible; round-8's "L" was a valid forced choice, not a mistake. **Price cells STILL don't render + NO right-side tax card** (DOM: section+attrs+compliance+Brand/Size/Desc+T&C; right rail=only Image Guidelines; only forward control="Submit Catalog"). `product_size_data` defines 4 price fields + SKU as PER-VARIATION columns but they're post-Submit-gated. Zero commission/payout/tax keys in ANY XHR. Caveat: this run's dropdowns stayed empty (selector mismatch) but r8 sibling fully-filled run + schema confirm the verdict. SAFETY: upload_count=2 unchanged, file_count=0, no Submit, Discarded. Payout source stays fetch-supplier-products.transfer_price (existing) + seller-input (new). Lesson: a `variation_name` and a Size `value` can share a label — verify the founder's UI read against the schema namespace.
- [ROUND 8: OTP-relay built + DECISIVE no-tax-card-in-create-flow 2026-06-18](#2026-06-18--round-8-otp-relay-built--decisive-no-right-side-tax-card-in-create-flow-mesell-price-calculator-data-session-8) — OTP relay built + armed (clear /tmp/meesho_otp.txt, OTP_PAGE_REACHED print + 5-min poll) but NOT exercised (warm Akamai cookies → straight to /home, no OTP page). Category "Hair Bands". **DECISIVE: the create-flow product-details step has NO right-side tax card AND the Meesho-Price/MRP/WDRP/Inventory cells never render — even with the FULL ~30-field form filled green (12 dropdowns + 9 compliance + weight + pincodes); right rail = only Image Guidelines; only forward control = "Submit Catalog" (forbidden).** Schema product_size_data = 4 raw NUMBER inputs, ZERO commission/payout/tax keys (65 "tax" = is_taxonomy_attribute metadata). No tax-card computing XHR exists. **⚠️ total_upload_count=2 NOT 0, but nothing live** — the counter tracks catalog UPLOAD-STARTS (draft creation on "Continue" past the image gate), +1/run; NO Submit ever clicked; each Discarded; file_count=0. Reaching the form inherently leaves a counted draft → upload_count=0 is unmeetable if the task requires the form. Payout source stays fetch-supplier-products.transfer_price (existing) + seller-input (new). Don't chase the create-flow tax card again.
- [option-2 read-the-preview proof: image gate passed, create flow has NO commission preview 2026-06-18](#2026-06-18--option-2-read-the-preview-proof-image-gate-passed-create-flow-has-no-commission-preview-mesell-price-calculator-data-session-7) — image gate trivially PASSED (1 placeholder → uploadSingleCatalogImages 200 → Continue); reached Add-Product-Details step; **DECISIVE: the single-catalog create flow has NO commission/payout/transfer preview — price section = 4 raw inputs (Meesho Price/WDRP/MRP/Inventory), zero commission keys in the 46KB fetchProductDetailsV3 schema**; price cells gated behind ~30 mandatory fields; only price XHR = priceRecommendation/fetchDuplicatePid (dup+shipping check). Category-wise sampling NOT VIABLE (no preview to sample). Payout source stays fetch-supplier-products.transfer_price (existing) + seller-input (new). NOTHING live (upload_count=0 all 8 runs; Submit never clicked; Discard each run). fetchProductDetailsV3 = clean per-category SCHEMA source read-only.
- [NEW-account install + single-catalog commission proof 2026-06-18](#2026-06-18--new-account-install-8754713794--single-catalog-commission-proof-mesell-price-calculator-data-session-6--preview-is-image-gated) — cred 8754713794 installed (still gitignored, never echoed); maps to **supplier 4359160 / oinpw / "meesell"** (the old batch-scraper account, NOT Curl Candy/3661229). Single-product ADD-CATALOG reached by REAL sidebar nav (`/cataloging/oinpw/catalogs/single/select-category`); category "Hair Spa" selected. **Create wizard = category → MANDATORY image upload → price; the live payout preview is IMAGE-GATED, NOT read-only-reachable** → no price entered, no preview/computing XHR captured, NOT templatizable via create flow. e-sig wall present (is_agreement_accepted=false), flat 4% / ₹70 shipping. ZERO writes/submits. Programmatic payout source stays fetch-supplier-products.transfer_price (existing products).
- [DEEP re-scrape round 4 — full payout table + e-sig re-diagnosis 2026-06-18](#2026-06-18--deep-re-scrape-round-4-full-payout-table--e-sig-re-diagnosis-mesell-price-calculator-data-session-4) — FULL 14-product table captured; **payout = seller_price − ₹9 FLAT (NOT 56% / NOT %-commission)**; customer_price = seller_price + shipping (buyer-paid pass-through, neutral to seller); round-3's 56% was a misread (divided by customer_price incl. shipping). Goal 2: e-sig NOT done server-side (clickwrap fields all "-"); rate-card/settlement pages fire ZERO data XHR — root cause = guessed-route shell + unsigned agreement; pricing NOT blocked (data came from e-sig-free fetch-supplier-products). Backend handoff updated.
- [Catalog EDIT/CREATE commission re-scrape 2026-06-18](#2026-06-18--catalog-editcreate-commission-re-scrape-mesell-price-calculator-data-session-3--breakthrough-per-product-payout-source) — BREAKTHROUGH: `fetch-supplier-products` XHR returns per-product `current_transfer_price` (=seller PAYOUT net of all deductions) + `current_price`, reachable WITHOUT e-sig wall; sample 106→47 (≈56% blended, not 4%); effective deduction = price−transfer; no static category commission (transfer nets everything, dynamic); edit-DOM + create-widget NOT reached (Akamai 403, paced re-run open); handoff → backend
- [Settlement/payout investigation 2026-06-18](#2026-06-18--settlementpayout-read-only-investigation-mesell-price-calculator-data-session-1) — supplier_id 3661229 auto-detected; e-sig wall STILL UP (is_agreement_accepted=false) blocks rate-card + settlement; 0 orders; only flat default_monetization_percent=4.0 + shipping=70/bracket=1000 + logistic_fee_enabled exposed; commission confirmed dynamic; pricing model rework handed to backend
- [Category seeding Wave 0+1 2026-06-16](#2026-06-16--category-seeding-wave-0-ratified--wave-1-local-seed-landed-mesell-category-seeding-session-1) — D1 local-only / D2 real-commission-via-scrape two-phase / D3 pure-upsert; commission ABSENT from all corpus+xlsx (must scrape); seed scripts had stale app.config/app.models imports (fixed to app.shared.*); PR #245 open for founder merge; alembic head now f31c75438e61
- [Knowledge-sync survey 2026-06-10](#2026-06-10--knowledge-sync-survey-stale-stub--naming-drift-findings-mesell-knowledge-sync-data-session-1) — category_attributes.json + meesho_categories.json are stale hand-stubs; real corpus is meesho_category_tree.json + DB seed; no scraper exists yet; count tolerances locked in seed_all.py
- [Session mesell-repo-management-session-1](#session-mesell-repo-management-session-1--step-5--data-engineer-spec-evolved-into-data-lead-spec-feature_board_datamd-initialised) — Data Lead spec rewrite + feature_board_data.md initialisation per MASTER_PLAN §6 + §7
- [Founder's batch workflow preference](#2026-06-04--founders-batch-workflow-preference-locked) — full-corpus, batch-by-batch, discussion-gated
- [Workspace hook conflict fallback](#2026-06-04--workspace-hook-conflict--one-time-fallback-pattern) — when meesell-* dispatch is blocked, use nexus python-developer-agent with memory routed to meesell-*/MEMORY.md
- [SSoT integration is manual](#2026-06-04--ssot-integration-is-manual-never-automated) — coordinator + founder write it, never the specialist
- [All deliverables complete](#2026-06-04--all-data-layer-deliverables-complete) — SSoT locked 2026-06-04; downstream sessions unblocked
- [Gap analysis 2026-06-05](#2026-06-05--mvp_architecture-gap-analysis-dispatched--delivered) — 18 gaps catalogued, 4 Tier-1 BLOCKER-V1, R1–R5 reconciliation plan; root cause of doc drift = draft sections inserted without renumbering
