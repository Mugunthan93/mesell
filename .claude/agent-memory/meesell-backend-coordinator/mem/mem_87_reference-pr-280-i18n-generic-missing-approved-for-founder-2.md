## [reference] PR #280 i18n generic-missing — APPROVED-FOR-FOUNDER (2026-06-18)
Session: mesell-i18n-generic-missing-backend-session-1 (HYBRID step-3 merge-gate).
- Branch fix/i18n-generic-missing → develop (off develop@0087562, head 2dc60fd).
- Production diff = messages_en.py ONLY (+10 keys, additive, zero removals) + test_i18n_generic_fallback.py. No resolver.py / core/errors.py change — VERIFIED correct: resolver Step-2b already derives validation.generic.<rule>; the keys were simply absent. The blank-error bug was a CATALOG GAP, not a resolver bug.
- All 10 new keys 3-segment validation.generic.<rule> (Contract-10 clean). No 2-seg added. No dups.
- 5 bespoke .missing keys (validation.q.missing, catalog.draft.missing, pricing.commission.missing, export.front_image.missing, auth.token.missing) untouched + guarded by parametrized test. auth.token_missing (underscore) lives ONLY in resolver L_iam_1 allowlist, untouched.
- I re-ran the 3 test files myself in a detached worktree w/ master-tree 3.11 venv + full §5.D dummy env: 61 passed (matches builder). Founder-case test (validation.description.missing → generic string, zero missing_key logs at DEBUG) PASSED. Contract-10 gate PASSED. ruff (homebrew, --line-length 100) All checks passed.
- CI on PR: Gate1 unit PASS, Gate2 smoke PASS, Gate3 lint(10 contracts) PASS. Gate4 integration pending=advisory(§2.1).
- LESSON: master-tree .venv has NO ruff module — use /opt/homebrew/bin/ruff for lint verification. Config needs the FULL §5.D env registry (14 vars incl MSG91_TEMPLATE_ID, RAZORPAY_WEBHOOK_SECRET, GCS_PROJECT_ID, LANGFUSE_*, AUDIT_PII_SALT, CORS_ALLOWED_ORIGINS) or it FATALs before tests run.
- Verdict: APPROVE-FOR-FOUNDER. Founder owns develop merge; session never self-merges. Did NOT merge.

- **PR #285 price-calculator forward-estimator MERGE-GATE → REJECTED (2026-06-18)** (`mesell-price-calculator-backend-session-1`, HYBRID step 3): Code is excellent — independently reproduced 106→46.84 / 86→27.98 within TOLERANCE 3.00 against the REAL service module (not just PR claim); ship=70 low-band misses at −0.36 so the ₹30-low/₹70-high banding is GENUINELY required (item 4a JUSTIFIED). Zero Meesho calls under app/ (CLEAN; transfer_price/supplier.meesho.com only in test_estimator_calibration.py). CommissionMissingError fully deleted, get_commission gone from path (only docstring mentions remain), router drops 422, default 4% commission works w/o input, neg-payout returns 200 with all 3 alerts, margin+markup both present, 3-price model + full breakdown present, i18n 3-segment delta correct, migration 12 additive nullable cols + symmetric downgrade, down_rev=f31c75438e61 (correct single linear head), import-linter 27/0. **REJECT on 2 DOC/CODE defects (item 4b + doc-integrity):** (1) `docs/V1_FEATURE_SPEC.md` Feature-7 amendment line 321 says "shipping (**₹70 bracketed by price band**)" — FACTUALLY WRONG: code does ₹30 (≤1000) / ₹70 (>1000), and the ₹106 calibration sample uses ₹30. This is EXACTLY the founder's item-4b reject condition (amendment must not claim "₹70 bracketed" when code does ₹30/₹70). It's NEW PR text (original said "Shipping not included in V1"), not carried-over. Fix: rewrite to "₹30 (price ≤ ₹1000) / ₹70 (price > ₹1000)". NOTE: BACKEND_ARCHITECTURE §12.M uses the correct ABSTRACT form (SHIPPING_FLAT ≤ SHIPPING_BRACKET else SHIPPING_HIGH) + calibration test docstring is correct — only the V1 SPEC prose is wrong. (2) §2.D breakdown sentence (line 592) is SELF-CONTRADICTORY: the PR rewrote the original locked "8 ✓" sentence IN PLACE to open "7 ✓" but left it ending "...the matrix locks **8 ✓** — the minimum service-graph." Same paragraph says both 7 and 8. ALSO stale: §13 (4915,5229,5235,5366,4156) + §16 (6464,6486,6529) still assert "exactly 8 ✓ cells" — now wrong after matrix dropped to 7. LESSON: builders amending a count that's referenced in N places must grep ALL occurrences (§2.D + §13 + §16 here) — a single AMENDMENT block doesn't fix downstream prose that re-asserts the old count. LESSON: when the calibration deviates from the spec literal (₹70→₹30/₹70), the spec amendment text is the FIRST thing to reconcile, not just the code constants. Could NOT run pytest in sandbox (no local Postgres; conftest hard-requires *_test DB at import → asyncpg role "root" error) — verified the green claim by directly importing+calling the real service module instead (all values matched PR body exactly). Founder owns develop merge (D1) — I do NOT merge regardless.

- Session `mesell-price-calculator-backend-session-1` (2026-06-18) — PR #285 RE-GATE round 2 (Price Calculator forward-payout rework, §12.M) → VERDICT **APPROVE-FOR-FOUNDER**. R1 (V1_FEATURE_SPEC Feature 7 shipping prose ₹30≤₹1000/₹70-above, was "₹70 bracketed") + R2 (§2.D 8→7 cascade + §13/§16 "8 ✓" assertions → "7 ✓") both FIXED in commit `74ade7c`, verified DOCS-ONLY (`git show --stat`). **GOTCHA #N (re-gate hygiene):** the local working tree was on `develop`, so a naive `grep docs/BACKEND_ARCHITECTURE.md` read the PRE-FIX develop state and falsely showed all the "8 ✓" still present — ALWAYS grep the branch-under-review blob via `git show <branch>:<path> | grep` when verifying a PR whose branch is not checked out. **RULING precedent (doc-owner call on a pre-existing latent surfaced by a PR):** the §2.D L592 per-source enumeration (10) ≠ matrix-table ✓-cell count (10) ≠ asserted total (7) — but this gap PRE-DATES the PR (pre-PR develop: enumeration 11 vs "8 ✓", table 11). When a PR edits one line of a doc that has a pre-existing whole-section accounting inconsistency, and the PR's edit is a FAITHFUL consistent decrement of only the numbers it owns (here −1 for the retired pricing→category cell), DO NOT block the PR on the inherited gap — APPROVE and FILE a separate follow-up ticket (BE-DOC-2D-COUNT-1, P3 docs-only, §7.3-LOCKED → founder approval). The bar: did the PR introduce/widen the inconsistency? If no → ticket, don't loop. **PR #285 targets develop DIRECTLY (not a feature/{name} integration branch)** → the develop merge is the FOUNDER gate per D1; lead renders APPROVE-FOR-FOUNDER and does NOT merge. Verdict posted as PR comment + STATUS_BACKEND update + board row (price-calculator IN REVIEW→APPROVE-FOR-FOUNDER) + new "Follow-up doc tickets" board section for BE-DOC-2D-COUNT-1.

- **PR #290 catalog cross-field rule engine MERGE-GATE (2026-06-18)** (`mesell-catalog-field-dependency-rules-backend-session-1` topic; built by meesell-services-builder) → VERDICT **REQUEST CHANGES, 1 blocker (14/15 pass)**. The engine itself is excellent: 20 compliance rules (FSSAI/AYUSH/BIS/warranty/size/fabric/country-of-origin/HSN/kids-age/legal-metrology), `_predicate_met` covers eq/in/contains/any cleanly, inert-guard when target_field not in schema, `enforce_required` gate demotes hard→soft on autosave (never 422s) and only enforces on status=ready, reuses existing `ValidationFailedError` (zero new exception type), `/schema` grows a trimmed `dependency_rules[]` FE projection (excludes description/category_match/error_message), category↔catalog circular import avoided (both load the JSON independently — category never imports catalog). Apparel super_ids [10,11,29,13,25] verified PRESENT in `meesho_category_tree.json` seed (not guessed; `_meta.apparel_super_id_resolution` documents it). No migration (super_id pre-exists), no new endpoint (§17 unchanged), ruff clean, 20 i18n `validation.cross_field.{id}` keys all 3-segment-valid. **BLOCKER = test→CI invisibility:** `backend/tests/test_catalog_dependency_rules.py` has ONLY `@pytest.mark.asyncio` and NO module-level `pytestmark = pytest.mark.unit`. With `pytest.ini` `--strict-markers` + CI Gate 1 `pytest -m "unit"`, all 28 tests are DESELECTED by every gate (`no tests collected (28 deselected)` under unit/smoke/integration). They pass ONLY under an unfiltered local run (verified 28 passed in 0.03s, py3.11). So green CI would NOT cover this engine at all. Fix = add `pytestmark = pytest.mark.unit` (convention: sibling `tests/test_ai_ops_budget_cap.py:32`, `tests/modules/category/test_picker_helpers.py:27`). **NEW MERGE-GATE LESSON (recurring): "tests pass" is NOT sufficient — verify the tests are COLLECTED by the CI gate's marker filter.** Always run `pytest <file> -m "unit" --co -q` (the exact gate-1 selector) against the PR head, not a bare `pytest <file>`. A file with only `@pytest.mark.asyncio` and no `pytestmark` silently slips through `--strict-markers -m "unit"`. **ENV GOTCHA:** the new test file imports app modules → `app.shared.config` REQUIRED_FIELDS fail-fast SystemExit at settings load. To run any backend test locally you must export the CI Gate-1 dummy env block (.github/workflows/ci.yml lines 105-128: SECRET_KEY/JWT_SECRET/MSG91_*/RAZORPAY_*/REFRESH_TOKEN_PEPPER/AUDIT_PII_SALT/GEMINI_API_KEY/DATABASE_URL/VALKEY_URL/GCS_*/LANGFUSE_*/CORS_ALLOWED_ORIGINS/APP_ENV=development). **PYTHON GOTCHA:** system `python3` here is 3.9 → SQLAlchemy 2.0 `Mapped[str | None]` raises MappedAnnotationError (PEP-604 in class-body needs 3.10+). Use the project venv `/Users/mugunthansrinivasan/Project/mesell/backend/.venv/bin/python` (3.11) for any local backend test run. **GATE-OWNERSHIP:** PR #290 targets `develop` directly → develop merge is the FOUNDER gate per D1; lead renders the technical verdict only, does NOT merge. Self-approve was BLOCKED (same GitHub identity = PR author) → posted as PR issue comment (`gh pr comment`), not `gh pr review --request-changes` (which also errors on own PR). Verdict comment: https://github.com/Mugunthan93/mesell/pull/290#issuecomment-4742661839

- **google-auth backend-first SPLIT + §7.3 ratification (2026-06-18)** (FAST-MODE direct execution, no specialists). Founder wanted the combined PR #294 (8 commits, BE+FE intermixed on `feature/google-auth`) re-sequenced backend-first. Split by DISCIPLINE into two fresh branches off `origin/develop` (@4c12ded): `feat/google-auth-backend` (cherry-pick 5 BE commits 0b06c77/5474bab/86ca539/64209ff/5ad030f, all clean — one auto-merge on messages_en.py, no conflict) and `feat/google-auth-frontend` (cherry-pick 3 FE commits 3a56045/5eb575f/2f681dd, clean). The commits were DISJOINT by path (BE = backend/+CLAUDE.md+WAVE_PLAN_BACKEND.md; FE = frontend/+WAVE_PLAN_FRONTEND.md) so cherry-picks applied with zero manual resolution. Result: **PR #295 (backend, merge FIRST)** + **PR #296 (frontend, merge AFTER #295)**, both base develop, NOT merged (founder owns develop gate per D1). **#294 CLOSED** with cross-ref comment. `feature/google-auth` left on origin (harmless).
- **§7.3 amendment SHA = `5eda708`** — the formal locked-arch amendment the original #294 was MISSING. Added to BACKEND_ARCHITECTURE.md: (1) §7.A preamble note 6→7, (2) NEW §7.3 founder-ratified block (Decision #5 dual-identity; POST /api/v1/auth/google/verify; iam 6→7; §17 28→29; flag-gated FEATURE_GOOGLE_AUTH_ENABLED default False so counts stay 6/28 until enabled per namespace; adapters/google.py = 6th adapter, NO §2.D ✗→✓ since adapters aren't modules), (3) §17.B.2 + §17.G mounted-total 28→29 (flag-on) notes. Kept WORDING-CONSISTENT with the CLAUDE.md Key Decision #5 amendment already in commit 0b06c77 and design doc GOOGLE_AUTH_DESIGN_BACKEND.md §A.
- **LESSON — flag-gated endpoint count amendments:** the clean pattern for a LOCKED endpoint-count bump that ships behind a default-off flag is "X → Y (stays X until flag flipped)". This satisfies the §7.3 founder-amendment requirement WITHOUT making the boot-smoke-test assertion (§17.G "28 routes") wrong in the default state — the route isn't mounted when the flag is off, so 28 remains literally true on develop. I touched only the 2 primary count assertions (§17.B.2, §17.G) + the §7.3 block + §7.A note; deliberately did NOT churn the §17.D/§22 "all 28 routes" lines (accurate for flag-off default; §7.3 governs the increment globally). Minimal-edit discipline on a LOCKED doc.
- **GOTCHA — line-number drift across worktrees:** my pre-edit greps ran against the MASTER tree (parked on an older develop); the worktree was off the NEWER origin/develop (@4c12ded) so line numbers differed by ~30-60. The string CONTENT matched, so Edit-by-exact-string worked, but I had to Read the worktree copy first (Edit requires a prior Read of that exact file path). ALWAYS grep/Read the worktree blob you're editing, not the master-tree copy, when verifying placement.
- **VALIDATION pattern for discipline splits:** `git diff --name-only origin/develop..HEAD | grep -c "^<other-discipline>/"` must be 0 on each branch. BE branch: 0 frontend/ files (34 files, all backend/+CLAUDE.md+2 docs). FE branch: 0 backend/ files (18 files, all frontend/+1 doc). Each branch correctly carries ONLY its own WAVE_PLAN_*.md; BE branch additionally carries the BACKEND_ARCHITECTURE.md amendment.

---
