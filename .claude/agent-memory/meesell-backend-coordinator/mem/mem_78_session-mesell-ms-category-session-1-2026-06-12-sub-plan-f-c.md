## Session mesell-ms-category-session-1 — 2026-06-12 — Sub-Plan F (category extraction) AUTHORED (PHASE 1, hybrid step 1, docs-only)
Worktree `/tmp/mesell-wt/msF-docs` on `feature/microservices-category/docs-subplan0f` off origin/develop. Files written: `docs/plans/microservices_migration/SUB_PLAN_0F_category_extraction.md`, `spec_msF_backend.md`, `handoff_msF_infra.md`, board row + inter-lead row (additive F2), STATUS append. EXECUTION GATED ON MS-4 (MS-3 pricing+customer merged; parallel MS-G iam). No specialist dispatch (PHASE 1 + no Agent tool per HYBRID rule 7).
- **MS-A spec files ABSENT in worktree AND develop** (`spec_msA_backend.md` + `handoff_msA_infra.md` are untracked master-tree artifacts; `git show develop:...` = does-not-exist). Authored per common-rule-5 "if absent, author per hybrid step 1" but reproduced the two FROZEN category shim shapes verbatim from the MS-F dispatch prompt (which quotes them). LESSON: when a worktree is cut for a docs sub-plan, untracked memory files on the source tree do NOT travel — the dispatch prompt must carry frozen contracts inline (this one did). Open Question (c) flags locating the canonical MS-A doc for the implementing session.
- **category = pure CALLEE, ZERO outbound domain calls.** grep `from app.modules` inside category/{service,picker,repository}.py returns nothing outside its own subtree. So category authors NO outbound HTTP-shim clients (inverse of export pilot's 4-out/0-in). It implements server-side `/internal/*` shims for callers (catalog/export/pricing — verified imports: catalog/service.py:98, export/service.py:58, pricing/service.py:66).
- **The 2 frozen shims (NOT 3).** schema ← fetch_schema (service.py:467); field-enum ← get_field_enum (service.py:491). The MS-A correction DROPS fetch_xlsx_aliases (MASTER_PLAN §2.D line 68 implies a runtime category alias call but it's SEED-TIME only). As-built CONFIRMS: repository.py imports NO FieldAlias ORM (:42-44 = Category/FieldEnumValue/Template only); service __all__ (:615-624) = 8 methods, no fetch_xlsx_aliases. SOURCE-WINS over plan prose.
- **ai_ops VENDORING + SHARED budget brake (the load-bearing decision, D6/A1).** category is the FIRST extraction carrying real ai_ops (service.py:76-78). Vendor TRIMMED (only smart_picker_v1 prompt, not autofill/watermark). Budget brake STAYS SHARED: Valkey DB 0 (get_valkey_otp) keys `ai:cost:daily:{date}` (cost_tracker.py:92 _DAILY_KEY_FMT), `ai:cost:pending:{date}` (budget_cap.py:124), `ai:budget:reservation:{id}` (budget_cap.py:125), `ai:cost:user:{u}:hourly:{hr}` (cost_tracker.py:93); + `public.audit_events` DB (cost_tracker.py:55). **CRITICAL carve-out I derived:** §2.E (MASTER_PLAN:209) mandates per-service `{service}:` Valkey prefixing — but the `ai:*` budget keyspace MUST be EXEMPT or the global ₹500 cap splits into N per-service caps. This carve-out is NOT spelled out in MASTER_PLAN — I made it an explicit acceptance item + contract test (budget-brake-shared assertion) + infra carve-out instruction. Atomic via Lua (_RESERVE_LUA budget_cap.py:136, _RELEASE_LUA :153). The vendored brake code is byte-identical to source; cross-service serialization happens inside Valkey's single thread.
- **PRIMITIVE_VALUES lives at app/i18n/schema_contract.py:175 (NOT a category file).** 11 primitives. ENVELOPE_KEYS:147 = 7 (dispatch prompt said "6"; schema_contract.py:17-18 self-corrects to 7 — SOURCE wins). schema_contract is DOC-IN-CODE (:8-15 "No runtime business logic imports the TypedDicts") — VENDORED read-only into each AI/schema service, OWNED by none. The source-of-truth WRITER is `scripts/build_template_schemas.py` at REPO ROOT (find confirms ./scripts/..., NOT backend/) — it STAYS with the seed/data pipeline (data-engineer territory), NOT category-svc runtime. category SERVES the templates.schema_jsonb rows (templates table migrates to category schema) but does NOT own the contract or the writer.
- **DB: 4 GLOBAL tables migrate to schema `category`** (categories 3,772 leaves + templates + field_enum_values 291 blobs + field_aliases). field_aliases travels for locality but is seed-time-consumed only. pg_trgm GIN indexes (migration a1b2c3d4e5f6) must survive SET SCHEMA — assert via post-migration EXPLAIN Bitmap Index Scan. NO scope_to_user (§9.D GLOBAL exception).
- **SOURCE-WINS corrections logged in sub-plan + STATUS:** (1) filename 0F not 06; (2) dependency MS-3-parallel-MS-G not A–E-serial; (3) fetch_xlsx_aliases dropped; (4) 7 envelope keys not 6. Repeat of the Wave-6 fabricated-enum discipline — every contract cited file:line from source.
- **D7 parallel-safety:** category ‖ iam (both MS-4). Local JWT (D7) is exactly what makes parallel-extracting the auth authority + an auth-consumer safe — category validates with Secret-Manager JWT_SECRET, never calls iam-svc per-request. No request-path coupling.

---
