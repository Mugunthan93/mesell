### microservices migration (MS program)
- [recipe_ms_extraction.md](recipe_ms_extraction.md) — **VALIDATED SP01 extraction recipe (copy for waves MS-2..5).** Step sequence (branch+F3 → Phase A db ∥ infra → services → routes → lead Phase C → gates), §16.G AST diff-proof (RECURSIVE import strip — lazy imports in function bodies are the gotcha), wire-shape parity via `model_json_schema()` (NOT model_fields repr; strip `description`; sys.modules-register + model_rebuild to resolve `from __future__` ForwardRefs), frozen shim transport pattern, schema-split pointer (database-builder memory), worktree/isolation-guard relay, validation floor, per-wave variation points (callee /internal/* for C/E/F/H, ai_ops vendoring F/H per A1/D6, cookie-path G per A2/D7).
- [spec_msC_backend_EXECUTION.md](spec_msC_backend_EXECUTION.md) — **MS-C Phase 2 step-1 DONE 2026-06-13 (`mesell-ms-image-backend-session-1`).** FINAL EXECUTION SPEC (supersedes Phase-1 `spec_msC_backend.md`). Integration branch STOOD UP @ `feature/microservices-image/integration` = origin/develop tip `d4aa572` (F3 applied, pushed). `list_images` /internal shim RECONCILED vs FROZEN SHIM_CONTRACT §2.6 → ZERO drift, contract wins (keys `image_id,idx,status,signed_url,precheck_jsonb`; sig `list_images(user_id,product_id,*,db)` exact @ service.py:232; NO escalation). §0.10 RULED Option B baked at repository layer: 8 cite-sites drop `scope_to_user(select(ProductORM.id))` → scope by `product_id`/`image_id` direct; remove imports repository.py:50+:52; **§16.G byte-for-byte EXEMPT for repository.py ONLY (founder-RULED Option B requires the products-join rewrite); service.py+tasks.py STILL byte-for-byte** (only import lines, call sites :162/:248 unchanged). LIVE-STATE CORRECTIONS vs Phase-1: (a) **PR #143 MERGED** — monolith celery_app now routes image.precheck→queue `image-tasks` (celery_app.py:126-127); svc-image still gets OWN queue `svc-image`; cutover now drops THREE monolith items not two; (b) **test floor = 698** (not 649) re-counted @ d4aa572; (c) **image own tests = 14** (not 29) — layout changed. Build order: db→{svc,routes}→lead Phase C; heavy-lift owner = services-builder; auth-builder NOT needed. Group branches: db→/tmp/mesell-wt/msC-db, svc→/tmp/mesell-wt/msC-svc, routes→/tmp/mesell-wt/msC-routes, infra→infra-lead. handoff_msC_infra.md updated: I5 Option B → NO products read grant (transitional grant DISCARDED). NO escalations.
- SP01 Phase C session entry — 2026-06-12 (`mesell-ms-export-backend-session-1`, in-file below).
- **SP01 FOUNDER GATE MERGED — 2026-06-13** (`mesell-ms-export-session-1`, in-file below): PR #191 squash `79525f3` to develop; #181 max_connections terraform conflict resolved (MS-0 var-based form kept); PgBouncer + max_connections=200 LIVE on dev (#192). **MS-2 wave OPEN** (B dashboard ‖ C image). Strangler-fig: monolith export STAYS LIVE; cutover = separate founder gate.
- **MS-B (dashboard) Phase-2 COMPLETE — Phase-C docs landed — 2026-06-13** (`mesell-ms-dashboard-session-1`, worktree `/tmp/mesell-wt/msB-integration`): SECOND validated extraction; recipe held VERBATIM. Merge-gate PASSED — group PRs #195 (backend) + #196 (infra) squash-merged into `feature/microservices-dashboard/integration`; integration→develop left OPEN as the FOUNDER gate (D1 — I do NOT approve). svc-dashboard = 35 app files + 30 svc test cases green; §16.G AST + wire-shape parity PROVEN; monolith UNTOUCHED (698 `def test_` monotonic). **Dashboard variation:** owns ZERO tables → database-builder VERIFY-ONLY (NO schema-split, NO Risk#5 pre-scan, NO Alembic chain); NO Celery; NO ai_ops; LEAF consumer exposes NO `/internal/*`; lightest service (50m/128Mi api-only, ZERO D3 spend). 2 frozen shims (catalog `list_products`, customer `get_onboarding_completeness`) BOTH mock-tested (`httpx.MockTransport`) — callees still in-monolith (customer=MS-3/E, catalog=MS-5/H); NO live round-trip, NO cross-schema audit INSERT (audit_mw INERT on read-only GET). 5 Phase-C docs: board flip (keep-both header, MS-C lane untouched), STATUS block, recipe MS-B entry, MASTER_PLAN §4 row-B EXECUTED annotation + §1.C prose fix (`get_profile_completeness`→`get_onboarding_completeness`, plan-PROSE NOT §13 LOCKED) + Rev v1.4 row, `CI_HYBRID_MODE_dashboard.md` (callees composed: NONE). **3 founder-gate flags carried to PR body:** (1) §1.C prose fix for awareness; (2) `BACKEND_ARCHITECTURE.md §13` extraction note LOCKED → NOT self-applied, needs founder approval (§7.3); (3) NEW SM secret `dev-dashboard-db-password`. **LESSON:** recipe step-0 (ground-truth-against-LIVE-tree) caught the §1.C plan-text drift AGAIN — same class as SP01's 2 mis-named shims. Strangler-fig: monolith dashboard STAYS LIVE; cutover = separate founder gate.
