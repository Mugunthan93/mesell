## Session mesell-gate1-eventloop-backend-session-1 — 2026-06-12 — PR #150 landed (founder override) + Gate-4 follow-up SPEC'd
**Index:** [spec_ci_gate4_loop_contamination.md](spec_ci_gate4_loop_contamination.md) — STEP-1 spec for the Gate-4 cross-loop regression (the 6f/13e); named specialist meesell-api-routes-builder; honest-merge (no override).

**PART 1 — PR #150 (`fix/gate1-eventloop`) merged `--admin` per EXPLICIT founder authorization.** Squash SHA `2d9b8af`. My step-3 verdict was APPROVED-but-held (PR sound; only red was the pre-existing Gate-4). Founder chose "Override-land #150, then fix Gate 4". Executed: PR comment recording authorization → `gh pr merge 150 --squash --admin` → `gh api -X DELETE .../refs/heads/fix/gate1-eventloop` → worktree `/tmp/mesell-wt/gate1-fix` removed + local branch deleted. PR #150 = the answer to infra's HIGH inter-lead `handoff_ci_gate1_eventloop.md` (13 catalog-form unit tests RED with `RuntimeError: There is no current event loop` on py3.12 under live branch protection). Part A removed the stale `scope="session" def event_loop()` fixture + `import asyncio` from root conftest; Part B templated `tests/unit/test_catalog_routes.py` flag-gate. Post-merge develop Gate-1 GREEN.

**Post-merge develop run `27391715982`** (build+deploy ON per 2026-06-12 ruling): Gate 1 GREEN, Gate 2/3 GREEN, 8 frontend GREEN, **Gate 4 FAILURE** (6 failed / 162 passed / 16 skipped / 13 errors), Build container + Deploy-to-K3s SKIPPED (gated on Gate-4 → no deploy). Overall: failure (solely the pre-existing Gate-4).

**KEY LESSON (record loud): a Gate-1 fix unmasks a latent Gate-4 regression via the skip/collection cascade.** Before #150, Gate-1 died at collection (and the old `event_loop` session fixture masked loop hazards). Removing the `event_loop` fixture shifted every async fixture onto the pytest.ini `asyncio_default_fixture_loop_scope=session` default, which exposed the #115–#149-wave integration tests' cross-loop contamination. **PROTOCOL: after ANY conftest/pytest.ini change, ALWAYS re-baseline Gate-4 — do not assume the #110 saga close still holds.** The Gate-4 saga (#104→#110) reached exit-0 on the *then-visible* suite; the catalog-form/flag wave added new integration files that re-opened the lane.

**Override discipline note:** founder authorization was SCOPED to #150 only. My docs PR for this session must NOT be `--admin`'d if it inherits the Gate-4 red — hold it open and note it instead. (At authoring time the docs PR rides develop tip `fe3f3ff`; the post-#150 develop runs give green Gate-1 contexts, so the docs PR's blocker is only the still-red Gate-4 — if blocked, leave OPEN, do not override.)

**PART 2 — Gate-4 root cause (verified against merged tree `2d9b8af`, not just quoted):** integration tests driving the real FastAPI app through `BaseHTTPMiddleware` resolve `Depends(get_db)` against the **app-global** `AsyncSessionLocal`/engine (module-import-time singleton, pool bound to the first loop that touches it = the session loop via the `scope="session"` autouse seed/migration fixture) while the test runs on a **function-scoped** loop → the middleware awaits a Future born on the session loop → `got Future attached to a different loop` (+ `Event loop is closed` at teardown). The `iam_client` fixture (integration/conftest.py) HAS `loop_scope="function"` but its docstring states verbatim "Does NOT override Depends(get_db)" → that is the seam (failures 1–4). `test_shared_database.py::test_get_db_yields_async_session` calls `get_db()` directly (same app-global seam, failure 5). The customer_client/export_client twins already override get_db with a function-loop NullPool engine BUT still throw `Event loop is closed` at teardown (13 errors) because the **lifespan** state engine outlives the loop. Catalog `test_full_lifecycle` (failure 6) is `assert False is True` at L80 — flagged H1 (loop-contamination symptom, likely cleared by the fix) vs H2 (genuine G7 auto-apply app-drift → STOP+escalate, out of test-only fence); default H1. NB: the as-built `catalog/service.py` STILL retains the confidence-floor auto-apply path (L685 `applied[name]=True`) — so my earlier memory note "applied always False per G7" is NOT a clean as-built fact; the service kept a branch. Confirm via isolation run post-fix.

**Specialist for Part 2:** `meesell-api-routes-builder` (sonnet) — test-tooling wiring, same domain as #150. Acceptance bar = `pytest -m integration` exit 0 (0f/0e), ALL 13 PR checks green, **NO override** (must merge honestly). Fence: test-harness/fixtures only; pytest.ini §19.D + app/ + ci.yml + alembic + LOCKED conftest blocks (the pass-1→4 savepoint substrate) untouched. Worktree `/tmp/mesell-wt/gate4-fix`, branch `fix/gate4-loop-contamination`.

**Mechanics gotcha:** this session ran in a bg sub-session whose checkout-write guard blocked direct writes to the shared tree. All docs/spec/memory landed via worktree `/tmp/mesell-wt/gate4-docs` (branch `docs/gate1-resolved-gate4-spec`). The spec file therefore lives at BOTH the worktree path (committed) and will sync to the shared memory dir on develop merge. Pattern: when bg-isolation blocks, route docs through a Model-C docs worktree (same as the Part-1 plan anticipated).
