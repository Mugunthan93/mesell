### ci-activation
- [spec_ci_gate1_event_loop.md](spec_ci_gate1_event_loop.md) — **CI Gate-1 event-loop fix, Rule-7 STEP 3 merge gate (2026-06-12, `gate/ci-gate1-event-loop-review`). VERDICT: REJECT PR #156 — close unmerged.** The targeted problem ("Gate-1 unit RED on develop") was ALREADY FIXED by parallel **PR #150** (`2d9b8af`, merged 03:02 UTC) BEFORE #156 opened. #156 cut from STALE base `a732729` (predates `2d9b8af`; `git merge-base --is-ancestor 2d9b8af #156` = FALSE) → stale-base DUPLICATE that would REGRESS develop (reverts PR #150's `unauth_client` singleton-contamination hardening — saved-overrides-clear + `raise_app_exceptions=False` + lifespan — and swaps `patch()` for `monkeypatch.setattr`). **CI truth: develop tip `fe3f3ff` Gate-1/2/3 = SUCCESS** (only Gate-4 integration fails = ADVISORY per MASTER_PLAN §2.1); PR #150's own CI Gate-1 = pass (58s); #156 had NO CI checks. Local master venv (`backend/.venv`, Py3.11, pytest-asyncio **0.24.0** — same pin as CI, NOT a version drift) shows 2 catalog flag-guard reds on develop EVEN IN ISOLATION (`got 500` / `Event loop is closed`) → documented local-macOS-Py3.11-vs-CI-Linux-Py3.12 teardown artifact (gotcha #1), NOT a CI red. Report inaccuracies caught: (a) claimed "3 files", PR has **4** (omitted `api-routes-builder/MEMORY.md +79`); (b) claimed `test_catalog_routes.py` "+4/−4", actual **+6/−6**. Deviation 1 (importlib.reload Polluter B) mechanism REAL but redundant; Deviation 2 allowlist preserves semantics (moot). **P0 LESSON (repeat of #73/#74):** parallel-merge hazard struck AGAIN — PR #150 merged ~6h before STEP 1 authored the spec. NEW RULE: before dispatching ANY CI-hotfix spec, grep open AND recently-merged (~24h) PRs for the same gate/file. Follow-ups: CHORE-A close #156+#154; CHORE-B `_reload_config` reload-pollution latent debt (api-routes-builder, low pri); CHORE-C process rule.
- [spec_ci_gate1_fix.md](spec_ci_gate1_fix.md) — CI Gate-1 pytest-collection fix (Rule 7 3-step). SPEC + OUTCOME: `pythonpath = .` in `backend/pytest.ini` (PR #73 squash `1e95b2a`); §19.D additive ruling (no founder amendment); **double-merge P0** (#73 + #74 → duplicate `pythonpath` key → config-load error → repaired in #75); Finding A debt (zero `unit`-marked tests = Gate 1 runs nothing); Finding B → infra (5 missing §5.D env vars in ci.yml). LESSON: scan for already-open duplicate PRs before authoring a fix spec.
- [handoff_ci_gate1_envvars.md](handoff_ci_gate1_envvars.md) — backend→infra memo: Finding B (ci.yml Gate-1 env block missing `GCS_BUCKET`/`GCS_PROJECT_ID`/`LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`/`CORS_ALLOWED_ORIGINS` → config SystemExit). Inter-lead RESOLVED 2026-06-11 (infra PR #76; CONFIRMED green by PR #85 Gate-1).
- [handoff_ci_markers_resolved.md](handoff_ci_markers_resolved.md) — backend→infra memo: §19.D marker chain CLOSED (PR #85 squash `34d8b47` → develop). Gates 1/2/3 GREEN. NOTE (post-Gate-4-saga reconciliation, PR #88): the CI-INT-DB-PROVISION request this memo originally opened is SUPERSEDED — the Gate-4 saga (#104→#110, conftest `_provision_test_schema`) lands `pytest -m integration` at exit 0. openpyxl is a hard prod dep (export §14), now in requirements.txt.
