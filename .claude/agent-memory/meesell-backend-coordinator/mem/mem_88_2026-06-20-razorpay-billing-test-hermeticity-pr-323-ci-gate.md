## 2026-06-20 — Razorpay billing test hermeticity (PR #323 CI Gate 4)
- type: project/feedback
- CONTEXT: PR #323 (feature/razorpay → develop). CI Gate-4 integration suite got past alembic schema provisioning (merge migration fixed that) but 2 Wave-3 billing tests failed in CI's minimal env: test_subscribe_pro_recurring (`'ci-dummy-razorpay-key-id' == 'rzp_test_sentinel_key'`) and test_subscribe_pro_annual (`'' == 'plan_test_pro_annual'`).
- ROOT CAUSE (confirmed test-hermeticity, NOT a route bug): `backend/tests/test_billing_routes.py` set env via `os.environ.setdefault(...)` at module top, but `app.shared.config.settings` is a module-level singleton instantiated at IMPORT time (config.py L405 `settings = _load_settings()`). conftest imports config before the test module's env block, so (a) CI's `RAZORPAY_KEY_ID=ci-dummy-razorpay-key-id` already won (setdefault no-op), and (b) plan-id setdefaults landed too late — the singleton had already captured the empty CI values. Route is CORRECT prod behaviour: billing_router L95 returns `settings.RAZORPAY_KEY_ID`; service `_get_plan_id_for_tier` (service.py L915-924) does `getattr(settings, attr, "")` at call time. Real values arrive via Secret Manager at deploy.
- FIX: added autouse fixture `_hermetic_razorpay_settings` that does `monkeypatch.setattr(_config_module.settings, ATTR, value)` for RAZORPAY_KEY_ID + all 5 RAZORPAY_PLAN_ID_* + RAZORPAY_LTD_PRICE_PAISE. Mirrors the EXISTING pattern at `test_billing_flag_off_unmounts_routes` (L446 monkeypatches settings.FEATURE_BILLING_ENABLED). monkeypatch auto-restores. Both billing_router and iam.service read the SAME singleton object, so one patch drives both. Did NOT change asserted literals to ci-dummy/'' (would assert nothing).
- Tests touched (assertions made hermetic): test_subscribe_pro_recurring (key_id + plan_id), test_subscribe_pro_annual (plan_id), test_subscribe_ltd (LTD amount, now env-pinned too). Autouse fixture covers all 22 tests in the file.
- "Event loop is closed" VERDICT: benign teardown noise, NOT a failure cause. It correlated with the failing assertions (mid-fixture async teardown). With assertions passing, 0 occurrences (grep -ic = 0). No async fixture change needed.
- VERIFY: `pytest -m integration tests/test_billing_routes.py -q` → 22 passed. DB = meesell_rzpw2_test (disposable, never meesell). Proved failure WITHOUT fix via git stash (exact 2 CI failures reproduced), then pass WITH fix. Dev `meesell` categories still = 3,772 (read-only check, untouched).
- ENV GOTCHA for local verify: worktrees carry NO .venv. Used main repo venv `/Users/mugunthansrinivasan/Project/mesell/backend/.venv/bin/python` (Python 3.11, SQLAlchemy 2.0.32, pytest 8.3) + PYTHONPATH=<worktree>/backend so worktree's app/ is imported. System python3.9 fails on `Mapped[str | None]` (PEP604). Must supply CI's REQUIRED_FIELDS env (config validates at import) — supplied ci-dummy values incl RAZORPAY_KEY_ID=ci-dummy-razorpay-key-id and NO plan-id vars to prove hermeticity.
- COMMIT: 424d007 on feature/razorpay, 1 file +46. Did not push (Director handles git).
