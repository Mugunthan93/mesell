## 2026-06-22 — category-monitor Wave-3 Unit T RE-GATE PASS (after identity-map REJECT); WAVE 3 COMPLETE [project]
PR #472 head 32caeb1 re-gated after the prior identity-map aliasing REJECT (session-4). VERDICT PASS, squash-merged to feature/category-monitor/integration (f363db6).
- FIX confirmed: edge helper sig existing:SellerProfileORM|None -> prior_complete:bool; each of the 3 onboarding sites (upsert_profile L471, set_active_categories L577, set_compliance_extension L633) snapshots prior_complete=bool(existing.onboarding_complete) BEFORE the repo write; crossed_edge=onboarding_complete and not prior_complete. The L661 no_spec early-return passes the flag unchanged + returns before the edge helper (cannot cross) — correctly untouched.
- CRUX METHOD (reusable): to prove a regression test is GENUINE, checkout the PARENT's buggy source file into the PR worktree while KEEPING the new test, run -> must be RED; restore the fix -> GREEN. Did exactly this: reverted service.py to be89a0f (live-ORM edge) + kept the new test_customer_monitor_edge_identity.py -> 4 FAILED (got 0 enqueues, assert 0==2); restored 32caeb1 -> 4 passed. A regression test that doesn't fail on the old bug is worthless.
- The genuine identity-faithful test uses a REAL async session + REAL customer_repo (find_by_user_id + repo write share ONE identity-mapped object = prod path); only leaf-resolver + Celery .delay mocked. The 8 fast-unit tests masked the bug by mocking find_by_user_id to a DIFFERENT object than the repo write returned.
- LOCAL GATE INFRA (reproducible for category-monitor + any customer/iam integration test): meesell_test DB must exist + role meesell pw 'password'; conftest auto-provisions schema via `alembic upgrade head` subprocess WHEN TEST_DATABASE_URL is set. Steps that worked: createdb meesell_test OWNER meesell; ALTER ROLE meesell PASSWORD 'password'; valkey-server --port 6381 --daemonize yes (conftest VALKEY_URL=redis://localhost:6381/15); cp main backend/.env into the worktree backend (config reads .env from CWD); TEST_DATABASE_URL=postgresql+asyncpg://meesell:password@localhost:5432/meesell_test; run via /Users/mugunthansrinivasan/Project/mesell/backend/.venv/bin/python (py3.11, sqlalchemy 2.0.32). REMEMBER to rm the copied worktree .env + shutdown valkey 6381 after.
- MERGE-MECHANICS GOTCHA: gh pr view reported MERGEABLE but the squash returned "GraphQL: Pull Request has merge conflicts" because the PR BASE had moved — Unit S #477 (4ca5a56) landed to integration FIRST, append-colliding on STATUS_BACKEND.md. Resolution: merge integration into the PR branch in the worktree, union keep-both the STATUS append blocks (code auto-merged clean), re-verify merged-tree suite (26 green), push, THEN squash-merge. Always re-fetch + re-check mergeStateStatus:CLEAN before retrying.
- ROUTE-COUNT CHECK refinement: app.routes shows 35 /api/v1 ops (flag-mounted), NOT the locked "28". The real gate criterion is ZERO route DELTA: dump route tables at integration base vs PR head and diff -> identical. Don't assert the literal 28 against a live app boot.
- PRE-EXISTING-flake disposition: test_active_categories_replace_semantics setup-ERROR is a cross-fixture asyncpg loop/teardown ordering artifact (test_customer_routes.py then test_customer_onboarding_coverage.py in one process). Reproduces at integration base with ZERO PR changes + passes in isolation -> pre-existing, harness-ticket only, do NOT block. Proving pre-existence = run the same ordering at the merge-base.
- BOARD/STATUS COMMIT GOTCHA: master tree has a git guard (guard-master-tree-git) — cannot branch/commit there. Use a worktree off origin/develop. AND: develop may have moved since my master-tree edits (Unit S board #478 landed first) — do NOT blind-copy master-tree doc edits into the worktree; re-apply onto the CURRENT develop version (demote the existing 'Last updated' to 'Prior', prepend mine).
- WAVE 3 COMPLETE: integration f363db6 carries W1 schema (b7487e8) -> W2 dedupe gate (0d0abfb) -> W3 Unit S serving (4ca5a56) -> W3 Unit T triggers (f363db6). integration->develop is the FOUNDER gate (not taken). Founder-gate items stacked: §3.I/§18.B worker-count 3->4 + §2.D matrix (new monitor column + customer->catalog cell). Wave 4 = FE-contracted public serving route; monitor.service.get_served_category_data ready from Unit S (raises CategorySnapshotNotFoundError -> 404).
