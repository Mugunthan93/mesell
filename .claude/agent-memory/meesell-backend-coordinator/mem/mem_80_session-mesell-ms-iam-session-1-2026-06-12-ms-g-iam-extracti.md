## Session mesell-ms-iam-session-1 — 2026-06-12 — MS-G iam extraction Sub-Plan (PHASE 1, docs-only, MS-PAR-1 Wave MS-4)
Worktree /tmp/mesell-wt/msG-docs on docs/msG-subplan-0G off develop 6d6ee51. DOCS-ONLY (no code, no specialist dispatch). 5 files written + committed locally (no push).
- **Deliverables:** SUB_PLAN_0G_iam_extraction.md (canonical SUB_PLAN_01 shape) + spec_msG_backend.md (4 paste-able specialist prompts) + handoff_msG_infra.md (mirrors handoff_msA) + board MS-G row + STATUS UPDATE + this memory.
- **Filename ruling:** MASTER_PLAN §4 roster calls it SUB_PLAN_07; MS-PAR-1 dispatch (newer) names it SUB_PLAN_0G. Used 0G as filename, noted _07_ alias in header. Same artifact — do NOT author a second file.
- **iam is the ODD-ONE-OUT extraction — encode for waves that copy this:** iam is ALL-✗ in the §2.D matrix. ZERO outbound shims (it imports no other module's service), ZERO inbound /internal/* (no one calls iam — every service validates JWTs LOCALLY via vendored core/auth.py + shared JWT_SECRET per A2/D7). It is the ONLY extraction with NO HTTP-shim-contract deliverable. The single most likely over-build: a Phase-2 specialist inventing an iam /internal/token-validate endpoint — that is a REJECT-class offense (cite §5.A / D7 / §0.4).
- **core/auth.py is the vendored-everywhere surface** — import allowlist is shared.* + core.errors ONLY (auth.py:41-44), which is exactly what makes it cleanly vendorable into every service. iam-svc ships it BYTE-FOR-BYTE; iam-svc is the only service that ALSO calls the issuance/rotation half (issue_access_token/issue_refresh_token/rotate_refresh_token) from a ROUTE — other services call only the verification half (get_current_user). Same file, different call sites, zero divergence. The monolith's core/auth.py is NEVER deleted at iam cutover (stays the source-of-truth every service imports). Merge-gate check: `diff` svc-iam/core/auth.py vs monolith == EMPTY.
- **6 cross-schema FKs to users.id = the DROP set** (verified in BOTH ORM models AND baseline migration 935e55b4852c): audit_events(audit_event.py:54 / mig:66, RESTRICT, stays public), seller_profile(:123 fk_seller_profile_user_id / mig:105, CASCADE→customer schema), catalogs(catalog.py:42 / mig:118, CASCADE→catalog), products(product.py:55 / mig:149, RESTRICT→catalog), exports(export.py:48 / mig:168, RESTRICT→export), product_drafts(:76 fk_product_drafts_user_id / mig:194, CASCADE→catalog). NUANCE: by MS-4, A–F are extracted, so some of these 5 owning-schemas already moved — the database-builder MUST cross-check live via `SELECT conname,conrelid::regclass FROM pg_constraint WHERE confrelid='users'::regclass;` and drop EXACTLY the residual cross-schema FKs (waves MS-2/MS-3 may have dropped some). Risk #5 integrity pre-scan BEFORE any drop.
- **FE-D5 cited from source (LIVE/FROZEN, zero drift):** cookie Path=/api/v1/auth (router.py:64, NOT /auth), Domain=.mesell.xyz (:65), HttpOnly+Secure+SameSite=Strict (:76-78); allowlist key cache:refresh:v{N}:{hmac_sha256(token,pepper)} (auth.py:370); dual-pepper read current vN→fallback vN-1 (auth.py:401-410, landed PR #66); Lua rotation REFRESH_ROTATE_LUA verbatim (auth.py:435-443, EVALSHA→EVAL). The Traefik route MUST preserve /api/v1/auth EXACTLY (no StripPrefix) — a path rewrite silently breaks every live session's refresh cookie. This is the highest-blast-radius infra constraint in the whole migration (handoff I4 + Risk R2).
- **iam infra differs from export:** NO Celery worker (single api process), NO GCS SA (no storage), 2-replica MINIMUM + HPA 4-burst (Risk #2 — auth authority is a SPOF). Valkey DB 0 allowlist is SHARED + NOT key-prefixed (the cache:refresh:/otp: keyspaces are LOCKED-VERBATIM for the frontend + dual-pepper runbook — do NOT apply the §2.E {svc}: prefix to them). No backfill at cutover (DB 0 state survives the pod move — unlike export's Celery keyspace).
- **V1 gaps carried VERBATIM (extraction preserves behavior, does NOT fix):** DPDP no-op (repository.py:91-99, capture_dpdp passed but users has no dpdp_consented_at column) + razorpay webhook logs-not-rows (service.py:597-632, audit_events.user_id NOT NULL but webhook has no user_id → returns audit_event_id=0). Both → V1.5 hand-off notes.
- **No contradictions** between MASTER_PLAN §2.D prose and as-built. Source REFINES: FK drop set = 6 across 5 schemas, not the single `products` example §2.D names.
- **iam = 7 files (no tasks.py).** Mounted routes verified main.py:114. 1 JWT + 2 cookie + 2 public + 1 HMAC auth posture.
- Specialist set per MASTER_PLAN row G: auth-builder (heavy lift — sole iam owner per iam/__init__.py:3), api-routes (6 routes+schemas), services (repo/domain/exceptions+scaffolding), database (users schema-move+FK drop). SEAM: auth-builder owns service.py + core/auth.py vendor; services-builder owns the rest of the scaffolding — agreed so they don't both touch core/auth.py.
- Pattern reinforced: the docs-only Phase-1 sub-plan is a clean lead-direct fast-mode chore (rule-7 docs class) — no Agent tool needed; commit locally, master syncs memory back.

### AMENDMENT 2026-06-12 — re-ground at origin/develop `aa7513e` + sibling reconciliation (post-rebase)
Branch `docs/msG-subplan-0G` was rebased from local develop `6d6ee51` (BEHIND origin) onto origin/develop `aa7513e` (true tip). Re-grounding pass (no re-authoring):
- **`git diff 6d6ee51 aa7513e -- modules/iam/ core/auth.py` = EMPTY** → iam module + vendored core/auth.py byte-identical; every file:line citation (main.py:114 mount, §0.6 FE-D5 cookie/allowlist, §0.7 6-FK set) holds verbatim. This is the cheapest possible rebase outcome — the extraction surface didn't move.
- **3 changed core files, ALL inert for iam:** (1) `audit_mw._is_autosave` widened for bare `PATCH /products/{id}` (catalog autosave) — matches only `/products/*`, iam routes are `/auth/*`+`/webhooks/razorpay`; iam-svc's vendored audit_mw carries the widening but it's catalog-path-only/inert. (2) `tenancy._GLOBAL_TABLES` frozenset {categories,templates,field_enum_values,field_aliases}+`__all__` — doc-sentinel (NOT a §19 linter input yet, gated on founder R1), no iam table. (3) `main.py` catalog mount wrapped in `FEATURE_CATALOG_FORM_ENABLED` at :123+ (BELOW iam mount :114, unchanged).
- **MASTER_PLAN is v1.3 with new §3.A.1** on origin (local copy was pre-re-key) — citations updated to "v1.3 §3.A.1-aware".
- **Sibling reconciliation (read iam-touching claims of 0C/0D/0E/0F only):** (a) iam all-✗ → NO `/internal/*` invariant SURVIVES — no sibling froze any contract requiring iam to expose `/internal/*`; all 4 reference iam only as JWT-issuer, validate LOCALLY via vendored core/auth.py + shared JWT_SECRET (0E:70-72 explicit "no /internal exists yet"). (b) 0F D7 parallel-safety + shared files MATCH — 0F names main.py(additive-minimal)/Traefik/k8s(new manifests)/JWT_SECRET(shared); §0G now cites the symmetry. (c) §0.7 FK ownership ANNOTATED: FK#2 `seller_profile.user_id` dropped by CUSTOMER at MS-3 (SUB_PLAN_0E §0.6 / 0E:144,372) BEFORE iam MS-4; FKs#3/#4/#6 still iam's (catalog MS-5, in-public at MS-4); 0C `product_images` has no user_id col (0C:121), 0D `pricing_calcs` FK is to products.id NOT users.id (0D:234) → set stays exactly 6. Live pg_constraint cross-check (already mandated) covers the residual-drop discipline.
- **NO genuine contract collision → NO escalation.** No semantic change to deliverables, FK count, or invariants — re-grounding only.
- Amended (surgical): SUB_PLAN_0G §0 preamble + §0.1 (rebase reconciliation block) + §0.7 (sibling-ownership annotation) + header shared-file/parallel-safety + v1.1 revision row; spec_msG_backend.md §0 header SHA line. handoff_msG_infra.md needed NO edit (cut-point already `origin/develop` live tip, no SHA pin). Commit on docs/msG-subplan-0G (NOT pushed).
- **Op-learning:** the spec_msA §0.1 "always re-verify ls-remote, cut from live origin tip" rule proved itself — local develop trailed origin under us. Reinforced in §0.1.
