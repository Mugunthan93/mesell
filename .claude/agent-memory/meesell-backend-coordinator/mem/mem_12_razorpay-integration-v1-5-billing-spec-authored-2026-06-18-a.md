### razorpay-integration (V1.5 billing — SPEC authored 2026-06-18; **APPROVED DESIGN 2026-06-18**)
- **STATUS: APPROVED DESIGN — execution gated on Wave 0** (founder ruled F1–F10 on 2026-06-18; §7.3 architecture amendment APPROVED via F10). Spec v2. Flipped from "SPEC READY / amendment pending". NO code yet — Wave 0 hard blockers must clear first (see below).
- **4 FOUNDER BUSINESS RULINGS to remember:** (F3) refund = **7-day money-back + pro-rated monthly** (legal copy + impl must match); (F4) **upgrade = immediate / pay-difference-now, downgrade = at end of current paid period**; (F7) **plan read DB-FRESH, NOT a JWT claim** (JWT `plan` advisory only — touches LOCKED §4.B/§7); (F8) **trust Razorpay retry window, downgrade entitlement only on `halted`** (no fixed grace). Plus F1 plan-vocab ACCEPTED (`free·pro·pro_annual·business·business_annual·ltd`), F5 GST = Razorpay-generated (legal to CONFIRM compliance), F6 annual launches with monthly, F9 paise + official Razorpay Python SDK.
- **F2 OVERRIDE — LTD SHIPS IN WAVE 1 (at launch), NOT deferred.** Was my "defer LTD" rec; founder overrode. Build phasing RE-SEQUENCED: Orders-API/LTD path folded into Waves 1–3 (W1 schema `razorpay_order_id`+perpetual `current_period_end=NULL` sentinel+`ltd` tier; W2 `create_order` adapter + `payment.captured`→permanent `ltd` grant handler; W3 `subscribe` tier:"ltd" branch + LTD entitlement; W4 LTD lifecycle tests). NO separate LTD wave. Annual SKUs (F6) likewise first-class from W1.
- **WAVE 0 HARD BLOCKERS (all must be GREEN before cutting `feature/razorpay-integration/backend` or dispatching any specialist):** (1) INFRA populate `RAZORPAY_WEBHOOK_SECRET` SM version (container exists, NO version, k8s/secrets.yaml.example:73-80; DISTINCT from RAZORPAY_KEY_SECRET); (2) FOUNDER/INFRA create Razorpay dashboard Plan objects + `RAZORPAY_PLAN_ID_*` config NOW INCL annual (`_PRO_ANNUAL`/`_BUSINESS_ANNUAL`) + LTD price-constant; (3) INFRA register webhook URL in dashboard w/ §4.1 events incl `payment.captured`; (4) LEGAL finalize 7-day refund text (F3) + Razorpay KYC pack (entity OPC/Stellaxis, PAN=GST=bank name-match) + publish HTTPS URLs; (5) LEGAL confirm Razorpay-generated GST invoicing (F5) satisfies Indian GST/compliance. Cross-track memos to author at DISPATCH time: handoff_secret_razorpay_webhook (infra), handoff_refund_policy_razorpay + GST-confirm (legal), handoff_contract_razorpay (FE, Wave 5).
- **SPEC at `docs/plans/features/razorpay-integration/RAZORPAY_INTEGRATION_SPEC.md`** (`mesell-razorpay-integration-planning-session-1`, FAST-MODE docs class — spec-only, NO code). Amends the §1.E/§14 V1.5 deferral; founder approval of the spec = the §7.3 architecture-amendment trigger (§6 adapters + §7 iam are LOCKED) — NOW APPROVED.
- **2 DISPATCH-SUMMARY ERRORS corrected vs live tree** (recurring pattern — verify before quoting gap shape): (1) `users` table has NO `razorpay_sub_id` and NO `plan_expires_at` — it is exactly `{id,phone,email,plan,created_at,last_login_at}` (user.py:34-98, MVP_ARCH §2.1). `plan` is VARCHAR(20) DEFAULT 'free', comment "free | pro". Billing data model is NET-NEW, not a column add. (2) V1 webhook `capture_razorpay_webhook` (iam/service.py:590-644) persists NOTHING — only `logger.info`, returns `audit_event_id=0` placeholder. Blocked by the documented `audit_events.user_id NOT NULL` + no `payload_jsonb` column conflict. Spec resolves via NEW `webhook_events` table, NOT by relaxing audit_events.
- **Design spine:** Subscriptions API for Pro/Business/annual recurring; Orders API for LTD one-time. Webhook = SOLE source of plan truth (checkout response advisory). Idempotency via `INSERT webhook_events ON CONFLICT(event_id) DO NOTHING` INSIDE the same tx as the state mutation (atomic record+apply; rollback→Razorpay-retry→clean reprocess). Out-of-order guarded by `GREATEST(current_period_end,...)` monotonic + status-transition matrix. V1 `verify_webhook_signature` (sync/bool/never-raises, 2 locked §6.E/§6.G exceptions) preserved BYTE-FOR-BYTE; new async adapter methods follow §6.G typed-error (RazorpayAdapterError already pre-wired in adapters/__init__.py:80).
- **3 new tables:** `subscriptions`/`payments`/`webhook_events`, parent revision `f31c75438e61`. NO `users` column add (sub FK's to user). `users.plan` widens. Reconciliation Celery beat = safety net for lost webhooks (fetch_subscription reconcile).
- **10 FOUNDER DECISIONS (F1-F10):** F1 plan-string vocab (locked docs SILENT — rec `free·pro·pro_annual·business·business_annual·ltd`); F2 LTD-in-wave-1 vs scarcity-hold (rec defer); F3 refund-variant SELECT (rec 7-day+prorated, 3 variants already drafted by legal-writer); F4 proration (rec upgrade-immediate/downgrade-cycle-end); F5 GST invoice Razorpay-vs-own (rec Razorpay); F6 annual launch (rec yes); F7 plan-as-JWT-claim vs DB-fresh (rec DB-fresh, JWT plan advisory — touches LOCKED §4.B/§7); F8 grace policy (rec trust Razorpay retry window); F9 SDK-vs-httpx + paise unit; F10 §7.3 amendment approval (spec=the ask).
- **Cross-track ALREADY-DONE intel:** legal pack is COMPLETE (legal-writer MEMORY: Privacy/ToS/Refund-3variant/Cookie/DPA/Razorpay-KYC/GST/Invoice/in-product strings, entity=OPC/Stellaxis). `RAZORPAY_WEBHOOK_SECRET` SM container exists NO VERSION (k8s/secrets.yaml.example:73-80, founder action pending, exact gcloud cmd inline) — HARD blocker for webhook testing. razorpay-key-id/secret ARE populated. Config fields all wired (config.py:146-148, in REQUIRED_FIELDS).
- **Build phasing:** W0 founder+infra+legal unblock → W1 db → W2 adapter+webhook-router (services-builder primary) → W3 routes ∥ plan_guard entitlement → W4 reconciliation+integration tests → W5 FE. New billing endpoints are CONTRACT (grow the 28-mounted count); webhook stays infrastructure surface.
