## Session mesell-microservices-dashboard-backend-session-1 — 2026-06-13 — MS-B Phase C BUILD (lead-owned hybrid integration test)
- **Deliverable:** `backend/services/svc-dashboard/tests/test_dashboard_extraction.py` (14 tests, ALL GREEN, 0.18s no-tunnel) + `tests/__init__.py` + `tests/conftest.py`. Copied SP01 (svc-export) structure; adapted for dashboard's 2-shim/leaf-consumer/no-schema shape. ruff clean.
- **svc-dashboard had ZERO tests before this** — the api-routes/services specialists shipped NO `tests/` tree at all. The spec's "36 dashboard tests" are MONOLITH-side (`backend/tests/modules/dashboard/` + `backend/tests/integration/test_dashboard_*`), unchanged by this branch (touches zero monolith code). So this lead test is the ONLY svc-side coverage. FLAG for merge-gate: confirm whether specialists owed a `tests/` tree or whether the lead extraction test is the agreed sole svc-side suite (SP01 svc-export shipped 4 test files / 37 tests from specialists+lead; svc-dashboard shipped only the lead's 1 file). This is a coverage-parity gap vs SP01.
- **§16.G AST parity: PASS.** Both `service.py` twins are byte-identical after recursive docstring+import strip (dashboard has NO lazy in-function imports — the whole service is 2 functions; recursive transformer used anyway per recipe §2). The ONLY diff is the 4 import lines (catalog/customer `service as <x>_service` repointed to `extracted_clients`, domain types repointed to the shim modules) + docstring.
- **Wire-shape JSON-schema parity: PASS** for DashboardResponse/ProductListItem/ProfileCompletenessSummary. Monolith `dashboard/schemas.py` is byte-identical to the svc twin (unlike SP01 where the docstring path-param differed) — so even the `description` keys match; the strip is belt-and-suspenders. sys.modules-register + model_rebuild ForwardRef trick from recipe §3 applied.
- **Shim mock pattern (recipe §4) reused verbatim:** `_Recorder` + `httpx.MockTransport` + factory patch on `_transport.httpx.AsyncClient`. Proved both shims forward JWT+X-Request-ID, the 5s/2s timeout, GET verb, query-params (catalog: page/limit in querystring, user_id NOT in URL=tenant-from-JWT; customer: user_id IN path), real deserialize into frozen `PaginatedProductsInternal`/`Product` + 5-field `ProfileCompleteness`. Plus retry-once-on-503-then-200 (2 requests), no-retry-on-500 (1 request → HTTPStatusError), customer 404→typed ProfileNotFoundError.
- **Compose correctness:** `_compose_response` is module-private (not in `__all__`) — access via `import app.service as svc; svc._compose_response(...)`. Asserted Product.id→ProductListItem.product_id rename, 5 product fields 1:1, 5 completeness fields 1:1, total/page/limit passthrough, AND empty-inventory (items=()) → 200-shape DashboardResponse with `products==[]` total=0 (NOT 404).
- **Flag-guard pattern (NEW, cleaner than SP01's TestClient):** the `rate_limit` decorator is a PURE TAG ATTACH (`fn.__rate_limit__ = (...)`; returns fn unchanged) — so the route handler coroutine `router.list_products(...)` is DIRECTLY AWAITABLE in tests. Patched `settings.FEATURE_TRACKING_DASHBOARD_ENABLED=False` + monkeypatched `router_mod.dashboard_service.list_products_for_dashboard` to an `_explode` that raises AssertionError if reached → proved 404 fires BEFORE any service/shim call. Plus a counter-proof (flag True → service reached, args forwarded) so the 404 is genuinely the flag, not an unconditional 404. NO TestClient / no app boot needed.
- **venv GOTCHA:** there is NO `backend/.venv` inside the worktree (gitignored, not copied). Run with the MASTER-TREE venv: `PYTHONPATH=backend/services/svc-dashboard /Users/mugunthansrinivasan/Project/mesell/backend/.venv/bin/python -m pytest ...` from inside the worktree. Reads/execs of the master venv are allowed; only WRITES to the master tree are isolation-blocked. ruff at /opt/homebrew/bin/ruff.
- **DEFECT FOR MERGE-GATE: `docs/runbooks/svc-dashboard-rollback.md` is MISSING.** Dispatch said infra authored it; it does NOT exist (only README/auth-secret-rotation/image-pipeline/pgbouncer-cutover/svc-export-rollback present). spec §6 gate condition "Rollback runbook present (§3.C)" is UNMET. Did NOT author it (infra's file). Merge-gate must block until infra delivers it — should mirror svc-export-rollback.md but: Step 3 = NO DB rollback (dashboard owns no schema §13.D), 2 shims (catalog+customer) not 4, no Celery teardown, simplest of any service.
- **Anti-tautology bar held (pricing lesson):** every assertion checks REAL behavior — exact UUIDs round-tripped through hydration, exact timezone-aware datetimes, exact URL/header/param strings, the explode-if-reached flag proof. Zero `assert True` / string-in-repr echoes.
