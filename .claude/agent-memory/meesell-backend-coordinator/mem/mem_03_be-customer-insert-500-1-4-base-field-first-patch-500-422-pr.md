### BE-CUSTOMER-INSERT-500-1 — 4 base-field first-PATCH 500→422 (PR #428 MERGED to develop 2026-06-22)
- Session `mesell-fix-customer-insert-500-backend-session-1`, HYBRID step 3 (merge-gate review of a `meesell-services-builder` PR). VERDICT **APPROVE → squash-merged to develop @ `87551bb`**; board PR #433 squash `89b9e68` (final develop tip). Follow-up ticket BE-CUSTOMER-INSERT-500-1 CLOSED. This is the widen-to-base-fields sequel to PR #419 (qa-onboarding pincode-only guard).
- **The fix (verified, not trusted):** extends the landed `if existing is None:` INSERT guard in `customer.service.upsert_profile` to the **4** NOT-NULL base fields lacking a `server_default` — `manufacturer_name`, `manufacturer_address`, `packer_name`, `packer_address` — raising new `MissingRequiredProfileFieldError` (422, mirrors `MissingRequiredPincodeError`). Raise order interleaved: manufacturer name→address→pincode, then packer name→address→pincode. Landed pincode `detail` strings byte-preserved.
- **GROUND-TRUTH that locks the EXCLUSION:** `country_of_origin` is the ONLY base field with `server_default=text("'India'")` (ORM `app/shared/models/seller_profile.py` L69-73) — so missing → DEFAULTS, never 500s → correctly EXCLUDED. The 4 guarded fields + 2 pincodes are `nullable=False` with NO server_default. ALWAYS verify the server_default claim against the ORM model before accepting an exclusion rationale — this is the load-bearing question for any NOT-NULL-guard widen.
- **i18n:** `validation.{field}.missing` rides resolver Step-2b generic fallback → registered `validation.generic.missing`. ZERO new keys (grep the diff for `messages_en`/`.json`/key-mints — all matches were docstrings/assignments/assertions). All 4 ids §5A.H 3-segment-regex clean (validated in a python one-liner).
- **GATE-RUN MECHANICS that worked (reuse these):** no venv in a fresh worktree → used the master tree's `/Users/mugunthansrinivasan/Project/mesell/backend/.venv/bin/python` (fastapi 0.115, sqlalchemy 2.0.32, pytest 8.3, py3.11). Required §5.D startup secrets → `set -a && . /Users/.../backend/.env && set +a` then override `TEST_DATABASE_URL=postgresql+asyncpg://meesell:password@localhost:5432/meesell_test` + `DATABASE_URL=$TEST_DATABASE_URL`. PG on **:5432** (NOT 5433) had `meesell_test`. Result: **32 passed**. ruff NOT in venv → `pip install ruff` then `ruff check app/` (CI Makefile invocation) = clean. import-linter config = `backend/tests/lint/import_rules.toml` → `lint-imports --config tests/lint/import_rules.toml` = **27 kept / 0 broken**.
- **REVERT-CHECK pattern (proves tests load-bearing):** `cp service.py /tmp/bak`, python-strip exactly the manufacturer guard loop, re-run only the 8 base-field tests → the 4 MANUFACTURER cases went RED with `asyncpg NotNullViolationError` surfaced as 500 (`server.internal_error`), the 4 still-guarded PACKER cases stayed green. Surgical strip (one block) gives a precise red/green split that proves coverage per-field. Restore from backup, `git diff --quiet` to confirm.
- **CI:** all 5 gates (unit/smoke/lint/integration/golden_roundtrip) GREEN → `gh pr merge --squash` worked WITHOUT `--admin` (develop protected but no review-required block hit on a green PR). Did NOT need to STOP-and-report.
- **BOARD-LANDING in a dirty shared master tree (GOTCHA + recipe):** the master checkout was on `develop` but BEHIND origin (origin had the #428 merge) AND carried ~15 uncommitted files from other sessions. Master-tree git is guard-blocked (branch-switch/commit/branch-delete all refused — guard-master-tree-git). RECIPE THAT WORKED: edit the board file in the master tree → `git diff > /tmp/patch` → `git checkout -- <board>` (revert ONLY my file, leave others' dirty work) → `git worktree add --detach /tmp/wt <merge-sha>` → `git apply` the patch → branch+commit+push from the worktree → `gh pr create/merge --squash --admin` (docs-only fast-mode) → `git push origin --delete <branch>` + `git worktree remove --force`. The board file content was identical between local-develop and the merge SHA (the code PR touched no docs), so the patch applied cleanly. Branch-delete from master tree is BLOCKED by guard — delete the remote branch via `gh`/`git push --delete` instead.
- Diff scope was EXACTLY service.py + exceptions.py + test file — NO schema/model/migration/router/Pydantic/i18n. The "25-file stat" stale-base artifact noted in the #419 entry did NOT recur here (clean recompute vs develop).
