## Session mesell-ci-gate4-fix-session-1 — 2026-06-11 — SPEC authored for CI Gate-4 integration fix (Rule 7 STEP 1 of 3)
SPEC ONLY (no code/branch/PR). Spec at `spec_ci_gate4_fix.md`. Follows spec_ci_gate1_fix.md (Gates 1/2/3 now GREEN via PR #73/74/75/76/85). Gate-4 is the FIRST run against live service containers — CI run 27323036548 RED: 21 failed/23 passed/151 errors.
- **4 failure classes, all backend test-harness owned** (infra ci.yml service+env block is provably correct — confirmed by reading the full Gate-4 job: postgres meesell:password@5433/meesell_test + valkey 6381, env TEST_DATABASE_URL/DATABASE_URL/TEST_VALKEY_URL/VALKEY_URL all set; job = checkout→setup-python→pip→pytest, NO alembic step).
- **Class 1 (asyncpg auth):** conftest L56-59 `_DEV_DATABASE_URL` honors only DEV_DATABASE_URL, ignores TEST_DATABASE_URL. KEY ASYMMETRY: top-of-file DATABASE_URL (L14-17) ALREADY honors TEST_DATABASE_URL — so db_engine/client are fine; only the `_DEV_DATABASE_URL`-bound fixtures (dev_engine L309, db L328) are broken. Don't disturb L14-17.
- **Class 2 (Valkey 6379):** use_live_valkey L183 + valkey L398 default CORE_TEST_VALKEY_URL=6379; CI sets 6381 via VALKEY_URL/TEST_VALKEY_URL. HIGHEST-RISK fix detail: those fixtures APPEND `/0`,`/3` to base; CI's TEST_VALKEY_URL already carries `/0` → naive concat = `6381/0/0` INVALID. Must strip trailing `/<db>` before re-appending. test_core_auth_rotation.py has its OWN valkey fixture reading VALKEY_URL (already 6381) — its failure is loop, not port.
- **Class 3 (schema/extension):** meesell_test created EMPTY; integration conftest (tests/integration/conftest.py) deliberately AVOIDS create_all (docstring) because create_all can't build pg_trgm GIN indexes — so it REQUIRES `alembic upgrade head`. Locked Option (a): session-scoped autouse fixture runs CREATE EXTENSION + programmatic `alembic upgrade head` against TEST_DATABASE_URL, GATED on TEST_DATABASE_URL presence so a laptop run never mutates the live K3s dev DB. NO seeder needed (baseline 935e55b4852c creates categories EMPTY; tests self-seed Category rows).
- **Class 4 (loop + assertions) is mostly a CASCADE off 1+2+3.** The `assert 200==429` rate-limit fails because the mw FAILS OPEN on unreachable Valkey (test_valkey_unreachable_fails_open proves the 200-on-outage contract) — so it's a Class-2 symptom, self-resolves on 6381. `assert 2==1` audit-coalesce = stale Valkey keys never flushed (fixtures never connected). 151 errors = fixture-SETUP cascades. TRIAGE DOCTRINE LOCKED: connection/provisioning FIRST → re-run → assess survivors. Carve-out: a survivor that's a genuine APP bug must be FLAGGED in the PR, NOT fixed inline (separate review at merge gate).
- **Specialist = meesell-services-builder (opus)** — async fixture/loop hygiene + programmatic alembic + 2-iteration triage; heavier than gate-1's sonnet config change. Not auth-builder (harness, not OTP/JWT logic). Not database-builder (RUNS the chain, doesn't author migrations).
- **Verification reality:** `docker info` FAILS this session (daemon down; binary 28.4.0 present). Two-tier repro: preferred = start daemon + throwaway postgres:16-alpine/valkey:8-alpine; fallback = dev port-forward + scratch `meesell_test` db, state image-parity limitation honestly in PR. CI does NOT run on develop PRs (ci.yml = push/PR to main only) → LOCAL repro IS the gate; true re-green is the next develop→main PR (founder's gate).
- **Pattern reinforced:** infra's diagnosis was directionally exact on all 4 classes — but verifying against the actual conftest revealed the L14-17 asymmetry (half the DB path already correct) and the /0/0 suffix trap (the single most likely place a naive fix breaks). Always read the fixture code, not just the failure class.
- No BACKEND_ARCHITECTURE.md amendment (§19.D test contract preserved — env-honoring + alembic-provision + loop hygiene are IMPLEMENTATIONS of the real-DB policy). §17 stays 28. No FE/AI/data memo.
