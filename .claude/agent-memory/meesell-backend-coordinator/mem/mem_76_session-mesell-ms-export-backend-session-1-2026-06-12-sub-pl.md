## Session mesell-ms-export-backend-session-1 — 2026-06-12 — Sub-Plan A `export` Phase C (LEAD integration) BUILT

First microservices extraction Phase C. Worktree `/tmp/mesell-wt/msA-backend`, branch `feature/microservices-export/backend` (carried Phase A `5747189` alembic + B `1a585d0` services + `a3a8e71` routes + `48fe6b8` memory relays). Built the hybrid-mode integration test + 5 doc deliverables. Merge-gate review is a SEPARATE later dispatch — did NOT pre-approve.

**Test built: `backend/services/svc-export/tests/test_export_extraction.py` — 11 green (incl LIVE PG-16 cross-schema round-trip).** Non-tautological per the pricing-lesson reject class:
- §16.G AST parity (`test_service_py_executable_body_byte_for_byte_identical`): parse both service.py twins, strip docstring + ALL imports RECURSIVELY (NodeTransformer visit_Import/visit_ImportFrom→None), compare ast.dump. **GOTCHA: top-level-only strip FAILS** — the export pipeline has LAZY imports inside function bodies (app.tasks, app.domain, category-client exceptions) that are part of the §16.G permitted diff. Recursive transformer mandatory. 23 top-level nodes, byte-identical after recursive strip.
- Wire-shape parity (`test_wire_shape_matches_monolith_field_for_field`): `model_json_schema()` of all 4 export models vs monolith twins. **GOTCHA 1:** `from __future__ import annotations` → standalone importlib-loaded monolith model raises "not fully defined". FIX: `sys.modules[name]=mod` BEFORE exec_module + `model.model_rebuild(_types_namespace=vars(mod))`. **GOTCHA 2:** the JSON-schema `description` key carries the class docstring which legitimately differs (`{id}` vs `{product_id}` per §0.6) — strip `description` recursively; it is doc prose not wire contract. (model_fields annotation-repr is the WRONG method — ForwardRef('UUID') vs <class uuid.UUID> false diffs.)
- HTTP-shim mode: drove `catalog_service.get_product_for_export` (the REAL re-exported pipeline symbol) through mocked httpx transport — JWT + X-Request-ID forwarded to monolith ClusterIP, real ExportSnapshotInternal deserialized (asserts `snapshot.validation_summary.status`), 404→typed ProductNotFoundError no-retry.
- Cross-schema audit: model-binding (public.audit_events; export.exports coexists) UNCONDITIONAL; live INSERT-then-SELECT round-trip PG-gated. **PG bootstrap (sandbox runs as root):** local Homebrew PG 16 at localhost:5432 has bare roles — used `mugunthansrinivasan` superuser to `CREATE ROLE svc_export` + `CREATE DATABASE meesell_test` + `GRANT ALL ON SCHEMA public` (the I5-equivalent). Round-trip RAN and PASSED. `_pg_connectable()` does a REAL asyncpg connect (port-open is not enough — role/db must exist), skips with documented reason otherwise (auth-otp no-tunnel pattern); SQLite rejected (no schema-qualified DDL).

**Validation floor MET (real output):** svc-export 37 passed (was 26); ruff clean (homebrew `/opt/homebrew/bin/ruff` 0.15.11 — NOT in backend/.venv); full monolith `def test_` **698 ≥ 649 spec baseline** (the +49 came from the newer develop tip cut from, NOT this branch — `git diff --stat origin/develop...HEAD -- backend/app backend/tests` EMPTY); monolith unit 634p/4f, the 4 = known local-Py3.11 teardown artifacts (gotcha #1), proven not-ours by the empty diff; Contract 2 + M10 hold (grep). Ran with master `backend/.venv` (Py3.11) which has all deps; CI env block replicated from `.github/workflows/ci.yml:105-127`.

**5 doc deliverables landed in worktree:** SHIM_CONTRACT_export_callees.md (FROZEN 2026-06-12, program-level — 6 `/internal/*` endpoints for Sub-Plans C/E/F/H, each cited from callee source + shim impl + transport contract; PLAN mis-named 2 shims, corrected), CI_HYBRID_MODE_export.md (callees docker-composed: NONE), docs/runbooks/svc-export-rollback.md (5 steps + Rollback Log), MASTER_PLAN §4 row-A IN EXECUTION flip + status note, recipe_ms_extraction.md.

**FOUNDER-GATE NOTE:** `BACKEND_ARCHITECTURE.md §14` is LOCKED — the "Extracted to svc-export V1.5" amendment is NOT self-applied (§7.3); carried to the integration→develop founder-gate PR notes. Did NOT touch §14.

**LESSON:** the spec's test-count baseline (649) was a point-in-time figure; the branch cut from a newer origin/develop so the live count is higher — assert MONOTONIC ≥ baseline + quote live, never hardcode, and prove zero-monolith-change with the `origin/develop...HEAD` path-scoped diff before attributing any monolith failure elsewhere.

---

### GATE VERDICT — MS Sub-Plan A export extraction (PR #189) — 2026-06-12 (mesell-ms-export-session-1)
- **APPROVE.** First microservices extraction (MS-1 pilot) cleared the merge gate clean on the FIRST gate (no re-dispatch). All 11 checklist items + §6 PASS.
- **§16.G proof technique that worked (reusable for Sub-Plans B–H):** filter both service.py twins to non-import lines and `diff` — remaining deltas must be docstring-only. The builders ALSO shipped a CI test (`test_service_py_executable_body_byte_for_byte_identical`) that re-proves it via import-stripped `ast.dump` equality — adopt this as the standard §16.G CI gate for every future extraction. ast.dump is whitespace/comment-insensitive + order-sensitive = exact structural equality.
- **Gate-run gotcha:** no project venv exists; system python is 3.9 / asyncpg 0.29.0 won't compile on py3.13. Workaround that worked: `python3.13 -m venv`, install requirements but let asyncpg float to a py3.13-compatible wheel (test behavior doesn't depend on the exact asyncpg version since shim tests mock httpx and don't hit a live DB). 37 passed, ruff clean.
- **Pattern confirmed:** monolith ZERO-touch is verifiable in one command — `git diff --name-status <integration>...<backend> | grep -vE "^(backend/services/svc-export/|docs/|\.claude/agent-memory/)"` must be empty. Strangler integrity = a grep, not a vibe.
- **Non-blocking obs logged in verdict:** `tasks.py` uses `except (SQLAlchemyError, Exception)` (redundant — Exception subsumes); cosmetic, did NOT block. The 4 monolith test "failures" are local-Py3.11 macOS teardown artifacts on a zero-monolith-touch branch — mathematically not the branch's regression.
- **§14 LOCKED discipline held:** builders correctly did NOT self-apply the §14 amendment; carried it to the founder-gate PR notes per §7.3. BACKEND_ARCHITECTURE.md is absent from the diff. Good.
- **Verdict delivery:** posted as PR comment (not a self-merge). Board flipped IN REVIEW; STATUS UPDATE block appended. Squash executed by session window.
