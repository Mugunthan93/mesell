## Session mesell-ms-rekey-ruling-session-1 — 2026-06-12 — MS migration start condition RE-KEYED post-V1-launch → dev-complete (PR #178 merged)
Worktree /tmp/mesell-wt/ms-rekey on chore/ms-rekey-ruling from origin/develop tip 80cda29. FAST-MODE docs chore (rule-7 docs class, lead-direct, no specialist). 4 files. PR #178 → develop, squash --admin, **develop tip now afea672**. Ref-deleted via gh api, worktree removed.
- **Founder ruling "ms go" (2026-06-12) verbatim:** "The microservices migration's start condition is RE-KEYED from 'post-V1 launch' to 'dev-complete.'" Rationale (4 pts): all V1 lanes finished (Wave6/AI/dual-pepper); launch deferred no users; dev zero-traffic = lowest-risk extraction window; MS proven in dev by prod time.
- **Where landed:** MASTER_PLAN.md = NEW **§3.A.1 "Execution Start Condition"** (inserted between §3.A approach and §3.B order) + header STATUS line + Rev **v1.3** row. SUB_PLAN_01 = header note flip (execution unblocks at dev-complete imminent; first step D5 pool right-size + PgBouncer ₹0 THEN extraction on current node, 50m sizing fits). Board microservices-export row IN PROGRESS → **READY-TO-EXECUTE (pending dev-complete declaration)** + header line. STATUS_BACKEND UPDATE block.
- **ADDITIVE — zero locked-decision change.** A–H order (§3.B), rollback (§3.C), hybrid CI gate (§3.A), D3–D7/A1–A2 all untouched. Only the execution gate moved earlier. The **dev-complete DECLARATION by the master session is the trigger** — lane is READY but NOT in execution until then.
- **D3 VM spend standing rule (keeper):** the e2-standard-4 (~₹2,600/mo) pre-approval is PLAN-LEVEL ONLY. The spend gets an **EXPLICIT FRESH founder ask at the moment services outgrow the current node** (master-session standing rule) — NOT at execution start. Early extractions (A export, B dashboard) fit current node at 50m K3s sizing → no VM trigger. Re-key commits no money.
- **Process note:** D1 founder-gate applies to `feature/{name}`→develop FEATURE PRs, not lead-owned docs chores — so I self-approve + squash --admin docs chores (consistent with prior repo-management docs landings #39). develop did not move during the turn (frontend #177 already merged before I cut), so no rebase needed.
