### export validation aggregation (2026-06-18)
- Session `mesell-export-validation-aggregation-backend-session-1` — produced SPEC for meesell-services-builder: convert `initiate_export()` from fail-fast → collect-all `failed_checks:[{check_id,message_key}]`. LOAD-BEARING findings:
  1. **§4.F locked envelope = `{detail, code, validation_message_id, request_id}` — NO `error_code` field.** Machine slug = `code`. Task-prompt's `error_code`+`failed_checks` envelope does NOT match locked §4.F. RESOLVED: keep locked envelope, add `failed_checks` ADDITIVE (same precedent as `_pydantic_validation_handler` appending additive `errors` array). `_meesell_error_handler` taught to surface `getattr(exc,"failed_checks",None)`.
  2. **FE/BE DRIFT:** `frontend/apps/mfe-export/src/app/export.service.ts` L128-132 reads `body?.error_code` but backend emits `code` (the 422 `error_code` only exists on GET /exports/{id} worker-failure body — different surface). Pre-existing, not introduced here. Flagged as backend↔frontend memo. Do NOT add non-locked `error_code` to §4.F (founder approval per §7.3). FE should read `code` + new `failed_checks`.
  3. `initiate_export()` (service.py L148-225) has exactly 2 router-surface checks: (a) validation_summary.status!="ready"→ProductNotReadyForExportError; (b) front-image-missing (only fmt=="xlsx_with_images")→FrontImageMissingError. Ownership step-1 stays fail-fast 404 (auth precedes validation, never aggregate into 422). 6 worker 500-class checks live in `_run_export_pipeline`, OUT of scope.
  4. NEW `ExportValidationFailedError(ExportError)` 422 code `export.validation_failed` mid `export.validation.failed` carrying `failed_checks: list[dict]`. Do NOT overload ProductNotReadyForExportError (worker reuses it unchanged).
  5. i18n keys 3-segment per §5A.H; `export.check.{check_id}` ✓ (underscores ok within segment). §19 CI Contract 10 scans — verify each passes.
  6. Test marker (PR #290 lesson): `pytestmark = pytest.mark.unit` ADDITIVE module-default. New file `test_export_validation_aggregation.py`. No existing `test_export.py` — fills gap.
  - Branch `feat/export-validation-aggregation` off origin/develop. Specialist = meesell-services-builder.
