### razorpay-integration (feature/razorpay, PR #323 → develop)
- **2026-06-20 — DEV-MOCK MODE SPEC authored** (`docs/plans/features/razorpay-integration/DEV_MOCK_MODE_SPEC.md`, slug `razorpay-dev-mock`). Founder wants to manually test billing LOCALLY with NO Razorpay account. SPEC-only (no build). KEY DESIGN: mock does NOT fake end-state — it (1) returns fake `RazorpaySubscription`/`RazorpayOrder`/`RazorpayCustomer` dataclasses from a new `adapters/razorpay_mock.py` (zero network), selected via a `_rzp()` service-seam in `iam/service.py` gated on `settings.razorpay_mock_active`; (2) replays a SYNTHETIC webhook (`subscription.activated` / `payment.captured` / `subscription.cancelled`) through the REAL `_route_webhook_in_session(...)` (NOT `capture_razorpay_webhook` — that would force the LOCKED HMAC verify) so the real `_handle_*` + `_grant_plan_for_sub`/`_TIER_TO_PLAN` grant `users.plan` + set `current_period_end` + write `audit_events` + the ON-CONFLICT `webhook_events` dedupe row. Result: existing `GET /billing/subscription` poll flips active on poll #1, `resolve_entitlement` returns right tier — NO parallel grant code, NO new route, NO new Celery task (both are inventory-guarded — must not break `test_app_boot_integration` / `test_celery_app_include_list`).
  - **Config (lead-owned `shared/config.py`):** new `RAZORPAY_DEV_MOCK: bool = False` (copy the `DEV_OTP_BYPASS_CODE` idiom) + property `razorpay_mock_active = RAZORPAY_DEV_MOCK and APP_ENV != "production"` (force-disable centralised; every call site reads the PROPERTY never the raw flag; every `_mock_drive_*` opens with `assert settings.razorpay_mock_active`). NOT in REQUIRED_FIELDS. Founder turns on via `RAZORPAY_DEV_MOCK=true` in dev `.env`; no real Razorpay creds needed.
  - **FE signal (chosen):** `mock: bool` on `BillingCheckout` (NOT a `key_id="rzp_mock"` sentinel — explicit, doesn't overload public key_id). FE branch point = `plans.component.ts subscribe()` next-handler: `if (resp.checkout.mock) { set('pending'); _startPolling(tier); return; }` BEFORE `openWidget`. Everything else (poll util, checkout service, trial, cancel) UNCHANGED.
  - **Builder split:** lead authors the config field+property (fast-mode, root wiring); `meesell-services-builder` = `razorpay_mock.py` + service `_rzp()`/tails + mock-mode test; `meesell-api-routes-builder` = `BillingCheckout.mock` schema + router set; FE = angular-service-builder (model) + angular-component-builder (branch) via frontend lead memo.
  - **4 OPEN questions for founder:** Q1 keep synthetic `evt_mock_*` webhook_events rows (rec: yes — faithful pipeline); Q2 OK for lead to touch config.py in fast-mode; Q3 scope mock to subscribe/trial/cancel only (no `update_subscription` tail — no change-plan UI); Q4 poll timeout irrelevant on mock (grant synchronous, poll #1 wins).
  - **TRAP found:** master HEAD `fa61a61` is BEHIND merge `8b5dce2` — local `adapters/razorpay.py` only has `verify_webhook_signature` (no async surface). All spec line refs are vs `8b5dce2`. Builders must base off the integration tip carrying merged Razorpay code; verify `grep -c "async def create_subscription"` == 1.

- **2026-06-20 — CI Gate-2 smoke inventory-guard FIX (commit `65aeb96`, not pushed).** 5 smoke failures were tripwire guards hardcoding route/task inventory, never updated for the Razorpay additions (skipped in CI until Gate-1 went green). ALL 5 = legitimate inventory drift, FIXED by updating the guards (NOT by removing real routes/tasks). Confirmed billing is correctly V1-modular wired: `iam_billing_router` exported from `app/modules/iam/__init__.py`, mounted via `app.include_router(iam_billing_router)` under `FEATURE_BILLING_ENABLED` (dev default True) — same flag-gate pattern as google/catalog; NOT a legacy flat router.
  - **2 files changed:** `tests/test_app_boot_integration.py` + `tests/test_celery_app_include_list.py`.
  - **test_no_stray_legacy_routes:** added 4 billing paths `/api/v1/billing/{subscribe,start-trial,cancel,subscription}`. KEY: `/api/v1/webhooks/razorpay` was ALREADY in develop's allowed_paths+count (Wave-2 capture-only webhook on `iam_router` router.py:278, already on origin/develop) — NOT a Wave-3 addition; did not re-add.
  - **test_total_route_count:** 30→**34** (4 new billing paths only; webhook already counted in the 30).
  - **3 celery tests:** include list 2→3 (added `app.modules.iam.tasks` — Wave-4 beat tasks `billing.reconcile` + `billing.trial_expiry_sweep`, FIRST beat_schedule in repo). Renamed `_exactly_2_v1_modules`→`_3_`; removed `app.modules.iam.tasks` from `v0_forbidden` (now legit, comment left, other V0 leftovers still forbidden); renamed `_only_2_v1_tasks...`→`_only_v1_` and bumped expected user-task set 2→4 (added the 2 billing names), protective exact-pin kept.
  - **SWEEP:** grepped tests/ for route-total / path-inventory / openapi-op-count / conf.include / beat_schedule / task_routes. 3 OTHER route-introspection tests found but NONE need editing (all assert a path is PRESENT, none assert a total): `test_billing_routes.py::test_billing_routes_mounted` (integration=Gate-4 advisory), `test_catalog_routes.py::test_production_app_mounts_catalog_router_by_default`, `test_google_auth_integration.py` (conditional flag-gated mount). NO openapi op-count snapshot in tests. NO beat_schedule assertion guards anywhere. NO real contract violations.
  - **VERIFY:** `pytest -m smoke -q` = **26 passed, 0 failed, 1291 deselected** against `DATABASE_URL=...localhost:5432/ci_dummy` (CI throwaway DSN; smoke never connects). Dev `meesell` DB (3772 cats) NOT touched. Full §5.D CI smoke env required (SECRET_KEY, REFRESH_TOKEN_PEPPER, AUDIT_PII_SALT, APP_ENV=development — see ci.yml smoke block). ruff clean both files. Master venv `/Users/.../mesell/backend/.venv`.
  - **LESSON:** feature adds routes/celery modules ⇒ predictable breakage = `test_app_boot_integration` (total + allowlist) + `test_celery_app_include_list`. Update them in the SAME PR. The exact-count + named-allowlist style is intentional (catches accidental additions) → fix = add legit entries + bump count, never weaken to a range.
- **2026-06-20 — CI Gate-1 unit collection FIXED ([setuptools-pkg-resources-ci-fix.md](setuptools-pkg-resources-ci-fix.md)).** PR #323 Gate-1 failed with 16 `ModuleNotFoundError: No module named 'pkg_resources'` collection errors across all modules. ROOT CAUSE (verified, not assumed): `razorpay==1.4.2` → `razorpay/client.py:4` does `import pkg_resources` at import; pkg_resources ships in **setuptools**, which Py3.12 + actions/setup-python + modern pip no longer bundle; razorpay metadata declares only `requests`. Cascade via `conftest → app.main → modules.iam → adapters.razorpay → razorpay`. FIX: add `setuptools>=70,<81` to `backend/requirements.txt` (only req file; CI Gate-1 installs it). `<81` LOAD-BEARING (setuptools 81 removes pkg_resources). Proven in throwaway py3.11 venv: collect-only -m unit 17→0 errors. Commit **`4091d24`** (not pushed). LESSON: any future 3rd-party SDK adapter — check for import-time `pkg_resources`/setuptools use; bound setuptools `<81`.
- **2026-06-20 — develop→feature/razorpay merge, MeResponse conflict RESOLVED (keep-both).** develop advanced 1 commit `1a45199` (#322 "make /auth/me phone nullable for Google-only users"). `git merge --no-ff origin/develop` conflicted in EXACTLY ONE file: monolith `backend/app/modules/iam/schemas.py` MeResponse (both #322 and our Wave-3 edited it). Resolution = UNION: `phone: str | None = None` (#322 nullable, for Google-only users) AND Wave-3 widening intact (7-value `plan` Literal free|starter|pro|pro_annual|business|business_annual|ltd, `trial_ends_at`, `entitlement`, plus pre-existing `onboarding_complete`). No duplicate fields. Merge commit = **`794d4de`** (2 parents 63b84a9+1a45199; NOT squashed). Did NOT push, did NOT touch PR — Director owns git.
  - **KEY FINDING — svc-iam tree is NOT widened, by design.** `backend/services/svc-iam/app/schemas.py` auto-merged to #322-ONLY (nullable phone, `plan: Literal["free"]`, NO entitlement/trial_ends_at) — CORRECT because Razorpay was a monolith-only feature; feature/razorpay made ZERO commits to svc-iam schemas (verified `git log origin/develop..63b84a9 -- that file` = empty). svc-iam is the extracted microservice and intentionally carries none of the Razorpay billing widening. Do NOT "fix" this asymmetry.
  - **openapi.json both NOT stale.** monolith `postman/openapi.json` MeResponse = nullable phone + 7-enum plan + entitlement + trial_ends_at (matches monolith schema); svc-iam `openapi.json` = nullable phone, no widening (matches svc-iam schema). Git textual auto-merge landed correct (disjoint JSON regions). NO follow-up regen needed.
  - domain.py (both trees) auto-merged clean, 0 markers. `git diff --check` clean.
  - **VALIDATION GOTCHA (repeat):** importing `app.modules.iam.schemas` triggers config SystemExit (package `__init__` side-effect validates all §5.D env vars) even for a pure-Pydantic module. Workaround for NON-DB import check = run with the 16 §5.D env vars set to dummy values (DATABASE_URL/VALKEY_URL never actually connected for a schema import) under master venv `/Users/.../mesell/backend/.venv` (Py3.11, has razorpay+pydantic). `model_json_schema()` then confirmed phone anyOf[string,null]+default null AND plan 7-enum AND entitlement+trial_ends_at present. ruff clean on schemas.py (ruff from PATH; not in venv).
  - RESIDUAL RISK: none material. The dev-DB guard was honored (no pytest/alembic). PR #323 CI Gate-1/2/3 should still pass (schema is import-clean + lint-clean); Gate-4 advisory.
