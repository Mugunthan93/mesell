## Session mesell-ci-gate1-fix-session-1 — 2026-06-11 — SPEC authored for CI Gate-1 collection fix (Rule 7 STEP 1)
- Spec at [spec_ci_gate1_fix.md](spec_ci_gate1_fix.md). Infra handoff: `.claude/agent-memory/meesell-infra-builder/handoff_ci_gate1_collection.md`. First main pipeline (run 27318816408, PR #64) RED at Gate 1, exit 4, pytest COLLECTION: `ModuleNotFoundError: No module named 'app'`.
- **Root cause sharpened beyond infra's diagnosis:** the precise mechanism is `importmode=prepend` + MISSING `tests/__init__.py` and `tests/modules/__init__.py` (but PRESENT deep ones like `tests/modules/category/__init__.py`). Prepend walks the __init__ chain and inserts the package root it finds (`tests/modules/` or `tests/`), NEVER `backend/`. So `app/` is unreachable. Not "nothing puts backend on path" generically — it's the asymmetric __init__ layout. Verify __init__ presence/absence whenever a collection ImportError shows up.
- **Local box is NOT a witness for CI import failures.** Founder laptop = Python 3.9; the suite dies even earlier on PEP 604 `Mapped[str | None]` syntax, masking the app-import gap that clean 3.12 CI hits. `import app` confirmed FAILS from /tmp with mesell paths stripped. Always reason from CI's clean-checkout + 3.12, not local.
- **§19.D lock ruling (coordinator's call, no founder flag):** the §19.D block in BACKEND_ARCHITECTURE.md is a CONSOLIDATED ILLUSTRATIVE config (`[tool.pytest.ini_options]`, `testpaths = ["backend/tests"]`) that ALREADY diverges from the as-built `backend/pytest.ini` (`[pytest]`, `testpaths = tests`). The lock governs the marker set + asyncio mode + strict flags + real-vs-mock fixture policy — NOT a byte-for-byte ini mandate. Adding an ADDITIVE `pythonpath = .` preserves all locked semantics, so no founder approval needed. Pattern: distinguish "locked contract" from "illustrative example of the contract" before calling something a §7.3 amendment.
- **Chosen fix:** Option (a) `pythonpath = .` in pytest.ini — purely backend-owned, smallest diff, avoids the infra-owned ci.yml (Option b) and the imperative conftest shim (Option c, held as fallback). pytest 8.3.0 pinned -> pythonpath (>=7.0) supported.
- **Dispatch decision:** `meesell-api-routes-builder` (sonnet) for STEP 2 — pytest/route test-tooling domain, mechanical 2-line change, verification-gated; opus services-builder unwarranted.
- **Do-not-create list for this fix:** backend/__init__.py, tests/__init__.py, tests/modules/__init__.py, pyproject.toml, setup.py. Creating backend/__init__.py would make backend a package and change import semantics — actively harmful.
- This is a standalone CI hotfix (no feature/{name}/backend parent) so the D1 group-gate does not apply; fix -> develop, then develop->main is the founder's gate per the infra handoff sequence. Inter-lead request from infra is OPEN on feature_board_infra.md; close via memo after merge.
