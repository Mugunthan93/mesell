### category-monitor Wave-1 SCHEMA SPEC (2026-06-22, HYBRID step-1, SPEC only)
- Session `mesell-category-monitor-backend-session-1`. Feature = RETENTION_CATEGORY_MONITOR (#370, MERGED to develop @ `bbeb1ec`; amended monthly via branch `feature/scraper-cadence-reconcile/data`). V1.x retention add-on, NOT V1-surface. Spec for `meesell-database-builder` (sole Wave-1 specialist). NO code authored — step-1 only.
- **LIVE alembic single head = `e9415bdcae20`** (Razorpay billing merge migration `e9415bdcae20_merge_razorpay_billing_develop_heads.py`, down=(d4e5f6a7b8c9, f8fa7a36383f)). The new Wave-1 migration down_revision MUST be `e9415bdcae20`. GOTCHA: c2d3e4f5a6b7's FIRST `down_revision = b7c2e1a9d3f4 — the LIVE monolith head re-confirmed at` is a DOCSTRING comment (line 12), the REAL assignment is line 40. `alembic heads` confirmed `e9415bdcae20 (head)` — always run the CLI / compute the graph, never trust a single grep.
- **LOAD-BEARING SCHEMA GROUND-TRUTH (verified vs live ORM):** `catalogs.category_id` is **NULLABLE** (`ForeignKey(...ondelete="SET NULL")`, `Mapped[uuid.UUID | None]`) — a catalog may defer its category. `products.category_id` is **NOT NULL** (`ondelete="RESTRICT"`). So the founder-design premise "catalogs already keys (user_id, category_id)" is only PARTLY true — the durable non-null category link lives on `products`, not `catalogs`. This is the #1 ambiguity for the `category_subscription` VIEW: key on catalogs.category_id (misses catalogs w/ products but null catalog category), products.category_id (the real per-listing category), or UNION both. RECOMMENDED + spec default = UNION (catalog-level non-null + product-level), DISTINCT, so fan-out audience = every seller with a catalog OR product in category X. Flagged to Director/founder; builder needs the answer before writing the VIEW SQL.
- **MODULE-PLACEMENT reconciliation (decided, no escalation):** ORM models live FLAT in `app/shared/models/` registered in `__init__.py` in strict FK order — LOCKED §5.E ("single canonical import surface", no module redefines a model). So the new `category_snapshot` + `notification` ORM CLASSES go in `app/shared/models/` (NOT a per-module subdir), appended to `__init__.py` after webhook_events. The founder's "new `monitor` module" default = the FUTURE service/router/task code (`app/modules/monitor/` — Wave 2+), NOT the models. Wave-1 schema touches ONLY the models layer + one migration. Modules dir today: catalog/category/customer/dashboard/export/iam/image/pricing (each = domain.py/exceptions.py/repository.py/router.py/schemas.py/service.py).
- **Catalog flag columns go on `products` NOT `catalogs`** — recheck/reprice/export are per-listing actions; §4.2 says "flag each affected catalog" but the actionable unit + the NOT-NULL category link is the product. RECOMMENDED: `needs_recheck`/`needs_reprice`/`needs_export` BOOLEAN NOT NULL DEFAULT false on `products`. (Flagged as the #2 confirm — products vs catalogs for flags.) Idempotent set (= true), never appended.
- **No new subscription table, no new demand-count table** (founder ruling 2026-06-22): demand-count = COUNT over the VIEW; subscription = the VIEW itself. notification = idempotent UNIQUE (user_id, category_id, content_hash), channel-agnostic (bell-only V1.x but no channel column).
- Merge-gate criteria for step-3: additive-only (CREATE TABLE / CREATE VIEW / ADD COLUMN ... DEFAULT only — zero ALTER-of-existing-column, zero backfill), reversible downgrade (DROP VIEW + DROP TABLE + DROP COLUMN), indexes present (latest-snapshot-per-category partial/composite + notification unread lookup), VIEW correct vs the UNION decision, alembic-skill compliant (UUID PK, TIMESTAMPTZ, JSONB, NOW() server_default), disposable TEST_DATABASE_URL for any test (NEVER the live dev `meesell` DB w/ 3772 cats), zero V1-surface regression (no existing model/route/schema touched).
- **WAVE-1 MERGE-GATE STEP-3 — VERDICT PASS → SQUASH-MERGED to `feature/category-monitor/integration` (`b7487e8`) (2026-06-22).** PR #450 (`feature/category-monitor/backend`→integration). Builder commit `3dd86b2` schema + `949e352` STATUS. ALL 8 CRITERIA PASS, every one verified INDEPENDENTLY (not builder report). Board+STATUS landed to develop via docs PR #452 squash `ff91e75` (worktree-recipe — see GOTCHA below).
  - **GATE-RUN MECHANICS (reuse):** PG on **:5432** has TWO DBs — live `meesell` (3,772 cats, MUST NOT touch) + disposable `meesell_test` (0 cats, already at `e9415bdcae20` = exactly the down_revision). The `meesell` role has NO CREATEDB and there is NO `postgres` superuser role → could NOT create a fresh gate DB. **Use `meesell_test` as the disposable gate DB** (NOT live `meesell`). Verify disposable by `SELECT count(*) FROM categories` = 0 (vs live 3772). Alembic needs the FULL §5.D secret set → `set -a && . /Users/.../backend/.env && set +a` then OVERRIDE `DATABASE_URL=postgresql+asyncpg://meesell:password@localhost:5432/meesell_test` + `TEST_DATABASE_URL=$DATABASE_URL`. Run alembic from a worktree of the branch tip via master venv `/Users/.../backend/.venv/bin/alembic` (1.13.2, sa 2.0.32). upgrade head → downgrade -1 → re-upgrade round-trip; `alembic heads` (STATIC graph, no DB) is the fastest single-head confirm.
  - **DRIFT-CLAIM VERIFICATION pattern (criterion 7, reuse for any schema PR):** run `alembic revision --autogenerate -m PROBE` on the upgraded test DB, python-parse the generated `upgrade()` body — assert (a) it contains ONLY the KNOWN pre-existing drift (here: comment-only `op.alter_column` on pricing_calcs×26 + users×4, logged "Detected column comment ...") and (b) ZERO references to ANY of the PR's new objects. ZERO new-object refs PROVES the migration matches the ORM exactly (no drift introduced). DELETE the probe file after. Definitive way to confirm a builder's "pre-existing drift, not mine" claim.
  - **VIEW seed-test (criterion 4):** seeding a catalog+product pair needs the FULL FK chain: `templates`(schema_hash,schema_jsonb) → `categories`(6 NOT-NULL incl. template_id is a UUID FK to templates) → `users`(phone OR google_sub — dual-identity CHECK `ck_users_at_least_one_identity` BLOCKS phone=NULL) → `catalogs`(user_id,name,category_id) → `products`(catalog_id,user_id,category_id). DO IT IN ONE ATOMIC TRANSACTION — a mid-txn FK error rolls back ALL prior inserts (lost 3 round-trips to piecemeal failed txns). UNION VIEW catalog branch projects `c.id AS catalog_id`, product branch projects `p.catalog_id`; for a product in that catalog they're IDENTICAL so UNION DISTINCT collapses to 1 row — demand-count = COUNT(DISTINCT user_id). Soft-deleting product drops product branch, catalog branch survives.
  - **DESC-index non-issue:** spec said `(category_id, captured_at DESC)` but builder built `(category_id, captured_at)` (no DESC). NON-BLOCKING — B-tree bidirectionally scannable so `ORDER BY captured_at DESC` queries use it identically. DESC only matters for multi-column MIXED-direction sorts. Did NOT reject.
  - **CONTAMINATION TRAP (#428 recipe FIRED AGAIN — board landing):** master tree on `develop` but its working-tree `feature_board_backend.md` was DIRTY and STALE (BEHIND origin/develop — missing the #428/#419/BE-CUSTOMER-INSERT-500-1 rows that ARE on origin/develop). Editing directly would have REVERTED those landed rows (numstat showed bogus 2+/5- on a divergent base). RECIPE: `git checkout -- <board>` in master tree to drop stale dirty file (leave OTHER sessions' dirty files) → `git worktree add /tmp/wt -b chore/... origin/develop` (FRESH off origin) → re-apply edits THERE (grep-confirm `BE-CUSTOMER-INSERT-500-1` count=5 BEFORE editing) → commit+push+`gh pr create/merge --squash --admin` → `git push origin --delete` + `git worktree remove --force`. ALWAYS grep-confirm the target file has the EXPECTED latest landed rows before editing.
  - **GH self-approve blocked:** `gh pr review --approve` fails "Can not approve your own pull request" (author=Mugunthan93=merger). Post verdict as `gh pr comment` instead — review CONTENT is the gate, squash-merge is the lead action (D1 M-step-1).
  - **RECOMMENDATION made: integration→develop FOUNDER gate NOT opened** — schema-only not independently useful; let `feature/category-monitor/integration` accumulate Wave-2 (services/repo/router/Celery) before the founder's single develop merge. Mine to recommend; founder owns the gate.
  - **WAVE-2 CARRIED CONSTRAINT:** `category_subscription` is a VIEW, NOT an ORM table (`'category_subscription' not in Base.metadata`). Wave-2 service/repository MUST query via `text()`/raw SQL, never a mapped class. Demand-count = `COUNT(DISTINCT user_id)` over the VIEW. Fan-out idempotency = `INSERT ... ON CONFLICT DO NOTHING` on `uq_notification_user_cat_hash`.
