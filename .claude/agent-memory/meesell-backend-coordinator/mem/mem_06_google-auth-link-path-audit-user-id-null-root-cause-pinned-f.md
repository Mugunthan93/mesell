### google-auth LINK-path audit user_id=NULL — ROOT CAUSE PINNED + FIX VALIDATED (2026-06-20, HYBRID step-1 spec only)
- **DEFECT (xfail `test_google_links_to_existing_phone_user_by_email`):** Google account-link branch (phone user + Google login on matching verified email) writes `auth.login.success`+`auth.google.linked` audit rows with `user_id=NULL` → `NotNullViolationError` at request commit. Reproduced LIVE (disposable `meesell_glink_test`, dropped after; dev DB 3772 untouched).
- **ROOT CAUSE (NOT the dispatch hypothesis):** the smoking-gun SQL is `UPDATE audit_events SET user_id=NULL WHERE id IN (1,2)` — the SAVEPOINT INSERT lands user_id CORRECTLY, then SQLAlchemy's unit-of-work NULLS it on the LINK path. Trigger = the bidirectional `back_populates` between `User.audit_events` ⇄ `AuditEvent.user` (`app/shared/models/user.py:116-119` + `app/shared/models/audit_event.py:88-91`). On the link path `by_email` is a session-managed persistent User whose `audit_events` collection (never appended-to) is treated as authoritative → the unit-of-work nulls the child FK to match the (empty/None) relationship. CREATE path (rule-3 new User) escapes because the fresh transient object's collection isn't established the same way. NOT in `_write_audit_direct` (it's correct), NOT in the repo (resolution is right), NOT expire_on_commit (prod+test both `False`).
- **FIX (monolith-only, NO migration):** decouple the relationship. `User.audit_events` → drop `back_populates`, add `viewonly=True`; `AuditEvent.user` → drop `back_populates` (plain relationship). Validated: link test passes, OTP `auth.login.success` path + 57 audit/auth/db tests + boot test all GREEN, zero readers of these relationships exist (all audit access is scalar `user_id` query). **NO DDL/migration** — relationship loading directive only.
- **DUAL-TREE: svc-iam is NOT affected** — `services/svc-iam/app/shared/models/{user,audit_event}.py` DELIBERATELY DROP all ORM relationships (vendored copies, header says so). Fix is MONOLITH-ONLY (2 files). These tree copies were already divergent by design; no byte-parity guard covers them.
- **TEST FIX:** flip xfail→passing; assert `auth.login.success`+`auth.google.linked` audit rows exist with `user_id==original_id`; FIX CLEANUP — must `DELETE FROM audit_events WHERE user_id=...` BEFORE deleting the user (RESTRICT FK now correctly blocks user-delete while audit rows reference it; the old test's user-delete cleanup only "worked" because the bug nulled the FK).
- **LANDING:** branch `fix/google-link-audit-userid` off `feature/google-auth` (NOT develop — google code isn't on develop; lives on feature/google-auth + PR #322 `fix/me-phone-nullable-google`). Target = `feature/google-auth`. **ORDER: #322 must merge first** (both touch iam area; #322 owns the test file → rebase after). Severity = **fix-before-staging** (NOT launch-blocker: route flag-gated OFF in dev/prod; but real Google-link logins 500 the moment the flag flips, so MUST land before google-auth go-live / staging enablement). Spec handed to Director; specialist = `meesell-services-builder`. No i18n (internal audit-write bug).
- **MERGE-GATE STEP 3 — LANDED ON DEVELOP 2026-06-20 (`mesell-google-auth-backend-session-2`, PR #324 CLOSED, fix = cherry-pick `d1bd031`).** My step-1 LANDING note above was SUPERSEDED by the builder's correct re-base: by 2026-06-20 the entire google stack had ALREADY squash-merged to develop (#295 google BE, #296 google FE, #299 infra flag, #322 me-phone-nullable harness). So `feature/google-auth` was STALE (8 pre-squash commits already on develop; develop 32 ahead). Builder correctly branched off `develop @ fa61a61` and targeted develop, editing the #322-REPAIRED test (not the original #295 one). I RATIFIED this with git (clean throwaway clone): merge-base of PR branch = develop tip `1a45199`; develop carries adapter/migration/service+test + the #322 fix; develop was correct base+target. **LESSON (repeat of the recurring stale-base hazard — see ci-gate1 #156):** my own step-1 spec gave a now-stale target (`feature/google-auth`); always re-derive the topology from LIVE git at gate time, never trust the spec's base/target verbatim. The builder caught it; I confirmed it.
- **REJECTED the squash-merge — branch contamination (NEW LESSON, important).** PR #324's CODE fix was perfect (3 files, CI Gate 1/2/3 + advisory Gate 4 all GREEN; zero navigational readers of `User.audit_events`/`AuditEvent.user` confirmed across audit_mw/cost_tracker/image-tasks/iam-service/export-tasks — all writes are scalar `AuditEvent(user_id=...)`). BUT the branch carried **2 extra chore commits** (`fa61a61` statusline config, `cfc2bd7` "persist agent memory/knowledge/status before develop sync") bundling **~9,500 lines** of unrelated content (agent MEMORY dumps, meesho probe scripts 584–1369L each, razorpay/google design docs) **AND a DESTRUCTIVE revert of 1,626 lines of live frontend status** (`docs/status/STATUS_FRONTEND.md` develop=9,176L vs branch=7,714L — the chore commit was snapshotted from a local master tree BEHIND develop on docs). A squash-merge would have applied that revert to develop. Per no-creep rule + the "do not merge contaminated PR" stop-condition, I did NOT squash. **REMEDIATION = cherry-pick ONLY the self-contained fix commit `09eeaf9` onto develop (clean throwaway clone, recovery-prefix `MESELL_ALLOW_MASTER_GIT=1` since the master-tree git guard fires on ANY commit/cherry-pick even in a /tmp clone), pushed as `d1bd031`, closed PR #324 with full explanation, deleted the branch.** Delivers exactly the founder's intent (fix on develop) with zero contamination. main untouched, no deploy.
- **PROCESS PATTERN to reuse:** when a PR's *source* diff is clean but the branch bundles chore/memory/status commits that revert develop → don't squash; cherry-pick the isolated fix commit to develop, close the PR explaining the contamination, recommend a separate reconciled PR for the chore content. Check `git show origin/develop:STATUS_*.md | wc -l` vs the branch BEFORE merging any PR that touches status/memory files.
- **FOLLOW-UP for founder:** RECOMMEND deleting stale `feature/google-auth` (8 commits fully superseded by develop squashes #295/#296/#299) — did NOT delete it (founder sign-off needed; only the #324 fix branch was deleted).
