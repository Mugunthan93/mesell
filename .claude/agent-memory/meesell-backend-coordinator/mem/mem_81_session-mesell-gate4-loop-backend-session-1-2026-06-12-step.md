## Session mesell-gate4-loop-backend-session-1 — 2026-06-12 — STEP 3 merge-gate REJECT of PR #159 PASS 1 (arc since CLOSED: rejected→repaired→merged)
**ARC OUTCOME (added when correcting the record via docs PR #162):** the pass-1 REJECT below is NOT the final state. Repair loop 1 (commits `0059272` + `b9d1557`) fixed all three defects exactly as the lesson predicted (D1 drop SAVEPOINT→commit-for-real, D2 patch `_otp_client` in all 4 fixtures, D3 honest G7 test-stale fix). Re-gate CI run `27394517680` came back **180p/17s/0f/0e**, all 13 checks green; I re-gated and PR #159 MERGED HONESTLY (squash `fdfef68` → develop, NO `--admin`-over-red). Post-merge develop run `27395272576` was the first fully-green develop run (Build + Deploy both green). BE-CATALOG-G7-AUTOAPPLY-1 dispositioned test-stale/no-app-bug (D3 fix accepted as honest G7 conformance). GATE-4 SAGA 2 CLOSED. The pass-1 reject detail below is retained verbatim — it is the durable lesson.
- **PASS-1 VERDICT: REJECT** (real gate, repair loop 1 of 2). PR #159 `fix/gate4-loop-contamination` → develop, api-routes-builder, from spec `spec_ci_gate4_loop_contamination.md`. Gate-4 RED on CI run `27393519389`: **5f/162p/17s/13e** vs develop baseline 6f/13e → net ~zero. NO --admin (the #150 override is single-use, spent).
- **Fence + template were CLEAN** — only the 4 test files + STATUS + own MEMORY.md; zero app/pytest.ini/alembic/ci.yml/root-conftest-LOCKED diff; template fully filled. The defect was FIX CORRECTNESS, not scope. Lesson: a clean fence and a green local unit run (634p) tell you nothing about Gate-4 — the specialist could NOT run `-m integration` locally (no dev DB tunnel), so the PR's OWN Gate-4 CI check is the only proof. ALWAYS wait for + read the Gate-4 job log; never take the diff on faith.
- **D1 root cause = SPLIT-ENGINE incompatibility with SAVEPOINT isolation (the key gotcha for next pass).** The 3 failing integration tests (`test_customer_cross_module_eligibility` ×4, `test_customer_full_onboarding_flow` ×1) create the user by driving the REAL route through `iam_client`, then read/insert profile state via a SEPARATE engine built by `_make_session_factory()` on `os.environ["DATABASE_URL"]` (independent committed-read connection). When `iam_client` got a SAVEPOINT-isolated get_db override (copied from customer_client), the user creation went into an uncommitted outer-txn → the second engine's connection can't see it → `ForeignKeyViolationError fk_seller_profile_user_id`. On develop, `iam_client` had NO override → wrote to the app-global engine which COMMITTED → second engine saw the user. **RULE: SAVEPOINT per-test isolation is INCOMPATIBLE with any test that reads via an independent second engine. For such split-engine tests, the route path must commit for real (function-loop NullPool engine + commit, prefix-cleanup teardown), NOT savepoint.** The cross-loop error is fixed by the function-loop engine ALONE; savepoint is an over-application.
- **D2 = the 13 `Event loop is closed` teardown errors are STILL UNCHANGED across the whole Gate-4 saga (passes 1-4 AND this pass).** Pass-3/pass-4 board rows claimed "0 errors" but those were measured on the local savepoint substrate, NOT real CI. On real CI the 13 teardown errors (export ×4 + customer ×9) persist. The explicit `app.state.db_engine.dispose()` after lifespan exit did NOT clear them — the dangling asyncpg connection-lost callback belongs to a DIFFERENT pool (likely the request-override NullPool engine or a Valkey client), and/or fires after the loop's pending callbacks. Next pass must get the FULL traceback of ONE error and target the actual leaking object. **Lesson: "0 errors" claims from prior passes were substrate-optimistic; the real-CI Gate-4 error count was never actually zero. Re-baseline against real CI, not the local savepoint harness.**
- **D3 = H1 was ASSUMED in the PR body, not proven; it was disproven by CI.** catalog `test_full_lifecycle` `assert autofill_result.applied.get("product_name") is True` still `assert False is True`. The catalog-form G7 ruling (consumed in PR #115: "remove auto-apply; autofill writes ONLY to ai_suggestions_jsonb, applied always False") means the AS-BUILT app correctly returns `applied={}` — so the TEST is the stale party (asserts the pre-G7 behavior). This is **H2 = genuine drift**, but it's TEST-stale not APP-stale (app honors G7; test predates it). Still: per spec §6 the specialist must STOP and report, NOT silently flip — whether to update the test to `is None/False` or treat as app-bug is a founder-scoped call given G7 history. Likely outcome: a tiny separate test-only fix asserting `applied.get("product_name")` is falsy, cross-referenced to the G7 ruling. Track as BE-CATALOG-G7-AUTOAPPLY-1.
- **KEEP across repair:** (a) `test_shared_database` AsyncSessionLocal `with _patch(...)` rebind — sound, no longer failing; (b) `iam_client` function-loop engine + get_db override MECHANISM — cleared `test_iam_replay_attack` (pure-iam_client, no second engine). Only the SAVEPOINT layer + the unsolved teardown disposal need rework.
- **Write-guard gotcha (repeat of specialist's experience):** the master/shared checkout blocks writes for an un-isolated bg subagent. Did board/STATUS/memory edits in a dedicated worktree `/tmp/mesell-wt/gate4-reject` (branch `docs/gate4-loop-rejected` off develop `00abb05`) and committed there. NOTE: the SHARED checkout's develop was STALE (`00abb05`/earlier mirror) vs the worktree's fresh `origin/develop` which carries the fuller board header (flag-parity + xlsx + image-precheck + catalog-form Prior lines) — ALWAYS fetch + edit the board in a fresh worktree off origin/develop, never trust the master checkout's working copy for board state.
- (Pass-1 status, since superseded:) develop was NOT fully unblocked at the moment of this reject — the post-#110 "Gate-4 READY TO RE-FIRE" was invalid because #150 unmasked latent contamination. **RESOLVED:** PR #159 itself (not a separate successor) cleared repair loop 1 and merged Gate-4-green; develop run `27395272576` is fully green. Re-fire UNBLOCKED.
