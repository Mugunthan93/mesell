# Memory — meesell-frontend-coordinator

## Agent Identity
Frontend coordinator for MeeSell. Orchestrates the 3 frontend specialists (component, service, ui-styler). Owns STATUS_FRONTEND.md, app.config.ts, app.routes.ts, FRONTEND_ARCHITECTURE.md, integration smoke tests. Decentralized memory ecosystem.

## MEMORY.md
- [Framework gate](framework_gate.md) — Angular 18 ratified 2026-06-05; React scaffold to be deleted
- [Architecture doc authored](architecture_doc_authored.md) — FRONTEND_ARCHITECTURE.md SKELETON+§0 LOCKED+§1 DRAFT
- [Third-party tool picks](third_party_picks.md) — locked dependency set for V1
- [Locked decisions inherited](locked_decisions_inherited.md) — CLAUDE.md D9-D14 + MVP §4 + Philosophy M1/M9
- [Cross-track contracts](cross_track_contracts.md) — what backend/data/ai must hand us
- [Backend handoff: JWT session pattern](backend_handoff_jwt_session_pattern.md) — FE-D5+FE-D6 deltas the founder takes to backend session 2026-06-05
- [Section 1 locked](section_1_locked.md) — §1 LOCKED 2026-06-05 post backend ratification + 3 founder-ratified strengthenings (Lua EVAL, HMAC-pepper, Path=/api/v1/auth) + pre-deploy checklist
- [Section 3 locked](section_3_locked.md) — §3 LOCKED 2026-06-05 with design-system reframed as style architecture surface; 5 locked decisions; dispatch order for 3 specialists
- [Discipline: no premature dispatch](discipline_no_premature_dispatch.md) — FE-D7 founder ruling 2026-06-05; full architecture must be LOCKED end-to-end before any specialist dispatch; roadmap of 22 remaining sections in recommended LOCK order
- [Section 2 locked](section_2_locked.md) — §2 LOCKED 2026-06-05 with auth+onboarding merger into single account feature (9 folders, 4 routes, 7 endpoints, 2 backend peers iam+customer); AMENDMENT 2026-06-05B propagated to §3 + §6 + §7 + §8 (reserved) + §23 + TOC
- [Section 4 locked](section_4_locked.md) — §4 LOCKED 2026-06-05; AuthService API + 4 interceptors + ApiClient with retryOn503 opt-in + ErrorService + NetworkService + 9 cross-feature models. Looks 2/3 deferred to V1.5. FE-D8: coordinator owns drill-down depth call for upcoming sections.
- [FULL DOC LOCKED](full_doc_locked.md) — FRONTEND_ARCHITECTURE.md fully LOCKED end-to-end 2026-06-05; 22 of 23 sections LOCKED (§8 RESERVED merged into §7); FE-D7 satisfied; specialist dispatch authorisation ACTIVATED; pre-deploy gates remain on REFRESH_TOKEN_PEPPER + backend iam + CORS
- [Angular 18 → 20 upgrade](angular_20_upgrade_2026_06_08.md) — founder-approved 2026-06-08; supersedes locked D9 "Angular 18"; Material v20 variant-prefixed tokens fixed in _component-overrides.scss (button/card/form-field/menu); dev build 5.4s, ng serve returns HTTP 200; transloco 7→8; @angular-eslint + vitest-angular still on v18 toolchain (non-blocking)
- [Wave 2B scaffold](wave_2b_scaffold_2026_06_08.md) — 2026-06-08 clean re-scaffold: OLD Material stack archived to archive/frontend_angular_material/; NEW = Angular 21.2.16 + PrimeNG 21.1.9 + @primeuix/themes 2.0.3 + Tailwind 4.3.0. Tailwind wired via PostCSS (postcss.config.mjs + styles.css `@import "tailwindcss"`); @angular/build:application builder; vitest 4 default; zoneless+strict defaults. Build green 2.278s. Coordinator executed via Bash (no specialist dispatch); ui-styler takes 2B-2. PrimeNG theme via providePrimeNG() not CSS import.
- [Module Federation Master Plan DRAFT](module_federation_master_plan_2026_06_10.md) — 2026-06-10 authored `docs/plans/module_federation/MASTER_PLAN.md`. 6 remotes (auth/onboarding/dashboard/catalog/pricing/export); strangler-fig migration; pricing pilot, auth last; Native Federation (NOT Webpack MF — preserves esbuild builder); shared libs `@mesell/{ui-kit,composites,core,design-tokens}`; auth singleton via federation manifest; Tailwind single-build at shell. DRAFT — awaits founder approval. Zero code changes. 8 sub-plans queued.

## Session mesell-repo-management-session-1 — Step 5 — Coordinator spec evolved into Frontend Lead spec; feature_board_frontend.md initialised

- **D1 verbatim (locked 2026-06-10):** "Lead reviews/merges `feature/{name}/<group>` → `feature/{name}`. Founder reviews/merges `feature/{name}` → `develop`." — encoded into spec's Merge gate section as explicit do/don't and into Hard Constraints (NEVER approve `feature/{name}` → `develop`).
- **D2 verbatim (locked 2026-06-10):** "Specialist marks `IN REVIEW` on PR open. Lead marks `MERGED` on PR merge." — encoded into spec's Update protocol section as a who-does-what event table and into Operating Procedure steps 6/8.
- **D3 verbatim (locked 2026-06-10):** "Replace, not extend… The 'coordinator' term is retired in those files; the agent slugs stay unchanged (no rename)." — executed: slug `meesell-frontend-coordinator` preserved; title rewritten to "MeeSell Frontend Lead"; identity reframed from orchestrator to lead with explicit merge gate ownership; coordinator concept retired throughout body.
- **Files written:** `.claude/agents/meesell-frontend-coordinator.md` (152 → 324 lines, full top-to-bottom rewrite via heredoc — Write/Edit perms denied for agent specs, Bash heredoc accepted); `docs/status/feature_board_frontend.md` (new, 41 lines, all 3 tables empty per §6.2 template).
- **Behavioural change (coordinator → lead):** I now (a) own a sole-writer board surface and sweep it at session start + end with 7-day staleness flagging, (b) gate `feature/{name}/frontend` PRs with explicit checklist (90 s build, 360/1280 screenshots, a11y, CI 1+3), (c) coordinate cross-lead via memo + own-board inter-lead rows with 48 h SLA, (d) enforce session-name format `mesell-{feature}-frontend-session-{N}` on every specialist dispatch. New sections added: Owns, Merge gate, Update protocol, Cross-lead coordination, Session naming, Specialists you dispatch.
- **Pointers:** `docs/plans/repo_management/MASTER_PLAN.md` §6 (feature_board template) + §7 (lead responsibilities, esp. §7.1 sole-writer surfaces, §7.3 architecture-amendment escalation, §7.5 cross-lead memo protocol) are the canonical reference. Module federation master plan is now framed as my plan (owner) — sub-plans 0–7 queued under `docs/plans/module_federation/` pending founder approval of the master plan DRAFT (2026-06-10).

## Session mesell-knowledge-sync-frontend-session-1 — 2026-06-10 — as-built audit findings (non-obvious only)

- **BOUNDARY VIOLATION (real, 2 files):** PrimeNG imported OUTSIDE `src/app/ui/`. (1) `layouts/shell/shell.component.ts` imports `Drawer`, `Menu`, `MenuItem` directly from primeng — Layer 3 leaking PrimeNG. There is NO `mee-*` drawer/menu wrapper in the ui barrel (no MeeDrawer/MeeMenu exist), so the shell had nowhere to delegate → architectural gap, not just laziness. (2) `app.config.ts` imports `providePrimeNG`, `MessageService`, `ConfirmationService` from primeng — this is config-layer wiring and is arguably the legitimate exception (theme provider must live at root), but FRONTEND_ARCHITECTURE.md §2 "PrimeNG imports forbidden outside ui/" has no documented carve-out for app.config or shell. ACTION when shell feature reopens: either add MeeDrawer+MeeMenu wrappers to Layer 2, or amend §2 with an explicit allowlist (shell-chrome + root config). Founder approval needed for the §2 amendment (doc LOCKED).
- **ui layer is 19 components + 2 services, NOT 17.** Barrel `src/app/ui/index.ts` exports: button, input, otp-input, badge, card, table, dialog, file-upload, steps, select, tree-select, skeleton, progress-bar, toast(+MeeToastService), confirm-dialog(+MeeConfirmService), password-input, textarea. The "17 mee-* wrappers" figure in my MEMORY/spec is STALE — password-input + textarea (and otp-input split) pushed it to 19. Update spec Layer 2 count when convenient.
- **NO HttpClient ANYWHERE. All data is simulated.** `provideHttpClient`/`withInterceptors` absent from app.config.ts. The 3 feature API services (dashboard, catalog-form, smart-picker) return `of(SEED_DATA).pipe(delay(N))`. AuthService is a pure in-memory signal store with NO login HTTP call (`setSession` is called by... nothing real yet). jwtInterceptor + errorInterceptor + ApiClient + NetworkService + ErrorService from §4-LOCKED DO NOT EXIST as files. core/ has only auth.guard + auth.service + theme/meesell-preset. This is the entire Wave 6 gap: §4 service layer was DESIGNED (locked) but never BUILT. Waves 3-5 delivered UI shells + Layer1-4 components against seed data only.
- **@angular/cdk NOT in package.json.** live-preview brief mandates CDK drag-drop for the image carousel — that dep must be added when live-preview frontend session starts. Currently no CDK.
- **transloco NOT in package.json** despite angular_20_upgrade note mentioning "transloco 7→8". The Wave 2B clean re-scaffold dropped it. i18n is currently unwired.
- **Route drift vs FRONTEND_ARCHITECTURE.md §6 / V1 spec:** routes.ts registers EXTRA routes not in the canonical 10: `/onboarding` (under shell) and `/catalogs` list (CatalogListComponent). `/catalogs/:id/edit` uses `loadChildren` (catalog-form.routes.ts) not loadComponent. Auth pages (`/login` `/signup` `/otp-verify`) are top-level (each self-wraps AuthLayout) — matches §2 amendment. All 10 canonical V1 routes ARE present + wired. Wildcard `**` → `/login`.
- **Builder/tooling confirmed as-built:** Angular ^21.2.0, primeng ^21.1.9, @primeuix/themes ^2.0.3, tailwind ^4.3.0, @angular/build ^21.2.14 (esbuild), vitest ^4.0.8, rxjs ~7.8, TS ~5.9.2. tsconfig strict:true + strictTemplates:true confirmed ON. design-system = single `_tokens.css` (Layer 1 intact).
- **Test counts:** ui 17 specs / shared 5 specs / features 13 specs / core 0 specs / layouts 0 specs. core has ZERO tests (auth.guard + auth.service untested) — flag when §4 service layer is actually built.
