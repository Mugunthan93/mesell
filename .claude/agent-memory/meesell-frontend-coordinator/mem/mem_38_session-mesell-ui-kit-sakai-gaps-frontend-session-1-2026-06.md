## Session mesell-ui-kit-sakai-gaps-frontend-session-1 — 2026-06-19 — MERGE-GATE REVIEW (PASS) — feat/ui-kit-sakai-gaps

- **VERDICT: PASS.** 8 new ui-kit components (checkbox, radio, breadcrumb, tabs, message, panel, divider, scroll-panel) + 8 specs + 2 types files + MEE_SURFACE aggregator group. ui-kit now 29 component classes in MEE_UI_ALL (was 21; index exports 30 incl pre-existing MeeMultiselect not in any aggregator — pre-existing, not this PR).
- **16/16 checklist passed.** All standalone+OnPush+`mee-*` selector. CVA (checkbox/radio) correct: NG_VALUE_ACCESSOR forwardRef multi, writeValue/registerOnChange/registerOnTouched + setDisabledState(noop, handled via disabled() input). No raw `pi pi-*` in templates — all icon use goes through `resolveIcon()`/`MEE_ICONS` registry (FE-2 allow-list: registry is the ONLY file with raw pi classes). Focus-ring: exactly 4 `--mee-focus-ring-*` tokens, theme.ts semantic.focusRing uses var() refs + shadow:'none', styles.css uses `:focus-visible` (not :focus).
- **All 4 builder deviations ACCEPTED + verified against primeng@21.1.9 typings:** (1) mee-radio fallback to individual p-radiobutton+shared [name]+[ngModel] — RadioButtonGroup genuinely not exported v21; sound. (2) mee-tabs drops TabPanel import (consumers project `<p-tabpanel>` directly inside) — correct, NG8113 avoided. (3) mee-panel `panelHeader` input aliased to 'header' to dodge `#header` ng-template name clash — verified Panel exposes header content-child + collapsedChange:EventEmitter<boolean|undefined> (component normalizes undefined→false, exact match). (4) mee-scroll-panel host-div for maxHeight — VERIFIED ScrollPanel v21 exposes ONLY styleClass+step inputs, NO [style]; host-div necessary.
- **KEY MERGE-GATE EVIDENCE NUANCE — worktree shared-spec-compile is poisoned by PRE-EXISTING debt.** `ng test` (unit-test builder) compiles the whole tsconfig.spec graph, so it FAILS regardless of this PR due to untouched files: offline-banner (NetworkService not in @mesell/core — the Wave-6 §4 service-layer gap), tree-select.spec (originalEvent TreeNodeSelectEvent drift), topbar.component (raw 'pi pi-*' assigned to MeeIconName — icon-registry drift), mfe-{export,onboarding,pricing} (ApiClient/ApiErrorEnvelope/NetworkService not built), shell/onboarding/profile specs (property drift). PROVED clean: `git status` shows NONE of these files touched by PR; `tsc -p tsconfig.spec.json` filtered to PR paths (checkbox|radio|breadcrumb|tabs|message|panel|divider|scroll-panel|aggregators|index) = ZERO errors. So this PR's own files are green; the red is debt that must be tracked separately. LESSON: when gating a lib-only PR in this worktree, scope tsc evidence to the PR's files — never expect a green full-suite compile until §4 service layer + topbar/tree-select drift are fixed.
- **FOLLOW-UP DEBT to escalate (not this PR):** topbar.component.ts uses raw 'pi pi-user'/'pi pi-sign-out' as MeeIconName (boundary violation, same class as the §2 PrimeNG-leak finding) + offline-banner/mfe-services blocked on unbuilt @mesell/core ApiClient/NetworkService (Wave-6 gap). These keep the full FE Gate red.
