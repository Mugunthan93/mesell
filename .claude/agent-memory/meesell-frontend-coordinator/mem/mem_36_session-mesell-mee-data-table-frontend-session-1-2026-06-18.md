## Session mesell-mee-data-table-frontend-session-1 — 2026-06-18 — PR #289 mee-data-table merge-gate: REQUEST CHANGES (1 blocker, spec require())
- **VERDICT REQUEST CHANGES on PR #289 (feat/mee-data-table → develop), built by component-builder off my spec.** ONE blocker; everything else verified clean and well-built. Posted as a PR **issue comment** (#289 issuecomment-4742028530) because `gh pr review --request-changes` is BLOCKED when the PR was opened by the same GH identity ("Can not request changes on your own pull request"). The comment is the canonical no-silent-block reject record. (Recurring: self-review is blocked; fall back to a clearly-labeled issue comment carrying the full verdict.)
- **THE BLOCKER — the "28/28 pass" claim was FALSE under the real gate.** `data-table.component.spec.ts` scenario-3 (debounce) uses CommonJS `require('rxjs')` / `require('rxjs/operators')` inline at lines 305/306, 318/319, 332/333. `CI=true ng test frontend` (@angular/build:unit-test = esbuild + angular-compiler, strict ESM, no `node` types) **fails compile** with 6× `TS2591: Cannot find name 'require'` → EXIT 1 → the spec's tests NEVER RUN. The specialist's "28/28 Vitest" came from a looser local run; the project gate rejects the file. **LESSON: always run the real `CI=true ng test frontend` builder myself at the gate — do not trust a specialist's "N/N pass" if the harness differs; the @angular/build:unit-test compiler enforces TS strict at compile time and one non-compiling spec fails the whole run.** Fix is trivial (top-level ESM `import { Subject } from 'rxjs'` + `import { debounceTime, distinctUntilChanged } from 'rxjs/operators'`, mirroring the component's own module-top import; delete the require lines).
- **ATTRIBUTION METHOD (reusable):** `git diff --name-only origin/develop...origin/PR` to confirm PR file scope (here exactly 7 files); then run the same gate on a fresh develop-baseline worktree. develop @ fd4331d ALSO fails `ng test frontend` with a LARGE pre-existing red set (mfe-auth `errorMessage`, mfe-onboarding `never`/`manufacturer_name`, shell index-signature, tree-select `originalEvent`) — 36 spec-tsc errors — but ZERO `require` errors. So the 6 require errors are unambiguously NEW and attributable to #289; the rest are pre-existing baseline rot (NOT this PR's problem, and a separate cleanup concern). NOTE for next gate: develop is currently RED on the unit gate — a future merge-gate must attribute carefully, not assume a green baseline.
- **VERIFIED PASS independently:** (1) `cd frontend && node tools/contracts/run-all.mjs --strict` = 5/5 CLEAN exit 0 — FE-1 p-table sealed in ui-kit, FE-2 ellipsis-v parity (both registries; alt = `material-icons mi-more_vert`, pi-free). (2) Component SOURCE tsc 0 errors via `apps/shell/tsconfig.app.json --noEmit` (this config includes `../../libs/**/*.ts`, so it's the right way to typecheck a libs/ component source — there is NO `libs/ui-kit/tsconfig.lib.json`; the brief's path was wrong). (3) Acceptance 1-10 all met: unknown-rooted union + narrowing helpers (no `any`); `[lazy]` + `Math.floor(first/rows)+1` + `lastEmitted` page/sort decomposition dup-fetch guard; `selectionEnabled=computed(bulk_actions().length>0)`; bulk bar base `position:sticky;bottom:0` promoted to `fixed`+`env(safe-area-inset-bottom)` under `@media(max-width:640px)` + reduced-motion; a11y row tabindex0/enter+space, checkbox stopPropagation, pSortableColumn+aria-sort, kebab/clear ≥44px, empty `role=status`, all `--mee-*` tokens. (4) Native-`<button>` kebab deviation ACCEPTED (MeeButtonVariant genuinely has no 'icon-only'; token-based, 44px, aria-label, type=button). Deviations 2 (TestBed bypass = pricing/export pure-fn house style) + 3 (keydown.space typed Event = strict-template constraint) ACCEPTED.
- **OUT-OF-SCOPE clarified:** the task's "mfe-catalog live-listings.component.ts xlsx error" does NOT exist on develop OR on this branch — it lives on a different in-flight branch. PR #289 touches zero mfe-catalog files. No separate ticket spawned from #289; not my gate's concern.
- **OPS:** review worktree /tmp/mesell-wt/dt-review off origin/feat/mee-data-table; node_modules SYMLINKED from main tree (`ln -s .../frontend/node_modules node_modules`) — fresh worktrees have none and the `pnpm install` postinstall-build gotchas recur, the symlink is the fastest clean path for a read+test review. Develop-baseline worktree /tmp/mesell-wt/dt-dev (same symlink). Both to be removed.
