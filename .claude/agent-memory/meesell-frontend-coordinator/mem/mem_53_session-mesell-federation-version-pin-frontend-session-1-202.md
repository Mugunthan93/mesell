## Session mesell-federation-version-pin-frontend-session-1 — 2026-06-20 — MERGE-GATE PASS + PR #328 OPEN

- **HYBRID step-3 merge-gate** on `fix/federation-shared-version-pin` @ 38c7934 (worktree /private/tmp/mesell-wt/fed-version-pin). Browser-confirmed by founder before review (shell→catalog stays logged in). All 5 checks PASS → **PR #328** → develop OPEN (lead does NOT approve, D1 founder gate).
- **THE BUG (resolves master-memory finding `finding-federation-auth-singleton-not-shared.md`):** `@mesell/*` workspace libs had NO `package.json` → `version=""` in every remoteEntry.json → Native Federation can't dedup by version → each remote loaded its OWN `@mesell/core` → 2nd AuthService instance, in-memory token=null (no localStorage per D#14) → authGuard→/login on shell→remote nav.
- **THE FIX (config-only, no runtime code):** explicit shared override `{singleton:true, strictVersion:true, requiredVersion:'1.0.0', version:'1.0.0'}` for `@mesell/{core,env,ui-kit,composites}` placed AFTER `shareAll(...)` (so the pin WINS) across ALL 7 federation.config.js + new `libs/{core,env,ui-kit,composites}/package.json` each `"version":"1.0.0"`.
- **Gate verdicts:** (1) diff = exactly 7 configs + 4 new package.json = 11 files, 0 stray/dist/creds; (2) all 7 configs grep-identical (`MESELL_SHARED_VERSION='1.0.0'` + same 4-lib block) — a mismatch would re-break dedup, so this was the load-bearing check; (3) 4 package.json all `"version":"1.0.0"`; (4) ZERO removed non-comment lines (verified `git diff | grep '^-' | grep -v '^-//' | grep -v '^-$'` = empty → every deletion is comment; shareAll + skip arrays untouched, no dropped dep); (5) config-only → no test surface, FE Gate unaffected.
- **KEY NF GOTCHA (transparency note landed in PR):** Native Federation 3.5.5 hardcodes strictVersion:false + requiredVersion:'' for tsconfig-path-aliased workspace libs REGARDLESS of the shared{} config → dedup keys on packageName+version+singleton. The fix makes all three consistent across all 7. This is the SAME class of NF-import-map gotcha as F-001 (root-key-only import map) — federation configs are a recurring P0 surface; always grep all 7 for consistency.
- **OPS:** founder constraint = NO builds (local stack already serving fix, cleanup agent freeing memory) → review+PR only, no rebuild/serve. Honored. Worktree fetch'd origin/develop before 3-dot diff.
