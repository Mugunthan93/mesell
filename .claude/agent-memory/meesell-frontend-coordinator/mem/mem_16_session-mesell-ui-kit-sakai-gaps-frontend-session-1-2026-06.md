## Session mesell-ui-kit-sakai-gaps-frontend-session-1 — 2026-06-19 — HYBRID STEP 1: SPEC authored for 8 ui-kit Sakai gaps + focus-ring tokens (no code)

- **SPEC at `docs/specs/ui-kit-sakai-gaps-spec.md`** (worktree design-figma-ui-screens). Section A = 8 components for angular-component-builder; Section B = focus-ring token group for ui-styler. Branch `feat/ui-kit-sakai-gaps`.
- **8 components:** mee-checkbox (P0, CVA-A, MEE_FORM), mee-radio (P0, CVA-A GROUP control w/ options[], MEE_FORM), mee-breadcrumb (P1, reuse MeeMenuItem, MEE_COMMON), mee-tabs (P1, MeeTab type, MEE_DATA), mee-message (P1 INLINE not toast, MeeMessageSeverity, MEE_FEEDBACK), mee-panel (P2, theming done, NEW MEE_SURFACE), mee-divider (P2, MEE_SURFACE), mee-scroll-panel (P2, MEE_SURFACE). aggregators 21→29.
- **NON-OBVIOUS as-built facts (drove the spec):** ui-kit is at `frontend/libs/ui-kit/` (NOT src/app/ui — this worktree is post-SP0 libs/ split + SP07 styles relocate). styles.css = `apps/shell/src/styles.css`. TWO CVA patterns coexist: input.component.ts = classic NG_VALUE_ACCESSOR+forwardRef (use for checkbox/radio); multiselect.component.ts = NgControl self-inject + auto-error-message (don't need for checkbox/radio). icon registry = ONLY file w/ raw `pi pi-*` (FE-2). MeeMenuItem = the breadcrumb item shape (narrowed PrimeNG MenuItem). aggregators.ts groups + counts + aggregators.spec.ts assert counts — must bump all.
- **PrimeNG v21 gotchas confirmed:** `p-tabview` REMOVED → mee-tabs uses new `Tabs/TabList/Tab/TabPanels/TabPanel` from `primeng/tabs`. All 8 subpaths verified in node_modules/primeng exports (checkbox/radiobutton/breadcrumb/tabs/message/panel/divider/scrollpanel). primeng@^21.1.9 + @primeuix/themes@^2.0.3 + primeicons@7.0.0 ALL already in root package.json → ZERO installs.
- **Theme:** MeeSellPreset already has `components.panel.root.borderRadius:16px` (panel theming pre-done). NO `semantic.focusRing` block yet — ui-styler adds it mapping `var(--mee-focus-ring-*)` → `--p-focus-ring-*` + global `:focus-visible` in shell styles.css for non-PrimeNG els.
- **5 build-time VERIFY flags (not blockers):** RadioButtonGroup export/selector in 21.1.9; semantic.focusRing key path in @primeuix/themes 2.0.3; mee-tabs `[(value)]` on signal model; global :focus-visible double-ring on PrimeNG; MEE_SURFACE naming (avoided MEE_LAYOUT collision w/ @mesell/layout lib).
- **SCOPE DIVERGENCE flagged in spec §0:** dispatch list (8 comps + focus-ring) is authoritative but differs from sakai-upgrade-path.md — mee-scroll-panel was P3-DEFER there (built here, removable), mee-chip/typography/card-shadow-cleanup were P1/P2 ALIGN there (NOT in this build). Recorded so nothing silently lost.
