## Session mesell-razorpay-dev-mock-frontend-session-1 — 2026-06-20 — wired mfe-billing (:4207) into static-dev tooling

- **CONTEXT:** mfe-billing is the **7th remote** (merged develop via PR#323) — the FIRST new remote since the original 6 (pricing/export/onboarding/dashboard/catalog/auth on 4201–4206). The static-dev local-boot tooling in `frontend/tools/dev/` only knew the original 7 served targets (shell + 6 remotes), so the founder's low-RAM static stack skipped billing and `/billing/plans` never loaded. Worktree /tmp/mesell-wt/razorpay-dev-mock, branch feature/razorpay-dev-mock (off develop which already had the mfe-billing project + shell wiring + federation.manifest.json mfe-billing:4207). Commit 3ccf82b, NOT pushed (Director handles git).
- **angular.json ALREADY had mfe-billing serve port 4207** (PR#323 wired the project + serve target). So `start:mfe-billing` = `ng serve mfe-billing` (NO `--port` flag — port comes from angular.json, exactly mirroring `start:mfe-pricing`). `start:mfe-billing` did NOT exist → I added it to package.json after start:mfe-auth.
- **5 tooling files + 1 package.json + 2 docs touched (8 total), ALL ADDITIVE — no existing remote config changed:**
  1. `build-static.mjs` — added `'mfe-billing'` to ALL_APPS (after mfe-auth).
  2. `serve-static.mjs` — added `{label:'mfe-billing',app:'mfe-billing',port:4207}` to SERVERS + a `'\x1b[33m'` (yellow) entry to LABEL_COLOURS (array maps colours POSITIONALLY via `LABEL_COLOURS[i % len]`, so the 8th colour must exist).
  3. `start-all.mjs` — added `{label:'mfe-billing',script:'start:mfe-billing'}` to SERVERS + the 8th LABEL_COLOURS entry + the `4207  mfe-billing` printBanner port line.
  4. `route-check.mjs` — added `{label:'/billing/plans (<- mfe-billing)',path:'/billing/plans',protected:true}` to TARGETS (now 12 routes; harness counts dynamically via TARGETS.length so no logic change needed).
  5. `dev-static.mjs` — comment-only count updates (it delegates, no hardcoded app list).
  6. `package.json` — `start:mfe-billing`.
  7+8. `STATIC_DEV.md` + `README.md` — port tables `4207 | mfe-billing`; count wording 7→8 apps, 4200–4206→4200–4207, ~129 MB→~147 MB, 11/11→12/12.
- **COUNT NOMENCLATURE settled for this tooling:** "8 apps / 8 served targets" = shell + 7 remotes; ports "4200–4207"; route-check "12 routes / 12/12". Memory estimate scaled 129→147 MB (~18 MB per extra serve.js, consistent with the documented ~15 MB RSS each). These are the strings to update next time an 8th remote lands.
- **VERIFICATION DONE:** `node --check` GREEN on all 5 edited .mjs; package.json valid JSON; grep-confirmed mfe-billing present in every list. **Build dry-run SKIPPED** — this worktree's `node_modules/esbuild` is NOT extracted (the recurring pnpm-worktree ERR_PNPM_IGNORED_BUILDS gotcha; `node_modules/.bin/ng` present but esbuild binary missing) → `ng build mfe-billing` would fail for an ENVIRONMENT reason unrelated to my edits, and the watchdog build could burn ~180s. Skipped per the brief's "skip if risky" clause. If a future session needs to actually build here, run `pnpm install --config.dangerously-allow-all-builds=true` first (the clean worktree fix from sub_plan_01_pricing memory).
- **GIT DISCIPLINE:** staged the 8 files by EXPLICIT PATH (never `git add -A`) — a pre-existing uncommitted `docs/status/STATUS_FRONTEND.md` from a sibling session was left UNSTAGED + UNCOMMITTED as instructed. `git show --stat` confirmed exactly 8 files in the commit.
