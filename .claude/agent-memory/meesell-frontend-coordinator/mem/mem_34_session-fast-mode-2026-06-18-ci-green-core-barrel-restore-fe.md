## Session (FAST MODE) — 2026-06-18 — CI green: core-barrel restore + FE-2 icon migration (PR #284 OPEN) + Task-B pricing spec authored
- **CONTEXT:** develop CI red on merge `da588f3` (#283). Two FE jobs down: (1) Frontend boot smoke — `@mesell/core` barrel took OURS in rebase, dropped interceptor/ApiClient/Network/Error exports → MFEs fail to compile; (2) FE Gate lint — 28 FE-2 `pi pi-*` violations triggered for the first time because #283 touched `libs/ui-kit/`.
- **Task A + C EXECUTED, committed to `fix/ci-core-exports-and-icons` (commit 6281ef4), PR #284 → develop OPEN.** Task B = pricing rewrite spec returned to Director (NOT executed — HYBRID: dispatch component-builder).
- **FE-2 MIGRATION PATTERN (reusable — the 3 shapes):**
  1. Decorative `<i class="pi pi-*">` → `<mee-icon name="<semantic>" />` + add `MeeIconComponent` to `imports:[]`.
  2. `mee-button.icon` / `mee-page-header.cta_icon` are ALREADY typed `MeeIconName | undefined` — so a raw `icon="pi pi-download"` is BOTH a FE-2 violation AND a type error; fix = semantic name string (`icon="download"`). `MeeMenuItem.icon` is also `MeeIconName` (mee-menu resolves via `MEE_ICONS[name]` internally) → topbar userMenu just needs `'user'`/`'logout'`.
  3. Typed nav-item models rendered via `<i [class]="item.icon">` (shell bottom-nav `BottomNavItem`, sidebar `NavItem`): change field to `icon: MeeIconName`, store semantic names, add `meeIconClass()` helper + bind `[class]="iconClass(item.icon)"`. ADDED `export function meeIconClass(name): string` to `icon.registry.ts` (alias of resolveIcon) + exported from ui-kit barrel — the FE-2-compliant escape hatch for the `[class]`-binding case (registry stays the sole raw-`pi pi-*` file).
- **CRITICAL GOTCHA — the alt registry has KEY-PARITY ENFORCEMENT.** `libs/ui-kit/icon/icon.registry.alt.ts` ends with `satisfies Record<MeeIconName, string>`. ADDING any icon to `MEE_ICONS` (which DEFINES `MeeIconName`) makes the alt registry a compile ERROR (TS1360 missing keys) until you add the SAME keys to `MEE_ICONS_ALT` (Material Icons equivalents). The Phase-7 swap-proof seam means every icon addition is a TWO-FILE edit. Plain `tsc -p apps/shell/tsconfig.app.json` catches it; the FE-2 contract scan does NOT (type check, not lint). Added 11 icons → 11 keys in both files.
- **HEADLINE FINDING for Director — boot smoke is BROADER than the 2 documented root causes.** The rebase dead-symbol split-brain that killed `pricing.component.ts` (computePnlBreakdown/PnlBreakdown) ALSO leaves dead imports in `mfe-export` (MOCK_DOWNLOAD_URL), `mfe-catalog` (catalog-form autofill arg-count + live-listings missing `xlsx` dep, StatusCounts), `mfe-dashboard` (StatusCounts/ProductListItem.id/LoadProductsParams.status_filter). ALL pre-existing at da588f3 (verified `git show`), NOT mine. Boot smoke stays red on those until each gets its own reconciliation. PR #284 fixes only Task A+C cleanly. Recommended separate split-brain tickets.
- **PROOF METHOD (reusable): tsc error-count diff before/after.** `git stash push -u`, count `tsc --noEmit` errors per project on pristine da588f3, `git stash pop`, recount. My edits REDUCED every project's count (shell 3→0, onboarding 10→0, auth 4→0; export 8→1, pricing 8→2, catalog 25→4, dashboard 13→6) — proves Task A's barrel restore un-cascaded import failures and Task C added zero regressions. The residuals == the out-of-scope split-brain.
- **ENV/OPS:** fresh worktree off origin/develop had NO node_modules; esbuild binary NOT extractable in sandbox (`pnpm rebuild esbuild` ran postinstall but `@esbuild/<platform>` dir never appeared) → full `ng build` boot-smoke INFEASIBLE locally; substituted `tsc --noEmit` per-project + `run-all.mjs --strict`. Symlinked master-tree node_modules for tsc (`ln -s .../frontend/node_modules`), `rm`'d it before staging so it never commits. FE-2 scan = `node tools/contracts/fe2_no_raw_pi_icons.mjs`; full suite = `node tools/contracts/run-all.mjs --strict`. Bash heredoc memory-append DENIED again + master-tree memory path blocked (session isolated to worktree) → completed via Edit tool on the WORKTREE copy of MEMORY.md.
- **D1 NOTE:** PR #284 targets develop — CI-rescue chore on surfaces I co-own (libs/core barrel + libs/ui-kit registry + app icon usages), NOT a feature integration. Founder/Director merges.
