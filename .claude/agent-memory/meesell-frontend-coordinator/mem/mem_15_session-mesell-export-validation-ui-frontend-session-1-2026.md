## Session mesell-export-validation-ui-frontend-session-1 — 2026-06-18 — HYBRID STEP 1: export failed_checks[] UI SPEC authored (no code)

- **WHY:** backend PR#291 added an additive `failed_checks[]` array to the `POST /products/{id}/export-xlsx` 422 body. Current FE export checklist is the GAP-1 Option A DISPLAY-ONLY `SIMULATED_PASSING_CHECKS` (4 hardcoded all-true items) — decorative, never reflects real readiness. Spec replaces it with the real array. Branch `feat/export-validation-ui` off origin/develop; PR → develop = FOUNDER gate (feature→develop per D1, NOT my gate); my role = HYBRID step 3 merge-gate review of the specialist PR.
- **CONFIRMED-BY-READING (do NOT trust brief verbatim — verified against live files on this branch):**
  - **DRIFT CONFIRMED #1 (the brief's headline fix):** `export.service.ts` L128 reads `body?.error_code` AND L70 `InitiateValidationError.error_code` AND L133 sets `error_code: body?.error_code`. Backend PR#291 emits `code` (locked §4.F field), NOT `error_code`. So the existing 422 path has been silently reading `undefined` for the code. Fix: read `body.code`. Also extract `body.failed_checks`.
  - **NEW FINDING — ICON REGISTRY GAP (load-bearing, brief was wrong):** `frontend/libs/ui-kit/icon/icon.registry.ts` `MEE_ICONS` has NEITHER `times-circle` NOR `check-circle`. Valid `MeeIconName` keys: dashboard, catalog, add, browse, user, logout, menu, sparkles, forward, back, check, close, delete, send, warning. CLOSEST: error→`close` (`pi pi-times`) or `warning` (`pi pi-exclamation-triangle`); success→`check` (`pi pi-check`). Spec FORBIDS inventing registry keys — adding to the registry touches `libs/ui-kit/` → triggers the contract scanner the brief said is NOT needed + breaks FE-2 allow-list. Reuse existing keys only.
  - **NEW FINDING — component does NOT currently render `mee-icon`.** Checklist renders `mee-badge` PASS/FAIL (L183-190), NOT icons. `MeeIconComponent` not imported. RECOMMENDED to specialist: keep `mee-badge` for the result column (zero new import); the NEW failed-checks itemized block uses ONE new import `MeeIconComponent` from `@mesell/ui-kit` with valid key `warning` (or `close`).
  - **422 currently routes to `notReadyMessage` (warning banner), status→'idle'** (L392-401, `errShape.kind==='validation'`). New `failedChecks` signal populated on the SAME branch; CLEARED on retry/success/idle-restart. Existing warning banner (detail string) stays as headline; itemized list renders below.
- **i18n REALITY:** transloco still unwired. FE ships local `EXPORT_CHECK_MESSAGES: Record<string,string>` fallback keyed on `message_key` — 4th+ instance of FE-local-string-map-because-i18n-unwired (pricing ALERT_MESSAGES / preview / validation.q.missing). Unknown keys → generic fallback string, NEVER blank or raw-key (recurring founder pain).
- **SPECIALIST:** single dispatch to `meesell-angular-component-builder` (model + service + component all in `apps/mfe-export`; service change = 1-field rename + array extract, no separate service-builder needed). ui-styler NOT needed (token-only reuse). NO contract scanner (only `apps/mfe-export` touched). `npx tsc --noEmit -p frontend/apps/mfe-export/tsconfig.app.json` zero-errors gate before commit.
- **EXACT EXPORT_CHECK_MESSAGES seed (2 keys from PR#291):** `'export.check.quality_status'` → "Your product isn't ready. Complete the required fields and resolve quality issues first." ; `'export.check.front_image_missing'` → "A front image is required. Upload an image in slot 1 before exporting with images."
