## Session mesell-pricing-fe-rework-frontend-session-2 — 2026-06-20 — localdev sync + rebuild for founder Price Calculator demo

- **AUTH-SINGLETON FIX VALIDATED IN PRACTICE.** The finding-federation-auth-singleton-not-shared logout bug is mitigated when shell + the target remote are built from the SAME commit: `@mesell/core` outFileName hash then MATCHES (`_mesell_core-MNJTYQMY.js` identical across dist/frontend + dist/mfe-pricing built from cfc2bd7). Native Federation resolves the same chunk URL → one AuthService instance → no logout on shell→pricing nav. The empty-`version:''` config defect REMAINS (still latent for cross-remote dedup when builds diverge) — real fix = give @mesell/* libs explicit versions (angular-service-builder). But same-commit rebuild is the reliable local-stack workaround. ALWAYS rebuild shell + the demoed remote together.
- **federation.manifest.json is FIXED on develop** — now committed pointing at :4201-4206 (the static serve.js ports), so the manifest-pins-worktree-ports finding is resolved upstream. No local override needed after pull anymore. (Verify per pull, but as of cfc2bd7 it's correct.)
- **esbuild "MISSING" in node_modules/esbuild/bin is a FALSE ALARM** in this pnpm layout — esbuild is a transitive dep living only in `node_modules/.pnpm/esbuild@0.27.3/...`, not hoisted top-level, and `node -e require('esbuild')` fails too. The real test is `ng build` itself, which resolves it via .pnpm fine. `pnpm rebuild esbuild @parcel/watcher lmdb msgpackr-extract` extracts the native binaries cleanly (no dangerous flag) if a build actually errors on IGNORED_BUILDS. Don't chase the top-level ls.
- **guard-master-tree-git hook blocks `git checkout --theirs`** during a legit rebase conflict resolution on the master tree (it pattern-matches `git checkout`). For founder-directed local sync, prefix with `MESELL_ALLOW_MASTER_GIT=1` (the documented deliberate-recovery escape). Rebasing develop onto origin/develop IS allowed under that escape — we never switched to a feature branch.
- **Backend pricing endpoints live on develop:** `POST /api/v1/products/{id}/price-calc` (#305 settlement engine, returns 401 unauth) + `POST /api/v1/products/{id}/apply-price` (#316 W4b "Use this price", 401 unauth). FE wiring = apps/mfe-pricing/src/app/pricing.service.ts (server-calc only, NO local fallback per DECISION-1/R-W6-1). Shell route /catalogs/:id/pricing → loadRemoteWithFallback('mfe-pricing','./PricingComponent').
- **Local stack canonical model (8GB machine):** shell + 6 remotes all as static serve.js on :4200-4206 (NOT ng serve fleet). Two redundant ng-serve fleets (:4210/:4217-4222) + a 2nd uvicorn (:8001) were running and thrashing memory (4126 free pages); killing them recovered ~1.4GB. Backend = single uvicorn --reload :8000. serve.js invocation: `node frontend/tools/boot-smoke/serve.js dist/<app>/browser <port>`.
