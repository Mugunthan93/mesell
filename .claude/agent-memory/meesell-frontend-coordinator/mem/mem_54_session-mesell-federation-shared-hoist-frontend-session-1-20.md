## Session mesell-federation-shared-hoist-frontend-session-1 — 2026-06-21 — MERGE-GATE REVIEW PR #346 (verdict: APPROVE, founder merge)

- **PR #346** `feature/federation-shared-hoist/frontend` → develop (Step 3A). Hoist inline `mesellShared` block (4 cross-boundary libs core/env/ui-kit/composites, `singleton:true + strictVersion:true + version/requiredVersion '1.0.0'`) into NEW `frontend/libs/federation/shared.config.js`; 8 configs `require` + spread `...mesellShared`; mfe-billing gains the spread (was missing). +24/-77, 9 files all federation config. NO exposes/skip/name/mappingVersion/backend/component drift. CI Gate1+Gate3+FE-Gate+all 8 per-app builds GREEN; Gate4+boot-smoke pending (advisory).
- **VERDICT: APPROVE** (refactor safe+correct, no regression). #328 singleton pin NOT weakened — shared.config.js byte-identical to prior inline. All 8 spread it correctly after shareAll. libs/*/package.json all version 1.0.0 (confirmed on branch). mfe-onboarding remoteEntry byte-identical to develop (no regression on pinned remotes); logout smoke GO.
- **THE mfe-billing CATCH (builder self-flagged, CORRECT):** `...mesellShared` has ZERO runtime effect for mfe-billing because NF 3.5.5 only emits the pinned `version` for tsconfig-alias `@mesell/*` libs when `features.mappingVersion:true` is set — and mfe-billing's `features` block has ONLY `ignoreUnusedDeps:true`, NO `mappingVersion:true` (all other 7 configs have it). So mfe-billing's emitted remoteEntry shared map is IDENTICAL before/after (`version="" strictVersion=False`). The PR's stated "fix mfe-billing drift" is NOT delivered by this change. Dedup is still a real safe improvement; the spread is correct future-proof insurance. Does NOT change verdict (no regression; the missing fix is a gap, not a defect introduced).
- **FOLLOW-UP TICKET (the actual mfe-billing fix):** add `mappingVersion: true` to `frontend/apps/mfe-billing/federation.config.js` `features` block (one line). THEN rebuild mfe-billing → remoteEntry emits `@mesell/*` with `version="1.0.0"` matching the other 6 remotes + shell, closing the #328 drift for billing. Verify: built mfe-billing remoteEntry shows `version="1.0.0"` on core/env/ui-kit; runtime shell→/billing nav no logout. Recommended as SEPARATE PR (keeps #346 narrow) OR fold into #346 if founder prefers single landing — flagged to founder in verdict. Owner = meesell-angular-service-builder via HYBRID.
- **gh api glob gotcha:** URLs with `?ref=` trip zsh no-match — always quote the whole api path string.
