# Feature Board — Data Lead

**Lead agent:** `meesell-data-engineer`
**Domain:** data
**Last updated:** 2026-06-18 (option-2 ROUND 9 DONE — DECISIVE: "Free Size" is the VARIATION name, NOT a Size-dropdown value (7 sizes L/M/Onesize/Regular/S/Xl/Xs); still no right-side tax card; price cells post-Submit-gated)
**This file is the single domain-level status surface for the lead.**

---

## Active features

| Feature | Group branch | Status | Current session | Last touched | Blocking | Notes |
|---|---|---|---|---|---|---|
| category-seeding | feature/category-seeding | RESOLVED (local) — PR #245 open for founder merge | mesell-category-seeding-session-1 | 2026-06-16 | founder — merge PR #245 | Architecture APPROVED (rev 1.0). D1=local-only, D2=real-commission-two-phase, D3=pure-upsert. Wave 1 DONE: `make seed` wired, ran 2× idempotent (categories 3772 / aliases 67 / templates 3566 / enum 49259), seed-script stale-import defect fixed (`app.shared.*`). Wave 3 verified: catalog→wizard chain proven end-to-end on localhost (0 FK orphans, 71-field /schema, seeded enums). Visual-gate blocker RESOLVED. Durable landing on PR #245 merge. |
| price-calculator (settlement/payout investigation) | — (read-only, no branch) | DONE — investigation report delivered | mesell-price-calculator-data-session-1 | 2026-06-18 | founder — e-signature wall + pricing-model decision | READ-ONLY probe of Meesho supplier panel (supplier_id 3661229, auto-detected). Captured account-level fee config: flat `default_monetization_percent=4.0`, `mall_commission_rate=0`, `shipping_charges=70`, `shipping_bracket=1000`, `cod_charges=0`, `logistic_fee_enabled=1`. **`is_agreement_accepted=false` — e-sig wall STILL blocks** category commission rate-card AND per-order settlement breakdown (no settlement/transaction XHR fires; account also has 0 orders). No code/PR. Probe script `backend/scripts/meesho_settlement_probe.py`. Real payout deductions = category-dynamic commission + shipping/logistics + RTO + GST-on-fee + TCS/TDS — NONE seedable as static reference data (confirms Wave-1.5 finding: commission is dynamic). Hand-off → BACKEND for pricing-model rework. |
| price-calculator (commission via catalog EDIT/CREATE) | — (read-only, no branch) | DONE — round-3 report delivered (paced re-run open for edit/create phases) | mesell-price-calculator-data-session-3 | 2026-06-18 | founder — pricing-model decision | READ-ONLY re-scrape, NEW source per founder. **BREAKTHROUGH:** catalog listing fires `GET /api/growth/activation/fetch-supplier-products` (200) returning REAL per-product economics WITHOUT the e-sig wall: `current_price` + `current_transfer_price` (=seller PAYOUT, net of all deductions) + `current_wdrp_price`/`current_wdrp_transfer_price` (wrong-defective set) + `minimum_recommendation_price`/`minimum_wdrp_price`. Sample: price 106 → transfer 47 (≈56% blended deduction, NOT flat 4%). Effective deduction derivable = current_price − current_transfer_price, per product. Also `GET /api/services/ipp/fetch-smart-pricing-products`. `is_agreement_accepted` STILL false (does NOT block this XHR). **NOT reached this run:** edit-view DOM + create-flow payout widget — probe hard-stopped on Akamai 403 (repeat prefetch on 2nd nav); pacing honored, did not hammer. Probe `backend/scripts/meesho_catalog_commission_probe.py` (snapshot-regex patched to grab full fetch-supplier-products body on a future paced run). ZERO writes confirmed. Conclusion: calculator can source per-product PAYOUT (transfer_price) programmatically for existing products; no clean per-category commission % (transfer_price nets commission+shipping+fees, varies per product/date); seller-input + line-items still needed for NEW products. Hand-off → BACKEND. |
| price-calculator (DEEP re-scrape, round 4) | — (read-only, no branch) | DONE — full payout table captured + e-sig re-diagnosed | mesell-price-calculator-data-session-4 | 2026-06-18 | founder — pricing-model decision + verify e-sig persisted | READ-ONLY round-4. Clean run, ZERO Akamai 403. **GOAL 1 DONE:** captured ALL 14 products from `fetch-supplier-products` (raw `logs/scraper/fetch_supplier_products_2026-06-18_17-52.json`, gitignored). **PATTERN (refutes round-3's 56% blended read): FLAT-FEE, not %.** customer_price = seller_price + shipping (exact); payout = seller_price − **₹9 flat** (exact across all 14, both cats 753948/754435, seller-price 43→86, AND the WDRP band). Shipping (₹50–54) is buyer-paid pass-through, neutral to seller. The variable "deduction %" was an artifact of a constant ₹9 over different prices. Single-point calibration does NOT generalize as a %; it generalizes as "seller_price − small flat fee" for this band (only 2 low-price hair-accessory cats sampled; flat fee may vary by category/slab at scale). **GOAL 2 DONE:** e-sig is NOT done server-side despite founder belief — a CLUSTER of unsigned fields (is_agreement_accepted=false, agreement_accepted="0", *_ip/_time="default", clickwrap_signing_name/mobile/business_name/gstin all "-", enable_supplier_signature_v2=false); KYC/bank/GST ARE done (bank_details_status=approved, gstin_otp_verified). rate-card + settlement pages fired ONLY 5 universal container XHRs (zero rate-card/settlement XHR) → content never mounts. ROOT CAUSE = (1) deep-link URLs were agent-GUESSED (SPA serves shell for any path; real pages via sidebar) + (2) agreement genuinely unsigned. Probes `meesho_full_payout_probe.py` + `meesho_goal2_diag.py` (untracked, no git in master tree). |
| price-calculator (PROOF-OF-ONE create-flow preview, round 5) | — (read-only, no branch) | IN PROGRESS — specialist spec prepared, awaiting MASTER dispatch | mesell-price-calculator-data-session-5 | 2026-06-18 | MASTER — dispatch `meesell-scraper-maintainer`; founder — prove create-form live payout preview | READ-ONLY PROOF-OF-ONE on supplier 3661229 (kdwec / Curl Candy). Closes the gap rounds 3-4 never reached: the ADD-CATALOG create flow's LIVE commission/payout preview + the XHR that computes it. Reach Add-Catalog via REAL sidebar nav (NOT guessed URL — round-3 guessed `/catalog-upload/single` etc. = shell only, no data XHR); pick ONE hair-accessory category; enter ₹150; READ commission %/₹ + payout/transfer + fee lines + WDRP from DOM; capture computing XHR (endpoint+request+response). Determine pre-submit-visible vs post-submit-only + category-specific vs flat. ABSOLUTE GUARDRAIL: ZERO submit/create/next-that-persists — abandon form when done. Pace it; stop 401/403/429 after flush; OTP→STOP. Specialist `meesell-scraper-maintainer`. **DISPATCH BLOCKER:** lead session has no Agent tool → MASTER must launch the specialist with the dispatch-ready spec (see STATUS_DATA round-5 start block + lead final message). Stays IN PROGRESS until specialist returns + lead reviews. |
| price-calculator (option-2 read-the-preview proof, round 7) | — (investigation, no branch) | DONE — image gate PASSED; decisive: create flow has NO commission/payout preview | mesell-price-calculator-data-session-7 | 2026-06-18 | founder — pricing-model decision | FOUNDER-REVISED guardrail (image-upload + advance + price-fill authorized, NEVER go-live). 8 paced auth-WebKit runs, ZERO Akamai 403, ZERO submits. Probe `backend/scripts/meesho_preview_capture_probe.py` (untracked). Placeholder /tmp/meesell_placeholder_1200.jpg. Account 4359160/oinpw/"meesell". **PASSED the image gate r6 hit** (uploadSingleCatalogImages 200, 1 image satisfies required_images_count). Reached the "Add Product Details" step. **KEY FINDING (decisive): the single-catalog CREATE FLOW HAS NO COMMISSION/PAYOUT/TRANSFER/NET-AMOUNT PREVIEW.** The price section (`product_size_data` in fetchProductDetailsV3) is EXACTLY 4 raw inputs — Meesho Price, Wrong/Defective Returns Price, MRP, Inventory — and grep of the full 46KB schema for commission/payout/transfer/referral/monetiz/net_amount = ZERO hits. The only price-namespace XHR is `priceRecommendation/fetchDuplicatePid` (dup-PID + shipping=57 check, not a payout compute). Price INPUT cells are per-size table columns gated behind ~30 mandatory fields (Size + all `*` attributes + 9 compliance fields); Size-select alone did not surface them. **Category-wise sampling via create flow = NOT VIABLE** (no preview to sample; heavy gated form per category; Akamai sensor pings). Per-category recipe DOES cleanly yield the attribute+price-field SCHEMA (login→Add Single Catalog→category→placeholder image→Continue→read fetchProductDetailsV3→Discard) but NOT a commission value. End-state: nothing live — getCatalogUploadPerformance total_upload_count=0 across all 8 runs; "Submit Catalog" never clicked; ended on Discard Catalog. Reinforces backend transfer_price handoff: payout source stays `fetch-supplier-products.current_transfer_price` (existing products) + seller-input for new products. |
| price-calculator (FOUNDER-CORRECTED Free-Size tax-card capture, round 9) | — (investigation, no branch) | DONE — DECISIVE: "Free Size" is the VARIATION name, NOT a Size-dropdown value; still no tax card | mesell-price-calculator-data-session-9 | 2026-06-18 | founder — pricing-model decision | Tested the founder's Free-Size fix. 1 paced auth-WebKit run (21-47), ZERO Akamai 403, ZERO Submit/Publish/QC/Go-Live, Discarded. Account 4359160/oinpw/"meesell". Category "Hair Bands". **★ DECISIVE: the founder conflated two things — "Free Size" is the VARIATION row name, NOT a Size-attribute dropdown value.** Hard evidence from `fetchProductDetailsV3` raw body: `variations=[{id:167, variation_name:"Free Size"}]` (single variation labelled Free Size), while the Size DROPDOWN (`other_data[1]`) has EXACTLY 7 values — **L, M, Onesize, Regular, S, Xl, Xs** — "Free Size" is NOT among them. So selecting "Free Size" in the Size attribute is IMPOSSIBLE for Hair Bands; the probe correctly logged "Free Size NOT found" and fell back to L. **The price cells STILL do not render and there is NO right-side tax card** on the product-details step (full DOM dump: section + attributes + 9-field compliance + Brand/Size/Desc + T&C + right rail = ONLY Image Guidelines; only forward control = "Submit Catalog", forbidden). `product_size_data` schema DEFINES the 4 price fields (Meesho Price/Wrong-Defective/MRP/Inventory) + SKU ID as PER-VARIATION columns, but they are gated behind "Submit Catalog" — they do not render pre-Submit regardless of Size. Grep of ALL captured XHR for commission/payout/transfer/referral/monetiz/settle/margin = ZERO real hits (only account-level config). No tax-card computing XHR exists. CAVEAT: this run's 12 attribute dropdowns stayed empty (probe combobox selector didn't match Meesho's `input[type=text placeholder=Select]` markup) — but round-8's sibling run DID fully fill the form and still got no price cells/no tax card, and the schema proves post-Submit gating, so the verdict is unchanged. ⚠️ SAFETY: total_upload_count=2 (UNCHANGED from r8 — did not increment this run); fetchBulkCatalogsListV2 file_count=0 → nothing live; "Submit Catalog" never clicked (grep-verified); Discarded. Creds+logs gitignored; probe untracked. VERDICT: create-flow tax-card sampling NOT VIABLE; payout source stays fetch-supplier-products.current_transfer_price (existing) + seller-input (new). Reinforces handoff_pricing_transfer_price.md (no new handoff). |
| price-calculator (right-side tax detail card capture, round 8) | — (investigation, no branch) | DONE — DECISIVE: create flow has NO right-side tax card; price cells gated behind "Submit Catalog" | mesell-price-calculator-data-session-8 | 2026-06-18 | founder — pricing-model decision + verify 2 discarded drafts | FOUNDER ROUND-2 of option-2, WITH OTP-relay login. Probe `backend/scripts/meesho_taxcard_probe.py` (OTP-relay + full-form-fill + dual-price tax-card capture). 3 paced auth-WebKit runs (20-16/20-22 sibling + 20-34), ZERO Akamai 403, ZERO Submit/Publish/QC/Go-Live. **OTP RELAY: built + armed (cleared /tmp/meesho_otp.txt, OTP_PAGE_REACHED print + 5-min poll) but NOT EXERCISED** — the authenticated WebKit session logged straight to /home with no OTP page (Akamai-valid cookies accepted user+pass directly). Account 4359160/oinpw/"meesell". Category "Hair Bands" (Women Fashion>Accessories>Hair Accessories>Hair Bands). Image gate PASSED. **FULL form filled** (20-22 run: all 12 attribute dropdowns color/generic_name/ideal_for/material/multipack/occasion/pattern/product_dimension_unit/type/country_of_origin/brand/size=L + 9-field compliance + weight + pincodes). ★ **DECISIVE FINDING: there is NO right-side tax detail card, and the Meesho-Price/MRP/WDRP/Inventory input cells NEVER render, in the create-flow product-details step — even with the entire form filled green.** Rendered DOM = section + attributes + compliance + Brand/Size/Desc + T&C + Image Guidelines; right rail = ONLY Image Guidelines; only forward control = "Submit Catalog" (forbidden). Schema fetchProductDetailsV3.product_size_data = 4 raw NUMBER inputs (meesho_price/only_wrong_return_price/product_mrp/inventory), grep for commission|payout|transfer|referral|monetiz|settle|margin = ZERO real hits (65 "tax" = is_taxonomy_attribute metadata). The price cells + any settlement/tax card are gated behind "Submit Catalog" or simply don't exist in this flow (consistent with r7). NO computing XHR for a tax card exists (only priceRecommendation/fetchDuplicatePid = dup+shipping check). **⚠️ SAFETY: getCatalogUploadPerformance total_upload_count = 2, NOT 0 — but NOTHING WENT LIVE.** Timeline proves: 0(before any run)→1(after 20-16)→2(after 20-22); +1 per run that passes the image gate + clicks "Continue" to product-details. NO run ever clicked Submit/Publish/QC (grep-verified); each clicked Discard Catalog. So the counter tracks catalog UPLOAD-STARTS (draft creation on advancing past the image step), NOT live listings; advancing past the image gate to reach the form is what increments it (unavoidable for this task). fetchBulkCatalogsListV2 file_count=0; nothing sellable. Recommend founder verify the 2 discarded drafts are inactive. VERDICT: create-flow category sampling NOT VIABLE (no tax card to sample; price cells unreachable pre-Submit; each attempt = an upload-start). Payout source stays r4's fetch-supplier-products.current_transfer_price (existing) + seller-input (new). Reinforces backend handoff_pricing_transfer_price.md (no new handoff). |
| price-calculator (NEW-account single-catalog commission proof, round 6) | — (read-only, no branch) | DONE — proof delivered: preview is image-gated (not read-only-reachable) | mesell-price-calculator-data-session-6 | 2026-06-18 | founder — pricing-model decision | Part 1: NEW Meesho credential (8754713794) installed into `.meesho_creds.env` (still gitignored + untracked; values never echoed), replacing prior 8220476727. Auto-detected NEW account: **supplier_id=4359160, identifier=oinpw, store name="meesell"** (email kskarthick93@gmail.com), registration_status=SUPPLIER_CREATED, is_agreement_accepted=false, flat default_monetization_percent=4.0, shipping_charges=70/bracket=1000, logistic_fee_enabled=1, cod=0. NOTE this matches the OLD hardcoded oinpw/4359160 in meesho_batch_scraper.py — different account from the round 1-5 Curl Candy/kdwec/3661229. #catalogs not surfaced (no listing XHR fired in this create-flow-only nav). Part 2: 3 paced authenticated WebKit runs, ZERO Akamai 403 (1 tolerated 463 at login each), ZERO hard stops, ZERO writes. Reached ADD-SINGLE-CATALOG via REAL sidebar nav → real URL `https://supplier.meesho.com/panel/v3/new/cataloging/oinpw/catalogs/single/select-category`. Selected ONE category: **"Hair Spa"** (path Personal Care > Hair Care > Hair Spa). **KEY FINDING:** the single-product create wizard is `category → MANDATORY image upload → product details/pricing`. After category select the page exposes ONLY [Search Category, Add Product Images, View Full Image Guidelines, Discard Catalog] + a single file input — NO Next/Continue, NO price field, NO step indicator. The live commission/payout preview is GATED behind mandatory product-image upload (cannot reach the PRICE section read-only without uploading images + advancing the wizard = building a listing, which the guardrail forbids). VERDICT = **NOT pre-submit read-only-safe in the create flow** (effectively post-image-gate). No price entered, no preview/computing XHR captured. **Templatizable for a paced category-wise sampling run? NO** via the create flow (image-gated). The proven e-sig-FREE programmatic source stays round-4's `GET /api/growth/activation/fetch-supplier-products` → per-product `current_transfer_price` (=seller payout). Probe `backend/scripts/meesho_create_preview_probe.py` (untracked). Logs gitignored. |
| category-seeding (commission backfill, Wave 1.5) | feature/category-seeding | CLOSED — won't-fix | mesell-category-seeding-session-1 | 2026-06-16 | — | D2 Phase 2 ABANDONED ON EVIDENCE. Scrape method proven (auth WebKit + Akamai bypass) but NO category rate-card exists — only account-level flat `default_monetization_percent=4.0`. Founder observed commission differs per-product/per-date at upload; internet cross-verification CONFIRMED Meesho commission is DYNAMIC (category × price-slab × time-bound promotions; no stable published rate-card). A static `commission_pct` is wrong by construction. DECISION (founder 2026-06-16): keep `commission_pct=NULL` (Wave-1 shipped state); pricing engine's NULL-tolerant 422 stands. Real commission, if needed, = per-product capture at upload time (pricing-feature item, NOT seeded reference data). Scrape scripts + `category_commissions.json` retained as record only, NOT a seed input. |

## Recently merged (last 14 days)

| Feature | Merged to | Date | PR | Notes |
|---|---|---|---|---|
| — | — | — | — | No recent merges. |

## Inter-lead requests open

| To lead | About feature | Request | Opened | Status |
|---|---|---|---|---|
| infra | category-seeding | Post-migrate K8s Job (alembic upgrade head → seed_all.py) for dev/staging, per §2 ④ / §8 | — | DEFERRED — founder ruled D1=LOCAL-ONLY (2026-06-16); handoff does NOT open this pass. Re-opens if/when dev/staging scope is later requested. |
| backend | price-calculator | Use Meesho `fetch-supplier-products.current_transfer_price` (= per-product seller PAYOUT, net of all deductions) + `current_price` (listing) as ground-truth for EXISTING products; seller-input commission (default 4%) + deduction line-items for NEW products. Do NOT add a category `commission_pct` migration (commission stays NULL, founder-locked). Memo: `.claude/agent-memory/meesell-data-engineer/handoff_pricing_transfer_price.md`. | 2026-06-18 | OPEN — 48h SLA (escalate via STATUS_MASTER blockers if unacked) |

---

## Status vocabulary

| Status | Meaning |
|---|---|
| `PENDING` | Feature is on the lead's backlog; no branch exists yet. |
| `IN PROGRESS` | A `feature/{name}/data` branch exists; specialist is actively committing. |
| `IN REVIEW` | A PR is open against `feature/{name}`; awaiting lead approval. |
| `MERGED` | The data group's PR has merged to `feature/{name}` — the group is done for this feature. |
| `BLOCKED` | Work stopped pending an inter-lead request, infra change, or founder decision. |

A feature row stays on the active features table until the data group's PR merges to `feature/{name}`; then it moves to "Recently merged" for 14 days before being removed.

---

## Acceptance gate

PRs from `feature/{name}/data` → `feature/{name}` are reviewed and merged by this lead. Approval requires the PR template at `.github/PULL_REQUEST_TEMPLATE/data.md` to be filled completely — including parser run command + stats, schema-impact decision (no change OR coordinated migration), and either an included or justified-deferred amendment to `MVP_ARCHITECTURE.md` / `MEESHO_CATEGORY_INTELLIGENCE.md`. Gate-1 unit (parser tests) must be green. Gate-5 golden_roundtrip must be green when the PR touches an XLSX surface.
