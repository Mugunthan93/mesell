# Feature Board — QA Lead

**Lead agent:** `meesell-qa-coordinator`
**Domain:** qa (test pillar — the fourth coordinator, peer to backend / frontend / ai)
**Last updated:** 2026-06-22 (**QA WAVE 3 (catalog vertical) — FULLY MERGED TO `develop` (FOUNDER-DIRECTED merge-gate, session `mesell-qa-wave-3-coord-session-2`).** PR **#460** (`feature/qa-wave-3/integration → develop`) merge-gate-reviewed by `meesell-qa-coordinator` → **APPROVE → MERGED to develop** as a true **merge-commit `e7470bf`** (develop HEAD `17171d1` → **`e7470bf`**; `--admin` per the founder's explicit "merge gate PR #460 whenever ready" — normally the FOUNDER's D1 gate, the lead acted only on that directive). **TESTS-ONLY: 12 files (+2599/-59), ZERO source/board/status/memory/ci, ZERO deletions** (4 net-new backend pytest + 1 upgraded export-ZIP test + 1 eval artifact + 2 FE specs + 4 e2e scaffold flows). **GATE INDEPENDENTLY RE-RAN: BE 59 passed/0 failed** (the 3 integration files run TOGETHER one-process, CI-Gate-4 style, 0 skipped/0 green-wash; `meesell_test` auto-reprovisioned to head `e9415bdcae20`), **FE 44 passed/0 failed** (standalone vitest), **E2E 29 specs parse/typecheck CLEAN** (`playwright --list`; the 4 new flows = 1 live skip-gated `wizard-save` + 3 documented `test.fixme` scaffolds, founder-approved). **CI checks on the head commit CONFIRM GREEN** (Gate 1-5 + all 8 FE matrix units incl. mfe-catalog). Zero real Gemini/MSG91/Razorpay/GCS (`gemini_calls:0`); `TEST_DATABASE_URL` `_test` guard intact; no assertion-free tests; W3-FE-8 negative-assertion regression guard present. Branch `feature/qa-wave-3/integration` (`ab1aa6d`) RETAINED. **The board-tracker PR #461 ("…#460 OPEN") is now MOOT → CLOSED.** All 3 wave-3 lane rows (BE #396 `edb875f`, FE #394 `4571048`, E2E #409 `c382f84`) are reachable from develop via this merge. — PRIOR: **CAT-BE-17-GUARD landed on develop — category-schema "seed gap" finding RESOLVED-as-env-issue (session `mesell-qa-wave-cat-guard-coord-session-1`).** PR #474 (`feature/fix-category-schema-guard/backend → develop`, a standalone feature-branch test PR — NOT a qa-wave `integration → develop` founder gate) gate-reviewed by `meesell-qa-coordinator` → **APPROVE → squash-merged** to develop (`ff38971` → **`11da147`**, no `--admin` needed — all 15 required checks green, MERGEABLE/CLEAN). ONE tests-only file, +169/-0, ZERO source/data code: `backend/tests/modules/category/test_all_seeded_categories_resolve_schema.py`. Iterates EVERY `categories.id` → `category_service.fetch_schema_dto(category_id, db)` (the real `categories→templates` JOIN, repository.py L165-202 + §5A.C DTO projection), collects ALL failures (`CategoryNotFoundError`/empty `fields[]`), then `assert len(failures) == 0` — a real collect-all-failures assertion catching the 404 class, NOT assertion-free. **GATE RE-RAN LIVE on a schema-only `meesell_test` (categories=0, field_enum_values=0) under full CI dummy env → 1 SKIPPED** (the BE-SEED-1 reason via `_seed_data_absent`'s `field_enum_values==0` gate) — HONEST skip, NOT a false-green; it cannot green-wash. `TEST_DATABASE_URL` `endswith("_test")` guard intact (not in diff); no real external calls (grep clean); service-level call so no `_otp_client` event-loop hazard. **The seeded green (3,772 categories, 0 failures expected) is OWED** at the next tunnel/CI window — local seeded data lives only in the non-`_test` `meesell` DB and the `meesell` role lacks CREATEDB to clone a seeded `*_test`; acceptable per gate brief (the test can't green-wash). **BE-SEED-1 guard landed; the category-schema "seed gap" is now RESOLVED-as-env-issue (data complete: 3,772 leaf categories each with a valid `template_id` FK; the Wave-1 CAT-E2E-05/06 404s were an unseeded-CI artifact, not data corruption).** Writer-memory scribed into `meesell-backend-test-writer/test_files_authored.md` (git-plumbing route, develop-based). — PRIOR: **qa-auth-contract `integration → develop` PR OPENED for the FOUNDER's gate (session `mesell-qa-wave-2-coord-session-1`).** All three qa-auth-contract salvage lanes now coexist in `feature/qa-auth-contract/integration` (tip refreshed with `origin/develop` → merge-commit `a2eee1d`): backend (PR #440, `8adbd57`), frontend (PR #438, `3caa18d`), e2e (PR #441, `4aa12da`). The `integration → develop` diff (`git diff origin/develop...HEAD`) is **exactly 17 tests-only files (+1588/-64), ZERO app source, ZERO deletions** (8 backend integration tests, 3 frontend `*.spec.ts`, 6 e2e harness/flow files). 15 auth test behaviors salvaged from the dead qa-wave-2 branch (backend 9 / frontend 3 / e2e 3), all gate-reviewed + independently re-run. The PR is OPEN — NOT merged (`integration → develop` is the FOUNDER's gate, D1). #427's auth fix already landed separately on develop. Board RECONCILED from origin/develop (concurrent qa-pricing/qa-image-ai/qa-catalog rows preserved); backend + e2e lane rows flipped PLANNED→MERGED-to-integration; 3 carry-forwards filed as Inter-lead requests (INFRA-1 manifest port mapping, INFRA-2 stale dev bundle, GIS dev-bypass seam). — PRIOR: **qa-image-ai Wave B (frontend) GATE PASSED → MERGED to integration (PR #442, session `mesell-qa-wave-image-ai-frontend-session-1`).** `feature/qa-image-ai/frontend` (`bc9d08b`) → `feature/qa-image-ai/integration` gate-reviewed by `meesell-qa-coordinator` → **APPROVE → squash-merged** (integration `0f4bfd9` → **`c87d800`**); branch preserved (NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 2 spec files, +696/-0, **tests-only — ZERO source, ZERO deletions, single commit**; no stale-base hazard (merge-base == integration tip `0f4bfd9`). **+42 net-new:** `image-uploader.component.spec.ts` 81→106 (IMG-FE-01..05) + `catalog-form.component.spec.ts` 94→111 (IMG-FE-07..10). **GATE RE-RAN `vitest run` of the 2 PR files = 217 passed / 0 failed.** IMG-FE-06 confirmed PRE-COVERED in `image.service.spec.ts` (22-test HttpTestingController error matrix) — NOT duplicated. **Ground-truth HONEST + source-verified:** `AutofillButtonComponent`/`FieldDiffComponent` do NOT exist as separate components (grep=NONE); autofill is INLINE in `CatalogFormComponent` (button L264, `onAutofill()` L720, dual-write `aiSuggestions.set`+`fieldValues.update` L732-733, error toast L738) — no phantom-component test; product-mismatch filed as a spec gap, not a red. No assertion-free tests; no real service injected; error assertions check non-empty strings. With Wave A (`0f4bfd9`) + Wave B (`c87d800`) both on integration, the qa-image-ai backend+frontend lanes now coexist (the AI `/ai` IA-RED-2 + any e2e lane are separate). Board IN-REVIEW soft gap logged (`coordinator_patterns.md`). — PRIOR: **qa-auth-contract (frontend) SALVAGE lane GATE PASSED → MERGED to integration (PR #438, session `mesell-qa-wave-2-frontend-session-1`).** `feature/qa-auth-contract/frontend` (`10328b7`) → `feature/qa-auth-contract/integration` gate-reviewed by `meesell-qa-coordinator` → **APPROVE → squash-merged** (`1fc73f4` → **`3caa18d`**); branch preserved (NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 3 files, +184/-1, tests-only (3 `*.spec.ts` appends; ZERO feature-code; the single deletion is a corrected `it.skip` flip). **GATE INDEPENDENTLY RE-RAN `ng test frontend --no-watch` in the worktree = 99 files / 1686 passed / 7 skipped / 0 failed** (matches the PR report exactly). FE-AUTH-07 auth-layout render + brand (`.auth-logo`=`M` — corrects a WRONG pre-existing `it.skip`) + real `<ng-content>` projection; FE-AUTH-08 `localStorage.length===0` after setSession/logout + `getToken()` ignores a seeded localStorage decoy (Decision #14 spec-side guard); FE-AUTH-11 `GisFailStub.load()` rejects → `errorMessage()` contains "Couldn't load Google sign-in" (component L219-220 confirmed). All 3 source-ground-truthed; no duplication (no pre-existing GIS-load-failure / localStorage-never case on develop); no stale-base hazard (merge-base == integration tip == develop `1fc73f4`; zero deletions); no real GIS/HTTP (GoogleIdentityService stubbed via `useClass`; HttpTestingController); no assertion-free tests. Closes FE-AUTH-07/08/11. — PRIOR: **qa-image-ai Wave A (backend) RE-GATE PASSED → MERGED to integration (PR #431, session `mesell-qa-wave-image-ai-backend-session-2`).** After the earlier REJECT was remediated (commit `b3937f8`), `feature/qa-image-ai/backend` (`b3937f8`) → `feature/qa-image-ai/integration` re-gate-reviewed by `meesell-qa-coordinator` → **APPROVE → squash-merged** into integration (tip `93a82eb` → **`0f4bfd9`**); branch preserved; `integration → develop` is the FOUNDER's gate, NOT touched. 7 tests-only files, rebased onto develop `1fc73f4` (stale-base contamination gone). **GATE INDEPENDENTLY RE-RAN:** reject reason FIXED (`test_route_integration.py` 4/4 as a UNIT, stable ×3; D2 `_otp_client` loop patch applied); full CI Gate-4 `pytest -m integration` = **354 passed / 18 skipped / 0 FAILED** in ONE process — no `Event loop is closed` failure from this lane. Stub-guard swap CONSISTENT: `TestStubStateGuards` DROPPED (obsoleted by the IA-RED-2 real scorer already on integration `93a82eb`), `TestRunnerAggregationLogic` KEPT. Integration tip `0f4bfd9` proven internally consistent + green (eval + harness = 16 passed / 0 failed; real scorer + aggregation co-run, NO stub-lock). IA-RED-1 fix (canonical audit columns ≤₹0.05) + IMG-BE-07=502 + zero real Gemini/GCS + zero assertion-free tests all re-confirmed. **NEW finding (NOT a #431 blocker):** `test_iam_logout_idempotency` throws `Event loop is closed` at setup — PROVEN PRE-EXISTING (reproduces on clean develop; non-failing GC warning today, does NOT red CI); same D2 class; filed → backend-coordinator. — PRIOR: **qa-pricing Wave B (frontend) GATE PASSED → MERGED to integration (PR #436, session `mesell-qa-pricing-frontend-session-1`).** `feature/qa-pricing/frontend` (`c344371`) → `feature/qa-pricing/integration` gate-reviewed by `meesell-qa-coordinator` → **APPROVE → squash-merged** into integration (tip `f938f0b` → **`4dd32ca`**); branch preserved (NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 1 file, +100/-0, tests-only (`M frontend/apps/mfe-export/src/app/export.component.spec.ts` ONLY; zero product code; zero deletions). +13 tests: PQE-FE-12 i18n non-raw-key guard (6) + PQE-FE-13 productId post-fix contract (2) + 5 others. **GATE RE-RAN via vitest in a worktree:** `export.component.spec.ts` = **75 passed** (was 62, +13 matches claim); mfe-export = 104 passed/0 failed; mfe-pricing = 131 passed/0 failed; the pre-existing 10-failing-files baseline is UNCHANGED (none in mfe-export/mfe-pricing; the diff touched ONE mfe-export spec, now green). `resolveExportProductId` fix CONFIRMED live on integration (`export.component.ts` L425 wires it; `'current-product-id'` placeholder GONE). No assertion-free tests; no real service injected; no stale-base hazard (merge-base == integration tip). **qa-pricing Wave C (e2e) remains BLOCKED on the mfe-pricing `data-testid` addition** (a parallel `meesell-angular-component-builder` is adding them now). — PRIOR: **qa-auth-contract SALVAGE lane STOOD UP (session `mesell-qa-wave-2-coord-session-1`).** Founder-approved FULL SALVAGE of qa-wave-2's UNIQUE/PARTIAL-with-extra auth coverage (~12 behaviors) into a fresh `feature/qa-auth-contract` lane off CURRENT develop. **PR #421 CLOSED** (superseded by `98cc02a`; branches NOT deleted/NOT resurrected). **`feature/qa-auth-contract/integration` CREATED off develop `1fc73f4`** (race-safe `show-ref`-guarded push). Specialist-ready spec authored: `docs/plans/features/auth-testing/SALVAGE_PLAN.md` (3 lanes — E2E 2 net-new files+1 un-fixme; FE 3 appends no-new-files; BE 6 net-new+3 upgrades). **KEY SEQUENCING UPDATE: the founder's "hold backend until #427 lands" is RESOLVED — PR #427 (clear-cookie on 401 refresh) is ALREADY MERGED to develop (`1fc73f4`, fix `e60cedd`, regression test `test_refresh_401_clears_stale_cookie` present)** → all three lanes can start immediately off `1fc73f4` with NO rebase; BE-AUTH-09/11 clear-cookie xfails stay DROPPED. The Director will dispatch the e2e + frontend specialists immediately and the backend specialist now too (hold satisfied). No test code written / no specialists dispatched this session (next step). — PRIOR: **QA-ONBOARDING WAVE — FULLY MERGED TO `develop` (FOUNDER's `integration -> develop` gate DONE).** All three qa-onboarding lanes are now on `develop` via the FOUNDER's `feature/qa-onboarding/integration -> develop` merge-commit **`98cc02a`** (PR #422): backend Wave A (`aa4345d`), frontend Wave B (#407 `7e86054`), e2e Wave C (#411 `e5a44a3`). Two product-bug fixes landed alongside: the seller-profile **422-not-500 on missing pincode** (BE, #419 `fdcdc5c`) and the **7-field Legal-Metrology profile reshape + manufacturer/packer pincode REQUIRED** (#416 `e3be41e`), plus the auth-storm spec reconcile absorbing develop #406 (`2147d5b`). `develop` tip is now **`f820903`** (handoff doc #423 on top). The qa-onboarding pincode-500 inter-lead item to frontend-coordinator is RESOLVED by #419/#416. STILL-OPEN carry-forwards (see Inter-lead requests + carry list below): mfe-export route-productId product bug (export-download e2e fixme), the image-precheck GCS/MinIO env fixme, the google-click cross-origin GIS fixme, BE-CUSTOMER-INSERT-500-1, OB-FE-18, the libs/composites empty-state red, the CI test-gate wiring memo (-> infra), and the pnpm-lock frozen-lockfile sync. This gate-scribe doc (#417) was reconciled onto `develop` post-#422 (the develop->integration reconcile had keep-both-merged the board + `git checkout --theirs`'d the `.claude` journals; this PR rebases the Wave-C scribe back on). — Prior: **QA ONBOARDING WAVE C (E2E) GATE PASSED -> MERGED to integration (PR #411).** `feature/qa-onboarding/e2e` (`1a13128`) -> `feature/qa-onboarding/integration` (`7e86054`) gate-reviewed by `meesell-qa-coordinator` (session `mesell-qa-onboarding-e2e-session-1`) -> **APPROVE (8/8) -> squash-merged** into integration **`e5a44a3`**; branch preserved (NO `--delete-branch`; `integration -> develop` is the FOUNDER's gate, NOT touched). 9 files (8 `frontend/e2e/**` + the board), tests-only, ZERO app source. Closes OB-E2E-01..08. **Verdict basis = rigorous STATIC + SOURCE-ground-truth — the live Playwright re-run was BLOCKED by a contaminated dev stack** (slot-1 shell :4210 + mfe-auth :4211 + mfe-onboarding :4216 up; other remotes on slot-0 ports; NO backend reachable :8000=404/:8010=down) -> per the checklist's explicit allowance the gate disclosed this and fell back to full diff + line-by-line page-object/fixture/spec read + ground-truthing EVERY selector + the persist fix against integration SOURCE `7e86054`. Writer's run = **11 passed / 3 test.fixme / 0 failed, stable x2**. **OB-E2E-03 = the #399 data-loss regression guard, CONFIRMED not assertion-free** — asserts `PATCH /seller-profile`->200 AND re-login (after clearCookies) lands `/dashboard` NOT `/onboarding` + dashboard heading; source L314 genuinely `patchProfile().subscribe()` (NOT the old setTimeout mock). Selectors LIVE-VERIFIED + source-confirmed (`getByLabel` Manufacturer/Packer fields, `getByRole('alert')` banner, `getByRole` skip link, `getByTestId('onboarding-submit')`; `onboarding-business-name` GONE/0 occurrences); zero hardcoded ports (all from `playwright.config.ts`); no localStorage token (Decision #14); rate-limit reset in HARNESS (`global-setup.ts` + auto `_rlReset` fixture + `rate-limit.ts`), never in a spec. **NO stale-base hazard** (merge-base == integration tip; `grep '^D'` empty; Wave B specs preserved). **Board-discipline IMPROVEMENT:** the e2e specialist FLIPPED its row to IN REVIEW on PR open (`f787541`) — first e2e lane to do so. 3 fixme honest+logged. **2 product bugs found LIVE -> frontend-coordinator (NOT swallowed):** NEW onboarding-pincode NOT-NULL 500 + the Wave-1 mfe-export route-productId carry. Writer-memory findings (onboarding/auth selectors + the §5-note-3 whole-stack-rebuild re-confirmation) SCRIBED into qa-coordinator `coverage_gaps.md`. **All three qa-onboarding lanes now coexist in integration** (backend `aa4345d`, frontend #407 `7e86054`, e2e #411 `e5a44a3`) — [since landed on develop via #422 `98cc02a`, see lead clause]. — Prior: **QA WAVE 3 (catalog) — E2E LANE GATE PASSED -> MERGED (PR #409) -> WAVE 3 NOW FULLY ASSEMBLED IN INTEGRATION.** `feature/qa-wave-3/e2e` -> `feature/qa-wave-3/integration` gate-reviewed by `meesell-qa-coordinator` (session `mesell-qa-wave-3-e2e-session-1`) -> **PASS -> squash-merged** into integration `c382f84` (was `4571048`; branch preserved `8128b68`, NO `--delete-branch`; `integration -> develop` is the FOUNDER's gate, NOT touched). E2E lane is a SCAFFOLD (founder-approved to land now): W3-E2-1 CODIFIED + honestly skip-gated (`flows/wizard-save.spec.ts`), W3-E2-2/4/6 documented `test.fixme` with exact inline un-fixme conditions, W3-E2-3 un-fixme condition documented. **Gate re-ran `playwright --list` (e2e config) = 16 tests parse + typecheck CLEAN** (the env-blocked catalog flows are real + documented; founder approved NOT requiring a full live run). W3-E2-1 asserts a real UUID + `catalog-save-status` reaching the "Saved" TEXT (not mere visibility) + a listed product; skip-gates do NOT green-wash. All selectors LIVE-VERIFIED in `selector_registry.md`; only non-testid is the structural `mee-input input, mee-textarea textarea` edit-field fallback (scribed). Zero hardcoded ports (all from `playwright.config.ts` env). **STALE-BASE HAZARD CAUGHT AT THE GATE (same as Wave-2 e2e):** the e2e branch's merge-base with integration was develop `3c63b55`, PRE-DATING the #396 (`edb875f`) backend + #394 (`4571048`) frontend lane merges -> a naive squash would have DELETED 4 BE test files + 4 FE spec files (`git diff integration..e2e` showed 8 deletions). The gate merged integration INTO the e2e branch FIRST (zero conflicts), making the squash clean-additive (ZERO deletions); the BE+FE lanes are verified PRESERVED on integration post-merge. **`dead_route_guard.mjs` + the `ci.yml` change in the diff are NOT scope creep** — they came from develop #403 (FED-2 dead-route guard, already on develop), present only because the e2e branch was cut from a newer develop than the integration base. **ALL THREE WAVE-3 LANES (backend `edb875f`, frontend `4571048`, e2e `c382f84`) NOW COEXIST in `feature/qa-wave-3/integration` (tip `c382f84`) — ready for the FOUNDER's integration->develop gate.** FOUNDER NOTE: the integration->develop diff also shows mfe-export `export.component.ts`/`.model.ts` + `refresh.interceptor.ts` + `auth.service.ts` as changed, but those source files are BYTE-IDENTICAL to the merge-base `3c63b55` on integration (QA never touched them) — they differ ONLY because develop advanced past the merge-base (mfe-export #398 + auth #406 landed on develop after integration branched); a 3-way merge-commit reconciles them cleanly (develop's newer versions win), the QA contribution stays tests-only. 6 inter-lead requests/blockers logged below. — Prior: **QA WAVE 1 — CONSOLIDATED TEST_REPORT LANDED.** The final Wave-1 deliverable, `docs/testing/TEST_REPORT.md`, is authored + merged to develop (`docs(qa): QA Wave 1 consolidated TEST_REPORT`, branch `docs/qa-wave-1-report`, base develop `9c2fc7f`, autonomous Wave-1 → founder pre-approved → `--admin`). It consolidates all 3 lanes (backend #380 `5da7af9` = 1314 passed/0 failed; frontend #383 `39b428a` = 1527 passed/61 PRE-EXISTING reds; e2e #385 `70fe9f3` = 9 passed/3 test.fixme), both product bugs (HIGH logout FIXED via #381 `456494e`; mfe-export `productId` FILED to frontend), the screenshot + verbal (/api/radar all-quiet, /api/logs clean) evidence, and the full Wave-2 carry-forward (8 named frontend reds W2-FE-1…8 + backend P1.11 export-ZIP + 3 e2e test.fixme + the export productId fix). **Wave 1 is now fully closed.** — Prior: **QA WAVE 1 — E2E CODIFY GATE PASSED → MERGED (PR #385).** PR #385 (`feature/qa-wave-1/e2e-codify` → develop) gate-reviewed by `meesell-qa-coordinator` (session `mesell-qa-wave-1-e2e-session-1`) → **APPROVE → squash-merged** to develop (autonomous Wave-1, founder pre-approved → `--admin`, NO `--delete-branch`). All 8 critical seller flows codified; `playwright test --workers=1` = 9 passed / 3 test.fixme, stable ×3. logout-guard confirms the #381 logout fix E2E. ONE product bug found live (mfe-export ignores route productId → export broken) filed to frontend-coordinator. The 3 test.fixme are env/product-bug-blocked with logged reasons (see Wave-2 carry-forward + `federation_quirks.md`). **This completes Wave-1 — backend (#380), frontend (#381/#383), and e2e (#385) all landed on develop; ZERO active QA rows.** — Prior: **FRONTEND GAP-FILL GATE PASSED → MERGED (PR #383).** PR #383 (`feature/qa-wave-1/frontend` → develop) gate-reviewed by `meesell-qa-coordinator` (session `mesell-qa-wave-1-frontend-session-2`) → **APPROVE → squash-merged** to develop `39b428a` (was `d13ed50`; autonomous Wave-1, founder pre-approved → `--admin`, NO `--delete-branch`). 5 files: 2 specs (`mfe-pricing/pricing.component.spec.ts` +2/-2, net-new `libs/core/guards/auth.guard.spec.ts` +82) + 2 writer-memory files + this board. **VERIFICATION RUN BY THE GATE:** (1) `tsc --noEmit -p tsconfig.spec.json` (whole spec project) = **EXIT 0** — the 2 pre-existing mfe-pricing TS errors (`:241 TS2352`, `:266 TS2367`) that BLOCKED the umbrella build are gone; (2) `ng test frontend --include …/auth.guard.spec.ts` = **3/3 GREEN** (real Vitest run, `runInInjectionContext` + computed-signal `AuthService` mock + UrlTree-redirect assertion — not assertion-free); (3) full `ng test frontend --no-watch` reproduced **1527 passed / 61 failed / 20 skipped (1608)** across **88 passed / 10 failed (98)** files — `pricing.component.spec.ts` + `auth.guard.spec.ts` are GREEN (absent from the 10 failed). **THE 61 REDS ARE PRE-EXISTING & NOT INTRODUCED:** #383 touches NONE of the 10 failing files (verified `git diff --name-only`), and all 10 failing files EXIST at merge-base `d13ed50` (verified `git cat-file -e`); the same 2 compile errors reproduce on a CLEAN develop checkout (build can't even reach the test phase on develop → that's why develop had no clean run). The 61 reds are logged below as **Wave-2 carry-forward findings**. — Prior: PR #381 (`feature/qa-wave-1/testids-logout/frontend` → develop) gate-reviewed by `meesell-qa-coordinator` → **APPROVE → squash-merged** to develop `456494e` (was `5da7af9`; autonomous Wave-1, founder pre-approved + integration step collapsed → `--admin` direct-to-develop, NO `--delete-branch`). 27 files, +314/-22. Two commits: (1) **logout cookie-revoke fix** — `auth.service.logout()`/`forceLogout()` now fire `authApi.logout()` POST (`withCredentials`) + navigate `/login`; topbar logout becomes a direct `<button data-testid="nav-logout">` (the popup `MeeMenuItem` couldn't carry a testid); **3 genuine regression tests** added + 6 existing tests updated to consume the new fire-and-forget POST so `controller.verify()` stays green. (2) **ui-kit `[testId]` passthrough** on 5 wrappers (`mee-input/mee-button/mee-otp-input/mee-textarea/mee-file-upload`; each spec = present-when-set + absent-when-unset assertion) + **~30 attribute-only `data-testid`s** across shell + all 6 remotes. **Verification:** `tsc --noEmit -p apps/shell/tsconfig.app.json` = EXIT 0; `tsc --noEmit -p tsconfig.spec.json` (full spec graph) = exactly **2 errors, BOTH the pre-existing mfe-pricing `pricing.component.spec.ts:241/266` TS2352/TS2367, ZERO referencing any of #381's 27 files**. Boundary grep `from 'primeng` in `frontend/apps/**` = **0**. NOTE: full `ng test frontend` could NOT execute the affected specs — the pre-existing mfe-pricing type error + the unsupported native-federation `buildTarget` block the umbrella `@angular/build:unit-test` build before any spec runs (gate fell back to diff review + tsc per the explicit allowance and SAID SO). **This unblocks the E2E lane** — ~30 stable `data-testid`s now exist in the app for the E2E writer to consume. — Prior: PR #380 (qa-wave-1 backend gap-fill) → **APPROVE → squash-merged** to develop `5da7af9`; spot-rerun of the 9 new/extended files vs `meesell_test` = **66 passed / 1 skipped / 0 failed**; 1 P1 gap (export-ZIP test self-skipped) carried to Wave 2.)
**This file is the single domain-level status surface for the QA lead.**

> ⚠️ **Board-reconcile note (2026-06-22, mesell-qa-wave-1-frontend-session-1):** at this session's start the on-develop board had reverted to the *pillar-bootstrap* version (concurrent-session collision — a prior session's #380/frontend/e2e row edits + a referenced `handoff_selectors_qa_wave_1.md` memo were never committed to develop, and that memo does NOT exist on disk). This file has been rewritten to the **true develop state**: #379 (pillar), #380 (backend gap-fill, `5da7af9`), and #381 (frontend blocker-fix, `456494e`) are all confirmed in `git log origin/develop`. Aspirational claims not backed by disk (the missing memo) were dropped, not resurrected.

---

## Active waves

| Wave | Group branch | Status | Current session | Last touched | Blocking | Notes |
|---|---|---|---|---|---|---|
| qa-image-ai (Wave A backend) | `feature/qa-image-ai/backend` (PR #431 → `…/integration`) | **BLOCKED (gate REJECT)** | mesell-qa-wave-image-ai-backend-session-1 (gate) | 2026-06-22 | `test_route_integration.py` fails 2/4 IN-PROCESS (`_otp_client` event-loop fixture bug) — re-dispatch backend writer | **GATE VERDICT: REJECT (one file).** Otherwise high quality: 6 of 7 files clean = **38 passed / 1 skipped** vs `meesell_test`; **IA-RED-1 genuinely fixed** (perf test reads `event_type="ai.call"` / `metadata_jsonb->>'cost_inr'` / `occurred_at` + asserts ≤₹0.05; revert-check confirms); AI-BE-13 producer→consumer contract real (captures live `cost_tracker.record`; `cost_inr` present, `inr_cost` absent); zero real Gemini/GCS (`adapters.gemini`/`adapters.gcs` seam); no assertion-free tests; **IMG-BE-07 = 502** (`GcsAdapterError`→`AdapterError.status_code=502`). BUT `tests/modules/image/test_route_integration.py` (IMG-BE-01/07/09/11) **fails 2/4 run as ONE process** (`TestPostImageGCSFail`, `TestGetImageListCrossTenant`) — each passes ALONE, fails TOGETHER (3× deterministic). Root cause: the file hand-rolls its own `AsyncClient(ASGITransport(app=app))` fixture instead of reusing `tests/integration/conftest.py`'s `_valkey_module._otp_client` per-loop patch → `rate_limit_mw._check_window → get_valkey_otp()` keeps a closed-loop redis conn across lifespans → `RuntimeError: Event loop is closed` → 500 (not the route's 502/404). This is the DOCUMENTED "D2 fix / Gate-4 repair-1" bug already solved in `integration/conftest.py`. File is `@pytest.mark.integration`; CI Gate-4 runs `pytest -m "integration"` (all integration tests in ONE process) → **WILL red the integration→develop CI gate** = the Wave-2 PR #421 failure mode (RETIRED at this gate). FIX (writer): reuse the loop-bound `_otp_client` patch / established conftest client fixture; rerun the file as a UNIT → 4/4. Re-open a fresh PR. develop UNTOUCHED; branch NOT deleted; verdict commented on PR #431. **IA-RED-2 reconciliation OWED + OWNED:** the lane's `test_ai_ops_eval_scoring_harness.py::TestStubStateGuards` LOCKS the current `_run_one_fixture` stub (30 fixtures → 0 passed) — VALID NOW. When the parallel IA-RED-2 `/ai` PR (`meesell-prompt-engineer`, gated by `meesell-ai-coordinator`) lands the real scorer in `…/integration`, **`TestStubStateGuards` MUST be retired/replaced** (the prompt-engineer's `test_ai_ops_eval.py` supersedes it) or the integration branch self-contradicts. `TestRunnerAggregationLogic` SURVIVES. Memo + inter-lead request to ai-coordinator opened. **PR-base hygiene:** integration branch stale at `3c63b55` (ancestor of develop `89b9e68`) → PR-vs-base shows 67 files but `develop...backend` = exactly the 7 Wave A files; on the re-do, fast-forward integration to develop's tip FIRST. |
| _(qa-onboarding wave)_ | — | — | — | 2026-06-22 | — | **The entire qa-onboarding wave (A backend, B frontend `#407`, C e2e `#411`) is MERGED to `develop`** via the FOUNDER's `feature/qa-onboarding/integration -> develop` merge-commit `98cc02a` (PR #422). See **Recently merged** for the per-lane gate verdicts. |
| qa-pricing (Wave C e2e) | `feature/qa-pricing/e2e` (→ `…/integration`) | **BLOCKED (selectors)** | — | 2026-06-22 | mfe-pricing component ships **ZERO `data-testid`** → PQE-E2E-02/03/04 price-calc flow cannot be codified. A parallel `meesell-angular-component-builder` is adding them now (cross-lead handoff to frontend-coordinator filed per §6). | **WAVE C BLOCKED** on the mfe-pricing data-testid addition. E2E lane held until the selectors land in develop and are live-verified into `selector_registry.md` (two-phase explore-then-codify mandate). Un-block condition: frontend-coordinator ships stable `data-testid`s on the pricing component → E2E writer dispatched. |
| qa-catalog (Wave A backend) | `feature/qa-catalog/backend` (PR #435 → `…/integration`) | **BLOCKED (gate REJECT)** | mesell-qa-catalog-backend-session-1 (gate) | 2026-06-22 | Combined `-m integration` run REDS (3 FAILED) — hand-rolled `_make_client()` re-opens the `_otp_client` event-loop-affinity bug (= #431); CAT-BE-19 skip MASKS it. Re-dispatch backend writer to reuse the loop-bound `client` fixture. | **GATE VERDICT: REJECT.** Verified independently vs `meesell_test` @ alembic head, full CI dummy-env (Postgres :5432 schema-only `categories`=0 = CI mode; Valkey :6379; master `.venv` pytest 8.3.0 / pytest-asyncio 0.24.0). **The writer's "45 passed / 9 skipped / 0 failed" does NOT reproduce.** (1) **Spot-rerun of the 10 advertised files TOGETHER = 42 passed / 9 skipped / 3 FAILED.** (2) **CI-STYLE `pytest -m "integration"` over the 8 integration-marked new files in ONE process (= CI Gate-4's `pytest -m "integration" -v`) = 28 passed / 9 skipped / 3 FAILED → WILL red the `integration→develop` CI gate.** (3) `test_preview_delete_draft_gap_fill.py` ALONE = 6 passed / 3 FAILED (failing 3 ROTATE run-to-run = loop-affinity flakiness). **Root cause = the SAME bug that rejected #431:** the gap-fill files hand-roll `_make_client()` with raw `ASGITransport(app=app)` + `app.router.lifespan_context(app)` instead of reusing the loop-bound client fixture → `rate_limit_mw → get_valkey_otp()` keeps the `_otp_client` singleton bound to a CLOSED loop across lifespans → `RuntimeError: Event loop is closed` → middleware **500** before the route runs (verbatim: `Expected 200/404/204 … got 500 {"code":"server.internal_error"}`). **CAT-BE-19's `if resp.status_code == 500: pytest.skip(...)` is a GREEN-WASHING skip — NOT an honest seed skip; it would FAIL not skip if forced** (the identical bug in preview_delete has no such guard → hard FAIL; skip DISPOSITION = MASKING, REJECTED). **FIX (writer):** delete every hand-rolled `_make_client`/`lifespan_context` client (`test_preview_delete_draft_gap_fill.py`, `test_autofill_gap_fill.py`, `test_field_enum_gap_fill.py`, `test_suggest_gap_fill.py`, `test_browse_gap_fill.py`, `test_tree_and_schema_gap_fill.py`); reuse `tests/conftest.py::client` (loop_scope=function + NullPool, no lifespan prewarm) OR the integration-conftest `_otp_client` D2 patch; remove the CAT-BE-19 masking skip; re-run as `pytest -m "integration"` in ONE process → 0 FAILED; re-open a fresh PR. **GOOD (keep on the re-do):** **CAT-BE-EVAL is solid** — genuinely asserts top-5 recall ≥80% (ran **50/50 = 100% PASS**) against the picker's deterministic trigram ranker; Gemini absent / no DB / no real calls; load-bearing assertion. Zero real external calls (grep clean). `TEST_DATABASE_URL` `endswith("_test")` guard untouched (conftest not in diff). Honest seed-conditional skips CAT-BE-11/13/14/16/18/20/21 (`categories`=0) ACCEPTABLE — carry. **CAT-BUG-1 (smart-picker live-stream death)** = FE product bug, owned by the PARALLEL frontend fix lane — NOT touched here, correctly deferred. **PR-base hygiene:** integration `c6a1be9` is an ancestor of develop `1fc73f4` (stale) → fast-forward integration to develop's tip BEFORE the re-do. develop + integration UNTOUCHED; branch NOT deleted; verdict commented on PR #435 (`#issuecomment-4766445218`). |
| qa-auth-contract (e2e) | `feature/qa-auth-contract/e2e` (PR #441 → `…/integration`) | **MERGED-to-integration** | — | 2026-06-22 | — | **GATE PASSED → squash-merged (PR #441, session `mesell-qa-wave-2-e2e-session-1`).** `feature/qa-auth-contract/e2e` → `feature/qa-auth-contract/integration` (`3caa18d` → **`4aa12da`**); branch preserved (NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 6 files, tests-only frontend/e2e (2 net-new flow files + 4 modified harness/spec; ZERO app source). **GATE RE-RAN LIVE** (slot-0 :4200 + backend :8000 + Valkey :6379 all up) = 4 passed + 1 skipped. **E2E-AUTH-03** silent-refresh across federation nav GREEN live (the E2E face of the prod refresh-storm bug); **E2E-AUTH-06** GIS-stub fires real POST `/auth/google/verify` (resp != 404) GREEN + the success path `test.fixme` (no backend google seam locally); **E2E-AUTH-01** isolated OTP-login→dashboard is develop-correct but blocked-live by the stale deployed mfe-onboarding bundle (serves the pre-#399 `onboarding-business-name` mock form; develop SOURCE has the 7-field Legal-Metrology form) — the spec is the deliverable ("spec correct, stack stale" precedent), the stale bundle is filed to infra. **STALE-BASE HAZARD CAUGHT (4th consecutive e2e lane):** merge-base `1fc73f4` pre-dated #440/#438 → merged integration INTO e2e first → ZERO deletions. Closes E2E-AUTH-01/03/06. Spec: `docs/plans/features/auth-testing/SALVAGE_PLAN.md` §2. |
| qa-auth-contract (frontend) | `feature/qa-auth-contract/frontend` (PR #438 → `…/integration`) | **MERGED-to-integration** | — | 2026-06-22 | — | **GATE PASSED → squash-merged (PR #438, session `mesell-qa-wave-2-frontend-session-1`).** `feature/qa-auth-contract/frontend` (`10328b7`) → `feature/qa-auth-contract/integration` (`1fc73f4` → **`3caa18d`**); branch preserved (NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 3 files, +184/-1, tests-only (3 `*.spec.ts` appends; ZERO feature-code; the single deletion is a corrected `it.skip` flip). **GATE INDEPENDENTLY RE-RAN `ng test frontend --no-watch` in the worktree = 99 files / 1686 passed / 7 skipped / 0 failed** (matches the PR report exactly). **FE-AUTH-07** (+7) source-ground-truthed: structural render (.auth-wrapper/.auth-card/.auth-brand) + brand (`.auth-logo`=`M` NOT `MeeSell` — corrects a WRONG pre-existing `it.skip`; `.auth-wordmark`=`MeeSell`; aria-label) + real `<ng-content>` projection via a HostWithContentComponent. **FE-AUTH-08** (+4) asserts `localStorage.length===0` after setSession / repeated setSession / logout, AND `getToken()` returns the in-memory value NOT a seeded `access_token` localStorage decoy (Decision #14 spec-side guard). **FE-AUTH-11** (+1) `GisFailStub.load()` rejects → `errorMessage()` contains "Couldn't load Google sign-in" (component L219-220 confirmed). No assertion-free tests; no real GIS/HTTP (GoogleIdentityService stubbed via `useClass`; HttpTestingController). No duplication (source-checked: no pre-existing GIS-load-failure / localStorage-never case on develop; auth-layout had 1 thin case). **No stale-base hazard** (merge-base == integration tip == develop `1fc73f4`; zero deletions). Closes FE-AUTH-07/08/11. |
| qa-auth-contract (backend) | `feature/qa-auth-contract/backend` (PR #440 → `…/integration`) | **MERGED-to-integration** | — | 2026-06-22 | — | **GATE PASSED → squash-merged (PR #440, session `mesell-qa-wave-2-backend-session-1`).** `feature/qa-auth-contract/backend` → `feature/qa-auth-contract/integration` (`1fc73f4` → **`8adbd57`**); branch preserved (NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 8 files, tests-only; 9 behaviors (BE-AUTH-05/08-recovery/10/12/14/15 net-new + 04/16/02-03 upgrades). **GATE RE-RAN full CI Gate-4 `pytest -m integration`** = 32 passed (lane) / 65 passed + 8 skipped (whole integration dir), ONE process. **The `plan_guard_client` ASGITransport fixture CORRECTLY applied the D2 `_otp_client` patch** (NOT the #431/#435 event-loop bug — reuses the loop-bound client). BE-AUTH-09/11 clear-cookie xfails stay DROPPED (hard-covered by #427's `test_refresh_401_clears_stale_cookie` on develop `1fc73f4`). Zero real MSG91/Google (adapter seam, AsyncMock/monkeypatch); every new 4xx/5xx asserts a non-empty i18n `validation_message_id`; no assertion-free tests. Spec: SALVAGE_PLAN.md §4. |

> **Session-end sweep + BOARD RECONCILE (2026-06-22, mesell-qa-wave-2-coord-session-1 — qa-auth-contract LAND prep):** Prepared the `feature/qa-auth-contract/integration → develop` PR for the FOUNDER's gate. **Refreshed integration with `origin/develop`** (clean merge-commit `a2eee1d`, NO conflicts — the on-disk board collision flagged in `qa_waves.md` did not recur; origin/develop's board merged additively into the integration board). **Post-refresh `git diff origin/develop...HEAD` = exactly 17 tests-only files (+1588/-64), ZERO app source, ZERO deletions** — 8 backend integration tests, 3 frontend `*.spec.ts`, 6 e2e harness/flow files. **BOARD RECONCILED FROM origin/develop (not clobbered):** took develop's CURRENT board as the base (carrying the concurrent qa-pricing/qa-image-ai/qa-catalog rows — none dropped) and ADDED the qa-auth-contract MERGED-to-integration rows that were OWED from the last e2e gate: backend (PR #440, `8adbd57`) PLANNED→MERGED-to-integration, e2e (PR #441, `4aa12da`) PLANNED→MERGED-to-integration (frontend #438 `3caa18d` was already MERGED on the board). Added the 3 carry-forwards as Inter-lead requests: INFRA-1 (manifest port-MAPPING / playwright default port map), INFRA-2 (stale deployed mfe-onboarding bundle), GIS-seam fixme (dev google bypass → auth-builder/ai). **ALL THREE qa-auth-contract lanes now coexist in integration `4aa12da`** (backend `8adbd57` + frontend `3caa18d` + e2e `4aa12da`). The integration→develop PR is OPENED for the FOUNDER (NOT merged — D1). #427's auth fix (clear-cookie on 401 refresh) already landed separately on develop (`e60cedd`). **STALE-ROW SWEEP:** no Active row untouched 7+ days (all dated 2026-06-22). Remaining Active: qa-pricing Wave C (e2e, BLOCKED on mfe-pricing data-testid); qa-catalog Wave A (backend, BLOCKED gate-REJECT PR #435 awaiting re-do). The standing OPEN inter-lead items are unchanged.
>
> **Session-end sweep (2026-06-22, mesell-qa-wave-image-ai-backend-session-2 — RE-GATE):** qa-image-ai **Wave A (backend) PR #431 RE-gate-reviewed (after the earlier REJECT was remediated) → APPROVE → squash-merged** into `feature/qa-image-ai/integration` (`93a82eb` → **`0f4bfd9`**); branch preserved (`b3937f8`, NO `--delete-branch`); `integration → develop` is the FOUNDER's gate, NOT touched. The qa-image-ai BLOCKED Active row is REMOVED → moved to Recently merged. **Gate INDEPENDENTLY re-ran the CI-Gate-4 invocation:** reject reason FIXED (`test_route_integration.py` 4/4 as a UNIT, stable ×3); full `pytest -m integration` = 354 passed / 18 skipped / 0 FAILED (one process); stub-guard swap consistent (`TestStubStateGuards` dropped, `TestRunnerAggregationLogic` kept); integration tip `0f4bfd9` green + internally consistent (real scorer + aggregation = 16 passed / 0 failed). **Inter-lead updates:** the IA-RED-2 stub↔scorer reconciliation row is **RESOLVED** (real scorer landed on integration `93a82eb`; stub-guard dropped in the same PR; verified no self-contradiction). **NEW inter-lead item → backend-coordinator:** the `test_iam_logout_idempotency` loop-closed finding (PROVEN pre-existing on develop, same D2 bug class, non-blocking today). **STALE-ROW SWEEP:** no Active row untouched 7+ days (all dated 2026-06-22). Remaining Active: qa-pricing Wave C (e2e, BLOCKED on mfe-pricing data-testid); qa-auth-contract 3 lanes (PLANNED off `1fc73f4`). Other standing inter-lead OPEN items unchanged. **Board-discipline note:** the backend writer did NOT flip an IN REVIEW row on PR open (same recurring omission as prior backend/e2e lanes; `coordinator_patterns.md`) — logged, not a reject; gate set the final MERGED state.
>
> **Session-end sweep (2026-06-22, mesell-qa-pricing-frontend-session-1 — GATE):** qa-pricing **Wave B (frontend) PR #436 gate-reviewed → APPROVE → squash-merged** into `feature/qa-pricing/integration` (`f938f0b` → `4dd32ca`); branch preserved. Row moved to Recently merged. Added a **qa-pricing Wave C (e2e) BLOCKED row** — held on the mfe-pricing `data-testid` addition (parallel component-builder adding them now; cross-lead handoff to frontend-coordinator). The gate INDEPENDENTLY re-ran the affected specs via vitest (75/104/131 passed, 0 reds) — did NOT trust the report alone. Soft board-discipline gap logged (specialist did not flip an IN REVIEW row — same recurring omission as Wave-1/2/3 e2e; `coordinator_patterns.md`). **STALE-ROW SWEEP:** no Active row untouched 7+ days (all dated 2026-06-22). **qa-image-ai (Wave A backend)** remains BLOCKED (gate REJECT, PR #431) — owned, awaiting backend-writer re-do; not stale. **qa-auth-contract** 3 lanes remain PLANNED (off `1fc73f4`). Inter-lead requests: the standing OPEN items are unchanged; the mfe-pricing data-testid request to frontend-coordinator is the active blocker for qa-pricing Wave C.
>
> **qa-wave-2 (auth) — SUPERSEDED → CLOSED (founder-decided 2026-06-22).** The standalone auth-only wave (`docs/plans/features/auth-testing/WAVE_PLAN.md`) was SUPERSEDED by `qa-onboarding` (folded into the broader onboarding wave); its backend cases landed via qa-onboarding Wave A (`aa4345d`) and the 2 HIGH frontend reds via Wave B. **PR #421 (`feature/qa-wave-2/integration → develop`) is now CLOSED** (this session) — superseded by the founder's `98cc02a` merge; comment points to `OVERLAP_ANALYSIS.md` + `SALVAGE_PLAN.md`. **The dead `feature/qa-wave-2/*` branches are NOT deleted (founder ruling) and NOT resurrected** — they predate `98cc02a` and conflict with `test_iam_onboarding_coverage.py`. The qa-wave-2 (e2e) row below is ARCHIVED — its UNIQUE coverage is SALVAGED into the qa-auth-contract (e2e) lane above. The `feature/qa-wave-2/integration` @ `2f9342c` is the READ-ONLY behavior reference for the salvage (cherry-pick BEHAVIORS, not files).

> **Session-end sweep (2026-06-22, mesell-onboarding-testing-session-2 — Wave A gate):** `qa-onboarding` Wave A (backend) GATE PASSED → squash on `feature/qa-onboarding/integration` (`aa4345d`); PR #391 reviewed + closed (its base was `develop` = the founder's gate, not mine). `qa-wave-2 (auth)` marked **SUPERSEDED** per founder this session. No rows >7 days stale. Inter-lead requests: **1 OPEN** (frontend / mfe-export route-productId — carried). Two onboarding gaps carried forward: OB-BE-24 (Google flag-OFF 404, process-singleton isolation) + OB-BE-38 (DPDP consent, not modelled in V1 schema). Waves B + C are PENDING (hard-dep A→B→C; B needs the onboarding-submit product fix via cross-lead memo to frontend-coordinator).

| qa-wave-2 (e2e) | `feature/qa-wave-2/e2e` | **ARCHIVED (SUPERSEDED → salvaged)** | — | 2026-06-22 | — | ~~E2E-AUTH-01…06~~. SUPERSEDED with the rest of qa-wave-2; PR #421 CLOSED. Its UNIQUE coverage (E2E-AUTH-01 `login.spec.ts`, E2E-AUTH-03 `silent-refresh.spec.ts`, E2E-AUTH-06 GIS-stub un-fixme) is SALVAGED into the **qa-auth-contract (e2e)** lane above. Branch NOT cut/NOT deleted. |

> **Session-end sweep (2026-06-22, mesell-qa-wave-2-coord-session-1 — qa-auth-contract salvage stand-up):** Closed out qa-wave-2: **PR #421 CLOSED** (superseded by founder merge `98cc02a`; comment → `OVERLAP_ANALYSIS.md` + `SALVAGE_PLAN.md`); dead `feature/qa-wave-2/*` branches NOT deleted/NOT resurrected (founder ruling); qa-wave-2 (e2e) row ARCHIVED. **Stood up the qa-auth-contract SALVAGE lane:** `feature/qa-auth-contract/integration` CREATED off develop `1fc73f4` (race-safe); `SALVAGE_PLAN.md` authored (3 lanes, ~12 behaviors, per-case REFERENCE→TARGET→"develop-already-covers"→assertion-target). Added 3 PLANNED rows (e2e / frontend / backend). **DISCOVERED at stand-up: PR #427 is ALREADY MERGED to develop** (`1fc73f4`, fix `e60cedd`) — the founder's "hold backend until #427" is therefore SATISFIED; all 3 lanes cut off `1fc73f4` with NO rebase; BE-AUTH-09/11 clear-cookie xfails stay DROPPED (now hard-covered by `test_refresh_401_clears_stale_cookie`). NO test code written / NO specialists dispatched (next step = Director dispatch). **STALE-ROW SWEEP:** no Active row untouched 7+ days (all dated 2026-06-22). **qa-image-ai (Wave A backend)** remains **BLOCKED (gate REJECT, PR #431)** — owned, awaiting backend-writer re-do (event-loop fixture fix); not stale. Inter-lead requests: the standing OPEN items (mfe-export route-productId, onboarding-pincode-500, IA-RED-2 stub↔scorer, the qa-wave-3 e2e frontend/infra gaps, the CI test-gate + lockfile memos) are unchanged — none touched by this salvage stand-up.
>
> **Session-end sweep (2026-06-22, mesell-qa-onboarding-e2e-session-1 — GATE):** Onboarding **Wave C (E2E) PR #411 gate-reviewed -> APPROVE (8/8) -> squash-merged** into `feature/qa-onboarding/integration` — tip `7e86054` -> **`e5a44a3`**; branch `feature/qa-onboarding/e2e` preserved (`1a13128`, NO `--delete-branch`). **ALL THREE qa-onboarding lanes now coexist in integration** (backend `aa4345d`, frontend #407 `7e86054`, e2e #411 `e5a44a3`); `integration -> develop` is the **FOUNDER's** gate (NOT actioned here). **The live Playwright re-run was BLOCKED by a contaminated dev stack** (slot-1 shell :4210 + mfe-auth/onboarding up, other remotes on slot-0 ports, NO backend reachable) -> per the checklist's explicit allowance the gate DISCLOSED this and fell back to rigorous STATIC + SOURCE ground-truth (full diff = 9 files, 8 e2e + board, ZERO app source; every page-object/fixture/spec read; every selector + the persist fix ground-truthed against integration SOURCE `7e86054`). Writer's run = **11 passed / 3 test.fixme / 0 failed, stable x2**. **OB-E2E-03 confirmed not assertion-free** = the #399 data-loss guard (PATCH 200 + re-login lands /dashboard not /onboarding; source L314 `patchProfile().subscribe()`). **NO stale-base hazard** (merge-base == integration tip; `grep '^D'` empty; Wave B specs preserved). **Board-discipline IMPROVEMENT** — the e2e specialist flipped its row to IN REVIEW on PR open (`f787541`), the FIRST e2e lane to do so (Wave-1/2/3 e2e all left it PENDING). Writer-memory SCRIBED into qa-coordinator `coverage_gaps.md` (onboarding/auth selectors + §5-note-3 whole-stack-rebuild re-confirmation). **2 product bugs found LIVE -> frontend-coordinator (logged below, NOT swallowed):** NEW onboarding-pincode NOT-NULL 500 + the Wave-1 mfe-export route-productId carry. NO rows >7 days stale. Inter-lead requests after this gate: the qa-onboarding pincode-500 (NEW) + the standing mfe-export carry remain OPEN to frontend-coordinator.
>
> **Prior session-end sweep (2026-06-22, mesell-qa-wave-3-e2e-session-1 — GATE):** Wave-3 catalog-vertical **E2E lane PR #409 gate-reviewed -> PASS -> squash-merged** into `feature/qa-wave-3/integration` (`c382f84`); branch preserved (`8128b68`). The qa-wave-3 (e2e) row moves to Recently merged (MERGED-to-integration WITH the fixme/scaffold caveat). **WAVE 3 (catalog) IS NOW FULLY ASSEMBLED** — all three lanes (backend #396 `edb875f`, frontend #394 `4571048`, e2e #409 `c382f84`) coexist in integration; `integration -> develop` is the **FOUNDER's** gate (NOT actioned here). The gate re-ran `playwright --list` = **16 tests parse/typecheck clean**; reconciled the stale-base hazard (merged integration into the e2e branch first so the squash did NOT delete the #396/#394 lane files — ZERO deletions confirmed). E2E-writer memory scribed (selector_registry Wave-3 RE-VERIFIED + CONFIRMED-ABSENT blocks; federation_quirks Wave-3 build/dep quirks; flow_status Wave-3 dispositions) via this same develop-based scribe commit (sanctioned write-protection workaround, pure-append). **6 inter-lead requests/blockers LOGGED** below (frontend: catalog-list delete+testids / mfe-pricing apply+testids / Live Preview page reintroduction; infra/backend: Gemini-dev-backend OR a no-spend fixture-product seam; frontend: PR #398 mfe-export productId fix to develop; infra: add `@playwright/test@1.52.0` to the lockfile). **Board-discipline soft gap (logged, not a reject):** the e2e specialist did NOT flip its row to IN REVIEW on PR open (it sat PENDING) — the gate set the final MERGED state; same recurring omission as Wave-1/2 (`coordinator_patterns.md`). NO rows >7 days stale.
>
> > **Session-end sweep (2026-06-22, mesell-qa-wave-3-coord-session-1 — GATE):** Wave-3 catalog-vertical **BACKEND (#396) + FRONTEND (#394) lanes both gate-reviewed → PASS → squash-merged** into `feature/qa-wave-3/integration` (integration first fast-forwarded from its base `6a02669` to develop `3c63b55`, so the develop-merged infra commits #388/#389/#390 — which the backend branch had picked up — are NOT net-new in the eventual integration→develop diff; only the 7 backend test-lane files + the 5 frontend specs are). **Backend squash `edb875f`** (gate independently re-ran the 5 files vs `meesell_test` = **59 passed / 0 failed**; the 2 broader-suite Valkey-6381 failures are in `test_flag_gate.py`, byte-identical at base, NOT in the PR diff → pre-existing infra). **Frontend squash `4571048`** (gate re-ran full `ng test` — all 5 authored files GREEN; the 73 reds are W2-FE carry-forward, none in the catalog vertical, none touched). Both PRs closed-with-verdict; branches preserved (no `--delete-branch`). Backend-test-writer Wave-3 memory scribed via PR #400 (`804d317`, sanctioned write-protection workaround, pure-append). **`integration → develop` is the FOUNDER's gate — NOT actioned here; integration now contains BOTH backend + frontend lanes.** Deferred/gated: W3-BE-11 (cost ceiling), W3-FE-6 + W3-E2-3 (mfe-export productId fix), W3-E2-5 (GCS env), and the entire W3-E2 e2e lane (not yet dispatched). NO rows >7 days stale. Inter-lead requests: **1 OPEN** (frontend / mfe-export route-productId — now also blocks W3-FE-6 + W3-E2-3).

> **Prior session-end sweep (2026-06-22, mesell-qa-wave-2-frontend-session-1 — GATE):** Wave-2 **FRONTEND lane PR #397 gate-reviewed → PASS → squash-merged** into `feature/qa-wave-2/integration` (`4086d00`; integration board reconciled at `e79d39c`); branch preserved. The frontend row moves to Recently merged. **Both Wave-2 backend (#392) + frontend (#397) lanes are now MERGED into integration; only the e2e lane remains.** Gate independently REPRODUCED the full `ng test frontend` = 1587 pass / 34 fail / 20 skip and confirmed the 2 HIGH reds (refresh-interceptor, otp-verify) are GREEN + tests-only (zero source edits). NO rows >7 days stale. Inter-lead requests: **1 OPEN** carried from Wave-1 (frontend / mfe-export route-productId product bug). `integration → develop` is the FOUNDER's gate — NOT actioned here.

> **Prior session-end sweep (2026-06-22, mesell-qa-wave-2-backend-session-1):** Wave-2 BACKEND lane PR #392 gate-reviewed → **PASS → squash-merged** to `feature/qa-wave-2/integration` (`7d4f1a0`); branch preserved. The backend row moves straight to Recently merged. `integration → develop` is the FOUNDER's gate — NOT actioned here.

> **Prior session-end sweep (2026-06-22, mesell-qa-wave-1-e2e-session-1):** the e2e codify lane (PR #385) is now MERGED → moved to Recently merged. **ZERO Active rows remain** — Wave-1 (backend #380, frontend #381/#383, e2e #385) is fully landed on develop. No rows >7 days stale. Inter-lead requests: **1 OPEN** (frontend / mfe-export route-productId product bug, found live in the e2e run); the mfe-pricing-spec request was RESOLVED by #383. **Wave-2 carry-forward = the 61 pre-existing frontend reds + the 1 backend P1 skip (export-ZIP) + the mfe-export product bug — all belong in the final TEST_REPORT.**

## Recently merged (last 14 days)

| Wave | Merged to | Date | PR | Notes |
|---|---|---|---|---|
| **qa-wave-3 (catalog vertical — integration → develop)** | **`develop`** | 2026-06-22 | **#460** (`feature/qa-wave-3/integration` → `develop`), **merge-commit `e7470bf`** | **MERGE-GATE PASS → MERGED to develop (FOUNDER-DIRECTED `--admin`, session `mesell-qa-wave-3-coord-session-2`).** The founder explicitly directed "merge gate PR #460 whenever ready" — so the QA lead ran the gate FIRST, then merged on the founder's instruction (this is normally the FOUNDER's D1 gate; the lead acted only on the explicit founder directive — the PR title's `[FOUNDER GATE — DO NOT MERGE BY LEAD]` standing policy was overridden by that direction, noted here for audit). Develop HEAD `17171d1` → **`e7470bf`** (a true merge-commit reconciling develop's 3 concurrent advances #480/#481/#482 with the 3 QA-wave-3 lane commits — `mergeStateStatus: CLEAN`, zero conflicts); branch `feature/qa-wave-3/integration` (`ab1aa6d`) RETAINED (NO `--delete-branch`). **Scope: TESTS-ONLY — 12 files (+2599/-59), ZERO board/status/memory/ci/source-logic, ZERO deletions** (`git diff origin/develop...integration` = 4 net-new backend pytest + 1 upgraded `test_export_zip_member_structure.py` + 1 eval-results artifact `eval_results.json` [trivial `worst_case_pillow_seconds` 0.028→0.0278, `gemini_calls:0`] + 2 net-new FE specs + 4 net-new e2e flows). **GATE INDEPENDENTLY RE-RAN (master `backend/.venv` py3.11, `meesell_test` auto-reprovisioned to head `e9415bdcae20` by the conftest `_provision_test_schema` fixture; Valkey :6379; FE via standalone vitest 4.1.8; Playwright `--list`):** **BE = 59 passed / 0 failed / 0 skipped** (DB-independent `test_pil_check_boundaries.py`+`test_watermark_asserting.py` = 50; the 3 integration files RUN TOGETHER as ONE process [CI-Gate-4 style, the event-loop-singleton trap guard] = 9 passed / 0 skipped — happy + cross-tenant-404 + unauthenticated-401 + autosave-reflect + export-ZIP member-structure, the DB-infra `pytest.skip` guards did NOT fire so nothing green-washed). **FE = 44 passed / 0 failed / 0 skipped** (`browse.component.spec.ts` 20 + `live-listings.component.spec.ts` 24; pure-function-mirror + imported-model-fn pattern, includes the W3-FE-8 unknown-key non-blank regression guard with the negative assertion). **E2E = all 29 specs in 16 files parse + typecheck CLEAN** via `playwright test --list`; the 4 new flows are a founder-approved SCAFFOLD — `wizard-save` (W3-E2-1) is a live skip-gated test asserting a real UUID + autosave reaching "Saved" TEXT + a listed product (honest skip-gate, no green-wash), `live-preview`/`catalog-edit-delete`/`price-apply-export` (W3-E2-2/4/6) are documented `test.fixme` with inline un-fixme conditions; zero hardcoded ports (all from `playwright.config.ts`); selectors from the live-verified page objects (`selector_registry.md`), no memory-written selector. **CI checks on the head commit independently CONFIRM GREEN:** Gate 1-5 (incl. Gate 4 integration in one process) PASS + all 8 frontend matrix units (incl. mfe-catalog) PASS. **8-box all PASS:** tests-run (re-reproduced + CI-corroborated); `TEST_DATABASE_URL` `endswith("_test")` guard intact (conftest L23-24, NOT in diff); zero real Gemini/MSG91/Razorpay/GCS (dummy CI env + adapter-seam mocks; `gemini_calls:0`); coverage met (BE 59 / FE 44 / E2E scaffold per the PR title); no assertion-free tests; E2E selectors live-verified + honest skip-gating; PR template complete; tests-only scope. **The board-tracker PR #461 ("…#460 OPEN") is now MOOT → CLOSED** (its +4/-4 also stale vs the current develop board). **No carry-forwards changed by this landing** (the 3 lane rows below already documented the per-lane verdicts). |
| qa-image-ai (Wave B — frontend) | `feature/qa-image-ai/integration` | 2026-06-22 | #442 (`feature/qa-image-ai/frontend` → `feature/qa-image-ai/integration`), squash `c87d800` | **GATE VERDICT: APPROVE → squash-MERGED into integration** (integration tip `0f4bfd9` → **`c87d800`**; branch preserved `bc9d08b`, NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 2 spec files, **+696/-0, tests-only — ZERO source touched, ZERO deletions** (verified `git diff integration..frontend --name-only` = exactly `image-uploader.component.spec.ts` + `catalog-form.component.spec.ts`; single commit `bc9d08b`). **NO stale-base hazard** — merge-base(frontend, integration) == integration tip `0f4bfd9` exactly. **+42 net-new tests:** `image-uploader.component.spec.ts` 81→106 (+25, IMG-FE-01..05 host gaps: onFilesSelected 202→pending+poll, featureDisabled flag-OFF, onReupload real-File D-IMG-2 guard, poll set→ready, CMYK FAIL hint + red-border) + `catalog-form.component.spec.ts` 94→111 (+17, IMG-FE-07..10 autofill UI: aiSuggestions staging, highlight-clear on edit, no-auto-apply dual-write contract, error/empty toast). **8-box, all PASS — GATE INDEPENDENTLY RE-RAN (vitest 4.1.8, master node_modules symlinked into worktree):** (1) tests-run — `vitest run` of the 2 PR spec files = **217 passed / 0 failed** (all `it()` across both files; the +42 new included, all green); (2) N/A (frontend lane); (3) **zero real external calls** — no real `ImageService`/HTTP injected; `of()`/`EMPTY`/typed-tracker mocks; **IMG-FE-06 confirmed PRE-COVERED** in `image.service.spec.ts` (22 tests, real `HttpTestingController` 401/404/500 upload+poll error matrix) — **NOT duplicated**; (4) coverage met — IMG-FE-01..05 + 07..10 delivered against IMPORTED model pure-functions (`buildPrecheckItems`/`resetSlot`/`mapImageSummaryToProductImage`/`computeCanContinue` + `clearAiSuggestion`/`isAiSuggested`/`mergeAiSuggestions`/`extractSuggestionEntries`/`dismissSuggestion`, all verified to exist); (5) **no assertion-free tests** — every test ≥1 `expect()`; error assertions check non-empty strings (IMG-FE-10 L1152-1153 toast `.toBeTruthy()` + `.length > 0`); (6) N/A (component specs, no E2E selectors); (7) PR template complete, no `<>` placeholders, run output pasted; (8) board IN-REVIEW (gate set MERGED — soft discipline gap, same recurring omission, logged to `coordinator_patterns.md`). **GROUND-TRUTH HONEST + SOURCE-VERIFIED — no phantom-component test:** `AutofillButtonComponent`/`FieldDiffComponent` do NOT exist as separate components (grep = NONE); autofill is INLINE in `CatalogFormComponent` — button at template L264 (`mee-ai-fill-row`), `onAutofill()` at L720, **dual-write `aiSuggestions.set(values)` + `fieldValues.update(...)` at L732-733** (the IMG-FE-09 no-auto-apply-overlay contract), error toast `'AI fill failed. Please try again.'` at L738 (matches IMG-FE-10 spec exactly). The product-mismatch (spec-named separate components don't exist) was filed as a spec gap, NOT invented as a red. **PATTERN NOTE (logged, not a reject):** a subset of Wave B tests use the documented PrimeNG-21-TestBed-crash workaround (inline pure-function-mirror of 2-3-line component guards, e.g. `>=4` slot guard, `autofilling` toggle, the dual-write) — these faithfully mirror source logic I source-verified and are NOT assertion-free; bulk of the suite asserts against genuinely-imported model functions. **No founder decision required for the gate.** |
| qa-image-ai (Wave A — backend, RE-GATE) | `feature/qa-image-ai/integration` | 2026-06-22 | #431 (`feature/qa-image-ai/backend` → `feature/qa-image-ai/integration`), squash `0f4bfd9` | **GATE RE-VERDICT: APPROVE → squash-MERGED into integration** (integration tip `93a82eb` → **`0f4bfd9`**; branch preserved `b3937f8`, NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). Re-gate after the earlier REJECT was remediated (commit `b3937f8`). 7 tests-only files, **rebased onto current develop `1fc73f4`** (merge-base == develop tip → the prior 67-file stale-base contamination is GONE; true lane = exactly the 7 Wave A files). **8-box, all PASS — GATE INDEPENDENTLY RE-RAN (master `.venv`, `meesell_test` @ head, full CI Gate-4 dummy-env, DB:5432/Valkey:6379):** (1) **reject reason FIXED** — `tests/modules/image/test_route_integration.py` as a UNIT = **4 passed** (`TestPostImageGCSFail` + `TestGetImageListCrossTenant`, the prior failing pair, now PASS), stable ×3; the D2 `_otp_client` loop-bound patch is correctly applied (swaps `_valkey_module._otp_client` to a fresh function-loop client, drains lifespan engine/valkey + restores on teardown). **Full CI Gate-4 `pytest -m integration` (ALL integration tests, ONE process) = 354 passed / 18 skipped / 0 FAILED** — the 4 image-route tests pass in-process; NO test FAILS from `Event loop is closed` (CI-faithful config; `filterwarnings` ignores only DeprecationWarning). (2) `TEST_DATABASE_URL` `_resolved_db.endswith("_test")` guard intact (conftest L23-27, NOT in diff). (3) zero real Gemini/GCS (`adapters.gemini`/`adapters.gcs` seam; grep clean). (4) coverage met — **IA-RED-1 genuinely fixed** (perf test reads canonical `event_type='ai.call'` / `metadata_jsonb->>'cost_inr'` / `occurred_at`, asserts mean ≤₹0.05); **IMG-BE-07 = 502** (`GcsAdapterError`→`AdapterError.status_code=502`). (5) **zero assertion-free tests** (AST-checked all 41 net-new test fns assert). (6) N/A (backend lane). (7) PR template complete, no `<>` placeholders, combined `-m integration` output pasted. (8) board IN-REVIEW soft gap (gate set MERGED). **STUB-GUARD SWAP CONSISTENT ON INTEGRATION** — `TestStubStateGuards` DROPPED (obsoleted by the IA-RED-2 real scorer landed on integration `93a82eb`); `TestRunnerAggregationLogic` KEPT (5 scorer-independent tests). **Integration tip `0f4bfd9` proven internally consistent + green:** `test_ai_ops_eval.py` (real scorer) + `test_ai_ops_eval_scoring_harness.py::TestRunnerAggregationLogic` co-run = **16 passed / 0 failed** (golden-set gate smart_picker-50/autofill-30/watermark-30 PASS through the REAL scorer; NO stub-lock); full `pytest -m integration` on the integration tip = **354 passed / 0 failed**. **NEW finding (NOT a #431 blocker, owed a separate fix):** `tests/modules/iam/test_iam_logout_idempotency.py::test_logout_first_call_revokes_then_second_call_is_noop` throws `Event loop is closed` at setup — **PROVEN PRE-EXISTING** (byte-identical on develop, NOT in #431's diff, reproduces on a CLEAN develop checkout; same D2 bug class — a hand-rolled client not reusing the loop-bound `_otp_client` patch). Surfaces only as a non-failing GC warning under normal pytest config (develop's own Gate-4 = 340 passed / 0 failed) → does NOT red develop's CI today; recorded as a new inter-lead item to backend-coordinator. **No founder decision required for the gate.** |
| qa-pricing (Wave B — frontend) | `feature/qa-pricing/integration` | 2026-06-22 | #436 (`feature/qa-pricing/frontend` → `feature/qa-pricing/integration`), squash `4dd32ca` | **GATE VERDICT: APPROVE → squash-MERGED into integration** (integration tip `f938f0b` → **`4dd32ca`**; branch preserved, NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, NOT touched). 1 file, +100/-0, **tests-only** (`git diff integration..frontend --name-status` = exactly `M frontend/apps/mfe-export/src/app/export.component.spec.ts`; ZERO product code; ZERO deletions). +13 new tests: PQE-FE-12 i18n non-raw-key guard (6) + PQE-FE-13 productId post-fix contract (2) + 5 others. **8-box, all PASS:** (1) tests-run **GATE-REPRODUCED via vitest in a worktree** — `export.component.spec.ts` = **75 passed** (was 62; +13 matches the claim), mfe-export (component+model) = 104 passed / 0 failed, mfe-pricing (component+utils) = 131 passed / 0 failed; all 8 cited PQE-FE-12/13 tests GREEN by name; (3) no real external calls — pure-function tests, NO TestBed/HttpClient/service injected (grep clean); (4) **coverage met** — PQE-FE-12 asserts `resolveCheckMessage(message_key)` → non-empty human string (NOT the raw dotted `export.check.*` key), BOTH failed checks aggregated/collect-all, unknown-key → `EXPORT_CHECK_FALLBACK` (never blank); PQE-FE-13 asserts `resolveExportProductId(paramMap)` reads the route `:id` → real UUID, never the `'current-product-id'` placeholder; (5) no assertion-free tests (16 `expect()` in added lines; no `assert true`/`xit`/`.skip`); (7) PR template complete; (8) board IN-REVIEW soft gap (specialist did not flip a row — gate set MERGED; recurring omission, logged not a reject). **resolveExportProductId fix CONFIRMED LIVE on integration** — `export.component.ts` L425 wires `resolveExportProductId(this.route.snapshot.paramMap)` and the `'current-product-id'` placeholder is GONE (0 occurrences) → PQE-FE-13 is a real post-fix contract, not asserting against an unused helper. **NO stale-base hazard** — merge-base(frontend, integration) == integration tip `f938f0b` exactly; zero deletions. **No founder decision required for the gate.** |
| qa-onboarding (backend — Wave A) | `develop` (via integration → #422 `98cc02a`) | 2026-06-22 | #391 (`feature/qa-onboarding/backend` → integration), squash `aa4345d` | **GATE VERDICT: APPROVE → MERGED to integration; now on `develop` via the FOUNDER's #422 `98cc02a` merge.** (session `mesell-onboarding-testing-session-2`; NOT delete-branch; `integration → develop` is the FOUNDER's step per D1). 3 files, +638/-0: `test_iam_onboarding_coverage.py` (11: OB-BE-01/02/05/06/07/16/17/18/22/23/24), `test_iam_nullable_identity_check.py` (3: OB-BE-25), `test_customer_onboarding_coverage.py` (1: OB-BE-31). **Gate RE-RAN (not the report) vs `meesell_test` @ alembic head `e9415bdcae20`, CI dummy-env:** 3 new files = **14 passed / 1 skipped / 0 failed**; existing iam/customer suites (otp-rate-limit, dev-otp-bypass, full-onboarding-flow, google-integration, customer-routes) = **28 passed / 1 skipped / 0 failed** (no regressions). All 7 gate boxes pass: `conftest.py` NOT in diff (`endswith("_test")` guard untouched); MSG91 monkeypatched + Google `AsyncMock` (zero real calls); every 4xx asserts a non-empty `validation_message_id` (OB-BE-22/23 pin exact keys); the 2 nullable-CHECK `assert True` cases are load-bearing (flush fires the CHECK) — NOT green-washed; OB-BE-27/30/32/33/34 verified to genuinely pre-exist in `test_customer_routes.py` (L382/503/581/601/681/705) — not double-counted. **2 HONEST carry-forward gaps:** OB-BE-24 (Google flag-OFF 404 — process-singleton isolation skip, clear message) + OB-BE-38 (DPDP consent — grep confirmed NOT modelled in V1 customer schema; filed in `deferred_coverage.md`, no test invented). PR #391's base was `develop` (wrong gate target) → the squash landed on integration as `aa4345d` (content identical to the branch); PR closed with the gate verdict recorded. |
| qa-onboarding (Wave C — e2e) | `develop` (via integration → #422 `98cc02a`) | 2026-06-22 | #411 (`feature/qa-onboarding/e2e` -> `feature/qa-onboarding/integration`), squash `e5a44a3` | **GATE VERDICT: APPROVE (8/8) -> squash-MERGED into integration; now on `develop` via the FOUNDER's #422 `98cc02a` merge.** (branch preserved `1a13128`, NO `--delete-branch`; integration tip `7e86054` -> **`e5a44a3`**; `integration -> develop` is the FOUNDER's gate, NOT touched). 9 files (8 `frontend/e2e/**` + the board row), **tests-only — ZERO app source** (verified `git diff integration..e2e --name-only` = only `frontend/e2e/` + the board). New page objects `auth.page.ts` + `onboarding.page.ts`; new fixtures `rate-limit.ts` + `global-setup.ts`; extended `fixtures/auth.ts` + `flows/onboarding.spec.ts` + `flows/logout-guard.spec.ts` (logout-guard only swaps to the `freshLoginTest` fixture for its rate-limit reset). Closes OB-E2E-01..08 (01/03 folded into one persist+resume flow; 02/04 folded into one wrong-OTP->resume flow to stay under the OTP-send budget). **`playwright test --workers=1` = 11 passed / 3 test.fixme / 0 failed, STABLE x2** (writer; the gate did NOT independently re-run — see below). **8-box, all PASS:** (1) tests-run — writer pasted 11/3/0 x2; **the gate's live re-run was BLOCKED by a contaminated dev stack** (slot-1 shell :4210 + mfe-auth :4211 + mfe-onboarding :4216 up; other remotes on slot-0 ports; NO backend reachable :8000=404 /:8010=down) -> per the checklist's EXPLICIT allowance the gate FELL BACK to rigorous static + SOURCE ground-truth and SAID SO; (3) no real external calls — dev OTP bypass `000000`, no real Google/MSG91; GCS+Google are exactly the env `test.fixme` blockers; (4) **coverage met & no false-green** — every GREEN flow asserts a VISIBLE outcome: **OB-E2E-03** asserts `PATCH /seller-profile`->200 AND re-login (after `clearCookies()`) lands `/dashboard` NOT `/onboarding` + dashboard heading (= the #399 data-loss regression guard; source-confirmed `onboarding.component.ts` L314 `patchProfile().subscribe()`, NOT the old setTimeout mock); OB-E2E-02 wrong-OTP -> `role=alert` banner visible + URL stays `/otp-verify`; OB-E2E-04 fresh user -> `/onboarding` + onboarding-submit visible; OB-E2E-05 skip -> `/dashboard`; OB-E2E-06 GIS host + iframe count 1; OB-E2E-08 logout->/login + back stays /login (FED-1 sentinel); (5) no assertion-free tests — the 3 `test.fixme` each carry REAL assertions in-body; (6) **selectors LIVE-VERIFIED + SOURCE-ground-truthed by the gate** — `getByLabel` for the no-testid mee-input onboarding fields (Manufacturer/Packer Name+Pincode = source L176/188/194/206), `getByRole('alert')` error banner, `getByRole('button', /set this up later/i)` skip link (source L239 `<a>`), `getByTestId('onboarding-submit')` (source L224) + pre-existing auth testids; **`onboarding-business-name` is GONE** (0 occurrences in source, old mock form removed by #399); **zero hardcoded ports** (all base URLs from `playwright.config.ts`, env-overridable slot-aware); **no token in localStorage** (Decision #14 — storageState seeds the HttpOnly cookie + fresh-login fallback on rotation-death); rate-limit reset in the HARNESS (`global-setup.ts` + the auto `_rlReset` fixture + `rate-limit.ts` clearing `meesell:rl:*` in Valkey DB0), never in a spec; (7) PR template complete; (8) **board row WAS flipped to IN REVIEW by the specialist** (`f787541`) — a discipline improvement over the Wave-1/2/3 e2e lanes which all left it PENDING. **NO stale-base hazard** — merge-base(e2e, integration) == integration tip `7e86054` exactly; `git diff integration..e2e --name-status | grep '^D'` = empty; Wave B frontend specs (otp-verify/onboarding `.spec.ts`) verified PRESERVED on integration post-merge. **3 test.fixme honest+logged** (google-click cross-origin GIS OAuth; export-download mfe-export product bug; image-precheck local-dev GCS 502). **2 product bugs found LIVE -> frontend-coordinator** (see Inter-lead requests: NEW onboarding-pincode NOT-NULL 500 + the mfe-export carry). **No founder decision required for the gate.** |
| qa-wave-3 (e2e) | `feature/qa-wave-3/integration` | 2026-06-22 | #409 (`feature/qa-wave-3/e2e` -> `feature/qa-wave-3/integration`), squash `c382f84` | **GATE VERDICT: PASS -> squash-MERGED into integration** (branch preserved `8128b68`, NO `--delete-branch`; `integration -> develop` is the FOUNDER's gate, not touched). **SCAFFOLD lane, founder-approved to land now** (most catalog E2E flows are env/feature-BLOCKED, documented). Net-new e2e payload: 4 new spec files + 2 modified + 1 page-object helper-add. **8-box, all PASS:** (1) tests-run **GATE-REPRODUCED** — `npx playwright test --config e2e/playwright.config.ts --list` = **16 tests in 13 files, parse + typecheck CLEAN** (full live run NOT required — env blocks real + documented + founder-approved); (2) N/A (E2E lane, no backend conftest); (3) no real external calls — W3-E2-1 drives the live dev stack via the worker-scoped authed fixture (Decision #14, no localStorage token); honestly SKIPS when Gemini-suggest absent rather than calling a real key; (4) coverage met — W3-E2-1 CODIFIED + 4 blocked surfaces documented with EXACT inline un-fixme conditions (Live Preview retired #278; no catalog-list delete/testids; no mfe-pricing apply/testids; export gated on #398); (5) **no green-wash** — W3-E2-1 asserts real UUID regex + `catalog-save-status` reaching the "Saved" TEXT (NOT mere visibility) + a listed product; the two `test.skip` gates (no-suggestions / no-schema-fields) are HONEST, never falsely green; no assertion-free tests; (6) **selectors all LIVE-VERIFIED in `selector_registry.md`** (smart-picker-description, category-suggestion(-select), catalog-form-next, catalog-save-status, catalog-ai-fill, dashboard-product-row); the ONLY non-testid is the structural `mee-input input, mee-textarea textarea` edit-field fallback (scribed to the registry); NO memory-written selector; **zero hardcoded ports** (all from `playwright.config.ts` env defaults); (7) PR template complete; (8) board IN-REVIEW soft gap (row sat PENDING — gate set MERGED; logged). **EVERY `test.fixme` carries an exact inline un-fixme condition + verified-live source citation; ZERO invented selectors** (the export spec explicitly documents the REAL selector `export-download`, NOT `export-download-button`). **NO scope creep** — `dead_route_guard.mjs` + `ci.yml` in the diff are develop #403 (FED-2), already on develop, present only via the stale base; `@playwright/test` is the runner dep (declared, pnpm-add'd offline). **STALE-BASE RECONCILE:** the e2e branch's merge-base with integration pre-dated the #396/#394 merges -> naive squash would have DELETED 4 BE + 4 FE lane files; the gate merged integration into the e2e branch FIRST (zero conflicts) so the squash is clean-additive (ZERO deletions); #396/#394 lanes verified PRESERVED. Carry-forward: W3-E2-2/4/6 fixme (frontend product gaps), W3-E2-3 fixme (#398), W3-E2-5 fixme (GCS), W3-E2-1 skip-gated (dev Gemini/fixture-product seam). **No founder decision required for the gate; the integration->develop merge is the founder's.** |
| qa-wave-3 (frontend) | `feature/qa-wave-3/integration` | 2026-06-22 | #394 (`feature/qa-wave-3/frontend` → `feature/qa-wave-3/integration`), squash `4571048` | **GATE VERDICT: PASS → squash-MERGED into integration** (branch preserved; `integration → develop` is the FOUNDER's gate, not touched). 5 spec files, +1195/-0, **tests-only** (verified `git diff --name-only` = exactly the 5 catalog-vertical specs; no source edits). 60 new specs: W3-FE-1 (`catalog-list.component.spec.ts` — row-per-product/empty/status+search filters), W3-FE-2 (`catalog-list-api.service.spec.ts` — real TestBed + `HttpTestingController` + `verify()`; GET params + wire→model mapping + 401/404 graceful + 500 re-throw), W3-FE-3 (`browse.component.spec.ts`), W3-FE-4 (`live-listings.component.spec.ts`), W3-FE-5 + W3-FE-8 appended to `catalog-form.component.spec.ts`. **8-box, all PASS:** (1) tests-run **INDEPENDENTLY REPRODUCED** — full `ng test frontend --no-watch` = total **1697**; all 5 authored files GREEN (verified per-file NOT in the fail list); (3) no real external calls — W3-FE-2 uses HttpTestingController, no real service injected into component specs; (4) coverage met — 4 unspec'd components/services + the `product_name`-not-`product_title` autofill guard (W3-FE-5 explicitly asserts product_title does NOT win) + the non-blank i18n fallback for unknown keys (W3-FE-8); (5) no assertion-free / green-wash patterns; (7) template complete; (8) IN-REVIEW on the lane branch. **The 73 reds are ALL pre-existing W2-FE carry-forward** (mfe-auth otp-verify/signup/auth-write, mfe-onboarding onboarding/profile, shell, libs/core refresh.interceptor, libs/composites empty-state, libs/ui-kit input/message/otp-input) — none in the catalog vertical, none touched by this PR (verified against the diff). **PATTERN NOTE (logged, not a reject):** component specs use the established pure-function-mirror approach (re-implementing the component's logic in the spec) due to the documented PrimeNG 21 + Angular 21 TestBed crash (Wave-5 F8); the mirrors faithfully match the real component logic. W3-FE-6 deferred (mfe-export productId fix). **No founder decision required.** |
| qa-wave-3 (backend) | `feature/qa-wave-3/integration` | 2026-06-22 | #396 (`feature/qa-wave-3/backend` → `feature/qa-wave-3/integration`), squash `edb875f` | **GATE VERDICT: PASS → squash-MERGED into integration** (branch preserved; `integration → develop` is the FOUNDER's gate, not touched). 7 files, +1933/-59 — 4 new test files + 1 rewritten + STATUS_BACKEND append + a regenerated `eval_results.json`. **8-box, all PASS:** (1) tests-run **INDEPENDENTLY REPRODUCED** vs `meesell_test` — the 5 files = **59 passed / 0 failed**; broader catalog+image+eval = 122 passed / 2 failed (both `test_flag_gate.py`, Valkey-6381 refused locally, **byte-identical at the integration base `6a02669`** + NOT in this PR's diff → confirmed pre-existing infra, not introduced); (2) `TEST_DATABASE_URL` `_resolved_db.endswith("_test")` guard intact (conftest L23-27, NOT in diff); (3) no real external calls (grep clean; GCS/Gemini/MSG91/Razorpay mocked at the adapter boundary; tests pass with zero vendor creds); (4) coverage met — Live Preview happy/cross-tenant-404/401/autosave (W3-BE-1/2/3/5, the largest TRUE backend gap), DELETE 204→404 + cross-tenant (W3-BE-15), PIL CMYK/resolution/white-BG pass+fail boundaries against the REAL `_check_*` functions (W3-BE-7/8/9), asserting watermark eval ≥85% (W3-BE-10), **export-ZIP P1.11 carry-forward GENUINELY un-skipped** + asserts member names against `_write_xlsx(XlsxRowSpec)` / `_package_images_zip()` (W3-BE-6); (5) no assertion-free tests (the only skips are narrow DB-connection-refused infra gates); (7) template complete; (8) IN-REVIEW on the lane branch. Confirm-only BE-12/13/14/16/17 verified covered, not duplicated. W3-BE-11 (cost ceiling) correctly deferred. **NOTE:** the branch had also picked up develop commits #388/#389/#390 (infra-builder memory/STATUS) because it was cut off a newer develop than the integration base — the gate refreshed integration to develop BEFORE merging, so those are not net-new in the integration→develop diff. **No founder decision required.** |
| qa-wave-2 (frontend) | `feature/qa-wave-2/integration` | 2026-06-22 | #397 (`feature/qa-wave-2/frontend` → `feature/qa-wave-2/integration`), squash `4086d00` | **GATE VERDICT: PASS → squash-MERGED into integration** (branch preserved, NO `--delete-branch`; integration board reconciled at `e79d39c`; `integration → develop` is the FOUNDER's gate, not touched). 8 spec files +474/-13. All 11 FE-AUTH-01…11 delivered. **8-box, all PASS:** (1) tests-run **INDEPENDENTLY REPRODUCED by the gate** — full `ng test frontend --no-watch` = **1587 passed / 34 failed / 20 skipped (1641)**, **5 files failed / 94 passed (99)** — EXACT match to the PR claim; (3) zero real external calls — only a `GisFailStub` class injected (FE-AUTH-11); real `AuthService` in `refresh.interceptor setupReal()` is INTENTIONAL (it IS the single-flight gate under test, mocking it would defeat the sentinel); (4) **coverage met — the 2 HIGH reds independently confirmed GREEN** (`refresh.interceptor.spec.ts` + `otp-verify.component.spec.ts` are ABSENT from the 5 failing files): case (h) `expect(refreshRequests.length).toBe(1)` for 20 concurrent 401s = the "N concurrent 401s → exactly ONE POST /auth/refresh" assertion; case (d) `toHaveLength(1)`; case (i) asserts `token-1`→`token-2` (no stale-token replay); otp-verify L182/193 `expect(comp.errorMessage()).toContain('Invalid or expired')` on a 401 (signal set; unchanged source `@if (errorMessage()){<mee-alert-banner ...>}` renders it); FE-AUTH-06 `auth-error-map.spec.ts` = 18 table-driven asserts; FE-AUTH-10 `forceLogout` once + navigate `/login`, with the negative 500 case; (5) no assertion-free (44 added `expect()`); (7) PR template complete; (8) IN-REVIEW row present on the lane branch. **TESTS-ONLY CONFIRMED:** `git diff integration...frontend` for non-spec `frontend/**/*.ts` = EMPTY — NO `otp-verify.component.ts` or any source edit (the permitted `errorMessage` edit was NOT needed; the signal pre-existed). **The 34 reds are ALL pre-existing/out-of-scope** — the 5 failing files (`onboarding`/`profile`/`shell`/`empty-state`/`message`.spec.ts = W2-FE-3/5/6/8) are NOT in this diff; pre-existing fail count DROPPED 61 → 34 (the 2 HIGH reds + auth-write smoke W2-FE-7 greened). **Board-ownership reconciled:** the specialist committed a `feature_board_qa.md` edit (`6d15431`) to the QA sole-writer surface (permitted under D2 for IN-REVIEW-on-PR-open, but cut from a stale base) — fixed at the gate; the backend #392 row (which the #392 merge never propagated to the integration board) was re-added in the same reconcile. **No founder decision required.** |
| qa-wave-2 (backend) | `feature/qa-wave-2/integration` | 2026-06-22 | #392 (`feature/qa-wave-2/backend` → `feature/qa-wave-2/integration`), squash `7d4f1a0` | **GATE VERDICT: PASS → squash-MERGED into integration** (branch preserved, NO `--delete-branch`; `integration → develop` is the FOUNDER's gate, not touched). 11 test files, +1344/-5, tests-only (verified `git diff integration..backend` for non-test paths = EMPTY — no app code, no conftest, no i18n edits). 20 cases BE-AUTH-01…20 across 8 new + 3 hardened integration files. **8-box:** (1) tests-run PARTIAL — BE-AUTH-16 flag-gate (no-DB) passes, 35 written-but-unrun (no venv/DB-5433/Valkey/tunnel; could not independently execute — Wave-1 precedent ACCEPTED, PR discloses); (2) `TEST_DATABASE_URL` guard untouched (conftest L23-27 `endswith("_test")` intact, not in diff); (3) zero real vendor calls (MSG91/Google mocked at adapter boundary via AsyncMock/monkeypatch — grep clean); (4) coverage met — 7 iam routes each ≥1 happy+≥1 error, plan-guard route-enforcement, DPDP both paths, every new 4xx/5xx non-empty i18n detail (1 noted partial: BE-AUTH-15 patches `enforce_plan_limit` not 50-product seed — still exercises route→PlanLimitExceededError→402 envelope, the real gap); (5) no assertion-free tests; (7) template complete; (8) board-IN-REVIEW soft gap (no develop row pre-existed). **DPDP deviation BE-AUTH-19/20 ACCEPTED, claim VERIFIED independently:** `dpdp_consented_at` genuinely absent from `app/shared/models/user.py`; V1 gap documented in `iam/repository.py` (L17-32, L92-100). Weakened to "upsert completes + user row created" — correct (can't assert a non-existent column); column-value assertion deferred to V1.5. Pre-existing schema gap, NOT a new defect. **No founder decision required** (Google-auth + plan-guard were the §8 confirm-items; the dispatch already ruled them IN). |
| qa-wave-1 (e2e codify) | develop | 2026-06-22 | #385 (`feature/qa-wave-1/e2e-codify` → develop), squash `70fe9f3` | **GATE VERDICT: APPROVE → MERGED** (autonomous Wave-1; founder pre-approved → `--admin`, NO `--delete-branch`; the integration step was collapsed direct-to-develop). 23 files, +803/-279. **All 8 critical seller flows codified** from the two-phase mandate (live exploration slot-1 shell :4210 → `selector_registry.md` corrected to ground truth, ~8 provisional selectors fixed; codification). `playwright test --workers=1` = **9 passed / 3 test.fixme**, STABLE ×3 (9 PNG screenshots `/tmp/e2e-run/screens/`). **Gate spot-check (diff + run evidence; full suite NOT re-run — needs a heavy slot rebuild, writer ran it 3× stably):** every GREEN flow asserts a VISIBLE outcome — logout-guard (URL=/login + `goBack()` stays /login + dashboard-heading count 0 + crosses the federation boundary pre-logout = the FED-1 sentinel, confirms #381 fix), onboarding (onboarding form → dashboard heading), catalog-creation (real product UUID + `/catalogs/{uuid}/edit` + dashboard row), category-picker (suggestion cards count>0 + nav to edit), plan-guard (upgrade prompt count>0 AND remote-failure-fallback count 0). The 3 `test.fixme` each have a LOGGED reason (export-download = mfe-export product bug; image-precheck-result = no-GCS-local 502; google-click = headless cross-origin OAuth) → ADDRESSED, not missing. **No assertion-free tests; selectors all `getByTestId` (live-verified #381) / page objects; zero hardcoded ports (all from `playwright.config.ts`); no token injected to localStorage (Decision #14 honoured via worker-scoped authed-context fixture).** ONE product bug found live (mfe-export route productId — inter-lead request to frontend + memo). PR base was a stale develop → board conflict (this file only, sole-writer) hand-resolved combining #383/#384 rows + the e2e updates. |
| qa-wave-1 (frontend gap-fill) | develop | 2026-06-22 | #383 (`feature/qa-wave-1/frontend` → develop), squash `39b428a` | **GATE VERDICT: APPROVE → MERGED** (autonomous Wave-1; founder pre-approved → `--admin`, NO `--delete-branch`). 5 files (2 specs + 2 writer-memory + board). **The 2 mfe-pricing fixes are surgical & intent-preserving** (verified against `pricing.component.ts` shapes): `:241` adds the `as unknown as Record<string, unknown>` bridge required by TS2352 (insufficient-overlap) — still asserts the retired `commission_missing` kind is absent; `:266` casts `(errorState as string)` to dodge the TS2367 narrowed-literal no-overlap error — still asserts the no_pricing_data banner renders, not server_error. **`auth.guard.spec.ts` is REAL** (3 assertions across 2 states: `toBe(true)` authenticated, `toBeInstanceOf(UrlTree)` + `toString()` contains `login` unauthenticated, `not.toBe(true)`; uses `runInInjectionContext` + computed-signal mock matching the real `authGuard` `isAuthenticated()` call — matches the skill's Auth-guard taxonomy row). **Gate ran:** `tsc -p tsconfig.spec.json` = EXIT 0 (umbrella build UNBLOCKED); `ng test frontend --include auth.guard.spec.ts` = 3/3 GREEN; full `ng test frontend` = 1527 pass / 61 fail / 20 skip — both #383 specs GREEN. **61 reds proven PRE-EXISTING & NOT introduced:** #383 touches none of the 10 failing files; all 10 exist at merge-base `d13ed50`; the 2 compile errors reproduce on clean develop. New develop HEAD = `39b428a` (was `d13ed50`). |
| qa-wave-1 (frontend blocker-fix) | develop | 2026-06-22 | #381 (`feature/qa-wave-1/testids-logout/frontend` → develop), squash `456494e` | **GATE VERDICT: APPROVE → MERGED** (autonomous Wave-1; founder pre-approved + integration step collapsed → `--admin` direct-to-develop, NO `--delete-branch`). 27 files, +314/-22. **Logout regression is GENUINE** (mental revert-check confirmed): the 3 new `auth.service.spec.ts` tests assert `logout()` fires exactly one POST `/api/v1/auth/logout` with `withCredentials=true` AND `router.navigate(['/login'])`, incl. navigate-on-401-revoke — all 3 FAIL without the `authApi.logout().subscribe()` + `router.navigate` lines added to `auth.service.ts`. `authApi.logout()` already existed (`POST .../auth/logout`, `withCredentials:true`) and is NOT modified — surgical reuse. ui-kit passthrough is zero-blast-radius (`[attr.data-testid]` omitted when the `testId` input is undefined; each of the 5 wrapper specs asserts present-when-set + absent-when-unset). testids are attribute-only (no logic touched); `nav-logout` is on the real `<button>`, sidebar uses `NavItem.testId`. **Boundary grep `from 'primeng` in `frontend/apps/**` = 0.** No test-count drop (every diff is additive). Verification: `tsc --noEmit` shell app = EXIT 0; full spec-graph tsc = 2 errors, both pre-existing mfe-pricing (`:241/:266`), ZERO in #381's files. New develop HEAD = `456494e` (was `5da7af9`). |
| qa-wave-1 (backend) | develop | 2026-06-22 | #380 (`feature/qa-wave-1/backend` → develop), squash `5da7af9` | **GATE VERDICT: APPROVE → MERGED** (autonomous Wave-1; founder pre-approved + collapsed the integration step → `--admin`, NO `--delete-branch`). 9 test files, +803/-3 (13 new tests across P0.2–0.7 + P1.8/9/10/11). Spot-rerun of the 9 new/extended files vs `meesell_test`: **66 passed, 1 skipped, 0 failed**. All 5 gate boxes PASS: tests run; `TEST_DATABASE_URL` guard untouched (conftest not in diff); zero real external calls (every vendor mocked at the seam); coverage targets met for all P0 + P1.8/9/10; every new 4xx asserts a non-empty `validation_message_id`; no assertion-free tests. **ONE P1 gap carried to Wave 2:** P1.11 export-ZIP test (`test_export_zip_member_structure.py`) **self-skipped** (wrong `_build_xlsx_bytes(...)` signature assumption → no assertion executed; honest skip, not green-washed). |
| qa-wave-infra (pillar bootstrap) | develop | 2026-06-22 | #379 (`feature/qa-wave-infra/integration` → develop, founder-gated), `240fbcd` | **QA pillar bootstrap — 4 agents + 3 skills + 4 memory dirs + Playwright E2E scaffold + registry updates.** Built by `meesell-infra-builder` per the APPROVED design. `.claude/**` artifacts landed via the git-plumbing route (write-protected). E2E flow specs are intentional STUBS (`test.fixme`) — the first QA wave fleshes them out after a live agent-browser exploration phase. Fleet count: CLAUDE.md + MEESELL_AGENT_REGISTRY.md updated 19 → 23. |

## Inter-lead requests open

| To lead | About wave | Request | Opened | Status |
|---|---|---|---|---|
| frontend-coordinator (Director deciding fix owner separately) | qa-onboarding (Wave C — e2e — NEW PRODUCT BUG found live) | **Onboarding pincode NOT-NULL 500.** The onboarding form (`mfe-onboarding/.../onboarding.component.ts`) treats manufacturer/packer pincode as OPTIONAL and sends `null` when empty, but the `seller_profile` DB columns are NOT NULL -> submitting onboarding without a pincode returns **500 `NotNullViolationError`** (a server error, not a clean validation message). Found LIVE during the Wave C exploration. Fix candidates: **FE** make pincode required (block submit on empty), and/or **BE** make the columns nullable or return a **422 not a 500**. The Director is deciding the fix owner separately. The E2E tests fill VALID pincodes so they pass — this is a confirmed live product finding logged for triage, not a test blocker. Owner TBD (frontend and/or backend). 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| frontend-coordinator | qa-wave-1 (e2e — PRODUCT BUG found live); **also gates qa-wave-3 W3-FE-6 + W3-E2-3 + qa-onboarding export-download fixme** | **`mfe-export` ignores the route productId.** `frontend/apps/mfe-export/src/app/export.component.ts` `onGenerate()` (~L431) hardcodes `const productId = 'current-product-id';` instead of reading `this.route.snapshot.params['id']` (the `ngOnInit` comment admits the route-read was never wired). Verified LIVE: clicking the export Generate button on `/catalogs/{realPid}/export` POSTs `/api/v1/products/current-product-id/export-xlsx` → 422 — so the `ready` state + `export-download` link are UNREACHABLE through the UI for any real product. The E2E export-download assertion is `test.fixme` until this is fixed (then un-fixme `flows/export.spec.ts`). Fix = inject `ActivatedRoute`, read `snapshot.params['id']` (or `paramMap`), use it in `onGenerate()`. Memo: `.claude/agent-memory/meesell-qa-coordinator/handoff_mfe_export_productid_bug.md`. **UPDATE 2026-06-22:** a fix branch `feature/mfe-export-productid/frontend` exists (worktree present) but is NOT yet on develop. Once it lands, QA Wave 3 W3-FE-6 (the component route-id guard spec) + W3-E2-3 (the export-download un-fixme) can be authored/un-fixme'd. 48h SLA running since 2026-06-22. | 2026-06-22 | OPEN |
| ai-coordinator | qa-image-ai (IA-RED-2 stub↔real-scorer swap) | **Reconcile the eval-runner stub guard when the IA-RED-2 `/ai` PR lands.** The qa-image-ai backend lane (PR #431, `feature/qa-image-ai/backend`) added `backend/tests/test_ai_ops_eval_scoring_harness.py::TestStubStateGuards`, which LOCKS the current `ai_ops/eval.py::_run_one_fixture` STUB (asserts 30 fixtures → 0 passed, `passed is False`). The parallel IA-RED-2 build (`feature/qa-image-ai/ai`, `meesell-prompt-engineer`, gated by YOU) REPLACES that stub with a real scorer. When you gate the `/ai` PR into `feature/qa-image-ai/integration`, **`TestStubStateGuards` MUST be retired/replaced in the SAME integration assembly** (the prompt-engineer's `test_ai_ops_eval.py` real-scoring test supersedes it) — otherwise the two lanes land a SELF-CONTRADICTING integration branch (the stub-guard reds the moment the stub is gone). `TestRunnerAggregationLogic` (mocked-scorer aggregation, scorer-independent) SURVIVES unchanged. NOTE: PR #431 is currently REJECTED (one-file fixture fix owed); the corrected re-do still carries `TestStubStateGuards`, so this reconciliation stands regardless. Memo: `.claude/agent-memory/meesell-qa-coordinator/handoff_eval_stubguard_iared2_image_ai.md`. | 2026-06-22 | OPEN |
| ai-coordinator | qa-image-ai (IA-RED-2 stub↔real-scorer swap) | **Reconcile the eval-runner stub guard when the IA-RED-2 `/ai` PR lands.** ~~The qa-image-ai backend lane (PR #431) added `TestStubStateGuards`, which LOCKS the `_run_one_fixture` STUB; the IA-RED-2 build replaces that stub with a real scorer → retire `TestStubStateGuards` in the same integration assembly.~~ Memo: `.claude/agent-memory/meesell-qa-coordinator/handoff_eval_stubguard_iared2_image_ai.md`. | 2026-06-22 | **RESOLVED (2026-06-22).** The IA-RED-2 real scorer ALREADY landed on `feature/qa-image-ai/integration` (`93a82eb`: `app/ai_ops/eval.py` + `test_ai_ops_eval.py` real-scoring test). The qa-image-ai backend RE-GATE (PR #431 `0f4bfd9`) **DROPPED `TestStubStateGuards`** in the same commit (`b3937f8`) and KEPT `TestRunnerAggregationLogic`. Gate VERIFIED on the post-merge integration tip `0f4bfd9`: real scorer + `TestRunnerAggregationLogic` co-run = 16 passed / 0 failed; NO stub-lock; integration internally consistent. No self-contradiction landed. |
| backend-coordinator | qa-image-ai RE-GATE (PR #431) — PRE-EXISTING test bug found at the gate | **`test_iam_logout_idempotency.py::test_logout_first_call_revokes_then_second_call_is_noop` throws `Event loop is closed` at setup in a combined `pytest -m integration` run.** Same D2 bug class as the #431 reject: the test hand-rolls a client that does NOT reuse the loop-bound `_otp_client` patch from `tests/integration/conftest.py`, so the `get_valkey_otp()` singleton retains a redis conn bound to a CLOSED function loop across the in-process run → the setup of this test errors. **PROVEN PRE-EXISTING & NOT from any QA lane** — byte-identical on develop, NOT in #431's diff, reproduces on a CLEAN develop checkout. Today it surfaces only as a NON-failing GC warning under normal pytest config (develop Gate-4 = 340 passed / 0 failed, integration tip = 354 passed / 0 failed) → it does NOT red CI today, but it is a latent landmine: a tightening of `filterwarnings` (promoting the unraisable warning to error) or a reordering would flip it to a hard ERROR. **FIX:** make the test reuse the loop-bound `_otp_client` client fixture / the `integration/conftest.py::iam_client` D2 pattern. Owner = backend test surface (`meesell-backend-test-writer` via backend-coordinator). 48h SLA from 2026-06-22 (low-urgency — non-blocking today). | 2026-06-22 | OPEN |
| frontend-coordinator | qa-wave-3 (e2e — W3-E2-4) | **catalog-list needs a DELETE control + per-row data-testids.** `catalog-list.component.ts` renders per-product cards with Edit + Preview mee-buttons ONLY (grep: zero delete|remove|trash) and NO per-row testid/[testId]. The backend DELETE /products/{id} exists (covered W3-BE-15) but is UNREACHABLE through the UI. Add a delete control + per-row testids (e.g. `catalog-list-row` / `catalog-list-delete`). UNBLOCKS W3-E2-4 (`flows/catalog-edit-delete.spec.ts` un-fixme). 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| frontend-coordinator | qa-wave-3 (e2e — W3-E2-6) | **mfe-pricing needs an APPLY-PRICE control + data-testids.** `pricing.component.ts` has a Calculate mee-button (no testid) + a read-only P&L breakdown; NO apply-price control and NO testids (grep: zero). Add testids on the calculate control + the P&L result + an apply-price control. UNBLOCKS W3-E2-6 (`flows/price-apply-export.spec.ts` un-fixme; also gated on W3-E2-3). Backend round-trip already covered (W3-BE-17). 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| frontend-coordinator | qa-wave-3 (e2e — W3-E2-2) | **Live Product Preview PAGE must be REINTRODUCED (founder ruling).** The frontend `/catalogs/:id/preview` route + component were RETIRED in PR #278; the catalog-list Preview button was repointed to `/edit` in PR #395 -> there is NO preview surface to E2E. The backend `GET /products/{id}/preview` still exists (covered by the BE lane). Re-introduce a frontend preview page (route + component) with LIVE-VERIFIED selectors for the product name + a field value. UNBLOCKS W3-E2-2 (`flows/live-preview.spec.ts` un-fixme). 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| infra / backend | qa-wave-3 (e2e — W3-E2-1) + 4 Wave-1 flows | **A Gemini-enabled DEV backend OR (preferred, NO-SPEND) a FIXTURE-PRODUCT seam** to create a valid product without a live `POST /categories/suggest` Gemini call. The dev backend's suggest returns ZERO suggestions when Gemini is unconfigured for the dev namespace ("No automatic suggestions found", verified live) -> the create-via-picker path cannot mint a product, so W3-E2-1 skip-gates + 4 Wave-1 flows that share the create path are blocked. **DEV-ONLY / NO-SPEND CONSTRAINT applies** — prefer a deterministic fixture-product seam (e.g. a dev-gated endpoint or a seeded product) over enabling a real Gemini key. UNBLOCKS W3-E2-1 + the Wave-1 create-dependent flows. 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| frontend-coordinator | qa-wave-3 (e2e — W3-E2-3) | **PR #398 (mfe-export route-productId fix) must LAND on develop.** `export.component.ts onGenerate()` still hardcodes `productId='current-product-id'` on develop (verified @ a94e013); the fix branch `feature/mfe-export-productid/frontend` exists but is NOT merged. Real download selector is `export-download` (NOT `export-download-button`). UNBLOCKS W3-E2-3 (`flows/export.spec.ts` un-fixme) + W3-FE-6. (Same product bug as the Wave-1 row above — restated as a Wave-3 blocker.) 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| infra | qa-wave-3 (e2e — toolchain) | **Add `@playwright/test@1.52.0` to the lockfile.** It is DECLARED in `frontend/package.json` (devDeps) but was NOT installed in node_modules; the e2e writer pnpm-add'd it OFFLINE (matches the cached chromium-1169, no browser download). CI must have it in the lockfile so the e2e runner resolves without a re-resolve. 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| infra | qa-auth-contract (e2e — INFRA-1 manifest port mapping) | **The dev `federation.manifest.json` port MAPPING does not match the running stack; `REMOTE_PORTS` defaults in `playwright.config.ts` encode DECLARATION-order (mfe-auth=4206) which matches the BROKEN running stack** — so by default the `applyManifestPortFix()` workaround is a no-op-equivalent. The e2e gate proved the specs pass by overriding to the ALPHABETICAL ports the remotes actually run on (mfe-auth=4201 etc.). FIX: align the default port map to alphabetical OR document the override. Found at the qa-auth-contract e2e gate (PR #441). 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| infra | qa-auth-contract (e2e — INFRA-2 stale deployed bundle) | **The deployed mfe-onboarding bundle on the running dev stack serves the OLD `onboarding-business-name` mock form; develop SOURCE has the #399/#416 7-field Legal-Metrology form** (`Manufacturer Name`/`Manufacturer Pincode`/`Packer Name` labels). This is the "spec correct, stack stale" precedent (same as qa-onboarding Wave C + Wave-3 e2e): E2E-AUTH-01's develop-correct `fillOnboarding` helper fails on the stale bundle. FIX: rebuild the dev remote bundles against develop tip. 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| auth-builder / ai-coordinator | qa-auth-contract (e2e — GIS-seam fixme) | **A `DEV_GOOGLE_BYPASS` (or equivalent dev-gated google seam) is needed to un-fixme the E2E-AUTH-06 google SUCCESS path.** The GIS-stub correctly fires a real `POST /auth/google/verify` (resp != 404, GREEN), but the success path is `test.fixme` because local dev has no backend google verification seam (and the real GIS button is a cross-origin `accounts.google.com` OAuth iframe, not drivable headless). FIX: a deterministic dev-gated google verify bypass to drive the post-verify redirect. Found at the qa-auth-contract e2e gate (PR #441). 48h SLA from 2026-06-22. | 2026-06-22 | OPEN |
| frontend-coordinator | qa-wave-1 (frontend gap-fill) | **Fix the pre-existing mfe-pricing spec type errors that block the whole `ng test frontend` umbrella build.** | 2026-06-22 | RESOLVED & MERGED — fixed in PR #383 (`39b428a`); gate verified `tsc -p tsconfig.spec.json` = EXIT 0. |

---

## Wave-2 carry-forward findings — the 61 PRE-EXISTING frontend reds (for the final TEST_REPORT)

Confirmed by the PR #383 gate against develop `39b428a` (full `ng test frontend --no-watch`: 1527 pass / **61 fail** / 20 skip; 88/10 files). All 10 failing spec files exist at merge-base `d13ed50` and are NOT touched by #383 — these are pre-existing defects/flakes owed to Wave 2, NOT regressions from any QA PR. Two are HIGH-signal (they map onto known production bugs in master-session memory).

| # | Spec file | Fails | Root cause (from gate's failure-detail read) | Owner lane | Severity |
|---|---|---|---|---|---|
| W2-FE-1 | `libs/core/interceptors/refresh.interceptor.spec.ts` | 3 of 12 (cases i, j, k) | **The auth-refresh-storm sentinel.** `setupReal()` uses the REAL `AuthService` + multiple `HttpTestingController`s and races: failures are `Error: Cannot configure the test module when the test module has already been instantiated` (TestBed re-config after instantiation) and an unmet `controller.expectOne('/api/v1/auth/refresh')` in the "fresh window after stampede — no stale-token replay" case + an `afterEach` `verify()` leak. This is the test-side face of the **auth-refresh-storm / cascade-logout** bug (master-session memory: `finding-auth-refresh-storm-logout.md`). | frontend (angular-service) + qa | **HIGH** |
| W2-FE-2 | `apps/mfe-auth/src/app/otp-verify.component.spec.ts` | 15 of 15 (whole file) | Suite-level red: `afterEach` `httpMock.verify()` trips on a leaked `POST /api/v1/auth/logout` (Expected no open requests, found 1) + component public-API mismatch (`mee-auth-layout` render, the `errorMessage` property the spec expects is absent from the component). Every `it` inherits the setup/teardown failure. The **OtpVerifyComponent.errorMessage gap** is the surface-error the founder will care about. | frontend (component) + qa | **HIGH** |
| W2-FE-3 | `apps/mfe-onboarding/src/app/onboarding.component.spec.ts` + `profile.component.spec.ts` | multiple | Component mismatch with `mee-auth-layout` (pre-existing). | frontend | MED |
| W2-FE-4 | `apps/mfe-dashboard/src/app/landing.component.spec.ts` | multiple | Pre-existing component/template mismatch. | frontend | MED |
| W2-FE-5 | `apps/shell/src/app/layouts/shell/shell.component.spec.ts` | ≥1 | Sidebar nav assertion (`'Onboarding'` nav-item expected true) + an unhandled `NG04002: Cannot match any routes. URL Segment: 'onboarding'` rejection during the run. | frontend (shell) | MED |
| W2-FE-6 | `libs/composites/empty-state/empty-state.component.spec.ts` | 1 | Pre-existing UI mismatch — icon not rendering in the test DOM. | frontend (ui-kit) | LOW |
| W2-FE-7 | `apps/mfe-auth/src/app/auth-write.smoke.spec.ts` | ≥1 | Carries the `NG04002` unhandled rejection (no `onboarding` route in the test router config). | frontend (auth) | LOW |
| W2-FE-8 | `libs/ui-kit/message/message.component.spec.ts` + `libs/ui-kit/otp-input/otp-input.component.spec.ts` | multiple | Pre-existing ui-kit component spec mismatches. | frontend (ui-kit) | LOW |

> **Wave-2 entry criteria from this gate:** (a) fix the `refresh.interceptor` `setupReal()` TestBed-reinstantiation race FIRST (it is the auth-refresh-storm sentinel — green it and we have a regression guard for the production bug); (b) reconcile `OtpVerifyComponent`'s public API with its spec (the `errorMessage` gap) + stop the leaked logout POST; (c) the `mee-auth-layout` mismatch is shared across onboarding/profile/otp-verify — likely one root fix; (d) add the missing `onboarding` route (or router stub) so `shell`/`auth-write.smoke` stop throwing `NG04002`. NB the `@angular/build:unit-test` builder warns the native-federation `buildTarget` is unsupported — benign for the umbrella run but note it for the CI-gate wiring memo to infra.

### Wave-1 e2e codify findings (PR #385 — for the TEST_REPORT)

The e2e codify lane (PR #385, `meesell-e2e-test-writer`) ran the two-phase mandate (live exploration on slot-1 shell :4210 → codification) and landed all 8 critical seller flows: `playwright test --workers=1` = **9 passed, 3 test.fixme**, stable across 3 runs (screenshots in `/tmp/e2e-run/screens/`). GREEN flows each assert a VISIBLE outcome (DOM/URL): onboarding, catalog-creation, category-picker, plan-guard, **logout-guard** (the FED-1 regression sentinel — confirms the #381 logout fix E2E), google-signin(render), image-precheck(uploader renders), export(page renders).

| Finding | Type | Detail | Disposition |
|---|---|---|---|
| **mfe-export ignores route productId** | PRODUCT BUG | `mfe-export/.../export.component.ts` `onGenerate()` (~L431) hardcodes `productId='current-product-id'` → POST `/api/v1/products/current-product-id/export-xlsx` → 422; the `ready` state + download link are UNREACHABLE for any real product. Verified live. | Filed → frontend-coordinator (inter-lead request above + memo). E2E export-download is `test.fixme` until fixed. |
| export-download `test.fixme` | addressed gap | Blocked by the mfe-export product bug above. The fixme body carries the real assertions (download event + `.xlsx/.zip` filename) — un-fixme once the component reads `ActivatedRoute.snapshot.params['id']`. | Wave-2 / on bug-fix |
| image-precheck-result `test.fixme` | env limitation | LOCAL dev has no GCS creds → `POST /products/{id}/images` returns 502 `gcs.unavailable` BEFORE rembg runs. The PrimeNG uploader interaction itself is verified live (the render test is GREEN). | Un-fixme on a GCS-credentialed env / MinIO (CI or staging) |
| google-signin-click `test.fixme` | env limitation | GIS renders its button inside a cross-origin `accounts.google.com` iframe driving REAL OAuth; the dev origin is also not an allow-listed GIS origin. Cannot drive headlessly. The render test (GIS host + injected iframe) is GREEN. | Needs a Google test identity + allow-listed origin, or a stubbed GIS credential callback |

> **Auth architecture note (federation_quirks.md):** the single-use rotating refresh token (Decision #14 / FE-D5) breaks the standard shared-`storageState` pattern (the 2nd flow reusing the rotated cookie 401s). The codified suite uses a **worker-scoped authed-context fixture** (`fixtures/auth.ts`) that logs in ONCE per worker into a shared browser context (seeded from the setup project's storageState → zero extra OTP sends) — this is a durable Wave-2+ asset. OTP send is rate-limited 3/3600s per IP; the run clears `meesell:rl:*` in Valkey DB0 beforehand (a dev-env reset, NOT in the specs).

---

## Status vocabulary

| Status | Meaning |
|---|---|
| `PENDING` | Wave is on the QA backlog; no branch exists yet. |
| `IN PROGRESS` | A `feature/qa-wave-N/<group>` branch exists; a test specialist is actively committing. |
| `IN REVIEW` | A PR is open against `feature/qa-wave-N/integration`; awaiting the QA coordinator's merge-gate review. |
| `MERGED` | The group's test PR has merged to `feature/qa-wave-N/integration` (or, in collapsed autonomous Wave-1, direct to develop). |
| `BLOCKED` | Work stopped pending a missing selector / fixture, an inter-lead request, or a founder decision. |

A wave row stays on Active waves until its group PR merges to `feature/qa-wave-N/integration`; then it moves to "Recently merged" for 14 days before being removed.

---

## Acceptance gate

Group-PR approval (the QA coordinator's merge-gate review) requires every box:

- [ ] Tests actually run — `pytest` / `ng test` / `playwright test` output pasted in the PR description
- [ ] `TEST_DATABASE_URL` safety guard (`_resolved_db.endswith("_test")`) not bypassed (backend)
- [ ] No real external calls — Gemini / MSG91 / Razorpay / GCS all mocked at the adapter boundary
- [ ] Coverage target from the test spec is met (the measurable exit criterion)
- [ ] No assertion-free test (none that passes while asserting nothing)
- [ ] E2E: selectors came from a live exploration (present in `selector_registry.md`), not from memory; no hardcoded ports
- [ ] PR template filled completely (no `<>` placeholders)
- [ ] The board row for this wave/group is `IN REVIEW`

The QA wave runs as a dedicated sprint AFTER feature waves merge to develop — it never blocks an individual feature PR. The `feature/qa-wave-N/integration` → `develop` merge is the **founder's** gate (D1); the QA coordinator owns only the `feature/qa-wave-N/<group>` → `feature/qa-wave-N/integration` gate.
