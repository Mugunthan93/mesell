# Feature Board — Infra Lead

**Lead agent:** `meesell-infra-builder`
**Domain:** infra
**Last updated:** 2026-06-13 (**MS-C A2 — svc-image INFRA lane AUTHORED + offline-VALIDATED (₹0, dev-only).** Sub-Plan C (image extraction), recipe-copy of the PROVEN MS-A svc-export pattern. 7 files on `feature/microservices-image/infra` (cut from `…/integration` `3dc0f91`): Dockerfile (Pillow+gcs+ai_ops; **rembg DEFERRED**), deployment (api 50m/128Mi + **heavy worker 500m/1Gi lim 1000m/2Gi**), service (ClusterIP svc-image:8001), ingressroute (2 public image routes; `/internal/*` NOT exposed), schema-role.sql (**§0.10 OPTION B — NO products grant; only `GRANT INSERT public.audit_events`**), secrets.yaml.example (AI-consuming: +GEMINI +LANGFUSE; NO MSG91/RAZORPAY), gcs-sa.yaml.example (keyless; VM SA already has objectAdmin on `gs://meesell-images` — verified). **D3 VM-FIT VERDICT: node OVERFLOWS — FRESH FOUNDER ASK NEEDED.** Projected MS-2 load = live 1675m + svc-export 250m + svc-dashboard 50m + svc-image 550m ≈ **2525m > 2000m allocatable (126%, overflow ~525m)** → e2-standard-4 (~₹2,600/mo) decision required BEFORE the deploy/apply; NOT silently provisioned; NOT a merge blocker (manifests are dev-only authored surfaces). 4 appliable manifests yaml.safe_load_all PASS; cluster UNREACHABLE (6443 firewall to founder IP) → server dry-run + dev smoke DEFERRED to deploy time (§15 F3 branch). PR opened to integration (infra-lead-reviewed; backend lead runs merge gate). See microservices-image Active row + STATUS_INFRA UPDATE + inter-lead row. PRIOR: **MS-0 / D5 step 1 MERGED (#181, develop 29ed457) + APPLIED LIVE to dev — founder-authorized.** Runbook steps 1–2 executed: Postgres `max_connections` 100→**200 LIVE** (TF `module.postgres_dev`, plan `0 add, 1 change, 0 destroy`, postgres-0 rolled clean, `SHOW max_connections`=200, pg_isready ok); PgBouncer transaction-pool **LIVE** (`pgbouncer.dev.svc.cluster.local:6432`, pod 1/1 Running, SELECT-1 smoke through :6432 → `1`, `SHOW POOLS` `meesell`=`transaction`); `pgbouncer-userlist` Secret populated (scram plaintext, never committed/printed, temp shredded). **Two apply-time manifest fixes (folded into k8s/pgbouncer.yaml, ride the apply-record PR):** image `1.23.1`→`v1.23.1-p3` (bare tag 404 on Docker Hub → ErrImagePull) and `auth_type` `md5`→`scram-sha-256`+plaintext userlist (Postgres `password_encryption=scram-sha-256` → md5 gave "wrong password type"). **Zero routing impact:** api 2/2 + worker 2/2 untouched (AGE preserved, `/health` ok — nothing flipped to :6432). **Step 3 (DATABASE_URL flip) NOT applied** — separate founder gate, blocked on backend R-MS-8. postgres mem limit 1.5Gi live; node CPU req 83%, mem req 44% — headroom OK. ₹0/mo. See MS-0 Recently-merged row + STATUS_INFRA UPDATE. PRIOR: MS-0 AUTHORED + VALIDATED (NOT applied — founder-gated). PgBouncer (transaction-pool) + Postgres `max_connections=200` for the microservices migration connection-pool budget (infra plan §3.3, MASTER_PLAN D5). DEV ONLY, current hardware, ₹0. TF `module.postgres_dev` targeted plan = `0 add, 1 change, 0 destroy` (args `-c max_connections=200` + memory limit 1Gi→1.5Gi, in-place, no destroy). New `k8s/pgbouncer.yaml` (ConfigMap `pool_mode=transaction` / Deployment **1 replica** dev-justified / Service :6432) — client **+ SERVER dry-run clean** (cluster reachable this session, §15 [MANDATORY GATE] MET not deferred). Userlist Secret as `.example` + populate procedure (no creds committed, §10). Cutover/rollback runbook `docs/runbooks/pgbouncer-cutover.md`. Backend handoff R-MS-8 (asyncpg/SQLAlchemy transaction-pool compat) via memo + inter-lead. **D5 PREREQUISITE — MANDATORY before any service cutover flip.** Branch `feature/microservices-pgbouncer/integration` → develop, founder-gate PR LEFT OPEN. See MS-0 Active-features row + STATUS_INFRA UPDATE.)
**Prior:** 2026-06-12 (**FINAL DEV REDEPLOY @ develop tip `80cda29` (#177, the frozen-surface amendment bundle — last code of V1 dev) — VERIFIED, hands-free CI lane.** The develop push of `80cda29` auto-fired CI run **27415579375**: all 5 gates GREEN → Build (SHA-tagged `api:80cda29...`) → **Deploy to K3s (dev ns) SUCCESS** via IAP-SSH (set image api+worker=`api:80cda29...`, alembic head, both rolled out 2/2, in-pipeline smoke HTTP 200, "Deploy complete: 80cda2922617..."). NO manual redeploy needed — VERIFIED per brief. **Independent laptop verify GREEN:** api `/health` 200 {postgres:ok,valkey:ok} ×3; `/openapi.json` MeeSell API 0.1.0, 25 paths (auth/webhooks/seller-profile/images/export/pricing — the develop-tip surface); TLS api+dev valid to Sep-3-2026 (cert-manager auto-renew healthy). **App code unchanged vs `067d664` (#155, last pod-introspected 5/5):** `git diff 067d664..80cda29 backend/app` = 0 files, `k8s/config.yaml` delta = 0 → the 8 feature flags + worker `{celery,image-tasks}` queue bindings (verifications c+d) carry forward unchanged; only `backend/tests/**` + `frontend/**` changed (not in api runtime / not deployed in dev). **FRONTEND/SHELL = NOT in dev V1 deploy (EXPECTED):** `dev.mesell.xyz` → 404 (Traefik default backend) because no `frontend/Dockerfile` exists (only `frontend/docker/Dockerfile.shell`) → CI shell-image build + `k8s/frontend.yaml` apply both gated-skipped; shell + federated remotes hosting is the PARKED production-move package (D13, founder cost gate) — do not touch. **DEV-COMPLETE: yes** for the deployable V1 dev surface (api+worker @ `80cda29`). DEV ONLY — no staging/prod/D13/CSP-activation. ₹0/mo. NOTE: a docs-only push `afea672` (#178, MS-plan re-key, 0 code/manifest files) became the develop tip mid-verify — its eventual deploy produces a byte-identical-app image; the running V1 dev app code is `80cda29`. PRIOR: **GEMINI_API_KEY_CI SET + founder-verified — last pending CI-activation item now DONE.** GitHub Actions secret set 2026-06-12T01:55Z, sourced from SM `gemini-api-key` (proven-valid, HTTP 200; founder verified in AI Studio); SAME key as prod/local — capped-key swap is optional future hardening. NEW FLAG: SM container `meesell-gemini-api-key` is DEAD/invalid (HTTP 400) — backend/AI lane to confirm no manifest/secret sources THAT name vs the valid `gemini-api-key`. ci-activation row + header mention flipped founder-pending → DONE. ₹0/mo.)
**Prior:** 2026-06-12 (dead-gemini-key-cleanup — backend audit ALL-CLEAR on live path; F1 deleted `scripts/secrets-from-gcp.sh` + fixed 4 referencing files; F2 annotated `INFRASTRUCTURE_TERRAFORM_AUDIT.md`. **FOLLOW-UP: dead `meesell-*`-prefixed SM duplicates still exist in GCP Secret Manager — verify + delete next session (SM mutation NOT done in that chore).**)
**Prior:** 2026-06-12 (**DEV DEPLOY-FROM-DEVELOP @ tip `067d664` (#153) — direct hands-free deploy.** Founder "Deploy now": built api+worker from develop tip via Cloud Build (build `2f480e16`, SHA tag `067d664...`, +`latest`; 6m11s SUCCESS) → pushed to AR → proactive `refresh-ar-token.sh` on VM (no k3s restart needed, 0× ErrImagePull) → `set image` api+worker → rolled out 2/2 to dev. **5/5 VERIFICATIONS GREEN:** (a) api 2/2 + worker 2/2 on `api:067d664...`; (b) `api.mesell.xyz/health` 200 postgres+valkey ok; (c) both worker pods bind `{celery,image-tasks}`; (d) all 8 FEATURE_* in fresh pods, live_preview=false ships dark; (e) `GET /products/{id}/images` → 401 clean (NOT 500), OpenAPI shows new routes mounted (images/export-xlsx/exports/autofill/price-calc, 25 paths). alembic at head `f31c75438e61` (no new migration). Race-check CLEAN (no clobber). **NOTE — CI auto-deploy lane is gated RED at Gate-1 (backend catalog-form event-loop bug, inter-lead OPEN→backend, fix in flight PR #150); deploy+build jobs `skipped` on every develop push → direct deploy used. ci.yml needs NO change (already deploys-from-develop); when backend Gate-1 fix lands, next develop push auto-redeploys.** Records = STATUS deploy block + this note via docs-only PR (F2 convention; no manifest changed). ₹0/mo, dev ns only. PRIOR: **8-FLAG SET COMPLETE — price-calc/dashboard/live-preview flags injected into ConfigMaps.** infra-flags-2 session: backend #149 (flag-parity sweep) landed the final 3 backend flags; backend inter-lead row 54 (`flag-parity (final 3 flags)`, OPEN) requested ConfigMap injection. Added `FEATURE_PRICE_CALCULATOR_ENABLED`/`FEATURE_TRACKING_DASHBOARD_ENABLED` (dev=true) + `FEATURE_LIVE_PREVIEW_ENABLED` (dev=**false** — SHIPS DARK per founder ruling, only default-False V1 flag) to k8s/config.yaml → server-dry-run clean → APPLIED to dev ns (26→29 keys verified live on `35.234.223.66`) → rolling-restarted api+worker (envFrom cache) → all 3 new vars verified `true/true/false` in fresh api+worker pod env. Staging overlay (k8s/overlays/staging/config.yaml) = all 3 `false` → MANIFEST-ONLY (server-dry-run clean, NOT applied — D2 soak gates + live-preview dark). Race-check PASS (29 keys, 3 flags intact). AR-token 401 hiccup on the rolling restart (stale registries.yaml cache) fixed by refresh-ar-token.sh + k3s restart per MEMORY Phase-D note. PR `chore/infra-flags-2`→develop, founder-gated (leave OPEN). ₹0/mo. PRIOR: **IMAGE-TASKS QUEUE ACTIVATED + dev ConfigMap reconciled** — infra-tail session: worker now consumes `-Q celery,image-tasks` (backend #143 task_routes resolved the dependency); inter-lead image-tasks row RESOLVED. Also discovered+fixed the 5 FEATURE flags + GCS_BUCKET_IMAGES were never actually live on the cluster — applied develop config.yaml + rollout-restarted api+worker, all 5 flags now verified in fresh pod env. PR `chore/infra-tail-image-queue`→develop, founder-gated. ₹0/mo. PRIOR: **BRANCH PROTECTION APPLIED + UPGRADED — FOUNDER APPROVED 2026-06-12 ("go branch protection").** SUPERSEDES the develop-only/strict/1-review state recorded by PR #140/#132: protection now applied to BOTH `develop` AND `main` with the 13 required CI contexts (5 gates + 8 frontend: detect + shell + 6 mfe), `strict: false`, required reviews `0` (was 1), `enforce_admins: false` (admin `--admin` escape hatch retained). Build/Deploy/AI-eval/Nightly deliberately EXCLUDED (never report on a PR → would deadlock merges). main owner-only push restriction NOT applicable (`restrictions` is org-only → 422 on a User repo; owner-only push already true by ownership). Sanity-tested via throwaway PR #142 → develop: `mergeable_state` blocked while the 13 checks ran, clean once green; PR closed + branch deleted, no litter. Repo is PUBLIC → protection free (₹0). This doc PR itself rides the new gate. **POST-APPLY FINDING: protection immediately surfaced that develop HEAD is RED at Gate 1 (unit) — 13 catalog-form/ai-autofill tests fail with `no current event loop` (backend pytest-asyncio bug, PRs #115/#118); inter-lead → backend opened. Until backend fixes it, all develop merges are blocked except via founder `--admin` (which is exactly the protection doing its job).**)
**Prior:** 2026-06-12 (deploy-from-develop — **FIRST EVER FULLY HANDS-FREE DEPLOY. FOUNDER RULING: dev deploys fire from develop, NOT main.** PR #137 squash `eb84779` → run **27388030304** ALL GREEN end-to-end. ci-activation close-out: **DONE — CI ACTIVE** run 9 (`27366269839`, PR #132); `GEMINI_API_KEY_CI` now SET 2026-06-12 (nightly ai_eval only, non-blocking).)
**Prior:** 2026-06-12 (image-precheck infra slice session-2 — 5th feature flag `FEATURE_XLSX_EXPORT_ENABLED` JOINED THE LANE on PR #138 [xlsx-export backend-gate inter-lead request]: dev=true APPLIED to dev ns + verified live (all 5 flags now genuinely live on `35.234.223.66`; this also RECONCILED session-1's 4 flags + GCS_BUCKET_IMAGES which were NOT actually present on the live cluster — session-1's apply landed on the stale default kubeconfig context `34.180.58.185`, not the VM); staging overlay=false MANIFEST-ONLY (server dry-run clean, NOT applied — xlsx-export D2 gate: 15 golden fixtures ×3 consecutive develop-HEAD GREEN + manual Meesho supplier-panel upload accepted). ₹0/mo. | session-1: GCS bucket `meesell-images` TF-APPLIED LIVE; 4 feature-flag ConfigMaps; worker concurrency=4 + `-Q image-tasks` scaffold; GEMINI staging template; runbook. Flat branch off origin/develop.)
**Prior:** 2026-06-12 (**microservices-export infra lane — Sub-Plan A I1–I8 IN PROGRESS** on `feature/microservices-export/infra` cut from origin/develop `c859955`. svc-export Dockerfile + k8s/svc-export/{deployment,service}.yaml + Traefik IngressRoute + export schema/role SQL + GCS-export SA spec + secret-injection doc + I8 max_connections=200 TF arg. dev-only, ₹0, current e2-standard-2. Cluster UNREACHABLE (`34.180.58.185:6443` conn refused) → offline-validated per playbook §15. I8 overlap check CLEAN — no MS-0/pgbouncer PR exists. Session-start sweep: no rows untouched 7+ days.)
**This file is the single domain-level status surface for the lead.**

---

## Active features

| Feature | Group branch | Status | Current session | Last touched | Blocking | Notes |
|---|---|---|---|---|---|---|
| dev-final-redeploy | (no branch — verify of auto-fired CI deploy; records via docs-only PR) | DONE (live) | — | 2026-06-12 | — | **FINAL DEV REDEPLOY @ develop tip `80cda2922617a25b54196a5ca9b57b3fb0040e38` (#177, frozen-surface amendment bundle — last code of V1 dev).** Method = **hands-free CI deploy-from-develop** (canonical lane per #137/#141/#155). The `80cda29` develop push auto-fired CI run **27415579375** → **VERIFIED rather than redeployed** (per brief). **Pipeline (proven from deploy-job log):** 5 gates GREEN → Build images SHA-tagged `api:80cda29...` (AR push verified 18:15:58) → Deploy via IAP-SSH: refresh-ar-token → k3s readyz → `kubectl apply` config/api/worker → rollout-settle → `alembic upgrade head` (no new migration) → `set image deployment/{api,worker} = api:80cda29...` → both "successfully rolled out" → in-pipeline smoke `api.mesell.xyz/health` HTTP 200 → "Deploy complete: 80cda2922617...". No auto-rollback fired. §15 [MANDATORY GATE] note: cluster 6443 firewall-blocked from laptop (per memory IP-rotation) so a laptop server-dry-run is impossible; the gate is satisfied at deploy time by the CI deploy-job's own `kubectl apply` on the VM (cluster-reachable) + clean rollout-status + health-smoke + auto-rollback safety net (playbook §15 "deferred-to-deploy-time" branch). **Independent laptop verification:** (1) api `/health` HTTP 200 {postgres:ok,valkey:ok} ×3 stable; (2) `/openapi.json` = MeeSell API 0.1.0, 25 paths (develop-tip surface); (3) TLS api+dev valid → Sep-3-2026 (auto-renew healthy); (4)+(5) worker `{celery,image-tasks}` queues + 8 FEATURE_* flags (live_preview=false dark) — carried forward from #155 `067d664` because `git diff 067d664..80cda29` touches 0 `backend/app` files + 0 `k8s/config.yaml` lines (only tests/frontend, neither in dev runtime). **Federated remotes / shell:** `dev.mesell.xyz` → 404 (Traefik default backend) — EXPECTED + CORRECT: no `frontend/Dockerfile` → shell-image build + `k8s/frontend.yaml` apply both gated-skipped; shell+remotes hosting is the PARKED D13 production-move package (founder cost gate) — NOT touched. The brief's "RemoteFailureComponent fallback" cannot render in dev because the shell itself is not hosted (parked); this is the one brief-expectation that is N/A-by-park, not a failure. **DEV-COMPLETE: yes** for the deployable V1 dev surface (api+worker @ `80cda29`). DEV ONLY — no staging, no prod, no D13/CSP-activation. Cost ₹0/mo, dev ns only. |
| deploy-develop-s2 | (no branch — live deploy action; records via docs-only PR) | DONE (live) | — | 2026-06-12 | — | **DEV DEPLOY-FROM-DEVELOP @ develop tip `067d664be7d4f327389d463869fa26f1d224cdab` (#153).** Founder ruling "Deploy now" (hands-free dev deploy lane). CI auto-deploy lane GATED RED at Gate-1 (backend catalog-form event-loop bug — inter-lead OPEN→backend, fix in flight PR #150) → ran the SAME deploy procedure by hand. **Build:** Cloud Build `2f480e16-212e-4f64-8030-e82546c73516` SUCCESS 6m11s (develop-pinned cloudbuild.yaml — clone branch main→develop); pushed `api:067d664...` (sha256 bd148050) + `worker:067d664...` (sha256 954363e1), both +`latest`. Shell/frontend image SKIPPED (no frontend/Dockerfile; no frontend Deployment in dev). **AR-token:** proactive `refresh-ar-token.sh` on VM pre-rollout (02:59:39Z); NO k3s restart needed, 0× 401 ErrImagePull. **Rollout (lane seq):** pre-settle → `alembic upgrade head` (f31c75438e61, no new migration) → `set image` api+worker=`api:067d664...` → rollout status both clean. **5/5 VERIFY GREEN:** (a) api 2/2 + worker 2/2 on new tag; (b) `api.mesell.xyz/health` 200 {postgres:ok,valkey:ok}; (c) both worker pods bind {celery,image-tasks}; (d) all 8 FEATURE_* in fresh pods (live_preview=false dark); (e) `GET /products/{id}/images`→401 clean (not 500), OpenAPI 25 paths incl new routes. **Race-check CLEAN** (03:02:53Z snapshot, no sibling clobber). Snapshots `/tmp/meesell-{pre,post}-deploy-develop-s2-state.txt`. Cost ₹0/mo, dev ns only. |
| auth-otp | feature/auth-otp/infra | IN REVIEW | — | 2026-06-11 | — | FE-D5 env-var wiring (dev=30/120; staging overlay=60/300) + auth-secret-rotation runbook. Base=feature/auth-otp/integration. |
| image-precheck | feature/image-precheck-infra (FLAT, off origin/develop) → develop | IN REVIEW | — | 2026-06-12 | **FOUNDER GATE — DO NOT MERGE** (founder-gate PR develop, leave open). No infra-side merge blocker. Backend `image-tasks` queue dependency tracked via inter-lead (memo handoff_image_tasks_queue.md) — NOT a blocker (pipeline works on default queue). | image-precheck infra slice (5 items) + the xlsx-export feature-flag lane-join (session-2). **(1) GCS bucket `meesell-images` TF-APPLIED LIVE** — module.gcs_images (new), `terraform plan -target=module.gcs_images` = `2 to add, 0 to change, 0 to destroy` (clean), applied with vaishnaviramoorthy ADC token; verified live in GCP (asia-south1, uniform BLA, public-access enforced, 1-yr DELETE lifecycle age=365, objectAdmin→888244156264-compute@ — the K3s-on-GCE metadata-ADC workload SA, mirrors meesell-prod-assets). **(2) Feature-flag ConfigMaps (5 flags as of session-2)** — k8s/config.yaml dev=true for SMART_PICKER/CATALOG_FORM/AI_AUTOFILL/IMAGE_PRECHECK + **`FEATURE_XLSX_EXPORT_ENABLED` (5th, joined via xlsx-export backend-gate inter-lead request, session-2)** + GCS_BUCKET_IMAGES=meesell-images → APPLIED to dev ns (server dry-run clean). **RECONCILIATION:** session-2 verified all 5 flags + GCS_BUCKET_IMAGES live on the VM cluster `35.234.223.66` (kubeconfig `~/.kube/meesell-dev.yaml`); session-1's "applied + verified" had landed on the STALE default kubeconfig context `34.180.58.185` (dead endpoint, connection refused), so the flags were NOT on the real cluster until session-2's apply. Staging overlay (k8s/overlays/staging/config.yaml) = false for all 5 flags per each feature's D2 → MANIFEST-ONLY (server dry-run clean, NOT applied — founder soak gate; xlsx-export D2 = 15 golden fixtures ×3 consecutive develop-HEAD GREEN + manual Meesho supplier-panel upload). **(3) Worker** — concurrency=4 (already live) + commented-out `-Q image-tasks` scaffold; the named-queue split needs a backend task_routes change first (memo) → worker.yaml MANIFEST-ONLY (applying would force an unrelated `:latest` rollout; comment-only change). **(4) GEMINI_API_KEY staging** — mechanism only: k8s/overlays/staging/secrets.yaml.example with a clearly-marked FOUNDER INJECTION step; NO key value committed/printed. **(5) Runbook** — docs/runbooks/image-pipeline-troubleshooting.md (stuck-job introspection, re-enqueue, D2-Gate-3 tenant-isolation verify, cost monitoring) + new docs/runbooks/README.md index. Cost ₹0/mo (bucket storage immaterial at V1). |
| image-tasks-queue-activation | chore/infra-tail-image-queue (FLAT, off origin/develop) → develop | IN REVIEW | — | 2026-06-12 | **FOUNDER GATE — DO NOT MERGE** (founder/master merges this develop PR). No infra-side blocker. | The image-precheck "tail": (1) **activated `-Q celery,image-tasks`** on `worker.yaml` (the #138 scaffold, now that backend #143 landed `task_routes`). BOTH queues named (image.precheck→image-tasks routed; export.xlsx→default celery, no route). Server-dry-run clean → APPLIED LIVE to dev. Verified `celery inspect active_queues`: both worker pods bind `{celery, image-tasks}`. (2) **Rolled config.yaml + restarted api+worker** — DISCOVERY: the 5 FEATURE flags + GCS_BUCKET_IMAGES were NOT actually live on the cluster (the #138 board claimed applied, but live `meesell-config` had only 20 keys, 0 FEATURE). Applied develop's config.yaml (server-dry-run clean, +6 keys → 26 total), then `rollout restart deployment/api deployment/worker`; both rolled out clean. Verified all 5 `FEATURE_*_ENABLED=true` + `GCS_BUCKET_IMAGES=meesell-images` in fresh api + worker pod env. Cost ₹0/mo. |
| infra-flags-2 (final 3 flags) | chore/infra-flags-2 (FLAT, off origin/develop) → develop | IN REVIEW | — | 2026-06-12 | **FOUNDER GATE — DO NOT MERGE** (founder/master merges this develop PR). No infra-side blocker. | Completes the **8-flag set** (backend #149 flag-parity sweep). Injects the final 3 V1 feature flags into the k8s ConfigMaps. **Dev (k8s/config.yaml):** `FEATURE_PRICE_CALCULATOR_ENABLED="true"`, `FEATURE_TRACKING_DASHBOARD_ENABLED="true"`, `FEATURE_LIVE_PREVIEW_ENABLED="false"` (SHIPS DARK per founder ruling — ONLY default-False V1 flag, do NOT flip true until founder lights the gated rollout). Server-dry-run clean → APPLIED to dev ns; live `meesell-config` 26→**29 keys** verified on the real cluster `35.234.223.66` (kubeconfig `~/.kube/meesell-dev.yaml`). Rolling-restarted api+worker (flags consumed via `envFrom: configMapRef`, cached at pod start) → both rolled out 2/2 → 3 new vars verified `true/true/false` in fresh api+worker pod env (`printenv`). **AR-token hiccup:** new ReplicaSet pods hit `401 ErrImagePull` (stale registries.yaml token); fixed per MEMORY Phase-D note — `refresh-ar-token.sh` + `systemctl restart k3s` (reloads registries.yaml; pods recover from cached layers). **Staging (k8s/overlays/staging/config.yaml):** all 3 = `"false"` → MANIFEST-ONLY, server-dry-run clean (`created (server dry run)` — staging ConfigMap not live yet), NOT applied (D2 soak gates + live-preview dark). **Race-check PASS** (post-work: live 29 keys, 3 flags intact, no sibling revert). 2 manifest files + board + STATUS. Cost ₹0/mo (config-only, no new primitive/secret). Resolves backend board inter-lead row 54 (`flag-parity (final 3 flags)`). |
| microservices-export | feature/microservices-export/infra | IN REVIEW | — | 2026-06-12 | — (cluster UNREACHABLE 34.180.58.185:6443 → offline-validated per playbook §15; NOT a merge blocker, server dry-run + dev smoke deferred to deploy time) | **GATE VERDICT: APPROVE on PR #190** (self-gate, infra lead; comment `#issuecomment-4693636063`). Tip `234e4d2` (was `aafcc30`). **IN-GATE FIX `234e4d2`** — adversarial check vs the ACTUAL landed backend tree `origin/feature/microservices-export/backend` @ `e23080c` found 3 would-not-run defects, all fixed (Dockerfile + deployment only; backend sole-writer files untouched): (1) gunicorn missing from landed `requirements.txt` → `RUN pip install gunicorn==22.0.0` in image layer (monolith pin); (2) worker `-A app.workers.celery_app` → `app.celery_app` (no `app/workers/` pkg in svc-export); (3) worker `-Q celery` → `-Q svc-export` (landed celery_app.py:39,77-78 `task_default_queue="svc-export"`). `app.main:app` confirmed. I1–I8 verified at handoff values; secret scan clean; dev-only; file-list scoped; yaml-parse + field assertions ALL PASS. D3: 250m CPU fits e2-standard-2, no ask, ₹0. Deploy-time bootstrap: founder creates SM `dev-export-db-password` (export_user pw). **Squash executed by the session window after verdict (NOT this lane).** — **Sub-Plan A (export extraction) INFRA lane — I1–I8.** Branch cut from origin/develop `c859955`, PRs → feature/microservices-export/integration (infra-lead-reviewed; founder gates integration→develop). dev namespace ONLY, current `e2-standard-2` hardware, ₹0 spend. Deliverables: I1 `backend/services/svc-export/Dockerfile` (python:3.12-slim, single image, api/worker entrypoint differs); I2 `k8s/svc-export/deployment.yaml` (api 1×req 50m/128Mi lim 200m/512Mi; worker 1×req 200m/512Mi lim 400m/1Gi); I3 `k8s/svc-export/service.yaml` (ClusterIP svc-export:8001); I4 Traefik IngressRoute (`/api/v1/exports/*` + `/api/v1/products/{product_id}/export-xlsx` → svc-export:8001; `/internal/*` NOT exposed); I5 Postgres `export` schema + `export_user` role + grants incl **`GRANT INSERT ON public.audit_events`** (R3); I6 GCS SA spec for `meesell-exports/{user_id}/...` prefix (keyless metadata-server pattern, storage.objectAdmin scoped); I7 secret injection (DATABASE_URL@schema export, VALKEY_URL, JWT_SECRET, GCS_*, APP_ENV — NOT GEMINI/LANGFUSE/MSG91/RAZORPAY); I8 MS-DB-3 pool right-size max_connections=200. **I8 OVERLAP CHECK: no MS-0/pgbouncer/max_connections PR or branch exists in origin (`gh pr list --search pgbouncer`/`git log develop`/`git ls-remote` all clean) → genuinely absent → minimal additive TF arg.** D3 capacity: svc-export 50m+200m=250m CPU fits current node (no D3 ask). Exact route paths cited from export/router.py:91+144. |
| microservices-image | feature/microservices-image/infra | IN PROGRESS | mesell-microservices-image-infra-session-1 | 2026-06-13 | **D3 VM-FIT OVERFLOW — DEPLOY GATED ON FRESH FOUNDER D3 ASK** (manifests authored+validated ₹0, but the projected MS-2 node load ~2525m > 2000m allocatable → e2-standard-4 ~₹2,600/mo decision needed BEFORE the apply; NOT a merge blocker — these manifests are dev-only authored surfaces). | **Sub-Plan C (image extraction) INFRA lane A2 — I1–I10.** Branch cut from `feature/microservices-image/integration` (`3dc0f91`); PR → integration (infra-lead-reviewed; founder gates integration→develop). dev namespace ONLY, ₹0 spend. Recipe-copy of the PROVEN MS-A svc-export pattern. Deliverables: I1 `backend/services/svc-image/Dockerfile` (python:3.12-slim, single image api/worker; Pillow+gcs+ai_ops deps; **rembg DEFERRED** — zero call sites, cheapest D3 mitigation, image stays low-hundreds-MB); I2 `k8s/svc-image/deployment.yaml` (api 1× req 50m/128Mi lim 200m/512Mi; **dedicated worker 1× req 500m/1Gi lim 1000m/2Gi** — heaviest early-wave worker); I3 `k8s/svc-image/service.yaml` (ClusterIP svc-image:8001, selects api only); I4 Traefik IngressRoute (`POST/GET /api/v1/products/{id}/images` → svc-image:8001 via tight PathRegexp `^/api/v1/products/[^/]+/images$`; `/internal/products/{product_id}/images` NOT exposed); I5 Postgres `image` schema + `image_user` role + grants — **§0.10 OPTION B: NO products read-grant; ONLY `GRANT INSERT ON public.audit_events` + image-schema DML** (PR #197 ruling); I6 GCS SA spec for `gs://meesell-images/{user_id}/{product_id}/{idx}.jpg` (keyless metadata-server, VM SA already holds objectAdmin on meesell-images — VERIFIED 2026-06-13, ₹0 Option A); I7 secret injection (DATABASE_URL@schema image, VALKEY_URL, JWT_SECRET, **GEMINI_API_KEY + LANGFUSE_SECRET_KEY** — image IS AI-consuming, GCS_*, APP_ENV — NOT MSG91/RAZORPAY); I8 Valkey budget-brake (shared instance DB 0, un-prefixed `ai:cost:*`/`ai:budget:*` global ₹500 cap per D6); I9 dedicated Celery queue `svc-image` (broker DB1/results DB2, keys `svc-image:`); I10 pool right-size (MS-DB-3 max_connections=200 already live per MS-0). Cluster UNREACHABLE (6443 firewall-blocked to founder IP) → yaml.safe_load_all offline-validated (4 appliable manifests PASS); server dry-run + dev smoke DEFERRED to deploy time (playbook §15 F3 branch). |
| mfe-cutover | feature/mfe-cutover/infra | IN REVIEW | — | 2026-06-11 | founder cost gate for D13 hosting (~₹1,600-1,800/mo >₹500/mo) — NOT a merge blocker (R-SP7-4); dev CSP smoke joint w/ frontend+backend | SP07 infra group (FIRST two-group sub-plan; frontend lead runs joint merge gate). CSP mechanism (D42) = nginx add_header in shell image (PRIMARY) + Traefik CSP-only Middleware (ALT), both ADD-ONLY-validated (no CORS/Set-Cookie strip — R-SP7-1 P0). Dev CSP smoke procedure (A remote-load + B 401→refresh→retry + C CORS) authored. Staging/prod cutover gated on dev-smoke-GREEN. D13 hosting work-package + cost sheet PREPARED, NOT PROVISIONED (founder cost gate). C-CI-1 matrix → all 6 remotes + D43 shell glob; cloudbuild version-pinned `{env}/mfe-<name>/{version}` (no latest, R-SP7-6). Branch cut from origin/develop (integration branch not yet created by FE lead — see handoff_mf_cutover.md NOTE). Files: frontend/docker/{nginx.conf.template,csp-policy.env,docker-entrypoint.sh,Dockerfile.shell}, k8s/csp/csp-middleware.yaml, docs/plans/infra/{SP07_CSP_AND_HOSTING.md,SP07_HOSTING_COST_SHEET.md}, .github/workflows/ci.yml, cloudbuild.yaml, docs/DEVOPS_ARCHITECTURE.md §9. Cluster unreachable → yaml.safe_load_all offline-validated; server dry-run deferred (playbook §15). Cost authored ₹0; provisioning gated. |

## Follow-up backlog (infra hygiene)

| Item | Opened | Priority | Notes |
|---|---|---|---|
| Delete dead `meesell-*`-prefixed SM duplicates | 2026-06-12 | medium | Backend audit (2026-06-12) verdict ALL-CLEAR on live path (cluster holds VALID `gemini-api-key`, hash `ef9bbd1ca21f`). The DEAD `meesell-gemini-api-key` (HTTP 400, unreferenced) + likely `meesell-jwt-secret` / `meesell-msg91-auth-key` / `meesell-msg91-template-id` / `meesell-postgres-password` / `meesell-razorpay-key-id` / `meesell-razorpay-key-secret` still exist in GCP Secret Manager. Repo surfaces defused this session (chore `dead-gemini-key-cleanup`: F1 deleted `scripts/secrets-from-gcp.sh` + 4 ref fixes; F2 annotated audit doc). NEXT SESSION: `gcloud secrets list --filter="name:meesell-"` → confirm unreferenced → `gcloud secrets delete` the dead duplicates (founder approval in-prompt; SM mutation NOT done in the cleanup chore). Also note: the whole `terraform/` tree (NOT `infra/terraform/`) is the OLD superseded TF root — candidate for a separate cleanup. |

## Recently merged (last 14 days)

| Feature | Merged to | Date | PR | Notes |
|---|---|---|---|---|
| microservices-pgbouncer (MS-0 / D5 step 1) | develop | 2026-06-12 | #181 (squash 29ed457) — applied 2026-06-12 | **MERGED + APPLIED LIVE to dev (founder-authorized).** Runbook steps 1–2 only (step 3 DATABASE_URL flip remains a separate founder gate, blocked on backend R-MS-8). **(1) Postgres `max_connections` 100→200 LIVE** — TF `module.postgres_dev`, plan `0 add, 1 change, 0 destroy` (in-place: `args+=["-c","max_connections=200"]`, mem **limit** 1Gi→1536Mi, request unchanged), applied from worktree (`terraform init` + saved-plan apply); postgres-0 rolled clean (RESTARTS 0), `pg_isready` ok, `SHOW max_connections`=**200**. **(2) PgBouncer transaction-pool LIVE** — `k8s/pgbouncer.yaml` applied; pod 1/1 Running; `SHOW POOLS` row `meesell`=`transaction` (sv_idle=1); end-to-end **SELECT 1 through `:6432` → `1`**. Endpoint `pgbouncer.dev.svc.cluster.local:6432`. **(3) `pgbouncer-userlist` Secret populated** — scram **plaintext** userlist from live `postgres-credentials`; password never committed/printed, temp file shredded (§10). **TWO apply-time manifest fixes (folded into k8s/pgbouncer.yaml on this PR):** (a) image tag `edoburu/pgbouncer:1.23.1`→`v1.23.1-p3` — the bare `1.23.1` tag 404s on Docker Hub (edoburu uses `vX.Y.Z-pN`), caused ErrImagePull; (b) `auth_type` `md5`→`scram-sha-256` + plaintext userlist — Postgres runs `password_encryption=scram-sha-256`, so an md5 userlist gave "cannot do SCRAM authentication: wrong password type". Runbook §2a + verify queries corrected to match. **Zero routing impact** — api 2/2 + worker 2/2 untouched (AGE preserved, `/health` postgres+valkey ok; nothing flipped to :6432). Node post-apply CPU req 83% / mem req 44% (headroom OK); postgres live mem 37Mi << 1536Mi limit; pgbouncer 1Mi/10m. Cost ₹0/mo (one tiny pod 25m/32Mi req + postgres mem-limit-only raise). |
| dead-gemini-key-cleanup | develop | 2026-06-12 | (PR pending — chore branch) | Audit F1/F2 closeout. Deleted stale `scripts/secrets-from-gcp.sh` (prefixed `meesell-` SM IDs + legacy `meesell` ns — dead path); fixed 4 referencing files (terraform/README.md, terraform/templates/startup.sh, terraform/outputs.tf, .nexus/results/ci-cd-terraform-gap-analysis.md); annotated INFRASTRUCTURE_TERRAFORM_AUDIT.md (~L185/~L210) SUPERSEDED. NO SM mutation (dead SM duplicates → follow-up backlog above). Cost ₹0. |
| ci-activation | main (CI ACTIVE) | 2026-06-12 | #132 (merge `62713935`) | **DONE — CI/CD PIPELINE ACTIVE.** Run 9 (`27366269839`, merge SHA `62713935`, PR #132) = **FIRST FULLY GREEN end-to-end run**: 5 gates + 8 frontend + Cloud Build + IAP deploy (token refresh → k3s restart → readyz → applies → settle wait → alembic migrate → image roll → rollout status → in-pipeline health check) + external health 200. The 6-rung deploy-bug ladder, all codified: (1) act-as on compute SA — PR #113; (2) compute.viewer — PR #116; (3) AR pull auth (registries.yaml token; SA-key alt #121 closed, puller resources TF-destroyed) — PR #119; (4) shallow-clone FETCH_HEAD — PR #123; (5) unescaped `$(seq)` runner-expansion — PR #127; (6) exec-on-terminating-pod settle wait — PR #131. **Branch protection applied + UPGRADED 2026-06-12 (founder "go branch protection")**: NOW on BOTH develop AND main — 13 required contexts (5 gates + detect + 7 frontend units), `strict:false`, required reviews `0` (was 1), `enforce_admins:false`. SUPERSEDES the earlier develop-only/strict/1-review record. main `restrictions` not set (org-only). Sanity-tested via throwaway PR #142 (blocked→clean), closed+deleted no litter. See STATUS_INFRA.md branch-protection UPDATE. `GEMINI_API_KEY_CI` GitHub secret **SET + founder-verified 2026-06-12 (updated_at 01:55:12Z) — DONE** (was the last pending CI-activation item). Sourced from SM `gemini-api-key` (proven-valid, HTTP 200; founder verified in AI Studio); SAME key as prod/local — no separate quota cap, so a capped-key swap is optional future hardening. Consumer = nightly ai_eval only (never blocking). **NEW FLAG:** SM container `meesell-gemini-api-key` is DEAD/invalid (HTTP 400, placeholder/revoked) — backend/AI lane to one-time-confirm no k8s secret/manifest/config sources THAT name instead of the valid `gemini-api-key`. Final follow-on same day: **FOUNDER RULING 2026-06-12 (#137 `eb84779`) — dev deploys fire from develop, NOT main** (build+deploy ref-guards flipped `refs/heads/main`→`refs/heads/develop`; VM checkout `fetch origin develop`+`reset --hard FETCH_HEAD`; main reserved for staging/prod promotion, V1.5); readyz #127 + FETCH_HEAD #123 + rollout-settle all preserved. Cost ₹0. |
| dual-pepper-secret-refs | develop | 2026-06-11 | #69 (squash) | R5 inter-lead (from backend PR #65/#66) RESOLVED. Added REFRESH_TOKEN_PEPPER_PREVIOUS+VERSION to k8s/secrets.yaml.example + SM onboarding note in INFRASTRUCTURE_ARCHITECTURE.md §4 (NOT new SM secrets — PREVIOUS=prior pepper SM version kept ENABLED during grace window per runbook §2; VERSION=operator int). Docs/example only, no cluster/SM ops. Cost ₹0. |
| mf-ci-c-ci-1 | develop | 2026-06-11 | #50 (squash 86e67c8) | C-CI-1 DISCHARGED-pending-activation: ci.yml frontend paths-filter matrix (shell + mfe-pricing pilot, libs fan-out) REPLACING single-frontend conditional; cloudbuild shell-image + INERT GCS remote publish (`_REMOTES_BUCKET`). Config+docs only. Cost ₹0. |
| auth-otp | feature/auth-otp/integration | 2026-06-11 | #45 (squash d2b734e) | FE-D5 env wiring (dev=30/120; staging overlay=60/300) + auth-secret-rotation runbook. Founder-flags F1 (APP_ENV), F2 (single-pepper backend follow-up), F3 (dry-run-server at deploy). Cost ₹0. |
| gate4-confirmation | develop | 2026-06-10 | #33 (merge f30d61f) | MF §9 Gate 4 hosting confirmation — VERDICT CONFIRMED-WITH-CONDITIONS (6 conditions feed Sub-plan 7) |
| housekeeping-v1 | feature/housekeeping-v1 | 2026-06-10 | #27 (squash 6096244) | dead GitLab CI removal + SA key disk hygiene |

## Inter-lead requests open

| To lead | About feature | Request | Opened | Status |
|---|---|---|---|---|
| frontend-coordinator (incoming) | mf-workspace-foundation | MF CI prep C-CI-1 (handoff_mf_ci_prep.md) — replace single-frontend CI with paths-filter matrix before SP1 | 2026-06-10 | RESOLVED via PR #50 (merged develop) — frontend lead marks CLOSED on its own board |
| frontend-coordinator (incoming) | mfe-cutover | SP07 cutover infra (spec_sp07_infra.md + handoff_mf_auth_deploy.md R-SP6-6/C-CSP-1): CSP ADD-ONLY mechanism (D42) + dev smoke + per-env manifest templating + the 4 infra-side Gate-4 C-conditions + D13 hosting work-package. Delivered on feature/mfe-cutover/infra (PR to integration); my content sign-off in handoff_mf_cutover.md. CSP=nginx add_header (primary)+Traefik Middleware (alt), both ADD-ONLY. Hosting NOT provisioned — founder cost gate (~₹1,600-1,800/mo). FE lead runs joint merge gate + collates Gate-4 discharge. | 2026-06-11 | OPEN — FE lead marks CLOSED on its own board when SP07 ships dev-validated CSP + the hosting wave (post cost-gate) |
| backend-coordinator | ci-activation | First main pipeline RED — Gate 1 `pytest -m unit` fails at COLLECTION: `from app.shared.database import` → `ModuleNotFoundError: No module named 'app'`. CI runs `pytest` in `working-directory: backend` with no PYTHONPATH and no installable pkg (`pytest.ini` §19.D LOCKED has no `pythonpath`; no pyproject/setup.py). Fix is backend-owned: add `pythonpath = .` to pytest.ini (founder OK — §19.D locked) OR add pyproject/setup.py + `pip install -e .`. See handoff_ci_gate1_collection.md. | 2026-06-11 | RESOLVED — backend pythonpath fix (#73/#74) closed collection; infra PR #76 (0f44d72) closed the resulting §5.D env-guard gap. Both sides done. |
| backend-coordinator | ci-activation | §19.D gate markers (`unit`/`smoke`/`integration`/`golden_roundtrip`/`slow`/`perf`/`ai_eval`) are REGISTERED in pytest.ini but applied to ZERO of 823 tests (0 files match `pytest.mark.<m>` for all 7; no `pytest_collection_modifyitems` hook). `pytest -m "unit"` selects 0 → exit 5 → RED. SYSTEMIC across all marker-gated gates + nightly; fixing only `unit` just moves red to Gate-2. Fix backend-owned: tag the suite per §19.D bucket (explicit `@pytest.mark` OR conftest auto-marking hook), complete across all buckets so each gate selects >0. First seen on PR #78 run 27320321536; RE-CONFIRMED on the develop trigger path — push run 27320468096 RED at Gate-1 exit-5. See handoff_ci_gate_markers.md. | 2026-06-11 | RESOLVED via PR #85 (`34d8b47`, merged develop) — 823 tests now §19.D-tagged; run 27323036548 Gates 1-3 GREEN. Backend marks CLOSED on its own board. |
| backend-coordinator | image-precheck | `image-tasks` Celery queue: FEATURE_PLAN §Infra row 4 wants a dedicated `image-tasks` queue at concurrency=4, but `image.precheck` @shared_task (tasks.py:415) has no `queue=` and celery_app.py has no `task_routes` → tasks publish to the default `celery` queue. Infra kept `--concurrency=4` + a commented `-Q image-tasks` scaffold in worker.yaml; backend must add `task_routes={"image.precheck":{"queue":"image-tasks"}}` to celery_app.py before infra can uncomment it (else the worker stalls). NOT a blocker — pipeline works on the default queue today. See handoff_image_tasks_queue.md. | 2026-06-12 | **RESOLVED 2026-06-12 (infra-tail session).** Backend landed `task_routes={"image.precheck":{"queue":"image-tasks"}}` in celery_app.py (#143). Infra activated `-Q celery,image-tasks` on the worker Deployment (BOTH queues named — once `-Q` is set, Celery stops auto-consuming the default `celery` queue, so `export.xlsx` would stall if `celery` were omitted) + applied live to dev. Verified via `celery inspect active_queues`: both worker pods consume `{celery, image-tasks}`. PR `chore/infra-tail-image-queue` → develop (founder-gated, leave open). Both sides done. |
| backend-coordinator (incoming) | xlsx-export | xlsx-export backend gate requested the 5th feature flag `FEATURE_XLSX_EXPORT_ENABLED` be wired into the k8s ConfigMaps, joining the existing image-precheck infra lane (PR #138). DELIVERED session-2: dev `meesell-config` ConfigMap += `FEATURE_XLSX_EXPORT_ENABLED: "true"` APPLIED to dev ns + verified live (all 5 flags now live on `35.234.223.66`); staging overlay += `FEATURE_XLSX_EXPORT_ENABLED: "false"` MANIFEST-ONLY (server dry-run clean, NOT applied — D2 staging gate: 15 golden fixtures ×3 consecutive develop-HEAD GREEN + manual Meesho supplier-panel upload accepted). Rode PR #138 (no new PR). ₹0/mo. | 2026-06-12 | RESOLVED (delivered on PR #138). Backend/xlsx-export lead marks CLOSED on its own board; staging flag flip is a future micro-feature once xlsx-export D2 soak clears. |
| backend-coordinator | ci-activation | Gate 4 (integration) RED on run 27323036548 — `21 failed, 23 passed, 151 errors`. Backend test-harness (`backend/tests/conftest.py`) does not honor the CI-provided `TEST_DATABASE_URL`/`TEST_VALKEY_URL` for its live/dev fixtures: `_DEV_DATABASE_URL` (L56-58) hardcodes the K3s-dev cluster password on 5433/db `meesell` (only `DEV_DATABASE_URL`-overridable) → `asyncpg InvalidPasswordError`; live-Redis fixtures (L183/398) default `CORE_TEST_VALKEY_URL=localhost:6379` (CI maps Valkey to 6381) → `redis.ConnectionError`; integration DB setup lacks `CREATE EXTENSION pg_trgm` + migrations/seed → `gin_trgm_ops`/`categories does not exist`; plus async loop-scope + `assert 2==1`/`assert 200==429` logic bugs. The infra ci.yml Gate-4 service+env block is CORRECT (verified) — fix is backend (conftest honors TEST_* + pg_trgm + seed + loop/assert fixes). See handoff_ci_gate4_integration.md. | 2026-06-11 | RESOLVED — 4 backend PRs (#104/#107/#108/#110) merged to develop; Gate 4 GREEN on run 27331720017. Backend marks CLOSED on its own board. |
| backend-coordinator | catalog-form | **Gate 1 (unit) RED on develop** — surfaced the moment branch protection went live. develop tip `97943e80` (PR #138 merge) push run **27389561127** = FAILURE; `13 failed, 619 passed` with `RuntimeError: There is no current event loop in thread 'MainThread'` + `coroutine never awaited` in `test_catalog_routes.py::{TestAutofillFlagGuard,TestCatalogFormFlagRouteMount}` + `test_catalog_unit.py::{TestAutofillNeverAutoApplies,TestAssertProductOwnership}` (catalog-form/ai-autofill slice, PRs #115/#118). Backend pytest-asyncio wiring bug (NOT infra — ci.yml Gate 1 unchanged + correct). CONSEQUENCE: with protection live, every PR to develop is now `mergeable_state: blocked` until this is green; `--admin` is the only meanwhile-merge path (used it to land docs PR #144). Fix: add `@pytest.mark.asyncio` / replace `asyncio.get_event_loop()` with `asyncio.run()` for py3.12 / await async helpers under a running loop. See handoff_ci_gate1_eventloop.md. | 2026-06-12 | OPEN |
| backend-coordinator (incoming) | flag-parity (final 3 flags) | Backend board inter-lead row 54: after `chore/flag-parity` (#149) merged to develop, inject the final 3 V1 feature flags into the k8s ConfigMaps so ALL 8 V1 flags are ConfigMap-consistent: `FEATURE_PRICE_CALCULATOR_ENABLED` **dev=true / staging=false**; `FEATURE_TRACKING_DASHBOARD_ENABLED` **dev=true / staging=false**; `FEATURE_LIVE_PREVIEW_ENABLED` **dev=false / staging=false** (SHIPS DARK — only default-False V1 flag; founder flips dev=true only when preview FE wires). Config-only, no new secret/primitive. Memo `.claude/agent-memory/meesell-backend-coordinator/handoff_secret_flag_parity_flags.md`. | 2026-06-12 | RESOLVED 2026-06-12 (infra-flags-2 session, PR `chore/infra-flags-2`). Dev: 3 flags applied to dev ns + verified live (`meesell-config` 26→29 keys on `35.234.223.66`) + api/worker rolling-restarted + 3 vars verified `true/true/false` in fresh pod env. Staging overlay: all 3 `false` MANIFEST-ONLY (server-dry-run clean, NOT applied — D2 soak + dark). Backend/flag-parity lead marks row 54 CLOSED on its own board; staging flag flips are future micro-features once each D2 soak clears (live-preview stays dark until founder lights it). |
| backend-coordinator | gate4-integration | **Gate-4 (integration) RED on develop as of 2026-06-12 ~03:24Z (first seen PR #158 run 27392278294):** multiple integration failures (customer eligibility ×2, customer onboarding, iam replay-attack, catalog lifecycle, shared-database get_db, export router setup-errors ×4+, customer seller-profile setup-errors). Suspected fallout of tonight's merges touching shared conftest/modules (#150 and feature slices). ASSIGNED: meesell-backend-coordinator. NOTE per CHORE-C: triage must FIRST sweep open + recently-merged (~24h) PRs for an existing fix before dispatching. | 2026-06-12 | OPEN |
| backend-coordinator (incoming) | microservices-export | Sub-Plan A INFRA work-package delivered per handoff_msA_infra.md (I1–I8). Acceptance items the backend merge gate depends on, all confirmed on `feature/microservices-export/infra`: **(1) I5 — `export_user` HAS `GRANT INSERT ON public.audit_events`** (R3; the integration test asserting an audit row lands will pass); **(2) I8 — `max_connections=200`** shipped as a minimal additive `args=["-c","max_connections=200"]` on the postgres TF StatefulSet (no MS-0/pgbouncer PR existed to defer to — overlap check clean; TF applied by founder, NOT this lane); **(3) I2 — svc-export deployed at the 50m api / 200m worker sizing** → 250m CPU fits the current `e2-standard-2` node alongside the monolith, so the §4 row-A "fits current node, no D3 ask" annotation holds. PgBouncer (MS-DB-4) deferred — NOT a Sub-Plan-A blocker (dev-only, zero-traffic). Cluster UNREACHABLE → manifests offline-validated (`kubectl --dry-run=client` + yaml parse); server dry-run + dev smoke deferred to deploy time (playbook §15). Cost ₹0/mo. | 2026-06-12 | OPEN — backend lead marks CLOSED on its own board after the integration merge gate consumes these confirmations |
| backend-coordinator | microservices-image | Sub-Plan C INFRA work-package (A2) delivered on `feature/microservices-image/infra` per handoff_msC_infra.md (I1–I10). Acceptance items the backend merge gate depends on: **(1) I5 §0.10 OPTION B CONFIRMED — `image_user` has ZERO `products` read-grant** (the ONLY cross-schema grant is `GRANT INSERT ON public.audit_events`; negative-asserted in schema-role.sql, grep-verified no active products GRANT line). **(2) I5 audit — `image_user` HAS `GRANT INSERT ON public.audit_events`** (the `image.precheck.completed` worker-audit row lands). **(3) I8 — svc-image VALKEY_URL hits the SHARED Valkey DB 0** (un-prefixed `ai:cost:*`/`ai:budget:*` global ₹500 brake stays coordinated). **(4) I7 — GEMINI + LANGFUSE secrets injected** (image is AI-consuming; both verified ENABLED in SM 2026-06-13). **(5) I9 — dedicated `svc-image` queue** (broker DB1/results DB2, keys `svc-image:`, worker `-Q svc-image`). **(6) I2/§3 D3 — svc-image worker authored at the heavy 500m sizing, but the node OVERFLOWS** (projected MS-2 ~2525m > 2000m allocatable) → the §3 "fits node, no D3 ask" annotation does NOT hold; a FRESH founder D3 ask (e2-standard-4 ~₹2,600/mo) is required BEFORE deploy. rembg-deferred is the cheapest mitigation, already applied. Cluster UNREACHABLE → offline-validated; server dry-run + dev smoke deferred (playbook §15). ₹0/mo (authoring). | 2026-06-13 | OPEN — backend lead marks CLOSED on its own board after the integration merge gate consumes these confirmations |
| backend-coordinator | microservices-pgbouncer (MS-0 / D5) | **asyncpg/SQLAlchemy transaction-pool compatibility (R-MS-8)** — before any service flips `DATABASE_URL` to `pgbouncer.dev.svc.cluster.local:6432`, the backend async DB layer must be transaction-pool-safe: asyncpg `statement_cache_size=0`, SQLAlchemy `pool_pre_ping=False` + `executemany_mode='values_only'`, and NO `SET LOCAL`/`LISTEN`/cross-transaction prepared statements (transaction pooling returns the backend at COMMIT, not disconnect). Validate each service's `pytest -m integration` against pgbouncer:6432. Fallback if transaction mode breaks: infra flips the ConfigMap to `pool_mode=session` (one line). NOT a blocker for the MS-0 PR — pgbouncer is additive; nothing routes through it until the founder-gated flip. PgBouncer side already defensive (`max_prepared_statements=0`, `server_reset_query=DISCARD ALL`). See `.claude/agent-memory/meesell-infra-builder/handoff_d5_pgbouncer_backend.md`. | 2026-06-12 | OPEN |

---

## Status vocabulary

| Status | Meaning |
|---|---|
| `PENDING` | Feature is on the lead's backlog; no branch exists yet. |
| `IN PROGRESS` | A `feature/{name}/infra` branch exists; lead is actively committing. |
| `IN REVIEW` | A PR is open against `feature/{name}`; awaiting lead self-review approval. |
| `MERGED` | The infra group's PR has merged to `feature/{name}` — the group is done for this feature. |
| `BLOCKED` | Work stopped pending an inter-lead request, infra change, or founder decision. |

A feature row stays on the Active features table until the infra group's PR merges to `feature/{name}`; then it moves to "Recently merged" for 14 days before being removed.

---

## Acceptance gate

Group-PR approval criteria live in the infra PR template at `.github/PULL_REQUEST_TEMPLATE/infra.md`. The lead approves a `feature/{name}/infra` → `feature/{name}` PR only when the template is filled completely — including the `terraform plan` output (`Plan: X to add, Y to change, Z to destroy`), `kubectl apply --dry-run=server` clean output, secret refs documented with no JSON keys, Workload Identity Federation paths confirmed, smoke deploy to `dev` succeeded (`kubectl get pods -n dev` Ready), cost impact estimate recorded (with explicit founder sign-off for any change > ₹500/month) — plus CI gate-3 (lint — manifest validation) green and the rollback procedure for the resource type documented per `docs/INFRASTRUCTURE_PLAYBOOK.md`.
