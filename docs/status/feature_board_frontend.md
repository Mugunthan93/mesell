# Feature Board — Frontend Lead

**Lead agent:** `meesell-frontend-coordinator`
**Domain:** `frontend`
**Last updated:** 2026-06-11 (smart-picker-wiring Phase A (component-builder) + Phase B (service-builder) COMPLETE. Phase B: HTTP CategoryService ported from e97c4f5 to mfe-catalog remote + provideHttpClient(withFetch()) added to shell app.config.ts AND remote main.ts. 45 spec files/444 tests/0 fail. Commit 6c3680d on feature/smart-picker-wiring/frontend (pushed). Group PR opened: feature/smart-picker-wiring/frontend → feature/smart-picker-wiring/integration. F2 smart-picker-wiring status: IN REVIEW. Prior: SP06 mfe-auth 6-remote topology COMPLETE on develop (#96 756d070); SP07 cutover pending.)
**This file is the single domain-level status surface for the lead.**

---

## Active features

| Feature | Group branch | Status | Current session | Last touched | Blocking | Notes |
|---|---|---|---|---|---|---|
| smart-picker-wiring | feature/smart-picker-wiring/frontend | IN REVIEW | mesell-smart-picker-port-frontend-session-2 | 2026-06-11 | none | Phase B complete (service-builder session-2). HTTP CategoryService ported from e97c4f5 to mfe-catalog remote. provideHttpClient(withFetch()) added to BOTH shell app.config.ts (root injector) AND apps/mfe-catalog/src/main.ts (standalone dev-serve). NEW category.service.spec.ts (20 tests). §9.E model verified field-for-field: no drift. Error matrix exact: 401→logout+EMPTY; 402/404/5xx→fallback; 400→EMPTY. Manual Bearer from AuthService.getToken() (Wave 7 interceptor migration note in JSDoc). ng build mfe-catalog 3.029s / shell 2.709s both <90s. tsc strict clean (app+spec). 45 spec files / 444 tests / 0 fail (Phase A 44 files preserved + 1 new category.service.spec.ts). Commit: 6c3680d. PR opened: feature/smart-picker-wiring/frontend → feature/smart-picker-wiring/integration. Awaiting lead merge gate. |

## Recently merged (last 14 days)

| Feature | Merged to | Date | PR | Notes |
|---|---|---|---|---|
| mfe-auth | feature/mfe-auth/integration | 2026-06-11 | #95 (squash 8e90363) | MF Sub-Plan 06 (R1 — the LAST extraction; **6-remote topology COMPLETE**). 3 PUBLIC auth pages (login/signup/otp-verify) → apps/mfe-auth Native Federation remote (port 4206), 3 exposes (./LoginComponent ./SignupComponent ./OtpVerifyComponent). Closes the bidirectional auth singleton loop: SP03=C5 (shell→remote READ via profile); **SP06=C4 (remote→shell setSession WRITE via otp-verify)**. **LEAD INDEPENDENTLY RE-VERIFIED on tip 6e5ec46 AND merged integration tip 4dd6b6d (skeptical-lead, 0 discrepancies vs builder):** 6 renames R100 (login/signup/otp-verify .ts+.spec byte-identical — D39 AuthLayout re-point already on develop = verified NO-OP, ZERO content edits on moved files, cleanest extraction yet) + clean creates (federation.config.js, main.ts routing ALL 3 exposes, index.html, tsconfig.app.json, public-api.ts, auth-write.smoke.spec.ts). Remote build GREEN 2.685–3.420s; shell GREEN 3.290–3.363s ≤90s, initial 60.62 kB (auth chunks LEFT shell = strangler shrink). **44 files/416 tests 0 fail/0 skip** (develop baseline 43 + 1 C4 file; all 4 mfe-auth specs discovered spec-apps-mfe-auth-*). **C4 WRITE go/no-go GREEN:** post-setSession isAuthenticated()===true + currentUser() name/id correct + getToken()==='mock-token' + guard flips false→true; single instance remoteAuth===shellAuth; setInterval resend cleared on destroy. **C2 no-dup:** @mesell/core shared singleton:true, exactly one _mesell_core.js chunk, setSession DEF only there + call-site only in OtpVerifyComponent.js (no inlined class). **AuthService diff EMPTY across branch (D22 C2 — hardest STOP, clear).** Boundary 0 primeng outside ui-kit (incl specs); 0 in apps/mfe-auth; **0 localStorage** (FE-D5 in-memory JWT). Manifest 6 entries (auth 4206); port 4206 in both serve keys. D37: 3 auth routes loadRemoteWithFallback, NO canActivate (public); only guard is the protected shell parent (unchanged); `**`→login preserved. Group PR #95 LEAD-GATE APPROVE comment + `--squash --admin`; remote frontend branch deleted via gh api. develop-sync: reset --hard origin/integration → merge origin/develop CONFLICT-FREE (develop advance since base 34d8b47 was docs/CI/status only — NO apps/manifest/angular.json/package.json/app.routes/app.config overlap, so NO union-merge of the 4 shared files needed); re-certified builds+tests GREEN on merged tip 4dd6b6d; pushed. Founder-gate PR #82-style **#96** [FOUNDER GATE — DO NOT MERGE] integration→develop OPEN — lead does NOT approve (D1). **R-SP6-6/C-CSP-1 escalated to SP07** (mfe-auth public-auth + SP04 public-landing = the 2 highest-stakes CSP surfaces; D42 ADD-ONLY CSP dev-smoke-first; infra memo handoff_mf_auth_deploy.md). |
| smart-picker (frontend slice) | develop (via SP05 #82 + founder gate #55) | 2026-06-11 | #55 (merge 25882a47) | **GATE OUTCOME: REJECT — slice CLOSED-OBSOLETED by base divergence, NOT merged as authored.** Frontend group branch feature/smart-picker/frontend (4 commits on base ba94543: D4 rename catalog-new->smart-picker in SHELL + provideHttpClient(withFetch()) + CategoryService HTTP rewrite + 439 specs) was built against the PRE-SP05 base where smart-picker was a SHELL feature at frontend/src/app/features/catalog-new/. BETWEEN dispatch and gate, SP05 mfe-catalog (#82, now on develop) RELOCATED smart-picker/catalog-new INTO the mfe-catalog Native-Federation remote (frontend/apps/mfe-catalog/src/app/catalog-new/), DELETED the shell features/catalog-new/, and REPLACED the shell /catalogs/new loadComponent with loadChildren->loadRemoteRoutesWithFallback('mfe-catalog','./CatalogRoutes'). Founder MERGED #55 (smart-picker integration->develop @ tip 4ff7c65) at 03:44 UTC — feature shipped to develop via the FEDERATED REMOTE (catalog-new.component.ts in apps/mfe-catalog, D4 rename NOT applied, NO HttpClient wiring), NOT via my shell-based group slice. Pushing/merging my branch would resurrect the deleted shell dir + removed route and create a DUPLICATE smart-picker (shell+remote), breaking strangler-fig. **Lead verdict: branch left UNPUSHED; NO group PR; NO merge.** Specialist work internally CLEAN (lead re-verified: build 3.297s <90s; 439 tests 0 fail; tsc clean; boundary 0; commission omitted; provideHttpClient clean/no-interceptor; D4 git-mv R022/R100 history-preserving) — failure is SEQUENCING/base-divergence, not code quality. This collision was PRE-PREDICTED in the lead's SP05 memory 'FORWARD COLLISION FLAG' and escalated as planned. **ESCALATED to founder via #55 comment** for reconciliation: are D4-rename + provideHttpClient deltas moot (remote already serves /catalogs/new) or to be re-applied onto apps/mfe-catalog as a NEW develop-based slice? NO same-work re-dispatch. |
| mfe-dashboard | feature/mfe-dashboard/integration | 2026-06-11 | #84 (squash a6ad02f) | MF Sub-Plan 04 (R3 — F1 landing PUBLIC + F6 dashboard AUTH). Two-expose remote (./LandingComponent + ./DashboardComponent) → apps/mfe-dashboard, port 4204. FIRST remote federating a public pre-auth route alongside an authenticated one. **LEAD INDEPENDENTLY RE-VERIFIED (worktree sp04-review, 0 discrepancies vs builder PR #84):** 6 moves R100 + blob-hash IDENTICAL (byte-identical, 0 logic edits); features/landing+features/dashboard GONE; shell federation.config.js untouched (name:'shell'). Remote build GREEN 3.067s (name:mfe-dashboard, exposes BOTH); shell GREEN 3.430s (≤90s D12), initial 60.80 kB (reduced — chunks left shell, no landing/dashboard chunks in shell dist). Tests **43 files/408 tests, 0 fail/0 skip** (exact baseline; relocated specs discovered spec-apps-mfe-dashboard-*; SP05's +3=43/411 lives on PR #82 NOT develop — reconciled honestly, no drop/no increase). Boundary 0 primeng. **D26/D27 auth boundary:** public '/' NO guard + pathMatch:'full' (top-level sibling); /dashboard child of canActivate:[authGuard] shell parent, NO self-guard (guard runs in shell pre-remoteEntry). **R-SP3-1 P0:** main.ts routes BOTH exposes; @mesell/core legitimately ABSENT from remoteEntry shared[] (no AuthService consumer — not drift). DashboardApiService providers:[…] preserved (line 35). 4 remotes in manifest. D12 fallback headless (remoteEntry 200/chunk 200/broken 404). Port 4204 in BOTH angular.json serve+serve-original. Group PR #84 LEAD-GATE APPROVE comment + squash --admin; branch deleted via gh api. Founder-gate PR #86 opened + left open (lead did NOT approve, D1). **FOUNDER MERGED #86 (90e3f0e) — SP04 IS ON DEVELOP.** KEEP-BOTH NOTE: between gate-open and merge the founder merged SP05 (#82) → develop gained mfe-catalog (port 4205), so the integration→develop union needed keep-both. Lead independently re-verified a keep-both refresh (worktree sp04-refresh, de7a01d): manifest+package.json+angular.json keep-both (all 5 remotes), app.routes.ts auto-merged (dashboard '' + /dashboard AND catalog loadChildren ./CatalogRoutes coexist); dashboard remote 2.816s/catalog remote 3.132s/shell 3.188s 60.59 kB GREEN; **43 files/411 tests 0 fail/skip** (SP05-inclusive baseline, SP04 adds 0 specs). Lead's resolution matched founder's on app.routes.ts; the 3 JSON files differ only in remote ORDER (semantically identical). Founder's 90e3f0e authoritative on develop; lead's de7a01d redundant; integration branch deleted. |
| mfe-catalog | feature/mfe-catalog/integration | 2026-06-11 | #77 (squash f11d0bf) | MF Sub-Plan 05 (R4 — the L one). 5-page catalog funnel (F7 smart-picker/catalog-new, F8 catalog-form, F9 images, F10 preview + catalogs list) → apps/mfe-catalog Native Federation remote (port 4205). **FIRST Routes-array expose** ./CatalogRoutes (D31) + NEW shell helper loadRemoteRoutesWithFallback (D12 fallback degrades WHOLE sub-tree to RemoteFailureComponent). 16 files renamed R100 (blob hashes IDENTICAL — byte-identical, 0 logic edits); 6 new files; catalog-form.routes.ts removed (D34, 0 dangling). Shell route table SHRANK 5 funnel children + CATALOG_FORM_ROUTES loadChildren → 1 catalogs loadChildren (strangler win); pricing/export siblings untouched. **LEAD INDEPENDENTLY RE-VERIFIED (not from builder report, 0 discrepancies):** remote build GREEN; shell build GREEN 3.278s (≤90s); 43 files/411 tests (baseline 43/408 +3 fallback tests, 0 fail/skip, no drop = R-SP5-3 PASS); 4 moved specs discovered under spec-apps-mfe-catalog-*; boundary 0 primeng; **§6.G singleton (P0):** @mesell/ui-kit + @mesell/composites = own shared chunks, @mesell/core legitimately ABSENT (no page imports AuthService — only a main.ts comment), AuthService NOT inlined anywhere — no drift; main.ts boots provideRouter(CATALOG_ROUTES) full 5-route set (R-SP3-1 fix). **D33 ZERO promotions** (libs/ diff EMPTY; 9 candidate types 0 cross-remote importers; no canonical Product/Catalog entity as-built) — matches Founder Ruling 2026-06-11 APPROVED-as-recommended, deferred to Wave-6 backend-contract. CatalogFormApiService route-scoped on :id/edit, SmartPickerApiService component-scoped — neither promoted to root (D32). Group PR #77 LEAD-GATE APPROVE comment + squash --admin; remote branch deleted via gh api. develop merged into integration (e1384c7) conflict-free (no SP04 on develop yet); both builds re-certified GREEN on merged tip. Founder-gate PR #82 integration→develop OPEN — lead does NOT approve (D1). |
| mfe-onboarding | feature/mfe-onboarding/integration | 2026-06-11 | #67 (squash e2035330) | MF Sub-Plan 03 (R2, F5 onboarding + F13 profile, routes /onboarding + /profile). FIRST multi-expose remote (D20: ./OnboardingComponent + ./ProfileComponent). D21 AuthLayout PROMOTED layouts/→libs/composites/ + barrel (founder RULED APPROVED); all 4 relative consumers re-pointed; 0 dangling (R-SP3-2). **D22 AuthService singleton C1–C5 = migration auth GO/NO-GO: PASSED.** R-SP3-1 (P0 auth-drift) root-caused+fixed: ignoreUnusedDeps/Sheriff pruned @mesell/core (inlined AuthService into remote) because main.ts referenced only OnboardingComponent → FIX = main.ts routes BOTH exposes → core stays shared+singleton (no AuthService change, C2). Remote build 2.85s/shell 3.37s (≤90s). 43 files/408 tests (406 baseline + 2 C5; 0 fail/skip, R-SP3-3). Boundary 0. Headless: remoteEntry+both chunks+core chunk 200, broken→404 (D12). Integration `git merge develop` conflict-free (SP02 remote not yet on develop). Founder-gate PR #68 OPEN. D13 hosting deferred to SP04-05. WAVE 1 parallel with SP02. |
| mfe-export | feature/mfe-export/integration | 2026-06-11 | #60 (squash 565d754) | MF Sub-Plan 02 (R6, F12 export). features/export → apps/mfe-export Native Federation remote; 2nd extraction, copied SP01 recipe (D15). Remote build 3.43s/shell 2.89s (esbuild preserved, <90s); 42 files/406 tests (== SP01 baseline, export spec discovered via apps/ glob, 0 drop, 0 fail); boundary 0 in apps/. NEW surfaces: two-remote manifest (pricing+export both remoteEntry 200 + chunk 200, broken→404; R-SP2-4 PASS) + D18 timer preserved across boundary (R100 rename, ngOnDestroy→clearInterval byte-identical). Integration→develop FOUNDER GATE PR #61 OPEN. §9.A: 5 PASS / 3 locally-proven (4,7,8) / 2 new-surface PASS / 0 FAIL. D13 hosting deferred to SP04-05 (no new infra request). WAVE 1 parallel with SP03. |
| mfe-pricing | develop | 2026-06-11 | #52 (squash a82cfcf) → #53 | MF Sub-Plan 01 PILOT — NOW ON DEVELOP. features/pricing → apps/mfe-pricing Native Federation remote; first loadRemoteModule + D12 fallback. Remote 3.35s/shell 3.29s (esbuild preserved); 42/406 tests; boundary 0. Integration→develop FOUNDER GATE PR #53 MERGED 2026-06-11 (bb37f5f) — founder approved the pilot. §9.A: 6 PASS / 3 locally-proven (4,7,8). Toolchain PROVEN; SP02–06 unblocked. |
| mf-workspace-foundation | feature/mf-workspace-foundation/integration | 2026-06-10 | #40 (squash e51761b) | MF Sub-Plan 0. libs/ relocation (@mesell/*) + Native Federation init (zero remotes). 401/401 tests, build 3.1s, boundary 0 violations. Integration→develop PR #41 MERGED 2026-06-11 (5198ba7). |

## Inter-lead requests open

| To lead | About feature | Request | Opened | Status |
|---|---|---|---|---|
| meesell-infra-builder | mfe-auth | D-auth SIXTH/FINAL remote prefix gs://meesell-frontend/{env}/mfe-auth/{version}/ (port 4206) + matrix unit apps/mfe-auth/** (C-CI-1) + prod manifest URL template remotes.mesell.xyz/{env}/mfe-auth/{version}/remoteEntry.json. **@mesell/core CONSUMER remote** (AuthService.setSession via otp-verify) — CDN MUST serve the SAME _mesell_core.js URL to shell + mfe-auth + mfe-onboarding (per-remote stale dup = singleton drift = C4 WRITE path breaks at runtime). **R-SP6-6/C-CSP-1 ESCALATION:** mfe-auth federates PUBLIC pre-auth routes (login/signup/otp-verify, no canActivate) — its remoteEntry is fetched UNauthenticated; with SP04 public landing these are the 2 highest-stakes CSP surfaces. D42 RULED: CSP ADD-ONLY, authored+dev-smoke-tested at SP07, staging/prod gated on green CSP smoke, must NOT strip CORS/refresh Set-Cookie; fe owns allowlist content, infra owns nginx/Traefik mechanism; discharges C-CSP-1. RECORD-ONLY hosting — D13 deferred to consolidated SP07 wave; completes the 6-remote topology. See handoff_mf_auth_deploy.md | 2026-06-11 | OPEN |
| meesell-infra-builder | mfe-dashboard | D29 fourth-remote prefix gs://meesell-frontend/{env}/mfe-dashboard/{version}/ (port 4204) + matrix unit apps/mfe-dashboard/** (C-CI-1) + prod manifest URL template remotes.mesell.xyz/{env}/mfe-dashboard/{version}/remoteEntry.json. FIRST remote federating a PUBLIC pre-auth route (landing) — the public remoteEntry is fetched with NO Authorization header; HIGHEST-stakes CSP surface alongside SP06 auth (escalated to SP07 per D14/D42). No new @mesell/core consumer (neither page injects AuthService) → no shared/**-rebuilds-all this slice; CDN must serve SAME @mesell/ui-kit + @mesell/composites URLs to shell+dashboard (singleton). RECORD-ONLY — D13 hosting deferred to consolidated wave per founder ruling. See handoff_mf_dashboard_deploy.md | 2026-06-11 | OPEN |
| meesell-infra-builder | mfe-catalog | D35 fifth-remote prefix gs://meesell-frontend/{env}/mfe-catalog/{version}/ (port 4205) + matrix unit apps/mfe-catalog/** (C-CI-1) + prod manifest URL template remotes.mesell.xyz/{env}/mfe-catalog/{version}/remoteEntry.json. FIRST Routes-array remote (./CatalogRoutes). D33 ZERO promotions → NO new @mesell/core consumer → no shared/**-rebuilds-all this slice; CDN must serve SAME @mesell/ui-kit + @mesell/composites URLs to shell+catalog (singleton). RECORD-ONLY — D13 hosting deferred to consolidated SP04-05 wave. See handoff_mf_catalog_deploy.md | 2026-06-11 | OPEN |
| meesell-infra-builder | mfe-pricing | D13 Option-C hosting: GCS bucket + Cloud CDN + remotes.mesell.xyz DNS/cert + cloudbuild.remote.yaml upload pipeline + per-env manifest URL templating. Locally dev-validated (localhost remote); prod surface pending. See handoff_mf_pricing_deploy.md | 2026-06-11 | OPEN |
| meesell-infra-builder | mfe-export | D19 second-remote prefix gs://meesell-frontend/{env}/mfe-export/{version}/ + confirm dorny/paths-filter matrix fans out apps/mfe-export/** as its own build unit (C-CI-1). RECORD-ONLY — D13 hosting deferred to SP04-05 per founder ruling; extends the open SP01 request, NOT a new active ask. See handoff_mf_export_deploy.md | 2026-06-11 | OPEN |
| meesell-infra-builder | mfe-onboarding | D24 third-remote prefix gs://meesell-frontend/{env}/mfe-onboarding/{version}/ (port 4202) + matrix unit apps/mfe-onboarding/** (C-CI-1). FIRST remote sharing @mesell/core (_mesell_core chunk) — CDN must serve the SAME core module URL to shell + all auth-consuming remotes (no per-remote stale dup, or singleton drift returns). RECORD-ONLY — D13 hosting deferred to SP04-05 per founder ruling. See handoff_mf_onboarding_deploy.md | 2026-06-11 | OPEN |
| meesell-infra-builder | mf-workspace-foundation | CI matrix rewrite (C-CI-1) ready before Sub-Plan 1; confirm new frontend/libs/** paths resolve against build-frontend glob. See handoff_mf_ci_prep.md. NOTE: chore/ci-matrix-c-ci-1 worktree observed in flight 2026-06-11 — SP01 now adds apps/mfe-pricing/** as the first per-remote build unit (cloudbuild.remote.yaml). | 2026-06-10 | OPEN |

---

## Status vocabulary

| Status | Meaning |
|---|---|
| `PENDING` | Feature is on the lead's backlog; no branch exists yet. |
| `IN PROGRESS` | A `feature/{name}/frontend` branch exists; specialist is actively committing. |
| `IN REVIEW` | A PR is open against `feature/{name}`; awaiting lead approval. |
| `MERGED` | The frontend group's PR has merged to `feature/{name}` — the group is done for this feature. |
| `BLOCKED` | Work stopped pending an inter-lead request, infra change, or founder decision. |

A feature row stays on the active features table until the frontend group's PR merges to `feature/{name}`; then it moves to "Recently merged" for 14 days before being removed.

## Acceptance gate

Every `feature/{name}/frontend` → `feature/{name}` PR is reviewed against the checklist in `.github/PULL_REQUEST_TEMPLATE/frontend.md`. The lead does not merge until every box is checked: build < 90 s (CLAUDE.md Decision 12), bundle delta noted, screenshots at 360 px and 1280 px attached, accessibility checks confirmed, CI gates 1 + 3 green. See repo management master plan §2.1 + the Merge gate section of `.claude/agents/meesell-frontend-coordinator.md`.
