// mfe-export — public surface (the federation typed boundary, MASTER_PLAN §6.5).
// Re-exports ONLY the exposed component. Keep this minimal: the shell's
// loadRemoteWithFallback helper resolves the single exported symbol.
export { ExportComponent } from './export.component';
