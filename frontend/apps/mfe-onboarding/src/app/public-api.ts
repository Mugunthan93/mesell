// mfe-onboarding — public surface (the federation typed boundary, MASTER_PLAN §6.5).
// FIRST multi-expose remote (D20): re-exports BOTH exposed components. The shell's
// loadRemoteWithFallback helper resolves each exposed symbol from one remoteEntry.json.
export { OnboardingComponent } from './onboarding.component';
export { ProfileComponent } from './profile.component';
