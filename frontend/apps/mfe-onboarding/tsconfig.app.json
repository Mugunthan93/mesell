/* mfe-onboarding remote — app build tsconfig. Extends the workspace base
   (inherits strict mode + the @mesell/* path aliases incl. @mesell/core for AuthService). */
{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "../../out-tsc/mfe-onboarding",
    "types": []
  },
  "include": [
    "src/**/*.ts",
    "../../libs/**/*.ts"
  ],
  "exclude": [
    "src/**/*.spec.ts",
    "../../libs/**/*.spec.ts"
  ]
}
