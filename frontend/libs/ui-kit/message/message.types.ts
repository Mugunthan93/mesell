/**
 * Semantic severity type for mee-message (inline, not toast).
 * Maps 1:1 to PrimeNG Message severity values (4 semantic values exposed).
 */
export type MeeMessageSeverity = 'success' | 'info' | 'warn' | 'error';
